openapi: "3.1.0"
info:
  title: GOTCHA Platform API
  version: "1.0.0"
  summary: "Multi-tenant customer engagement platform: inbox, AI employees, channels, commerce and billing."
  description: |-
    ## How this document is produced

    This specification is **generated from the source**, not written by hand. `scripts/api-docs/extract-routes.ts`
    parses the TypeScript AST of every Express router, resolves the middleware chain (including
    `router.use()` inherited middleware, path-scoped `router.use("/x", ...)` and spread arrays like
    `...ADMIN_ONLY`), reads Zod schemas referenced by `validate(...)`, and inspects each handler body for
    database operations and queue writes. `scripts/api-docs/generate-openapi.ts` then renders this file.

    Regenerate with `npm run api:docs`. CI can assert the checked-in file matches with `npm run api:docs:check`.

    ## What is trustworthy here, and what is not

    **Reliable**, because it is read directly from code: paths, methods, which middleware runs, the
    authentication and authorization requirements, path parameters, request bodies where a Zod schema
    exists, the Prisma models each handler touches, and the queues it writes to.

    **Inferred, and marked as such**: success status codes for handlers that never call `res.status()`
    explicitly (Express defaults to 200) carry `x-inferred`.

    **Not available from the source at all**: response body schemas. This codebase declares no response
    types anywhere - handlers call `res.json(...)` with ad-hoc objects. Successful responses are typed as
    `EnvelopeUnknown`, which documents the observed `{ data: ... }` convention without pretending to know
    the shape. Operations whose request body also has no schema are flagged `x-documentation-gap`.
    Closing that gap means adding schemas to the code, which is the only place it can be fixed honestly.

    ## Conventions

    - **Tenancy.** Almost every endpoint is tenant-scoped. The tenant is resolved from the access token by
      `resolveTenant`, never from a request parameter. Endpoints that deliberately cross tenants are marked
      `x-authorization.crossTenant`.
    - **Response envelope.** Handlers overwhelmingly answer `{ "data": ... }` on success and
      `{ "error": "..." }` on failure.
    - **Validation.** `validate(schema)` replaces `req.body` with the parsed value, so properties absent from
      the schema are stripped before the handler sees them.
    - **Async work.** Operations marked `x-async` enqueue background jobs (BullMQ); the HTTP response
      returns before that work finishes.
    - **Internal endpoints.** Operations tagged `Internal` are not routed by the gateway and are only
      reachable service-to-service. `x-publicly-exposed: false` marks them.
  contact:
    name: GOTCHA
    url: https://gotcha.co.il
servers:
- url: https://app.gotcha.co.il
  description: Production
- url: https://dev.gotcha.co.il
  description: Development
tags:
- name: AI Studio
  description: "Configuring AI employees: agents, tools, flows, funnels, approvals and the reasoning surfaces behind them."
- name: Analytics
  description: Reporting over conversations, agents and usage.
- name: Auth & Identity
  description: "Sign-in surface, the current principal, account self-service and the tenant-local permission model. GOTCHA implements no authentication itself: Authentik issues the token and this service verifies it."
- name: Billing
  description: Plans, pricing, checkout, invoices, AI credits and coupons.
- name: Channels
  description: Connecting and managing messaging channels, including the OAuth flows that link them.
- name: Contacts & CRM
  description: Customer records, identity resolution and the CRM panel that reads from connected systems.
- name: Conversations
  description: The inbox. Conversations, messages, routing rules, templates, broadcasts and scheduled sends.
- name: Embedded Chat
  description: The website widget that embeds on a customer's own site.
- name: GDPR & Compliance
  description: "Data subject rights: export, erasure and consent records."
- name: Historical Import
  description: One-time import of prior conversation history from a connected channel.
- name: Integrations
  description: Third-party business systems - CRMs, calendars and commerce platforms - and their OAuth connections.
- name: Internal
  description: Service-to-service only. Not exposed through the gateway; authenticated by a shared secret.
- name: Knowledge
  description: Knowledge bases, documents and the connected sources (Google Drive, Confluence) that keep them current.
- name: Notifications
  description: Notification delivery and per-user preferences.
- name: Onboarding
  description: The guided setup a new workspace runs once, plus the public invitation and waitlist endpoints that precede it.
- name: Platform Admin
  description: GOTCHA staff surface, not tenant-facing. Gated on platform admin rather than tenant role.
- name: Shopify
  description: "The Shopify app: storefront live chat, install flow and commerce context."
- name: Tenant Settings
  description: "Workspace configuration: departments, business policies, custom field definitions and general settings."
- name: Voice
  description: "The voice channel: inbound calls, live sessions, conference control and Twilio callbacks."
- name: Webhooks
  description: Inbound provider callbacks. These are authenticated by signature or shared secret, never by a user token.
- name: WhatsApp
  description: "WhatsApp Business specifics: numbers, Embedded Signup and per-number health."
security:
- bearerAuth: []
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: "An access token issued by **Authentik**, verified by `authenticate()` in `packages/shared/src/middleware/auth.ts` against Authentik's JWKS. GOTCHA implements no authentication of its own: it does not hash passwords, issue tokens, or store sessions. The token's `sub` resolves to `User.authentikSubject`, and authorization (roles, permissions, tenancy) is then evaluated entirely from local records."
    internalServiceToken:
      type: apiKey
      in: header
      name: x-internal-key
      description: Shared secret for service-to-service calls inside the Docker network. Never valid from outside the gateway.
    providerSignature:
      type: apiKey
      in: header
      name: x-hub-signature-256
      description: Provider webhook signature (Meta `x-hub-signature-256`, Shopify `x-shopify-hmac-sha256`, Twilio `x-twilio-signature`, or a shared push token for Google Pub/Sub). The exact header varies by provider; verification is mandatory and fails closed.
  schemas:
    EnvelopeUnknown:
      type: object
      description: "The success envelope. `data` is deliberately untyped: this codebase declares no response schemas, so the precise shape is only knowable by reading the handler named in `x-source`. Typing it here would be a guess presented as a contract."
      properties:
        data:
          description: Operation-specific payload.
    Error:
      type: object
      description: The failure envelope used across services.
      properties:
        error:
          type: string
          description: Human-readable failure reason.
        details:
          type: array
          description: Field-level validation failures, when the failure came from `validate()`.
          items:
            type: object
            properties:
              field:
                type: string
              message:
                type: string
      required:
      - error
paths:
  /api/account:
    get:
      operationId: getAccount
      tags:
      - Auth & Identity
      summary: List Account
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/auth/src/routes/account.ts:57` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/account.ts:57
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - user.findUnique
      - departmentMember.findFirst
      - tenant.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchAccount
      tags:
      - Auth & Identity
      summary: Update Account
      description: |-
        Update own profile: name, phone, locale. (Email is a verified flow - separate.)

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/auth/src/routes/account.ts:84` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/account.ts:84
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - user.findUnique
      - user.update
      - identity.update
      - user.updateMany
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/account/email-change:
    post:
      operationId: postAccountEmailChange
      tags:
      - Auth & Identity
      summary: Create Email Change
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/auth/src/routes/account.ts:402` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/account.ts:402
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - user.findUnique
      - identity.findFirst
      - user.findMany
      - user.findFirst
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/account/email-change/verify:
    post:
      operationId: postAccountEmailChangeVerify
      tags:
      - Auth & Identity
      summary: Create Verify
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/auth/src/routes/account.ts:448` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/account.ts:448
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - user.findUnique
      - identity.findFirst
      - user.findMany
      - user.findFirst
      - identity.update
      - user.updateMany
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/account/login-history:
    get:
      operationId: getAccountLoginHistory
      tags:
      - Auth & Identity
      summary: List Login History
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/auth/src/routes/account.ts:363` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/account.ts:363
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/account/mfa-gate:
    get:
      operationId: getAccountMfaGate
      tags:
      - Auth & Identity
      summary: List Mfa Gate
      description: |-
        Is this caller required to have MFA, and have they enrolled?

        The single source of truth the frontend enrolment gate polls. Enforcement is
        hierarchical (SYSTEM_ADMIN always; ADMIN/AGENT per the tenant's policy) and
        "enrolled" means an authenticator AND recovery codes. We stamp
        `User.mfaEnrolledAt` from the live Authentik state so the cheap local guard
        and the Workspace compliance counts stay in sync without their own IdP calls.

        Requires a valid Authentik access token.

        Implemented at `services/auth/src/routes/account.ts:176` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/account.ts:176
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - user.findUnique
      - identity.update
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/account/password-link:
    post:
      operationId: postAccountPasswordLink
      tags:
      - Auth & Identity
      summary: Create Password Link
      description: |-
        One-time link into Authentik's password-change flow, for the caller.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/auth/src/routes/account.ts:149` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/account.ts:149
      x-operation-type: write
      x-publicly-exposed: true
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/account/security:
    get:
      operationId: getAccountSecurity
      tags:
      - Auth & Identity
      summary: List Security
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/auth/src/routes/account.ts:136` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/account.ts:136
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/account/security/device/{type}/{id}:
    delete:
      operationId: deleteAccountSecurityDeviceTypeId
      tags:
      - Auth & Identity
      summary: Delete :Type
      description: |-
        Remove one of the caller's own authenticator devices (TOTP / passkey / recovery set).

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/auth/src/routes/account.ts:262` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/account.ts:262
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - user.findUnique
      - identity.update
      parameters:
      - name: type
        in: path
        required: true
        schema:
          type: string
        description: "`type` path segment."
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/account/sessions:
    delete:
      operationId: deleteAccountSessions
      tags:
      - Auth & Identity
      summary: Delete Sessions
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/auth/src/routes/account.ts:333` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/account.ts:333
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: includeCurrent
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: getAccountSessions
      tags:
      - Auth & Identity
      summary: List Sessions
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/auth/src/routes/account.ts:297` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/account.ts:297
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/account/sessions/{id}:
    delete:
      operationId: deleteAccountSessionsId
      tags:
      - Auth & Identity
      summary: Delete Session
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/auth/src/routes/account.ts:314` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/account.ts:314
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/action-contracts:
    get:
      operationId: getActionContracts
      tags:
      - AI Studio
      summary: List Action Contracts
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/action-contracts-admin.ts:26` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/action-contracts-admin.ts:26
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postActionContracts
      tags:
      - AI Studio
      summary: Create Action Contracts
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/action-contracts-admin.ts:45` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/action-contracts-admin.ts:45
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/action-contracts/{id}:
    delete:
      operationId: deleteActionContractsId
      tags:
      - AI Studio
      summary: Delete Action Contract
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/action-contracts-admin.ts:139` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/action-contracts-admin.ts:139
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: getActionContractsId
      tags:
      - AI Studio
      summary: Get Action Contract
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/action-contracts-admin.ts:34` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/action-contracts-admin.ts:34
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchActionContractsId
      tags:
      - AI Studio
      summary: Update Action Contract
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/action-contracts-admin.ts:105` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/action-contracts-admin.ts:105
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/action-planner/approvals:
    get:
      operationId: getActionPlannerApprovals
      tags:
      - AI Studio
      summary: List Approvals
      description: |-
        F4.2 - Approval queue: list pending high-risk actions that were blocked

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/action-planner.ts:249` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/action-planner.ts:249
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - auditLog.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/action-planner/classify:
    post:
      operationId: postActionPlannerClassify
      tags:
      - AI Studio
      summary: Create Classify
      description: |-
        POST /classify - decide chat vs execution mode for a prompt.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/action-planner.ts:126` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/action-planner.ts:126
      x-operation-type: write
      x-publicly-exposed: true
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/action-planner/execute:
    post:
      operationId: postActionPlannerExecute
      tags:
      - AI Studio
      summary: Create Execute
      description: |-
        POST /execute - run a previously planned ExecutionPlan (F3 Action Engine)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/action-planner.ts:273` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/action-planner.ts:273
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - auditLog.findFirst
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/action-planner/plan:
    post:
      operationId: postActionPlannerPlan
      tags:
      - AI Studio
      summary: Create Plan
      description: |-
        POST /plan - produce an ExecutionPlan for a prompt (dry-run by default)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/action-planner.ts:79` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/action-planner.ts:79
      x-operation-type: write
      x-publicly-exposed: true
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/action-planner/simulate:
    post:
      operationId: postActionPlannerSimulate
      tags:
      - AI Studio
      summary: Create Simulate
      description: |-
        POST /simulate - dual-mode. Classifies first: chat-mode returns a natural
        answer with no plan; execution-mode plans + dry-run executes as before.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/action-planner.ts:177` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/action-planner.ts:177
      x-operation-type: write
      x-publicly-exposed: true
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/analytics/backfill:
    post:
      operationId: postAdminAnalyticsBackfill
      tags:
      - Analytics
      summary: Create Backfill
      description: |-
        Backfill aggregates for conversations that closed before aggregation existed.

        Idempotent through the unique usageLogId link, so a partial run can simply be
        re-run rather than needing a reset.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-analytics.ts:148` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-analytics.ts:148
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - conversationUsageAggregate.findMany
      - conversation.findMany
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/analytics/conversation-costs:
    get:
      operationId: getAdminAnalyticsConversationCosts
      tags:
      - Analytics
      summary: List Conversation Costs
      description: |-
        Global or filtered actual conversation cost.

        The averages are WEIGHTED (total usage / total completed conversations).
        Averaging per-organization averages would let a five-conversation pilot count
        as much as a 50,000-conversation account.

        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/admin-analytics.ts:62` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-analytics.ts:62
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/admin/analytics/conversation-costs/by-tenant:
    get:
      operationId: getAdminAnalyticsConversationCostsByTenant
      tags:
      - Analytics
      summary: List By Tenant
      description: |-
        Per-organization breakdown. Each row is that organization's own weighted average.

        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/admin-analytics.ts:83` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-analytics.ts:83
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - tenant.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/admin/analytics/estimate-vs-actual:
    get:
      operationId: getAdminAnalyticsEstimateVsActual
      tags:
      - Analytics
      summary: List Estimate Vs Actual
      description: |-
        Configured public estimate vs actual internal average.

        Returns a warning and nothing else. `autoApplied` is always false, and no
        write happens on this path - only an explicit publish on the pricing router
        changes what customers see.

        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/admin-analytics.ts:106` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-analytics.ts:106
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      parameters:
      - name: warnThresholdPct
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/admin/analytics/settle:
    post:
      operationId: postAdminAnalyticsSettle
      tags:
      - Analytics
      summary: Create Settle
      description: |-
        Force settlement of due conversations (ops). Idempotent.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-analytics.ts:137` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-analytics.ts:137
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/billing/enforcement-preview:
    get:
      operationId: getAdminBillingEnforcementPreview
      tags:
      - Billing
      summary: List Enforcement Preview
      description: |-
        Who would stop being served if enforcement were switched on.

        Read-only, and worth having because enforcement mode is one environment
        variable that changes what happens to live customer conversations. Flipping
        it without knowing the blast radius means learning it from the organizations
        whose bots went quiet.

        No authentication middleware is applied to this route.

        Implemented at `services/billing/src/routes/admin-exchange-rates.ts:297` (billing service).
      security: []
      x-service: billing
      x-source: services/billing/src/routes/admin-exchange-rates.ts:297
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requirePlatformAdmin
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/admin/billing/exchange-rates:
    get:
      operationId: getAdminBillingExchangeRates
      tags:
      - Billing
      summary: List Exchange Rates
      description: |-
        Current state plus history, so the effect of a change is visible before it.

        No authentication middleware is applied to this route.

        Implemented at `services/billing/src/routes/admin-exchange-rates.ts:106` (billing service).
      security: []
      x-service: billing
      x-source: services/billing/src/routes/admin-exchange-rates.ts:106
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requirePlatformAdmin
      parameters:
      - name: base
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: quote
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postAdminBillingExchangeRates
      tags:
      - Billing
      summary: Create Exchange Rates
      description: |-
        Propose a MANUAL override. Created as DRAFT: proposing changes nothing.

        Normal daily rates never come through here - they are fetched from the Bank
        of Israel automatically. This is for the cases where a person is genuinely
        asserting a number, and those are the cases that need a second pair of eyes.

        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-exchange-rates.ts:142` (billing service).
      security: []
      x-service: billing
      x-source: services/billing/src/routes/admin-exchange-rates.ts:142
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requirePlatformAdmin
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/billing/exchange-rates/{id}/approve:
    post:
      operationId: postAdminBillingExchangeRatesIdApprove
      tags:
      - Billing
      summary: Create Approve
      description: |-
        Approve a draft, making it THE rate.

        Retires the previous one in the same transaction, and the database refuses a
        second ACTIVE row - so a race here fails loudly rather than leaving the
        charge amount to whichever row a query returned first.

        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-exchange-rates.ts:209` (billing service).
      security: []
      x-service: billing
      x-source: services/billing/src/routes/admin-exchange-rates.ts:209
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requirePlatformAdmin
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/billing/exchange-rates/{id}/retire:
    post:
      operationId: postAdminBillingExchangeRatesIdRetire
      tags:
      - Billing
      summary: Create Retire
      description: |-
        Retire a rate without replacing it.

        Leaves the platform unable to charge, on purpose. That is the point of having
        the lever.

        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-exchange-rates.ts:229` (billing service).
      security: []
      x-service: billing
      x-source: services/billing/src/routes/admin-exchange-rates.ts:229
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requirePlatformAdmin
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/billing/exchange-rates/preview:
    post:
      operationId: postAdminBillingExchangeRatesPreview
      tags:
      - Billing
      summary: Create Preview
      description: |-
        Preview a conversion without saving anything.

        So an approver can check a figure against a real plan price before it becomes
        the number on someone's statement.

        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-exchange-rates.ts:247` (billing service).
      security: []
      x-service: billing
      x-source: services/billing/src/routes/admin-exchange-rates.ts:247
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requirePlatformAdmin
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/billing/fx-refresh:
    post:
      operationId: postAdminBillingFxRefresh
      tags:
      - Billing
      summary: Create Fx Refresh
      description: |-
        Fetch the official rate now.

        Read-only against the Bank of Israel and safe to invoke by hand - useful when
        the feed has just recovered and nobody wants to wait for the next tick.

        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-exchange-rates.ts:192` (billing service).
      security: []
      x-service: billing
      x-source: services/billing/src/routes/admin-exchange-rates.ts:192
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requirePlatformAdmin
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/billing/fx-status:
    get:
      operationId: getAdminBillingFxStatus
      tags:
      - Billing
      summary: List Fx Status
      description: |-
        The official rate feed: what it says, how old it is, and whether charging is
        possible right now.

        Read-only. It reports the stored state and does not trigger a fetch, so
        opening the page cannot itself cause a call to the central bank.

        No authentication middleware is applied to this route.

        Implemented at `services/billing/src/routes/admin-exchange-rates.ts:177` (billing service).
      security: []
      x-service: billing
      x-source: services/billing/src/routes/admin-exchange-rates.ts:177
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requirePlatformAdmin
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/admin/billing/payment-readiness:
    get:
      operationId: getAdminBillingPaymentReadiness
      tags:
      - Billing
      summary: List Payment Readiness
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/admin-payment-readiness.ts:39` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-payment-readiness.ts:39
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/admin/billing/reconciliations:
    get:
      operationId: getAdminBillingReconciliations
      tags:
      - Billing
      summary: List Reconciliations
      description: |-
        Charges nobody could settle automatically.

        Deliberately read-only and on the platform surface: resolving one means
        looking at the provider's own records, and the action that follows is a
        refund or a manual activation, both of which already have their own audited
        paths. A one-click "mark as paid" here would be a way to grant a plan with no
        evidence at all.

        No authentication middleware is applied to this route.

        Implemented at `services/billing/src/routes/admin-exchange-rates.ts:269` (billing service).
      security: []
      x-service: billing
      x-source: services/billing/src/routes/admin-exchange-rates.ts:269
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requirePlatformAdmin
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/admin/billing/reconciliations/sweep:
    post:
      operationId: postAdminBillingReconciliationsSweep
      tags:
      - Billing
      summary: Create Sweep
      description: |-
        Run the sweep now.

        It only ever asks the provider what happened - it never re-submits a charge -
        so triggering it by hand is safe. Useful when a provider outage has just
        ended and nobody wants to wait for the next tick.

        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-exchange-rates.ts:280` (billing service).
      security: []
      x-service: billing
      x-source: services/billing/src/routes/admin-exchange-rates.ts:280
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requirePlatformAdmin
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/billing/shopify/grandfather/{tenantId}:
    delete:
      operationId: deleteAdminBillingShopifyGrandfatherTenantId
      tags:
      - Billing
      summary: Delete Grandfather
      description: |-
        Withdraw a grant. SYSTEM_ADMIN only, attributable, and never silent.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/shopify-billing.ts:350` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/shopify-billing.ts:350
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      parameters:
      - name: tenantId
        in: path
        required: true
        schema:
          type: string
        description: "`tenantId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: getAdminBillingShopifyGrandfatherTenantId
      tags:
      - Billing
      summary: Get Grandfather
      description: |-
        What the rules say, without acting on it.

        SYSTEM_ADMIN only - it exposes the evidence blob, which names subscription
        and payment dates that a tenant user has no reason to inspect through this
        surface.

        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/shopify-billing.ts:311` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/shopify-billing.ts:311
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      parameters:
      - name: tenantId
        in: path
        required: true
        schema:
          type: string
        description: "`tenantId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postAdminBillingShopifyGrandfatherTenantId
      tags:
      - Billing
      summary: TenantId action
      description: |-
        Grant grandfathering on an admin's authority.

        SYSTEM_ADMIN only, and `approvedBy` is taken from the AUTHENTICATED USER
        rather than the body. An override whose approver could be typed by the caller
        would be an audit trail that records whatever the caller wanted it to.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/shopify-billing.ts:332` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/shopify-billing.ts:332
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      parameters:
      - name: tenantId
        in: path
        required: true
        schema:
          type: string
        description: "`tenantId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/coupons:
    get:
      operationId: getAdminCoupons
      tags:
      - Billing
      summary: List Coupons
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/admin-coupons.ts:62` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-coupons.ts:62
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postAdminCoupons
      tags:
      - Billing
      summary: Create Coupons
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-coupons.ts:71` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-coupons.ts:71
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/coupons/{id}/active:
    post:
      operationId: postAdminCouponsIdActive
      tags:
      - Billing
      summary: Create Active
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-coupons.ts:92` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-coupons.ts:92
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/coupons/assignable-tenants:
    get:
      operationId: getAdminCouponsAssignableTenants
      tags:
      - Billing
      summary: List Assignable Tenants
      description: |-
        Organizations to assign a coupon to, for the sysadmin picker. Name + id
        only: this is a chooser, not a tenant directory.

        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/admin-coupons.ts:179` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-coupons.ts:179
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - tenant.findMany
      parameters:
      - name: q
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/admin/coupons/assignments/{assignmentId}:
    delete:
      operationId: deleteAdminCouponsAssignmentsAssignmentId
      tags:
      - Billing
      summary: Delete Assignment
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-coupons.ts:160` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-coupons.ts:160
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      parameters:
      - name: assignmentId
        in: path
        required: true
        schema:
          type: string
        description: "`assignmentId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/admin/coupons/tenants/{tenantId}:
    get:
      operationId: getAdminCouponsTenantsTenantId
      tags:
      - Billing
      summary: Get Tenant
      description: |-
        Every assignment for one organization, live ones flagged.

        Namespaced under /admin/coupons rather than /admin/tenants: the gateway
        proxies admin routes by prefix, so a second prefix would need its own
        location block - and a route the gateway does not know about is a feature
        that works in development and 404s in production.

        Read-level permission: seeing that a customer has 20% off is support
        information; granting it is not.

        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/admin-coupons.ts:119` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-coupons.ts:119
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      parameters:
      - name: tenantId
        in: path
        required: true
        schema:
          type: string
        description: "`tenantId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postAdminCouponsTenantsTenantId
      tags:
      - Billing
      summary: TenantId action
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-coupons.ts:128` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-coupons.ts:128
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      parameters:
      - name: tenantId
        in: path
        required: true
        schema:
          type: string
        description: "`tenantId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/evaluation:
    post:
      operationId: postAdminEvaluation
      tags:
      - Billing
      summary: Create Evaluation
      description: |-
        Provision evaluation access. Sysadmin only - there is no customer
        self-activation path anywhere in the system.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-analytics.ts:195` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-analytics.ts:195
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/evaluation/convert:
    post:
      operationId: postAdminEvaluationConvert
      tags:
      - Billing
      summary: Create Convert
      description: |-
        Convert an evaluation into a paid plan. Manual, never automatic.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-analytics.ts:219` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-analytics.ts:219
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/evaluation/templates:
    get:
      operationId: getAdminEvaluationTemplates
      tags:
      - Billing
      summary: List Templates
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/admin-analytics.ts:187` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-analytics.ts:187
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/admin/pricing/currency:
    get:
      operationId: getAdminPricingCurrency
      tags:
      - Billing
      summary: List Currency
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/admin-pricing.ts:910` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:910
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - fxRateSnapshot.findFirst
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putAdminPricingCurrency
      tags:
      - Billing
      summary: Replace Currency
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-pricing.ts:922` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:922
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - pricingCurrencyConfig.findFirst
      - pricingCurrencyConfig.update
      - pricingCurrencyConfig.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/pricing/currency/refresh-fx:
    post:
      operationId: postAdminPricingCurrencyRefreshFx
      tags:
      - Billing
      summary: Create Refresh Fx
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-pricing.ts:944` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:944
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/pricing/custom-plans:
    post:
      operationId: postAdminPricingCustomPlans
      tags:
      - Billing
      summary: Create Custom Plans
      description: |-
        Build an organization-specific negotiated plan.

        A custom plan is a CUSTOM-kind PlanVersion scoped to one tenant. It never
        mutates a public plan, it is versioned like any other, and it flows through
        the same entitlement resolver with no special casing.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-pricing.ts:959` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:959
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - tenant.findUnique
      - plan.findFirst
      - plan.create
      - planEntitlement.create
      - publicEstimationConfig.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "422":
          description: Semantically invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/pricing/custom-plans/{id}/approve:
    post:
      operationId: postAdminPricingCustomPlansIdApprove
      tags:
      - Billing
      summary: Create Approve
      description: |-
        Approve and publish a custom plan so the organization can be moved onto it.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-pricing.ts:1045` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:1045
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - plan.findUnique
      - plan.findFirst
      - plan.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/pricing/estimation:
    get:
      operationId: getAdminPricingEstimation
      tags:
      - Billing
      summary: List Estimation
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/admin-pricing.ts:670` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:670
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - publicEstimationConfig.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postAdminPricingEstimation
      tags:
      - Billing
      summary: Create Estimation
      description: |-
        Publish a new estimation version.

        Deactivates the previous config at the same scope and creates a new version.
        Nothing else is written - not the ledger, not an invoice, not a subscription.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-pricing.ts:768` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:768
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - publicEstimationConfig.findFirst
      - publicEstimationConfig.update
      - publicEstimationConfig.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/pricing/estimation/preview:
    post:
      operationId: postAdminPricingEstimationPreview
      tags:
      - Billing
      summary: Create Preview
      description: |-
        Preview a proposed ratio BEFORE publishing it.

        Shows the new monthly and daily estimates, the new displayed price per
        conversation, and which plans it would affect - alongside the explicit
        statement that existing subscriptions keep their snapshot.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-pricing.ts:695` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:695
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - plan.findMany
      - subscription.count
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/pricing/features:
    get:
      operationId: getAdminPricingFeatures
      tags:
      - Billing
      summary: List Features
      description: |-
        The feature catalog, including which keys are catalogued but not built.

        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/admin-pricing.ts:183` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:183
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/admin/pricing/packages:
    get:
      operationId: getAdminPricingPackages
      tags:
      - Billing
      summary: List Packages
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/admin-pricing.ts:814` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:814
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - creditPackage.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/admin/pricing/packages/{key}:
    delete:
      operationId: deleteAdminPricingPackagesKey
      tags:
      - Billing
      summary: Delete Package
      description: |-
        Remove a credit package from the catalog.

        Refuses in two cases, both of which would be silent damage:

          A tenant's automatic top-up points at packages BY KEY. Deleting one leaves
          that policy aiming at nothing, and the failure surfaces later - when their
          credits run low and the top-up they configured does not happen.

          A package that has been bought is the description of what somebody paid
          for. Purchases record it as `package:<key>` on the credit lot, so the row
          is what explains a real charge months later. That gets RETIRED, not
          deleted: retiring takes it off sale and keeps the explanation.

        Only a package nobody has bought and nothing points at is actually removed.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-pricing.ts:877` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:877
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - creditPackage.findUnique
      - autoPurchasePolicy.count
      - aiUnitLot.count
      - creditPackage.delete
      parameters:
      - name: key
        in: path
        required: true
        schema:
          type: string
        description: "`key` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putAdminPricingPackagesKey
      tags:
      - Billing
      summary: Replace Package
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-pricing.ts:830` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:830
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - creditPackage.upsert
      parameters:
      - name: key
        in: path
        required: true
        schema:
          type: string
        description: "`key` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/pricing/permissions:
    get:
      operationId: getAdminPricingPermissions
      tags:
      - Billing
      summary: List Permissions
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/admin-pricing.ts:203` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:203
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/admin/pricing/plans:
    get:
      operationId: getAdminPricingPlans
      tags:
      - Billing
      summary: List Plans
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/admin-pricing.ts:111` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:111
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - plan.findMany
      - subscription.groupBy
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postAdminPricingPlans
      tags:
      - Billing
      summary: Create Plans
      description: |-
        Create a plan that does not exist yet, of a chosen kind.

        The only previous way to get a new plan was POST /plans/:key/versions, which
        copies an EXISTING key and inherits its kind - so a POC or trial plan could
        not be created at all unless one already existed. Custom plans had their own
        endpoint, hardcoded to CUSTOM and requiring a tenant.

        Always DRAFT. A plan that could be created already published would skip the
        review the publish flow exists to provide.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-pricing.ts:410` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:410
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - plan.findFirst
      - plan.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/pricing/plans/{id}:
    delete:
      operationId: deleteAdminPricingPlansId
      tags:
      - Billing
      summary: Delete Plan
      description: |-
        Discard a DRAFT plan version.

        DRAFT only, and that is the whole safety argument. A published version is
        immutable because organizations hold a commercial snapshot of it; deleting
        one would remove the definition of what they agreed to pay for while they are
        still paying it. A draft has never been sold and can be discarded freely.

        Entitlements and volume options cascade. Nothing else references a draft.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-pricing.ts:372` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:372
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - plan.findUnique
      - subscription.count
      - plan.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchAdminPricingPlansId
      tags:
      - Billing
      summary: Update Plan
      description: |-
        Edit a DRAFT. An ACTIVE version is immutable by design.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-pricing.ts:306` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:306
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - plan.findUnique
      - plan.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/pricing/plans/{id}/entitlements:
    put:
      operationId: putAdminPricingPlansIdEntitlements
      tags:
      - Billing
      summary: Replace Entitlements
      description: |-
        Set feature entitlements and numeric limits on a DRAFT.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-pricing.ts:461` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:461
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - plan.findUnique
      - planEntitlement.upsert
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "422":
          description: Semantically invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/pricing/plans/{id}/preview:
    get:
      operationId: getAdminPricingPlansIdPreview
      tags:
      - Billing
      summary: Get Preview
      description: |-
        What publishing this draft would actually do.

        Shows the diff against the currently ACTIVE version and, crucially, how many
        organizations are on it and what happens to them (nothing - they keep their
        snapshot until explicitly migrated).

        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/admin-pricing.ts:529` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:529
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - plan.findUnique
      - plan.findFirst
      - subscription.count
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/admin/pricing/plans/{id}/publish:
    post:
      operationId: postAdminPricingPlansIdPublish
      tags:
      - Billing
      summary: Create Publish
      description: |-
        Publish a draft: it becomes the ACTIVE version, the previous one is RETIRED.

        Retiring does NOT touch subscriptions - they keep pointing at the retired
        version and keep their snapshot. That is what grandfathering means here.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-pricing.ts:591` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:591
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - plan.findUnique
      - plan.findFirst
      - plan.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/pricing/plans/{id}/volume-options:
    put:
      operationId: putAdminPricingPlansIdVolumeOptions
      tags:
      - Billing
      summary: Replace Volume Options
      description: |-
        Create or update a volume option on a DRAFT.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-pricing.ts:486` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:486
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - plan.findUnique
      - planVolumeOption.upsert
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/pricing/plans/{key}/versions:
    post:
      operationId: postAdminPricingPlansKeyVersions
      tags:
      - Billing
      summary: Create Versions
      description: |-
        Create a new DRAFT version of a plan key, cloned from an existing version.

        This is the ONLY way to change a live plan's commercial terms. The draft is
        invisible to customers until published.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-pricing.ts:215` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:215
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - plan.findFirst
      - plan.create
      - planEntitlement.create
      - planVolumeOption.create
      - publicEstimationConfig.create
      parameters:
      - name: key
        in: path
        required: true
        schema:
          type: string
        description: "`key` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/pricing/plans/order:
    post:
      operationId: postAdminPricingPlansOrder
      tags:
      - Billing
      summary: Create Order
      description: |-
        Reorder the public catalog and set the recommended plan.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-pricing.ts:627` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:627
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - plan.updateMany
      - plan.findFirst
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/admin/pricing/preview-customer:
    get:
      operationId: getAdminPricingPreviewCustomer
      tags:
      - Billing
      summary: List Preview Customer
      description: |-
        Exactly what a customer of this organization would see on the pricing page.

        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/admin-pricing.ts:1067` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:1067
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      parameters:
      - name: tenantId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: currency
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/admin/pricing/provisioning/plans:
    get:
      operationId: getAdminPricingProvisioningPlans
      tags:
      - Billing
      summary: List Plans
      description: |-
        Nested under /admin/pricing so it inherits the existing gateway location.


        Plans a Sysadmin may provision a paid tenant onto.

        ACTIVE and PUBLIC only. Draft, retired, sales-only, POC, TRIAL and any plan
        scoped to an organization are excluded here as well as in the provisioning
        service, so the UI cannot even offer something the backend would reject.

        Requires a valid Authentik access token.

        Implemented at `services/billing/src/routes/admin-pricing.ts:1085` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:1085
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      x-database:
      - plan.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/admin/pricing/provisioning/quote:
    post:
      operationId: postAdminPricingProvisioningQuote
      tags:
      - Billing
      summary: Create Quote
      description: |-
        The authoritative commercial summary for a selection.

        The UI displays THIS and never computes a total of its own, so what an
        operator sees is what provisioning will actually freeze into the snapshot.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/billing/src/routes/admin-pricing.ts:1125` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/admin-pricing.ts:1125
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/agent-loop/execute-approved:
    post:
      operationId: postAgentLoopExecuteApproved
      tags:
      - AI Studio
      summary: Create Execute Approved
      description: |-
        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/ai/src/routes/agent-loop-resume.ts:29` (ai service).
      security:
      - internalServiceToken: []
      x-service: ai
      x-source: services/ai/src/routes/agent-loop-resume.ts:29
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "422":
          description: Semantically invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/agent-scores:
    get:
      operationId: getAgentScores
      tags:
      - AI Studio
      summary: List Agent Scores
      description: |-
        GET / - get scores for all agents (admin)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/agent-scores.ts:9` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/agent-scores.ts:9
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - agentScore.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/agent-scores/{agentId}:
    get:
      operationId: getAgentScoresAgentId
      tags:
      - AI Studio
      summary: Get Agent Score
      description: |-
        GET /:agentId - get overview for specific agent

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/agent-scores.ts:31` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/agent-scores.ts:31
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - user.findFirst
      parameters:
      - name: agentId
        in: path
        required: true
        schema:
          type: string
        description: "`agentId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/agent-scores/{agentId}/history:
    get:
      operationId: getAgentScoresAgentIdHistory
      tags:
      - AI Studio
      summary: Get History
      description: |-
        GET /:agentId/history - get score history

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/agent-scores.ts:60` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/agent-scores.ts:60
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - user.findFirst
      - agentScore.findMany
      parameters:
      - name: agentId
        in: path
        required: true
        schema:
          type: string
        description: "`agentId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/agent/clear:
    post:
      operationId: postAgentClear
      tags:
      - AI Studio
      summary: Create Clear
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/agent.ts:113` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/agent.ts:113
      x-operation-type: write
      x-publicly-exposed: true
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/agent/run:
    post:
      operationId: postAgentRun
      tags:
      - AI Studio
      summary: Create Run
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/agent.ts:27` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/agent.ts:27
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - |-
          tenant.findUnique({
              where: { id: tenantId },
              select: { name: true },
            })
      - tenant.findUnique
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/agents:
    get:
      operationId: getAgents
      tags:
      - AI Studio
      summary: List Agents
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/agents.ts:29` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:29
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        mfa: required
      x-database:
      - user.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postAgents
      tags:
      - AI Studio
      summary: Create Agents
      description: |-
        Seat limit. Counts ACTIVE members only, so deactivating someone frees their
        seat rather than permanently consuming it.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:109` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:109
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - settings:members:manage OR role ADMIN
        planLimits:
        - limit:users
        mfa: required
      x-database:
      - department.findMany
      - departmentMember.createMany
      requestBody:
        required: true
        description: Validated by `inviteAgentSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  format: email
                name:
                  type: string
                  minLength: 1
                departmentIds:
                  type: array
                  items:
                    type: string
                    minLength: 1
                  maxItems: 20
              required:
              - email
              - name
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/agents/{id}:
    delete:
      operationId: deleteAgentsId
      tags:
      - AI Studio
      summary: Delete Agent
      description: |-
        Delete agent (admin only)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:236` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:236
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - settings:members:manage OR role ADMIN
        mfa: required
      x-database:
      - user.findFirst
      - conversation.updateMany
      - user.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchAgentsId
      tags:
      - AI Studio
      summary: Update Agent
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:166` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:166
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - settings:members:manage OR role ADMIN
        mfa: required
      x-database:
      - user.findFirst
      - user.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: true
        description: Validated by `updateAgentSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                isActive:
                  type: boolean
                phoneNumber:
                  type: string
                  nullable: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/agents/{id}/reset-password:
    post:
      operationId: postAgentsIdResetPassword
      tags:
      - AI Studio
      summary: Create Reset Password
      description: |-
        Re-issue an agent's password-setup link (admin only).

        This replaces the old admin-sets-a-password endpoint. An admin can help
        someone regain access, but never learns or chooses their credential: all
        this returns is a one-time link into Authentik's recovery flow.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:216` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:216
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - settings:members:manage OR role ADMIN
        mfa: required
      x-database:
      - user.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/agents/login-status:
    get:
      operationId: getAgentsLoginStatus
      tags:
      - AI Studio
      summary: List Login Status
      description: |-
        Login/invitation status for the tenant's members (admin only, lazy-loaded by
        the Users page so the main list stays fast). For each user we ask Authentik
        for last_login in one call and derive a status:
          disabled       - GOTCHA isActive=false
          invited        - active, never signed in (invite pending / link unused)
          active         - active, has signed in (returns lastLogin)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/agents.ts:73` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:73
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - settings:members:manage OR role ADMIN
        mfa: required
      x-database:
      - user.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/agents/me/locale:
    get:
      operationId: getAgentsMeLocale
      tags:
      - AI Studio
      summary: List Locale
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/agents.ts:753` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:753
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        mfa: required
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putAgentsMeLocale
      tags:
      - AI Studio
      summary: Replace Locale
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:773` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:773
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        mfa: required
      x-database:
      - user.update
      requestBody:
        required: true
        description: Validated by `localeSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                locale:
                  x-zod: union
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/agents/settings/approval-recipient:
    get:
      operationId: getAgentsSettingsApprovalRecipient
      tags:
      - AI Studio
      summary: List Approval Recipient
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/agents.ts:461` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:461
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putAgentsSettingsApprovalRecipient
      tags:
      - AI Studio
      summary: Replace Approval Recipient
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:474` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:474
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      x-database:
      - user.findFirst
      requestBody:
        required: true
        description: Validated by `approvalRecipientSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                userId:
                  type: string
                  minLength: 1
                phone:
                  type: string
                  minLength: 6
                  maxLength: 24
                enabled:
                  type: boolean
                  default: false
                maxRiskLevel:
                  type: string
                  enum:
                  - low
                  - medium
                  - high
                  default: medium
              required:
              - userId
              - phone
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/agents/settings/approval-recipient/{userId}:
    delete:
      operationId: deleteAgentsSettingsApprovalRecipientUserId
      tags:
      - AI Studio
      summary: Delete Approval Recipient
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:518` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:518
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      parameters:
      - name: userId
        in: path
        required: true
        schema:
          type: string
        description: "`userId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/agents/settings/auto-greeting:
    get:
      operationId: getAgentsSettingsAutoGreeting
      tags:
      - AI Studio
      summary: List Auto Greeting
      description: |-
        Auto-greeting settings

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/agents.ts:270` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:270
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putAgentsSettingsAutoGreeting
      tags:
      - AI Studio
      summary: Replace Auto Greeting
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:281` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:281
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/agents/settings/bot-config:
    get:
      operationId: getAgentsSettingsBotConfig
      tags:
      - AI Studio
      summary: List Bot Config
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/agents.ts:713` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:713
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      x-database:
      - tenant.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/agents/settings/business-hours:
    get:
      operationId: getAgentsSettingsBusinessHours
      tags:
      - AI Studio
      summary: List Business Hours
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/agents.ts:561` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:561
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putAgentsSettingsBusinessHours
      tags:
      - AI Studio
      summary: Replace Business Hours
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:590` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:590
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      requestBody:
        required: true
        description: Validated by `businessHoursSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled:
                  type: boolean
                timezone:
                  type: string
                  minLength: 1
                autoResponse:
                  type: string
                  default: ""
                aiOutsideHours:
                  type: string
                  enum:
                  - active
                  - silent
                  default: active
                outsideHoursHandoffMessage:
                  type: string
                  maxLength: 500
                  default: ""
                schedule:
                  type: object
                  properties:
                    sunday: {}
                    monday: {}
                    tuesday: {}
                    wednesday: {}
                    thursday: {}
                    friday: {}
                    saturday: {}
                  required:
                  - sunday
                  - monday
                  - tuesday
                  - wednesday
                  - thursday
                  - friday
                  - saturday
              required:
              - enabled
              - timezone
              - schedule
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/agents/settings/channel-config:
    get:
      operationId: getAgentsSettingsChannelConfig
      tags:
      - AI Studio
      summary: List Channel Config
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/agents.ts:410` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:410
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      x-database:
      - tenantChannelConfig.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putAgentsSettingsChannelConfig
      tags:
      - AI Studio
      summary: Replace Channel Config
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:430` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:430
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      x-database:
      - tenantChannelConfig.upsert
      requestBody:
        required: true
        description: Validated by `channelConfigSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                botFlowMode:
                  type: string
                  enum:
                  - UNIFIED
                  - PER_CHANNEL
              required:
              - botFlowMode
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/agents/settings/channels:
    get:
      operationId: getAgentsSettingsChannels
      tags:
      - AI Studio
      summary: List Channels
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/agents.ts:299` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:299
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      x-database:
      - channelAccount.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postAgentsSettingsChannels
      tags:
      - AI Studio
      summary: Create Channels
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:328` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:328
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      x-database:
      - channelAccount.findFirst
      - channelAccount.create
      requestBody:
        required: true
        description: Validated by `createChannelSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                channel:
                  type: string
                  enum:
                  - WHATSAPP
                  - MESSENGER
                externalId:
                  type: string
                  minLength: 1
                displayName:
                  type: string
                  minLength: 1
                credentials:
                  type: object
                  properties:
                    accessToken:
                      type: string
                      minLength: 1
                    appSecret:
                      type: string
                    webhookSecret:
                      type: string
                  required:
                  - accessToken
              required:
              - channel
              - externalId
              - displayName
              - credentials
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/agents/settings/channels/{id}:
    delete:
      operationId: deleteAgentsSettingsChannelsId
      tags:
      - AI Studio
      summary: Delete Channel
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:393` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:393
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      x-database:
      - channelAccount.findFirst
      - channelAccount.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchAgentsSettingsChannelsId
      tags:
      - AI Studio
      summary: Update Channel
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:367` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:367
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      x-database:
      - channelAccount.findFirst
      - channelAccount.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: true
        description: Validated by `updateChannelSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                displayName:
                  type: string
                  minLength: 1
                credentials:
                  type: object
                  properties:
                    accessToken:
                      type: string
                      minLength: 1
                    appSecret:
                      type: string
                    webhookSecret:
                      type: string
                  required:
                  - accessToken
                isActive:
                  type: boolean
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/agents/settings/copilot:
    get:
      operationId: getAgentsSettingsCopilot
      tags:
      - AI Studio
      summary: List Copilot
      description: |-
        Legacy copilot/first-take-care settings - now managed via AI Employees in AI Studio

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/agents.ts:727` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:727
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "410":
          description: Gone - the resource expired or was superseded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putAgentsSettingsCopilot
      tags:
      - AI Studio
      summary: Replace Copilot
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:731` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:731
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "410":
          description: Gone - the resource expired or was superseded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/agents/settings/first-take-care:
    get:
      operationId: getAgentsSettingsFirstTakeCare
      tags:
      - AI Studio
      summary: List First Take Care
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/agents.ts:735` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:735
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "410":
          description: Gone - the resource expired or was superseded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putAgentsSettingsFirstTakeCare
      tags:
      - AI Studio
      summary: Replace First Take Care
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:739` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:739
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "410":
          description: Gone - the resource expired or was superseded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/agents/settings/idle-automation:
    get:
      operationId: getAgentsSettingsIdleAutomation
      tags:
      - AI Studio
      summary: List Idle Automation
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/agents.ts:678` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:678
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putAgentsSettingsIdleAutomation
      tags:
      - AI Studio
      summary: Replace Idle Automation
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:700` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:700
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      requestBody:
        required: true
        description: Validated by `idleAutomationSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                reminderEnabled:
                  type: boolean
                reminderDelayMinutes:
                  type: number
                  minimum: 1
                  maximum: 10080
                reminderMessage:
                  type: string
                  maxLength: 1000
                autoCloseEnabled:
                  type: boolean
                autoCloseDelayMinutes:
                  type: number
                  minimum: 1
                  maximum: 10080
                autoCloseMessage:
                  type: string
                  maxLength: 1000
              required:
              - reminderEnabled
              - autoCloseEnabled
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/agents/settings/locale:
    put:
      operationId: putAgentsSettingsLocale
      tags:
      - AI Studio
      summary: Replace Locale
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:801` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:801
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      x-database:
      - tenant.update
      requestBody:
        required: true
        description: Validated by `localeSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                locale:
                  x-zod: union
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/agents/settings/sla:
    get:
      operationId: getAgentsSettingsSla
      tags:
      - AI Studio
      summary: List Sla
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/agents.ts:608` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:608
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putAgentsSettingsSla
      tags:
      - AI Studio
      summary: Replace Sla
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:623` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:623
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      requestBody:
        required: true
        description: Validated by `slaSettingsSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled:
                  type: boolean
                slaMinutes:
                  type: number
                  minimum: 1
                  maximum: 1440
                warningThreshold:
                  type: number
                  minimum: 0
                  maximum: 100
              required:
              - enabled
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/agents/settings/sla/department/{departmentId}:
    get:
      operationId: getAgentsSettingsSlaDepartmentDepartmentId
      tags:
      - AI Studio
      summary: Get Department
      description: |-
        Department-level SLA override

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/agents.ts:635` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:635
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      parameters:
      - name: departmentId
        in: path
        required: true
        schema:
          type: string
        description: "`departmentId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putAgentsSettingsSlaDepartmentDepartmentId
      tags:
      - AI Studio
      summary: Replace Department
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/agents.ts:650` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/agents.ts:650
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      x-database:
      - department.findFirst
      parameters:
      - name: departmentId
        in: path
        required: true
        schema:
          type: string
        description: "`departmentId` path segment."
      requestBody:
        required: true
        description: Validated by `slaSettingsSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled:
                  type: boolean
                slaMinutes:
                  type: number
                  minimum: 1
                  maximum: 1440
                warningThreshold:
                  type: number
                  minimum: 0
                  maximum: 100
              required:
              - enabled
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/ai-agents:
    get:
      operationId: getAiAgents
      tags:
      - AI Studio
      summary: List Ai Agents
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/ai-agents.ts:63` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agents.ts:63
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:employees:read OR role ADMIN
      x-database:
      - |-
          department.findMany({
                where: { tenantId: req
      - department.findMany
      - |-
          conversation.findFirst({
                  where: {
                    tenantId: req
      - conversation.findFirst
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postAiAgents
      tags:
      - AI Studio
      summary: Create Ai Agents
      description: |-
        Plan gates run BEFORE the handler:
          requireEntitlement("ai.employee")     - is this capability sold on the plan?
          requireCapacity("limit:ai_employees") - is there headroom under the limit?
        Both return a structured 402 the frontend can act on. Hiding the "New
        employee" button is not enforcement; this is.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-agents.ts:344` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agents.ts:344
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:employees:create OR role ADMIN
        entitlements:
        - ai.employee
        planLimits:
        - limit:ai_employees
      x-database:
      - aIAgent.create
      - aIAgentKnowledge.createMany
      - agentToolPermission.createMany
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "422":
          description: Semantically invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-agents/{id}:
    delete:
      operationId: deleteAiAgentsId
      tags:
      - AI Studio
      summary: Delete Ai Agent
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-agents.ts:910` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agents.ts:910
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:employees:delete OR role ADMIN
      x-database:
      - aIAgent.findFirst
      - routerRule.deleteMany
      - aIAgent.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: getAiAgentsId
      tags:
      - AI Studio
      summary: Get Ai Agent
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/ai-agents.ts:250` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agents.ts:250
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:employees:read OR role ADMIN
      x-database:
      - aIAgent.findFirst
      - agentToolPermission.findMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchAiAgentsId
      tags:
      - AI Studio
      summary: Update Ai Agent
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-agents.ts:482` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agents.ts:482
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:employees:update OR role ADMIN
      x-database:
      - aIAgent.findFirst
      - aIAgentKnowledge.count
      - |-
          routerRule.updateMany({
                  where: { tenantId: req
      - routerRule.updateMany
      - aIAgentKnowledge.deleteMany
      - aIAgentKnowledge.createMany
      - agentToolPermission.deleteMany
      - agentToolPermission.createMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "422":
          description: Semantically invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-agents/{id}/effective-permissions:
    get:
      operationId: getAiAgentsIdEffectivePermissions
      tags:
      - AI Studio
      summary: Get Effective Permissions
      description: |-
        The SINGLE source of truth for "what can this employee actually do right
        now": the runtime AND-rule - an operation is EFFECTIVE only when its
        capability is live (CONNECTED / bookable / KB attached) AND (for tool-governed
        domains) an AgentToolPermission grants it. Reuses the exact permissions bridge
        + capability world the kernel uses, so the UI never disagrees with the runtime.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/ai-agents.ts:810` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agents.ts:810
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:employees:read OR role ADMIN
      x-database:
      - aIAgent.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/ai-agents/{id}/reachability:
    get:
      operationId: getAiAgentsIdReachability
      tags:
      - AI Studio
      summary: Get Reachability
      description: |-
        Runtime routing is exclusively the FlowCanvas graph: an ACTIVE employee
        with no agent-node targeting it NEVER receives a conversation. This
        endpoint tells the UI the truth so "go live" can't be a broken promise.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/ai-agents.ts:778` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agents.ts:778
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:employees:read OR role ADMIN
      x-database:
      - aIAgent.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/ai-agents/{id}/tools/{tenantToolId}:
    put:
      operationId: putAiAgentsIdToolsTenantToolId
      tags:
      - AI Studio
      summary: Replace Tool
      description: |-
        Used by the IntegrationDrawer when opened from the agent page so that
        flipping one tool switch persists immediately (creates/updates a single
        `AgentToolPermission` row) instead of requiring the caller to send the
        full toolIds array via the PATCH path. The drawer also continues to
        hit `PUT /api/integrations/:slug/tools/:slug` to ensure the underlying
        `TenantTool` is enabled - both must be true for the bot to see the tool.

        Body: { isAllowed: boolean, requireApproval?: boolean }

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-agents.ts:705` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agents.ts:705
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:tools:assign OR role ADMIN
      x-database:
      - aIAgent.findFirst
      - tenantTool.findFirst
      - agentToolPermission.findFirst
      - agentToolPermission.update
      - agentToolPermission.create
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: tenantToolId
        in: path
        required: true
        schema:
          type: string
        description: "`tenantToolId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-agents/builder/{id}/complete:
    post:
      operationId: postAiAgentsBuilderIdComplete
      tags:
      - AI Studio
      summary: Create Complete
      description: |-
        Called when the wizard hands the admin off to the editor (readiness panel
        "Save live" / "Skip to editor"). Clears the resumable progress pointer so
        the editor renders instead of re-entering the builder, and promotes the
        finished employee DRAFT → ACTIVE. PAUSED/ACTIVE agents keep their status
        (only the step pointer is cleared) so this can't reactivate a paused agent.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-agent-builder.ts:404` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agent-builder.ts:404
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - aIAgent.findFirst
      - aIAgent.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "422":
          description: Semantically invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-agents/builder/{id}/draft:
    get:
      operationId: getAiAgentsBuilderIdDraft
      tags:
      - AI Studio
      summary: Get Draft
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/ai-agent-builder.ts:453` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agent-builder.ts:453
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/ai-agents/builder/{id}/finalize:
    post:
      operationId: postAiAgentsBuilderIdFinalize
      tags:
      - AI Studio
      summary: Create Finalize
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-agent-builder.ts:469` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agent-builder.ts:469
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-agents/builder/{id}/knowledge:
    post:
      operationId: postAiAgentsBuilderIdKnowledge
      tags:
      - AI Studio
      summary: Create Knowledge
      description: |-
        POST /:id/knowledge - attach/detach one knowledge base to the draft.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-agent-builder.ts:287` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agent-builder.ts:287
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - aIAgent.findFirst
      - knowledgeBase.findFirst
      - aIAgentKnowledge.createMany
      - aIAgentKnowledge.deleteMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-agents/builder/{id}/options:
    get:
      operationId: getAiAgentsBuilderIdOptions
      tags:
      - AI Studio
      summary: Get Options
      description: |-
        Returns the tenant's available KBs + connected action tools with an
        `attached` flag for this draft, so the panel can render selectable cards
        instead of forcing the admin to dictate them over chat.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/ai-agent-builder.ts:180` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agent-builder.ts:180
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - aIAgent.findFirst
      - tenantTool.findMany
      - knowledgeBase.findMany
      - agentToolPermission.findMany
      - aIAgentKnowledge.findMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/ai-agents/builder/{id}/readiness-test:
    post:
      operationId: postAiAgentsBuilderIdReadinessTest
      tags:
      - AI Studio
      summary: Create Readiness Test
      description: |-
        Generates the realistic customer questions this employee will face, scores
        how well it's covered (knowledge + tools), and lists concrete gap fixes.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-agent-builder.ts:487` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agent-builder.ts:487
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - aIAgent.findFirst
      - aIAgent.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-agents/builder/{id}/refinements:
    post:
      operationId: postAiAgentsBuilderIdRefinements
      tags:
      - AI Studio
      summary: Create Refinements
      description: |-
        POST /:id/refinements - save the optional creation-wizard refinements
        (display name, conversation flow, custom guardrails) DETERMINISTICALLY,
        from the dedicated wizard step - instead of relying on the conversational
        builder to volunteer them. Send only the fields you're changing; an empty
        array clears that field. Returns the refreshed draft snapshot.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-agent-builder.ts:314` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agent-builder.ts:314
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - aIAgent.findFirst
      - aIAgent.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-agents/builder/{id}/step:
    post:
      operationId: postAiAgentsBuilderIdStep
      tags:
      - AI Studio
      summary: Create Step
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-agent-builder.ts:377` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agent-builder.ts:377
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - aIAgent.findFirst
      - aIAgent.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-agents/builder/{id}/tool:
    post:
      operationId: postAiAgentsBuilderIdTool
      tags:
      - AI Studio
      summary: Create Tool
      description: |-
        POST /:id/tool - attach/detach one tool to the draft (checkbox toggle).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-agent-builder.ts:223` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agent-builder.ts:223
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - aIAgent.findFirst
      - tenantTool.findFirst
      - agentToolPermission.findFirst
      - agentToolPermission.update
      - agentToolPermission.create
      - agentToolPermission.deleteMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-agents/builder/{id}/tools/bulk:
    post:
      operationId: postAiAgentsBuilderIdToolsBulk
      tags:
      - AI Studio
      summary: Create Bulk
      description: |-
        POST /:id/tools/bulk - attach/detach many tools at once (Select all /
        per-category select-all in the wizard). Validates ids against the tenant,
        then flips them in two queries instead of one round-trip per tool.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-agent-builder.ts:250` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agent-builder.ts:250
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - aIAgent.findFirst
      - tenantTool.findMany
      - agentToolPermission.createMany
      - agentToolPermission.updateMany
      - agentToolPermission.deleteMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-agents/builder/run:
    post:
      operationId: postAiAgentsBuilderRun
      tags:
      - AI Studio
      summary: Create Run
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-agent-builder.ts:518` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agent-builder.ts:518
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - aIAgent.findFirst
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-agents/builder/start:
    post:
      operationId: postAiAgentsBuilderStart
      tags:
      - AI Studio
      summary: Create Start
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-agent-builder.ts:104` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agent-builder.ts:104
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - businessProfile.findUnique
      - aIAgent.findFirst
      - aIAgent.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-agents/generate:
    post:
      operationId: postAiAgentsGenerate
      tags:
      - AI Studio
      summary: Create Generate
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-agents.ts:115` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-agents.ts:115
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:employees:update OR role ADMIN
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-assist/{conversationId}/adapter-tools/execute:
    post:
      operationId: postAiAssistConversationIdAdapterToolsExecute
      tags:
      - AI Studio
      summary: Create Execute
      description: |-
        Bridge endpoint for adapter-framework providers (HubSpot, Salesforce,
        Monday, etc.) - they aren't HTTP-catalog tools, so the shared CRM client
        in `packages/shared/lib/crm.ts` reaches them through this endpoint.

        Body: { toolFunctionName: "hubspot.search_with_criteria", args: {...} }

        Returns the adapter result (whatever shape the provider's execute()
        function returns) on `data.output`, mirroring the catalog-tool execute
        envelope so callers can treat both paths uniformly.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-assist.ts:708` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:708
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-assist/{conversationId}/analyze:
    post:
      operationId: postAiAssistConversationIdAnalyze
      tags:
      - AI Studio
      summary: Create Analyze
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-assist.ts:651` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:651
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-assist/{conversationId}/chat:
    post:
      operationId: postAiAssistConversationIdChat
      tags:
      - AI Studio
      summary: Create Chat
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-assist.ts:543` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:543
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - conversation.findFirst
      - message.findMany
      x-service-calls:
      - aiService.getEffectiveCopilotConfig
      - aiService.chatWithAgent
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-assist/{conversationId}/crm/find:
    post:
      operationId: postAiAssistConversationIdCrmFind
      tags:
      - AI Studio
      summary: Create Find
      description: |-
        Search the connected system-of-record by email/phone through the
        CRMAdapter.findCustomer interface. Unlike the per-vendor catalog
        `lead_search`/`contact_search` tools (which only Zoho-style providers
        register), this covers EVERY source-of-truth integration connectable at
        onboarding - HubSpot, Salesforce, Zoho, Shopify, Fireberry, Airtable -
        through one code path. Shared's searchLeads/searchContacts fall back here
        for providers that don't expose the catalog search tools.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-assist.ts:741` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:741
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-assist/{conversationId}/followup:
    post:
      operationId: postAiAssistConversationIdFollowup
      tags:
      - AI Studio
      summary: Create Followup
      description: |-
        F6.3 - generate a contextual follow-up for a stale conversation

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-assist.ts:842` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:842
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-assist/{conversationId}/intelligence:
    get:
      operationId: getAiAssistConversationIdIntelligence
      tags:
      - AI Studio
      summary: Get Intelligence
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/ai-assist.ts:643` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:643
      x-operation-type: read
      x-publicly-exposed: true
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/ai-assist/{conversationId}/replay:
    get:
      operationId: getAiAssistConversationIdReplay
      tags:
      - AI Studio
      summary: Get Replay
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/ai-assist.ts:659` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:659
      x-operation-type: read
      x-publicly-exposed: true
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/ai-assist/{conversationId}/score:
    get:
      operationId: getAiAssistConversationIdScore
      tags:
      - AI Studio
      summary: Get Score
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/ai-assist.ts:765` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:765
      x-operation-type: read
      x-publicly-exposed: true
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postAiAssistConversationIdScore
      tags:
      - AI Studio
      summary: Create Score
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-assist.ts:780` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:780
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-assist/{conversationId}/suggestions:
    get:
      operationId: getAiAssistConversationIdSuggestions
      tags:
      - AI Studio
      summary: Get Suggestions
      description: |-
        Chat Copilot is sold separately from the core inbox, so the suggestion
        endpoint is gated rather than merely hidden in the UI.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/ai-assist.ts:366` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:366
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        entitlements:
        - ai.copilot
      x-database:
      - conversation.findFirst
      - message.findMany
      - department.findUnique
      - user.findUnique
      x-service-calls:
      - aiService.getEffectiveCopilotConfig
      - aiService.getSuggestions
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      - name: ri
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: locale
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/ai-assist/{conversationId}/summary:
    get:
      operationId: getAiAssistConversationIdSummary
      tags:
      - AI Studio
      summary: Get Summary
      description: |-
        Gated on communication.crm_summaries - the SAME key as the background
        pipeline, and deliberately NOT ai.copilot. Foundation denies Copilot and
        grants summaries; gating this route on ai.copilot would break exactly the
        plan combination the product sells.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/ai-assist.ts:500` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:500
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        entitlements:
        - communication.crm_summaries
      x-database:
      - conversation.findFirst
      - message.findMany
      x-service-calls:
      - aiService.getEffectiveCopilotConfig
      - aiService.summarizeConversation
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      - name: locale
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/ai-assist/{conversationId}/tools:
    get:
      operationId: getAiAssistConversationIdTools
      tags:
      - AI Studio
      summary: Get Tools
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/ai-assist.ts:669` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:669
      x-operation-type: read
      x-publicly-exposed: true
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/ai-assist/{conversationId}/tools/execute:
    post:
      operationId: postAiAssistConversationIdToolsExecute
      tags:
      - AI Studio
      summary: Create Execute
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-assist.ts:677` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:677
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-assist/compose:
    post:
      operationId: postAiAssistCompose
      tags:
      - AI Studio
      summary: Create Compose
      description: |-
        Drafts a single outbound message from a natural-language instruction.
        Used by: template editor, scheduled-message composer, inbox input,
        and the Command Center. Returns plain text (no JSON).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-assist.ts:274` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:274
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - conversation.findFirst
      - message.findMany
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-assist/config:
    get:
      operationId: getAiAssistConfig
      tags:
      - AI Studio
      summary: List Config
      description: |-
        Static routes BEFORE parameterized routes

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/ai-assist.ts:263` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:263
      x-operation-type: read
      x-publicly-exposed: true
      x-service-calls:
      - aiService.getTenantCopilotConfig
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/ai-assist/customer-state/{contactId}:
    get:
      operationId: getAiAssistCustomerStateContactId
      tags:
      - AI Studio
      summary: Get Customer State
      description: |-
        F7.2/F7.5 - compact customer state object (decisions, summaries, tags)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/ai-assist.ts:824` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:824
      x-operation-type: read
      x-publicly-exposed: true
      parameters:
      - name: contactId
        in: path
        required: true
        schema:
          type: string
        description: "`contactId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/ai-assist/discover-business:
    post:
      operationId: postAiAssistDiscoverBusiness
      tags:
      - AI Studio
      summary: Create Discover Business
      description: |-
        The deep 5-domain website scan that produces the Business Intelligence
        Report + first recommendation (Bible Part II). The auth onboarding route
        fetches the pages + detects signals (no LLM) and calls this with the admin
        JWT forwarded - this is the one place onboarding is allowed to make an LLM
        call. Best-effort: returns { ok:false } on a scan miss so the caller can
        fall back to the shallow understanding and never block onboarding.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-assist.ts:41` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:41
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-assist/generate-config/{departmentId}:
    post:
      operationId: postAiAssistGenerateConfigDepartmentId
      tags:
      - AI Studio
      summary: DepartmentId action
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-assist.ts:132` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:132
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - department.findFirst
      parameters:
      - name: departmentId
        in: path
        required: true
        schema:
          type: string
        description: "`departmentId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-assist/generate-configs:
    post:
      operationId: postAiAssistGenerateConfigs
      tags:
      - AI Studio
      summary: Create Generate Configs
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-assist.ts:104` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:104
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - aIAgent.findMany
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-assist/intent:
    post:
      operationId: postAiAssistIntent
      tags:
      - AI Studio
      summary: Create Intent
      description: |-
        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-assist.ts:152` (ai service).
      security:
      - internalServiceToken: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:152
      x-operation-type: write
      x-publicly-exposed: true
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-assist/onboarding-employee-chat:
    post:
      operationId: postAiAssistOnboardingEmployeeChat
      tags:
      - AI Studio
      summary: Create Onboarding Employee Chat
      description: |-
        Onboarding Movement 8 - chat with the recommended employee before deploy.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-assist.ts:74` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:74
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-assist/policy:
    get:
      operationId: getAiAssistPolicy
      tags:
      - AI Studio
      summary: List Policy
      description: |-
        F8.4 - business policy admin API (UI-neutral)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/ai-assist.ts:796` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:796
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putAiAssistPolicy
      tags:
      - AI Studio
      summary: Replace Policy
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-assist.ts:806` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:806
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-assist/prompt/{departmentId}:
    get:
      operationId: getAiAssistPromptDepartmentId
      tags:
      - AI Studio
      summary: Get Prompt
      description: |-
        Get effective copilot config for a department (SYSTEM_ADMIN only)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/ai-assist.ts:607` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:607
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - SYSTEM_ADMIN
      x-service-calls:
      - aiService.getEffectiveCopilotConfig
      parameters:
      - name: departmentId
        in: path
        required: true
        schema:
          type: string
        description: "`departmentId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/ai-assist/tools/registry:
    get:
      operationId: getAiAssistToolsRegistry
      tags:
      - AI Studio
      summary: List Registry
      description: |-
        Tools registry - MUST be before /:conversationId routes

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/ai-assist.ts:255` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-assist.ts:255
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/ai-bot/execution-message:
    post:
      operationId: postAiBotExecutionMessage
      tags:
      - Internal
      summary: Create Execution Message
      description: |-
        POST /api/ai-bot/oneshot
          body: { tenantId, aiAgentId, userInput, maxTokens?, feature? }
          returns: { reply, modelUsed, totalTokens }
        POST /api/ai-bot/execution-message
          The ONE generator for post-execution customer messages (approval
          continuations, proactive completion updates). Same humanizer/style layer
          as live replies + deterministic grounding validation with a safe
          template fallback - callers always get a sendable, fact-true message.
          body: { tenantId, aiAgentId, facts, inboundSample?, customerName? }

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-bot.ts:62` (ai service).
      security:
      - internalServiceToken: []
      x-service: ai
      x-source: services/ai/src/routes/ai-bot.ts:62
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-bot/oneshot:
    post:
      operationId: postAiBotOneshot
      tags:
      - Internal
      summary: Create Oneshot
      description: |-
        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-bot.ts:83` (ai service).
      security:
      - internalServiceToken: []
      x-service: ai
      x-source: services/ai/src/routes/ai-bot.ts:83
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-bot/reply:
    post:
      operationId: postAiBotReply
      tags:
      - Internal
      summary: Create Reply
      description: |-
        POST /api/ai-bot/reply
          body: { tenantId, conversationId, aiAgentId, incomingMessage, closedHours? }
          `closedHours` (worker-computed from the tenant's persisted business hours)
          marks the business as currently CLOSED with the "active" outside-hours
          policy - the reply must not imply immediate human availability.
          returns: AIBotReplyResult

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/ai/src/routes/ai-bot.ts:26` (ai service).
      security:
      - internalServiceToken: []
      x-service: ai
      x-source: services/ai/src/routes/ai-bot.ts:26
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "499":
          description: HTTP 499
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/ai-skills:
    get:
      operationId: getAiSkills
      tags:
      - AI Studio
      summary: List Ai Skills
      description: |-
        GET /api/ai-skills

        Returns the catalog of system skills available to compose into an AI
        Employee. Grouped by `kind` (operational / language / execution) for
        the wizard's checklist UI.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/ai-skills.ts:30` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/ai-skills.ts:30
      x-operation-type: read
      x-publicly-exposed: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/analytics/agents:
    get:
      operationId: getAnalyticsAgents
      tags:
      - Analytics
      summary: List Agents
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/analytics/src/routes/analytics.ts:50` (analytics service).
      security:
      - bearerAuth: []
      x-service: analytics
      x-source: services/analytics/src/routes/analytics.ts:50
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-service-calls:
      - analyticsService.getAgentStats
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/analytics/ai-performance:
    get:
      operationId: getAnalyticsAiPerformance
      tags:
      - Analytics
      summary: List Ai Performance
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/analytics/src/routes/analytics.ts:153` (analytics service).
      security:
      - bearerAuth: []
      x-service: analytics
      x-source: services/analytics/src/routes/analytics.ts:153
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-service-calls:
      - analyticsService.getAIPerformance
      parameters:
      - name: from
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: to
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/analytics/channel-performance:
    get:
      operationId: getAnalyticsChannelPerformance
      tags:
      - Analytics
      summary: List Channel Performance
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/analytics/src/routes/analytics.ts:129` (analytics service).
      security:
      - bearerAuth: []
      x-service: analytics
      x-source: services/analytics/src/routes/analytics.ts:129
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-service-calls:
      - analyticsService.getChannelPerformance
      parameters:
      - name: from
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: to
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/analytics/daily:
    get:
      operationId: getAnalyticsDaily
      tags:
      - Analytics
      summary: List Daily
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/analytics/src/routes/analytics.ts:71` (analytics service).
      security:
      - bearerAuth: []
      x-service: analytics
      x-source: services/analytics/src/routes/analytics.ts:71
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-service-calls:
      - analyticsService.getDailyVolume
      parameters:
      - name: days
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/analytics/dashboard:
    get:
      operationId: getAnalyticsDashboard
      tags:
      - Analytics
      summary: List Dashboard
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/analytics/src/routes/analytics.ts:40` (analytics service).
      security:
      - bearerAuth: []
      x-service: analytics
      x-source: services/analytics/src/routes/analytics.ts:40
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-service-calls:
      - analyticsService.getDashboardStats
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/analytics/department-performance:
    get:
      operationId: getAnalyticsDepartmentPerformance
      tags:
      - Analytics
      summary: List Department Performance
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/analytics/src/routes/analytics.ts:141` (analytics service).
      security:
      - bearerAuth: []
      x-service: analytics
      x-source: services/analytics/src/routes/analytics.ts:141
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-service-calls:
      - analyticsService.getDepartmentPerformance
      parameters:
      - name: from
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: to
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/analytics/events:
    post:
      operationId: postAnalyticsEvents
      tags:
      - Analytics
      summary: Create Events
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/analytics/src/routes/analytics.ts:16` (analytics service).
      security:
      - bearerAuth: []
      x-service: analytics
      x-source: services/analytics/src/routes/analytics.ts:16
      x-operation-type: write
      x-publicly-exposed: true
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/analytics/hourly:
    get:
      operationId: getAnalyticsHourly
      tags:
      - Analytics
      summary: List Hourly
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/analytics/src/routes/analytics.ts:60` (analytics service).
      security:
      - bearerAuth: []
      x-service: analytics
      x-source: services/analytics/src/routes/analytics.ts:60
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-service-calls:
      - analyticsService.getHourlyVolume
      parameters:
      - name: date
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/analytics/insights:
    get:
      operationId: getAnalyticsInsights
      tags:
      - Analytics
      summary: List Insights
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/analytics/src/routes/analytics.ts:165` (analytics service).
      security:
      - bearerAuth: []
      x-service: analytics
      x-source: services/analytics/src/routes/analytics.ts:165
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-service-calls:
      - analyticsService.getAIInsights
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/analytics/overview:
    get:
      operationId: getAnalyticsOverview
      tags:
      - Analytics
      summary: List Overview
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/analytics/src/routes/analytics.ts:92` (analytics service).
      security:
      - bearerAuth: []
      x-service: analytics
      x-source: services/analytics/src/routes/analytics.ts:92
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-service-calls:
      - analyticsService.getOverview
      parameters:
      - name: from
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: to
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/analytics/queue:
    get:
      operationId: getAnalyticsQueue
      tags:
      - Analytics
      summary: List Queue
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/analytics/src/routes/analytics.ts:82` (analytics service).
      security:
      - bearerAuth: []
      x-service: analytics
      x-source: services/analytics/src/routes/analytics.ts:82
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-service-calls:
      - analyticsService.getQueueDepth
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/analytics/tool-usage:
    get:
      operationId: getAnalyticsToolUsage
      tags:
      - Analytics
      summary: List Tool Usage
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/analytics/src/routes/analytics.ts:117` (analytics service).
      security:
      - bearerAuth: []
      x-service: analytics
      x-source: services/analytics/src/routes/analytics.ts:117
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-service-calls:
      - analyticsService.getToolUsageStats
      parameters:
      - name: from
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: to
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/analytics/top-questions:
    get:
      operationId: getAnalyticsTopQuestions
      tags:
      - Analytics
      summary: List Top Questions
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/analytics/src/routes/analytics.ts:104` (analytics service).
      security:
      - bearerAuth: []
      x-service: analytics
      x-source: services/analytics/src/routes/analytics.ts:104
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-service-calls:
      - analyticsService.getTopQuestions
      parameters:
      - name: from
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: to
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: limit
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/approvals:
    get:
      operationId: getApprovals
      tags:
      - AI Studio
      summary: List Approvals
      description: |-
        GET /api/approvals
        List approval requests for this tenant. Supports:
          - ?status=PENDING       default
          - ?conversationId=xxx   scope to one conversation
          - ?contactId=xxx        scope to one contact

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/approvals.ts:896` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/approvals.ts:896
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - user.findMany
      parameters:
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: conversationId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: contactId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/approvals/{id}:
    get:
      operationId: getApprovalsId
      tags:
      - AI Studio
      summary: Get Approval
      description: |-
        GET /api/approvals/:id
        Full record + enrichment for the rich in-conversation approval card:
          - conversation + recent messages (last 5)
          - contact snapshot

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/approvals.ts:951` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/approvals.ts:951
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - conversation.findFirst
      - contact.findFirst
      - message.findMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/approvals/{id}/approve:
    post:
      operationId: postApprovalsIdApprove
      tags:
      - AI Studio
      summary: Create Approve
      description: |-
        POST /api/approvals/:id/approve
        Body: { decisionReason?, modifiedParams? }

        Separation of duties: the approver MUST NOT be the same actor who
        authored the originating bot action. For bot-initiated requests
        (requestedBy starts with "bot" / "flow:" / "ai-agent:") this is
        trivially satisfied - the actor is the bot, not a human. For
        human-initiated requests (rare under the F4 model), we reject
        same-actor approvals.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/approvals.ts:1005` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/approvals.ts:1005
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - conversation.findFirst
      - conversation.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/approvals/{id}/dispatch-approved:
    post:
      operationId: postApprovalsIdDispatchApproved
      tags:
      - AI Studio
      summary: Create Dispatch Approved
      description: |-
        Called by the incoming-worker after a manager approved on WhatsApp. The
        decision was already recorded there (through the same atomic CAS the UI
        uses); this endpoint only runs the action, so both channels converge on ONE
        execution path. Service-to-service auth ONLY - registered above the user
        auth middleware because there is no user session behind a WhatsApp tap.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/conversation/src/routes/approvals.ts:805` (conversation service).
      security:
      - internalServiceToken: []
      x-service: conversation
      x-source: services/conversation/src/routes/approvals.ts:805
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/approvals/{id}/dispatch-rejected:
    post:
      operationId: postApprovalsIdDispatchRejected
      tags:
      - AI Studio
      summary: Create Dispatch Rejected
      description: |-
        POST /api/approvals/:id/dispatch-rejected  (internal, no user session)

        The rejection counterpart of dispatch-approved. A manager who taps "reject"
        on WhatsApp recorded the decision through the same atomic CAS as the web UI
        - and then the customer heard nothing at all, because the continuation and
        the ownership reset lived only in the web route's handler. The approve path
        had already been unified behind one endpoint for exactly this reason; the
        reject path had not, so "declined on a phone" and "declined in the app" were
        two different products.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/conversation/src/routes/approvals.ts:853` (conversation service).
      security:
      - internalServiceToken: []
      x-service: conversation
      x-source: services/conversation/src/routes/approvals.ts:853
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/approvals/{id}/reject:
    post:
      operationId: postApprovalsIdReject
      tags:
      - AI Studio
      summary: Create Reject
      description: |-
        POST /api/approvals/:id/reject
        Body: { decisionReason }
        Rejection reason is REQUIRED - no silent "just no". The bot resume
        worker uses it to craft the fallback customer message.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/approvals.ts:1204` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/approvals.ts:1204
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - conversation.findFirst
      - conversation.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/approvals/{id}/retry-execution:
    post:
      operationId: postApprovalsIdRetryExecution
      tags:
      - AI Studio
      summary: Create Retry Execution
      description: |-
        POST /api/approvals/:id/retry-execution

        Safe manual retry for an approval whose EXECUTION failed (the decision
        stands - only the action re-runs). Guarded by the same claimForExecution
        CAS as the original dispatch: only APPROVED rows in FAILED (or stranded
        NOT_STARTED) execution state can be claimed, so a double-click or a race
        with the sweeper can never run the business action twice concurrently.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/approvals.ts:1155` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/approvals.ts:1155
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/audiences/preview:
    post:
      operationId: postAudiencesPreview
      tags:
      - Conversations
      summary: Create Preview
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/audiences.ts:35` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/audiences.ts:35
      x-operation-type: write
      x-publicly-exposed: true
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/audiences/resolve:
    post:
      operationId: postAudiencesResolve
      tags:
      - Conversations
      summary: Create Resolve
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/audiences.ts:97` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/audiences.ts:97
      x-operation-type: write
      x-publicly-exposed: true
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/audiences/schema:
    get:
      operationId: getAudiencesSchema
      tags:
      - Conversations
      summary: List Schema
      description: |-
        Schema discovery for the audience builder. Returns CRM fields only -
        the platform's local Contact fields are not surfaced here anymore (the
        platform contact table is a write-side cache, not a source of truth
        for audience targeting).

          GET /api/audiences/schema?module=leads|contacts|accounts|deals

        The `local` field block is preserved in the response shape (always
        empty) to keep older clients that destructure `data.local.fields`
        from crashing.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/audiences.ts:62` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/audiences.ts:62
      x-operation-type: read
      x-publicly-exposed: true
      parameters:
      - name: module
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/auth/callback:
    get:
      operationId: getAuthCallback
      tags:
      - Auth & Identity
      summary: List Callback
      description: |-
        GET /api/auth/callback → complete the flow, create the session, set the cookie.

        No authentication middleware is applied to this route.

        Implemented at `services/auth/src/routes/session-auth.ts:83` (auth service).
      security: []
      x-service: auth
      x-source: services/auth/src/routes/session-auth.ts:83
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - oidcLoginState.findUnique
      - oidcLoginState.update
      - identity.findUnique
      - userSession.create
      parameters:
      - name: error
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: code
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: state
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
  /api/auth/login:
    get:
      operationId: getAuthLogin
      tags:
      - Auth & Identity
      summary: List Login
      description: |-
        GET /api/auth/login → start the server-side Authorization-Code + PKCE flow.

        No authentication middleware is applied to this route.

        Implemented at `services/auth/src/routes/session-auth.ts:52` (auth service).
      security: []
      x-service: auth
      x-source: services/auth/src/routes/session-auth.ts:52
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - oidcLoginState.create
      parameters:
      - name: next
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: returnTo
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: remember
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: login_hint
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
  /api/auth/logout:
    post:
      operationId: postAuthLogout
      tags:
      - Auth & Identity
      summary: Create Logout
      description: |-
        POST /api/auth/logout → revoke the current session + clear the cookie.
        (Minimal here; logout-all / session-list / IdP end-session are commit 9.)

        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/auth/src/routes/session-auth.ts:152` (auth service).
      security: []
      x-service: auth
      x-source: services/auth/src/routes/session-auth.ts:152
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - userSession.updateMany
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
      x-documentation-gap: request-body-unschematized
  /api/auth/me:
    get:
      operationId: getAuthMe
      tags:
      - Auth & Identity
      summary: List Me
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/auth/src/routes/auth.ts:51` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/auth.ts:51
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - user.findUnique
      - departmentMember.findFirst
      - tenant.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/auth/me/memberships:
    get:
      operationId: getAuthMeMemberships
      tags:
      - Auth & Identity
      summary: List Memberships
      description: |-
        The caller's memberships, standalone - used by the post-login tenant picker
        BEFORE an active tenant is chosen. authenticate() resolves a default
        membership even without an X-Tenant-Id header, so this endpoint is reachable
        exactly when the picker needs it.

        Requires a valid Authentik access token.

        Implemented at `services/auth/src/routes/auth.ts:116` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/auth.ts:116
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - user.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/auth/me/switch-tenant:
    post:
      operationId: postAuthMeSwitchTenant
      tags:
      - Auth & Identity
      summary: Create Switch Tenant
      description: |-
        Switch the ACTIVE tenant. Validates the target against the caller's
        memberships (never trusts the id), stamps last-used so future hint-less
        requests and the picker default follow the person, and returns the target
        membership. The client then re-issues requests with X-Tenant-Id set - no
        re-login: the Authentik session/token identifies the PERSON, not the tenant.

        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/auth/src/routes/auth.ts:144` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/auth.ts:144
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - user.findUnique
      - user.update
      - identity.update
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/auth/setup/{token}:
    get:
      operationId: getAuthSetupToken
      tags:
      - Auth & Identity
      summary: Get Setup
      description: |-
        Redeem a setup link.

        Always a redirect, never a JSON body: this URL is opened by a human in a
        browser straight from an email, and the only useful response is the next
        screen.

        No authentication middleware is applied to this route.

        Implemented at `services/auth/src/routes/setup-link.ts:55` (auth service).
      security: []
      x-service: auth
      x-source: services/auth/src/routes/setup-link.ts:55
      x-operation-type: read
      x-publicly-exposed: true
      x-rate-limit:
      - redeemLimiter
      parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
        description: "`token` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/auth/setup/resend:
    post:
      operationId: postAuthSetupResend
      tags:
      - Auth & Identity
      summary: Create Resend
      description: |-
        Ask for a replacement link.

        Always the same 200, whatever happened - including when the token is unknown
        or the mail fails. The token is the identifier, so a wrong one must be
        indistinguishable from a valid one, or this endpoint becomes a way to test
        whether a token (and therefore an account) exists.

        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/auth/src/routes/setup-link.ts:117` (auth service).
      security: []
      x-service: auth
      x-source: services/auth/src/routes/setup-link.ts:117
      x-operation-type: write
      x-publicly-exposed: true
      x-rate-limit:
      - resendLimiter
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/auto-buy/orders:
    post:
      operationId: postAutoBuyOrders
      tags:
      - Internal
      summary: Create Orders
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/conversation/src/routes/auto-buy.ts:33` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/auto-buy.ts:33
      x-operation-type: write
      x-publicly-exposed: false
      x-authorization:
        featureFlags:
        - AUTO_BUY
      requestBody:
        required: true
        description: Validated by `placeOrderSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                productSku:
                  type: string
                  minLength: 1
                  maxLength: 200
                quantity:
                  type: integer
                  exclusiveMinimum: 0
                  maximum: 1000
                maxPriceCents:
                  type: integer
                  exclusiveMinimum: 0
              required:
              - productSku
              - quantity
      responses:
        "202":
          description: Accepted for asynchronous processing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/auto-buy/status:
    get:
      operationId: getAutoBuyStatus
      tags:
      - Internal
      summary: List Status
      description: |-
        Tenant-level status check (no user-level gate - any authenticated user
        can see whether the feature is available to the org, even if they can't
        personally place orders). Useful for the frontend to hide/show UI.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/conversation/src/routes/auto-buy.ts:60` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/auto-buy.ts:60
      x-operation-type: read
      x-publicly-exposed: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/billing/auto-purchase:
    get:
      operationId: getBillingAutoPurchase
      tags:
      - Billing
      summary: List Auto Purchase
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/billing/src/routes/credits.ts:210` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/credits.ts:210
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      x-database:
      - autoPurchasePolicy.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putBillingAutoPurchase
      tags:
      - Billing
      summary: Replace Auto Purchase
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/billing/src/routes/credits.ts:227` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/credits.ts:227
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      x-database:
      - plan.findUnique
      - autoPurchasePolicy.upsert
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "402":
          description: Payment required - the workspace is not in a billable state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/billing/credit-summary:
    get:
      operationId: getBillingCreditSummary
      tags:
      - Billing
      summary: List Credit Summary
      description: |-
        Canonical customer-facing credit contract - ONE source for every number the
        Usage/Billing UI shows. Credits (not model tokens) are the unit throughout.
        Composes: the subscription's included allowance, the real ledger balance
        (getBalance), and the auto-purchase money-spend window. The two are kept
        strictly separate: `usage` is plan-CREDIT consumption; `usageCredits` is
        MONEY spent on automatic top-ups. Read-only for any tenant member.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/billing/src/routes/credits.ts:37` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/credits.ts:37
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - autoPurchasePolicy.findUnique
      - plan.findUnique
      - aiUnitLot.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/billing/credits/balance:
    get:
      operationId: getBillingCreditsBalance
      tags:
      - Billing
      summary: List Balance
      description: |-
        Separate INCLUDED (resets) vs PURCHASED (permanent) balances for the UI.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/billing/src/routes/credits.ts:162` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/credits.ts:162
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/billing/credits/buy:
    post:
      operationId: postBillingCreditsBuy
      tags:
      - Billing
      summary: Create Buy
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/billing/src/routes/credits.ts:188` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/credits.ts:188
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/billing/credits/packages:
    get:
      operationId: getBillingCreditsPackages
      tags:
      - Billing
      summary: List Packages
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/billing/src/routes/credits.ts:176` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/credits.ts:176
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - creditPackage.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/billing/invoices:
    get:
      operationId: getBillingInvoices
      tags:
      - Billing
      summary: List Invoices
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/billing/src/routes/invoices.ts:18` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/invoices.ts:18
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      x-database:
      - invoice.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/billing/payment-methods:
    get:
      operationId: getBillingPaymentMethods
      tags:
      - Billing
      summary: List Payment Methods
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/billing/src/routes/payment-methods.ts:78` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/payment-methods.ts:78
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      x-database:
      - billableEntityTenant.findUnique
      - billingProfile.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/billing/payment-methods/{id}:
    delete:
      operationId: deleteBillingPaymentMethodsId
      tags:
      - Billing
      summary: Delete Payment Method
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/billing/src/routes/payment-methods.ts:189` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/payment-methods.ts:189
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      x-database:
      - billableEntityTenant.findUnique
      - billingProfile.findUnique
      - paymentMethod.count
      - subscription.findUnique
      - plan.findFirst
      - paymentMethod.updateMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/billing/payment-methods/confirm:
    post:
      operationId: postBillingPaymentMethodsConfirm
      tags:
      - Billing
      summary: Create Confirm
      description: |-
        Confirm that a card was stored.

        Takes only the session id - no token, no card details, no success flag. The
        answer comes from querying the provider and comparing against the baseline
        captured when the session started, so a person who abandoned the page cannot
        end up with a card they never entered attributed to this attempt.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/billing/src/routes/payment-methods.ts:132` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/payment-methods.ts:132
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      x-database:
      - tokenizationSession.findUnique
      - paymentMethod.findUnique
      - paymentMethod.updateMany
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/billing/payment-methods/session:
    post:
      operationId: postBillingPaymentMethodsSession
      tags:
      - Billing
      summary: Create Session
      description: |-
        Start adding (or replacing) a card.

        Returns where to send the person. Their browser then comes back and calls
        confirm, which asks the provider what actually happened.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/billing/src/routes/payment-methods.ts:91` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/payment-methods.ts:91
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/billing/plans:
    get:
      operationId: getBillingPlans
      tags:
      - Billing
      summary: List Plans
      description: |-
        Tenant-scoped so an organization sees its OWN custom plan alongside the
        public catalog - and never another organization's negotiated terms.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/billing/src/routes/subscription.ts:16` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/subscription.ts:16
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/billing/pricing:
    get:
      operationId: getBillingPricing
      tags:
      - Billing
      summary: List Pricing
      description: |-
        The pricing page payload: the plan catalog, the display-currency options and
        the FX attribution behind an ILS rendering.

        Tenant-scoped so a custom plan negotiated for this organization appears
        alongside the public catalog - and no one else's does.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/billing/src/routes/pricing.ts:44` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/pricing.ts:44
      x-operation-type: read
      x-publicly-exposed: true
      parameters:
      - name: currency
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/billing/pricing/change:
    post:
      operationId: postBillingPricingChange
      tags:
      - Billing
      summary: Create Change
      description: |-
        Apply a plan / volume change. Owner-gated, provider-confirmed, and priced
        from the catalog rather than from anything the client sent.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/billing/src/routes/pricing.ts:185` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/pricing.ts:185
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/billing/pricing/current:
    get:
      operationId: getBillingPricingCurrent
      tags:
      - Billing
      summary: List Current
      description: |-
        The current subscription rendered from its stored snapshot, so a published
        plan change never restates what an existing customer sees.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/billing/src/routes/pricing.ts:115` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/pricing.ts:115
      x-operation-type: read
      x-publicly-exposed: true
      parameters:
      - name: currency
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/billing/pricing/packages:
    get:
      operationId: getBillingPricingPackages
      tags:
      - Billing
      summary: List Packages
      description: |-
        The purchasable credit packages for THIS organization's plan.

        Filters on customer visibility, the active window and plan eligibility, so a
        retired or plan-restricted package never renders as buyable and then fails at
        checkout.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/billing/src/routes/pricing.ts:135` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/pricing.ts:135
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - plan.findUnique
      - creditPackage.findMany
      parameters:
      - name: currency
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/billing/pricing/quote:
    post:
      operationId: postBillingPricingQuote
      tags:
      - Billing
      summary: Create Quote
      description: |-
        Price a concrete selection. The body carries KEYS only; price, credits and
        estimate are all recomputed server-side from the catalog.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/billing/src/routes/pricing.ts:79` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/pricing.ts:79
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: currency
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/billing/profile:
    get:
      operationId: getBillingProfile
      tags:
      - Billing
      summary: List Profile
      description: |-
        The identity a tax document is made out to.

        Deliberately separate from the payment method: the card and the entity being
        invoiced are different facts, and one is not evidence of the other.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/billing/src/routes/payment-methods.ts:235` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/payment-methods.ts:235
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putBillingProfile
      tags:
      - Billing
      summary: Replace Profile
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/billing/src/routes/payment-methods.ts:239` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/payment-methods.ts:239
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      x-database:
      - billableEntityTenant.findUnique
      - billingProfile.findUnique
      - billingProfile.update
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/billing/providers/icount/ipn:
    post:
      operationId: postBillingProvidersIcountIpn
      tags:
      - Billing
      summary: Create Ipn
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/billing/src/routes/icount-ipn.ts:88` (billing service).
      security: []
      x-service: billing
      x-source: services/billing/src/routes/icount-ipn.ts:88
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - billingWebhookEvent.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "413":
          description: Payload too large
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/billing/shopify/complete:
    post:
      operationId: postBillingShopifyComplete
      tags:
      - Billing
      summary: Create Complete
      description: |-
        Called by `/integrations/shopify/billing/complete` after Shopify sends the
        merchant back.

        Everything that matters happens server-side:

          1. the store is taken from the connection this SESSION owns, not from the
             `shop` parameter Shopify appended;
          2. `syncProviderSubscription` asks Shopify what the subscription actually
             is, and writes down what it says;
          3. entitlements move to match, inside that same function;
          4. the answer returned is the freshly computed state, so the page renders
             what was verified rather than what was hoped.

        A merchant who declined gets `PLAN_SELECTION_REQUIRED` back and no
        entitlements - the same answer as never having visited, which is correct,
        because declining and never arriving are commercially identical.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/billing/src/routes/shopify-billing.ts:204` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/shopify-billing.ts:204
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      parameters:
      - name: shop
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/billing/shopify/grandfather/evaluate:
    post:
      operationId: postBillingShopifyGrandfatherEvaluate
      tags:
      - Billing
      summary: Create Evaluate
      description: |-
        Evaluate and, if earned, record grandfathered eligibility.

        Idempotent: the underlying service returns a standing grant rather than
        deciding again. Exposed to the tenant's own billing manager because it takes
        NO input - eligibility is computed entirely from our own records, so there is
        nothing here for a caller to influence.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/billing/src/routes/shopify-billing.ts:285` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/shopify-billing.ts:285
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/billing/shopify/plan-selection:
    post:
      operationId: postBillingShopifyPlanSelection
      tags:
      - Billing
      summary: Create Plan Selection
      description: |-
        Where to send the merchant to choose and approve a plan.

        Returns a URL rather than redirecting so the caller can decide how to
        navigate, and so a merchant who is already grandfathered gets a refusal with
        a reason instead of a trip to a plan page they must never see.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/billing/src/routes/shopify-billing.ts:128` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/shopify-billing.ts:128
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "503":
          description: Temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/billing/shopify/plans:
    get:
      operationId: getBillingShopifyPlans
      tags:
      - Billing
      summary: List Plans
      description: |-
        The plans this store could be offered.

        Returns identifiers and our own metadata only - no price, no currency, no
        trial length. Those belong to Shopify and are shown on Shopify's page. A
        price rendered here would be a second copy of a number we do not own.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/billing/src/routes/shopify-billing.ts:106` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/shopify-billing.ts:106
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/billing/shopify/state:
    get:
      operationId: getBillingShopifyState
      tags:
      - Billing
      summary: List State
      description: |-
        Everything the UI needs to render a Shopify billing state.

        Readable by any authenticated member, like `/billing/subscription`: knowing
        whether your own workspace owes Shopify money is not privileged information,
        and hiding it behind the manage permission would leave most users staring at
        a disabled feature with no explanation.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/billing/src/routes/shopify-billing.ts:94` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/shopify-billing.ts:94
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/billing/subscription:
    get:
      operationId: getBillingSubscription
      tags:
      - Billing
      summary: List Subscription
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/billing/src/routes/subscription.ts:20` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/subscription.ts:20
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/billing/subscription/cancel:
    post:
      operationId: postBillingSubscriptionCancel
      tags:
      - Billing
      summary: Create Cancel
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/billing/src/routes/subscription.ts:60` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/subscription.ts:60
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:cancel
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/billing/subscription/change-plan:
    post:
      operationId: postBillingSubscriptionChangePlan
      tags:
      - Billing
      summary: Create Change Plan
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/billing/src/routes/subscription.ts:25` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/subscription.ts:25
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/billing/subscription/change-volume:
    post:
      operationId: postBillingSubscriptionChangeVolume
      tags:
      - Billing
      summary: Create Change Volume
      description: |-
        Change only the chat/voice volume selection, staying on the same plan.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/billing/src/routes/subscription.ts:45` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/subscription.ts:45
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/billing/subscription/migrate:
    post:
      operationId: postBillingSubscriptionMigrate
      tags:
      - Billing
      summary: Create Migrate
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/billing/src/routes/subscription.ts:78` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/subscription.ts:78
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/billing/subscription/resume:
    post:
      operationId: postBillingSubscriptionResume
      tags:
      - Billing
      summary: Create Resume
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/billing/src/routes/subscription.ts:69` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/subscription.ts:69
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:billing:manage
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/billing/webhooks/icount:
    post:
      operationId: postBillingWebhooksIcount
      tags:
      - Billing
      summary: Create Icount
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/billing/src/routes/webhooks.ts:54` (billing service).
      security: []
      x-service: billing
      x-source: services/billing/src/routes/webhooks.ts:54
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - billingWebhookEvent.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "413":
          description: Payload too large
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/broadcasts:
    get:
      operationId: getBroadcasts
      tags:
      - Conversations
      summary: List Broadcasts
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/broadcasts.ts:263` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/broadcasts.ts:263
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - broadcast.findMany
      - broadcastRecipient.groupBy
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postBroadcasts
      tags:
      - Conversations
      summary: Create Broadcasts
      description: |-
        Broadcasts are a sold capability, so the gate is server-side. Hiding the
        Broadcasts nav item is presentation, not enforcement.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/broadcasts.ts:380` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/broadcasts.ts:380
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        entitlements:
        - communication.broadcasts
      x-database:
      - broadcast.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/broadcasts/{id}:
    delete:
      operationId: deleteBroadcastsId
      tags:
      - Conversations
      summary: Delete Broadcast
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/broadcasts.ts:473` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/broadcasts.ts:473
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - broadcast.findFirst
      - broadcastRecipient.deleteMany
      - broadcast.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: getBroadcastsId
      tags:
      - Conversations
      summary: Get Broadcast
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/broadcasts.ts:310` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/broadcasts.ts:310
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - broadcast.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchBroadcastsId
      tags:
      - Conversations
      summary: Update Broadcast
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/broadcasts.ts:417` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/broadcasts.ts:417
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - broadcast.findFirst
      - broadcast.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/broadcasts/{id}/cancel:
    post:
      operationId: postBroadcastsIdCancel
      tags:
      - Conversations
      summary: Create Cancel
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/broadcasts.ts:804` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/broadcasts.ts:804
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - broadcast.findFirst
      - broadcastRecipient.updateMany
      - broadcast.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/broadcasts/{id}/recipients:
    get:
      operationId: getBroadcastsIdRecipients
      tags:
      - Conversations
      summary: Get Recipients
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/broadcasts.ts:341` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/broadcasts.ts:341
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - broadcast.findFirst
      - broadcastRecipient.findMany
      - broadcastRecipient.count
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postBroadcastsIdRecipients
      tags:
      - Conversations
      summary: Create Recipients
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/broadcasts.ts:509` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/broadcasts.ts:509
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - broadcast.findFirst
      - broadcastRecipient.createMany
      - broadcastRecipient.count
      - broadcast.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/broadcasts/{id}/resend:
    post:
      operationId: postBroadcastsIdResend
      tags:
      - Conversations
      summary: Create Resend
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/broadcasts.ts:853` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/broadcasts.ts:853
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - broadcast.findFirst
      - broadcast.create
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/broadcasts/{id}/send:
    post:
      operationId: postBroadcastsIdSend
      tags:
      - Conversations
      summary: Create Send
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Side effects: queue: broadcastQueue.add.

        Implemented at `services/conversation/src/routes/broadcasts.ts:554` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/broadcasts.ts:554
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - broadcast.findFirst
      - broadcastRecipient.count
      - broadcast.update
      - broadcastRecipient.findMany
      x-side-effects:
      - "queue: broadcastQueue.add"
      x-async: Enqueues background work; the response returns before that work completes.
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/broadcasts/{id}/validate:
    post:
      operationId: postBroadcastsIdValidate
      tags:
      - Conversations
      summary: Create Validate
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/broadcasts.ts:674` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/broadcasts.ts:674
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - broadcast.findFirst
      - broadcastRecipient.findMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/business-policies:
    get:
      operationId: getBusinessPolicies
      tags:
      - Tenant Settings
      summary: List Business Policies
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/business-policies.ts:70` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/business-policies.ts:70
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canRead
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/business-policies/{actionKind}:
    put:
      operationId: putBusinessPoliciesActionKind
      tags:
      - Tenant Settings
      summary: Replace Business Policie
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/business-policies.ts:86` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/business-policies.ts:86
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canManage
      parameters:
      - name: actionKind
        in: path
        required: true
        schema:
          type: string
        description: "`actionKind` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/business-policies/{actionKind}/preview:
    post:
      operationId: postBusinessPoliciesActionKindPreview
      tags:
      - Tenant Settings
      summary: Create Preview
      description: |-
        Dry-run a sample scenario against the ACTIVE policy - powers the settings
        UI "test this rule" box. Records a PolicyDecision like any evaluation (the
        evaluationPoint makes clear it was a preview via correlationId prefix).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/business-policies.ts:119` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/business-policies.ts:119
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canPreview
      parameters:
      - name: actionKind
        in: path
        required: true
        schema:
          type: string
        description: "`actionKind` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/channels:
    get:
      operationId: getChannels
      tags:
      - Channels
      summary: List Channels
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/channels.ts:147` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/channels.ts:147
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:read
      x-database:
      - channelAccount.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/{id}:
    delete:
      operationId: deleteChannelsId
      tags:
      - Channels
      summary: Delete Channel
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/channels.ts:1757` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/channels.ts:1757
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:update
      x-database:
      - channelAccount.findFirst
      - message.deleteMany
      - conversation.deleteMany
      - channelAccount.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/{id}/disconnect:
    post:
      operationId: postChannelsIdDisconnect
      tags:
      - Channels
      summary: Create Disconnect
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Side effects: outbound HTTP (axios.delete); outbound HTTP (axios.post).

        Implemented at `services/auth/src/routes/channels.ts:1667` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/channels.ts:1667
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:update
      x-database:
      - channelAccount.findFirst
      - channelAccount.update
      x-side-effects:
      - outbound HTTP (axios.delete)
      - outbound HTTP (axios.post)
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/channels/{id}/posts:
    get:
      operationId: getChannelsIdPosts
      tags:
      - Channels
      summary: Get Posts
      description: |-
        Returns a normalized list of the page's own posts so flow authors can
        select which post a Comment Trigger should listen on, instead of pasting
        raw IDs. Group posts intentionally NOT supported - the Meta Groups API
        was deprecated April 2024; for those, the trigger UI exposes a manual
        post-URL paste field that the runtime later matches via the page webhook.

        Supports MESSENGER (Facebook page) and INSTAGRAM (IG Business account).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Side effects: outbound HTTP (axios.get).

        Implemented at `services/auth/src/routes/channels.ts:207` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/channels.ts:207
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:read
      x-database:
      - channelAccount.findFirst
      x-side-effects:
      - outbound HTTP (axios.get)
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: limit
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/{id}/response-mode:
    patch:
      operationId: patchChannelsIdResponseMode
      tags:
      - Channels
      summary: Update Response Mode
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/channels.ts:1636` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/channels.ts:1636
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:update
      x-database:
      - channelAccount.findFirst
      - channelAccount.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: true
        description: Validated by `responseModeSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                responseMode:
                  type: string
                  enum:
                  - HUMAN_WITH_COPILOT
                  - AI_AUTO_REPLY
              required:
              - responseMode
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/{id}/status:
    get:
      operationId: getChannelsIdStatus
      tags:
      - Channels
      summary: Get Status
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Side effects: outbound HTTP (axios.get); outbound HTTP (axios.post).

        Implemented at `services/auth/src/routes/channels.ts:1800` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/channels.ts:1800
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:read
      x-database:
      - channelAccount.findFirst
      - channelAccount.update
      x-side-effects:
      - outbound HTTP (axios.get)
      - outbound HTTP (axios.post)
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/config:
    get:
      operationId: getChannelsConfig
      tags:
      - Channels
      summary: List Config
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/channels.ts:2044` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/channels.ts:2044
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:read
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/connect/email:
    post:
      operationId: postChannelsConnectEmail
      tags:
      - Channels
      summary: Create Email
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/channels.ts:1913` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/channels.ts:1913
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:update
        planLimits:
        - limit:channels
      x-database:
      - channelAccount.findFirst
      - channelAccount.create
      requestBody:
        required: true
        description: Validated by `connectEmailSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                emailAddress:
                  type: string
                  format: email
                displayName:
                  type: string
                  minLength: 1
                smtpHost:
                  type: string
                  minLength: 1
                smtpPort:
                  type: integer
                  minimum: 1
                  maximum: 65535
                smtpUser:
                  type: string
                  minLength: 1
                smtpPass:
                  type: string
                  minLength: 1
                imapHost:
                  type: string
                imapPort:
                  type: integer
                  minimum: 1
                  maximum: 65535
                imapUser:
                  type: string
                imapPass:
                  type: string
              required:
              - emailAddress
              - displayName
              - smtpHost
              - smtpPort
              - smtpUser
              - smtpPass
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/connect/whatsapp:
    post:
      operationId: postChannelsConnectWhatsapp
      tags:
      - Channels
      summary: Create Whatsapp
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Side effects: outbound HTTP (axios.get); outbound HTTP (axios.post).

        Implemented at `services/auth/src/routes/channels.ts:301` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/channels.ts:301
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:update
        planLimits:
        - limit:channels
      x-database:
      - channelAccount.findUnique
      - channelAccount.update
      - channelAccount.create
      x-side-effects:
      - outbound HTTP (axios.get)
      - outbound HTTP (axios.post)
      requestBody:
        required: true
        description: Validated by `whatsappConnectSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                code:
                  type: string
                  minLength: 1
                wabaId:
                  type: string
                phoneNumberId:
                  type: string
              required:
              - code
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/connect/whatsapp-session:
    post:
      operationId: postChannelsConnectWhatsappSession
      tags:
      - Channels
      summary: Create Whatsapp Session
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Side effects: outbound HTTP (axios.post); outbound HTTP (axios.get).

        Implemented at `services/auth/src/routes/channels.ts:536` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/channels.ts:536
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:update
        planLimits:
        - limit:channels
      x-database:
      - channelAccount.findUnique
      - channelAccount.update
      - channelAccount.create
      x-side-effects:
      - outbound HTTP (axios.post)
      - outbound HTTP (axios.get)
      requestBody:
        required: true
        description: Validated by `whatsappSessionSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                wabaId:
                  type: string
                  minLength: 1
                phoneNumberId:
                  type: string
              required:
              - wabaId
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/oauth/callback:
    get:
      operationId: getChannelsOauthCallback
      tags:
      - Channels
      summary: List Callback
      description: |-
        No authentication middleware is applied to this route.

        Side effects: outbound HTTP (axios.get); outbound HTTP (axios.post).

        Implemented at `services/auth/src/routes/channels.ts:786` (auth service).
      security: []
      x-service: auth
      x-source: services/auth/src/routes/channels.ts:786
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - channelAccount.findUnique
      - channelAccount.update
      - channelAccount.create
      x-side-effects:
      - outbound HTTP (axios.get)
      - outbound HTTP (axios.post)
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
  /api/channels/oauth/init:
    get:
      operationId: getChannelsOauthInit
      tags:
      - Channels
      summary: List Init
      description: |-
        No authentication middleware is applied to this route.

        Implemented at `services/auth/src/routes/channels.ts:625` (auth service).
      security: []
      x-service: auth
      x-source: services/auth/src/routes/channels.ts:625
      x-operation-type: read
      x-publicly-exposed: true
      parameters:
      - name: token
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: platform
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: wabaId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: phoneNumberId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/summary:
    get:
      operationId: getChannelsSummary
      tags:
      - Channels
      summary: List Summary
      description: |-
        The Inbox/History empty states need to know WHY a list is empty ("no
        channel connected" vs "channel broken" vs "no conversations yet"), and
        agents see those pages too - the full listing above is ADMIN-only. This
        returns health COUNTS only: no tokens, external ids, or error details.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/channels.ts:182` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/channels.ts:182
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - channelAccount.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/webchat/{id}/settings:
    get:
      operationId: getChannelsWebchatIdSettings
      tags:
      - Channels
      summary: Get Settings
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/channels.ts:2026` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/channels.ts:2026
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - channelAccount.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putChannelsWebchatIdSettings
      tags:
      - Channels
      summary: Replace Settings
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/channels.ts:1994` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/channels.ts:1994
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - channelAccount.findFirst
      - channelAccount.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/channels/webchat/create:
    post:
      operationId: postChannelsWebchatCreate
      tags:
      - Channels
      summary: Create Create
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/channels.ts:1954` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/channels.ts:1954
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:update
        planLimits:
        - limit:channels
      x-database:
      - channelAccount.findFirst
      - channelAccount.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/channels/whatsapp/connect:
    post:
      operationId: postChannelsWhatsappConnect
      tags:
      - WhatsApp
      summary: Create Connect
      description: |-
        Connect exactly one number.

        `phoneNumberId` is required, and that is the single most important
        difference from what this replaces: the previous route looped over every
        number on the customer's WABA and rebound all of them, so a customer adding
        their support line silently had their sales line rewritten too.

        The signup session is deliberately NOT consumed here. A customer who
        authorised once and has three numbers should be able to add all three
        without re-authorising for each, which is what makes multi-number setup
        feel like one task rather than three. The session expires on its own.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/whatsapp-numbers.ts:314` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/whatsapp-numbers.ts:314
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:update
        planLimits:
        - limit:channels
      x-database:
      - whatsAppNumber.findUnique
      requestBody:
        required: true
        description: Validated by `connectSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                sessionId:
                  type: string
                  minLength: 1
                phoneNumberId:
                  type: string
                  minLength: 1
                twoStepPin:
                  type: string
                dataLocalizationRegion:
                  type: string
              required:
              - sessionId
              - phoneNumberId
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "410":
          description: Gone - the resource expired or was superseded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/whatsapp/exclusions:
    get:
      operationId: getChannelsWhatsappExclusions
      tags:
      - WhatsApp
      summary: List Exclusions
      description: |-
        Coexistence delivers EVERY conversation on the number to us, including the
        ones that were never meant for a shared inbox. These rules are the only way
        to say "that one is mine" short of not connecting Coexistence at all.

        Scoped to a `channelAccountId` when the caller names a number, so a tenant
        running two WhatsApp numbers can exclude a contact on the one that lives in
        the app without silently dropping the same person on the other.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/whatsapp-numbers.ts:530` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/whatsapp-numbers.ts:530
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:read
      x-database:
      - inboundExclusion.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postChannelsWhatsappExclusions
      tags:
      - WhatsApp
      summary: Create Exclusions
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/whatsapp-numbers.ts:553` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/whatsapp-numbers.ts:553
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:update
      x-database:
      - channelAccount.findFirst
      - inboundExclusion.upsert
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/channels/whatsapp/exclusions/{id}:
    delete:
      operationId: deleteChannelsWhatsappExclusionsId
      tags:
      - WhatsApp
      summary: Delete Exclusion
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/whatsapp-numbers.ts:608` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/whatsapp-numbers.ts:608
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:update
      x-database:
      - inboundExclusion.deleteMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/whatsapp/inspect:
    post:
      operationId: postChannelsWhatsappInspect
      tags:
      - WhatsApp
      summary: Create Inspect
      description: |-
        Look at what the customer has, and say which flow each number would take.

        Read-only against Meta: no subscription, no registration, no database write.
        Safe to call repeatedly, including on a fully healthy workspace.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/whatsapp-numbers.ts:172` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/whatsapp-numbers.ts:172
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:update
      x-database:
      - whatsAppNumber.findMany
      requestBody:
        required: true
        description: Validated by `inspectSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                code:
                  type: string
                  minLength: 1
                path:
                  type: string
                  enum:
                  - new
                  - business-app
                  default: new
                businessPortfolioId:
                  type: string
                wabaIds:
                  type: array
                  items:
                    type: string
              required:
              - code
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/whatsapp/numbers:
    get:
      operationId: getChannelsWhatsappNumbers
      tags:
      - WhatsApp
      summary: List Numbers
      description: |-
        Every WhatsApp number on this workspace, each with its own health.

        Health is built from stored state rather than by calling Meta per number:
        a tenant with a dozen numbers would otherwise fan out a dozen Graph requests
        on every page render, which is slow and spends the customer's rate budget to
        learn nothing new. `POST /numbers/:id/refresh` is the deliberate re-read.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/whatsapp-numbers.ts:89` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/whatsapp-numbers.ts:89
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:read
      x-database:
      - whatsAppNumber.findMany
      - channelAccount.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/whatsapp/numbers/{id}:
    delete:
      operationId: deleteChannelsWhatsappNumbersId
      tags:
      - WhatsApp
      summary: Delete Number
      description: |-
        Remove ONE number.

        The cross-number safety is in `disconnectNumber`: Meta subscribes webhooks
        per WABA, so unsubscribing here would silence sibling numbers sharing that
        account. The service checks for siblings first and leaves the subscription
        alone when any remain.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/whatsapp-numbers.ts:507` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/whatsapp-numbers.ts:507
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/whatsapp/numbers/{id}/events:
    get:
      operationId: getChannelsWhatsappNumbersIdEvents
      tags:
      - WhatsApp
      summary: Get Events
      description: |-
        Every step taken against one number, with Meta's own responses.

        Phase 10 requires showing the exact reason and the API response when
        something fails. Bodies were redacted of anything token-shaped before being
        stored, so this is safe to return.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/whatsapp-numbers.ts:633` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/whatsapp-numbers.ts:633
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:read
      x-database:
      - whatsAppNumber.findFirst
      - whatsAppNumberEvent.findMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/whatsapp/numbers/{id}/refresh:
    post:
      operationId: postChannelsWhatsappNumbersIdRefresh
      tags:
      - WhatsApp
      summary: Create Refresh
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/whatsapp-numbers.ts:458` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/whatsapp-numbers.ts:458
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:read
      x-database:
      - whatsAppNumber.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/channels/whatsapp/numbers/{id}/repair:
    post:
      operationId: postChannelsWhatsappNumbersIdRepair
      tags:
      - WhatsApp
      summary: Create Repair
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/whatsapp-numbers.ts:479` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/whatsapp-numbers.ts:479
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:update
      x-database:
      - whatsAppNumber.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: true
        description: Validated by `repairSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                action:
                  type: string
                  enum:
                  - RESUBSCRIBE_WEBHOOKS
                  - REFRESH_STATUS
                  - REQUEST_HISTORY_SYNC
              required:
              - action
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/channels/whatsapp/numbers/{id}/resume:
    post:
      operationId: postChannelsWhatsappNumbersIdResume
      tags:
      - WhatsApp
      summary: Create Resume
      description: |-
        Supply what the customer was asked for and continue that number's pipeline.

        Scoped to one number; the other numbers on the workspace are not read, not
        written and not affected.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/whatsapp-numbers.ts:397` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/whatsapp-numbers.ts:397
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - channels:manage:update
      x-database:
      - whatsAppNumber.findFirst
      - channelAccount.findUnique
      - whatsAppNumber.findUnique
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: true
        description: Validated by `resumeSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                twoStepPin:
                  type: string
                verificationCode:
                  type: string
                  minLength: 4
                  maxLength: 8
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/chatbot-flows:
    get:
      operationId: getChatbotFlows
      tags:
      - AI Studio
      summary: List Chatbot Flows
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/chatbot/src/routes/chatbot.ts:21` (chatbot service).
      security:
      - bearerAuth: []
      x-service: chatbot
      x-source: services/chatbot/src/routes/chatbot.ts:21
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - chatbotFlow.findMany
      parameters:
      - name: channel
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postChatbotFlows
      tags:
      - AI Studio
      summary: Create Chatbot Flows
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/chatbot/src/routes/chatbot.ts:43` (chatbot service).
      security:
      - bearerAuth: []
      x-service: chatbot
      x-source: services/chatbot/src/routes/chatbot.ts:43
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:workflows:update OR role ADMIN
        entitlements:
        - communication.automations
        planLimits:
        - limit:workflows
      x-database:
      - chatbotFlow.create
      requestBody:
        required: true
        description: Validated by `flowSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                description:
                  type: string
                channel:
                  type: string
                  enum:
                  - WHATSAPP
                  - MESSENGER
                  nullable: true
                nodes:
                  type: array
                  items: {}
                edges:
                  type: array
                  items: {}
                isActive:
                  type: boolean
                expectedUpdatedAt:
                  type: string
              required:
              - name
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/chatbot-flows/{id}:
    delete:
      operationId: deleteChatbotFlowsId
      tags:
      - AI Studio
      summary: Delete Chatbot Flow
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/chatbot/src/routes/chatbot.ts:78` (chatbot service).
      security:
      - bearerAuth: []
      x-service: chatbot
      x-source: services/chatbot/src/routes/chatbot.ts:78
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:workflows:update OR role ADMIN
      x-database:
      - chatbotFlow.findFirst
      - chatbotFlow.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: getChatbotFlowsId
      tags:
      - AI Studio
      summary: Get Chatbot Flow
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/chatbot/src/routes/chatbot.ts:35` (chatbot service).
      security:
      - bearerAuth: []
      x-service: chatbot
      x-source: services/chatbot/src/routes/chatbot.ts:35
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - chatbotFlow.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putChatbotFlowsId
      tags:
      - AI Studio
      summary: Replace Chatbot Flow
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/chatbot/src/routes/chatbot.ts:59` (chatbot service).
      security:
      - bearerAuth: []
      x-service: chatbot
      x-source: services/chatbot/src/routes/chatbot.ts:59
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:workflows:update OR role ADMIN
      x-database:
      - chatbotFlow.findFirst
      - chatbotFlow.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: true
        description: Validated by `flowSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                description:
                  type: string
                channel:
                  type: string
                  enum:
                  - WHATSAPP
                  - MESSENGER
                  nullable: true
                nodes:
                  type: array
                  items: {}
                edges:
                  type: array
                  items: {}
                isActive:
                  type: boolean
                expectedUpdatedAt:
                  type: string
              required:
              - name
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/chatbot-flows/{id}/activate:
    post:
      operationId: postChatbotFlowsIdActivate
      tags:
      - AI Studio
      summary: Create Activate
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/chatbot/src/routes/chatbot.ts:87` (chatbot service).
      security:
      - bearerAuth: []
      x-service: chatbot
      x-source: services/chatbot/src/routes/chatbot.ts:87
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:workflows:publish OR role ADMIN
      x-database:
      - chatbotFlow.findFirst
      - chatbotFlow.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "422":
          description: Semantically invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/chatbot-flows/{id}/validate:
    post:
      operationId: postChatbotFlowsIdValidate
      tags:
      - AI Studio
      summary: Create Validate
      description: |-
        Advisory validation for the editor's validation panel - same authoritative
        rules, without mutating anything.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/chatbot/src/routes/chatbot.ts:105` (chatbot service).
      security:
      - bearerAuth: []
      x-service: chatbot
      x-source: services/chatbot/src/routes/chatbot.ts:105
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - chatbotFlow.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/checkout/{reference}/advance:
    post:
      operationId: postCheckoutReferenceAdvance
      tags:
      - Billing
      summary: Create Advance
      description: |-
        Ask the server to re-check and move the checkout forward.

        This is what the waiting page polls. Safe to call repeatedly: every step it
        drives is individually idempotent, so a customer refreshing during a charge
        cannot cause a second one.

        Authentication is optional; the caller is identified when a token is present.

        This is a write operation.

        Implemented at `services/billing/src/routes/checkout-session.ts:111` (billing service).
      security:
      - bearerAuth: []
      - {}
      x-service: billing
      x-source: services/billing/src/routes/checkout-session.ts:111
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: reference
        in: path
        required: true
        schema:
          type: string
        description: "`reference` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
      x-documentation-gap: request-body-unschematized
  /api/checkout/{reference}/payment-session:
    post:
      operationId: postCheckoutReferencePaymentSession
      tags:
      - Billing
      summary: Create Payment Session
      description: |-
        Start (or resume) payment setup.

        Returns where to send the customer. The URL is generated server-side from a
        configured page id - a client-supplied destination would be an open redirect
        into a page that asks for card details.

        Authentication is optional; the caller is identified when a token is present.

        This is a write operation.

        Implemented at `services/billing/src/routes/checkout-session.ts:84` (billing service).
      security:
      - bearerAuth: []
      - {}
      x-service: billing
      x-source: services/billing/src/routes/checkout-session.ts:84
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: reference
        in: path
        required: true
        schema:
          type: string
        description: "`reference` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
      x-documentation-gap: request-body-unschematized
  /api/checkout/{reference}/resend-welcome:
    post:
      operationId: postCheckoutReferenceResendWelcome
      tags:
      - Billing
      summary: Create Resend Welcome
      description: |-
        Send the post-payment welcome email again.

        A paid signup can complete the entire purchase without ever authenticating,
        so that email is the only thing carrying the admin a way to set a password.
        If it does not arrive they are locked out of what they just bought, and the
        ordinary repair (POST /agents/:id/reset-password) requires the very
        permissions they cannot yet hold. This is their own way to ask again.

        It discloses nothing: the address is never echoed and the link never
        returned, so a caller who reached this without standing learns only that
        something was posted. Delivery to the address on file stays the sole proof
        of who the recipient is.

        Authentication is optional; the caller is identified when a token is present.

        This is a write operation.

        Implemented at `services/billing/src/routes/checkout-session.ts:137` (billing service).
      security:
      - bearerAuth: []
      - {}
      x-service: billing
      x-source: services/billing/src/routes/checkout-session.ts:137
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - tenant.findUnique
      - plan.findFirst
      parameters:
      - name: reference
        in: path
        required: true
        schema:
          type: string
        description: "`reference` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
      x-documentation-gap: request-body-unschematized
  /api/checkout/{reference}/status:
    get:
      operationId: getCheckoutReferenceStatus
      tags:
      - Billing
      summary: Get Status
      description: |-
        Authentication is optional; the caller is identified when a token is present.

        Implemented at `services/billing/src/routes/checkout.ts:216` (billing service).
      security:
      - bearerAuth: []
      - {}
      x-service: billing
      x-source: services/billing/src/routes/checkout.ts:216
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - pendingCheckout.findUnique
      - paymentAttempt.findFirst
      - tenant.findUnique
      - plan.findFirst
      - tokenizationSession.findFirst
      parameters:
      - name: reference
        in: path
        required: true
        schema:
          type: string
        description: "`reference` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/checkout/mine:
    get:
      operationId: getCheckoutMine
      tags:
      - Billing
      summary: List Mine
      description: |-
        Safe summary + status.

        Optional authentication: a customer arriving from an email has a continuation
        token and no session, while a signed-in user has the reverse.


        The signed-in customer's own outstanding checkout.

        Without this, the ONLY way to reach payment is the reference in the
        original email. Lose that mail and the organization is stuck owing money
        with no route to pay it, which is not a state anybody should be able to
        get into by archiving a message.

        Returns a reference, not a summary: the caller then goes through the same
        `/checkout/:reference/status` every other visitor uses, so there is one
        authorization path rather than a privileged shortcut that could drift
        from it. Membership is what authorizes here - the reference is the
        ANSWER, so it cannot also be the question.

        Gated on PAYMENT_SETUP, so a suspended tenant cannot use it to self-serve
        back in.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/billing/src/routes/checkout.ts:183` (billing service).
      security:
      - bearerAuth: []
      x-service: billing
      x-source: services/billing/src/routes/checkout.ts:183
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePaymentSetupAccess
      x-database:
      - pendingCheckout.findFirst
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/commerce-context/{conversationId}:
    get:
      operationId: getCommerceContextConversationId
      tags:
      - Integrations
      summary: Get Commerce Context
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/commerce-context.ts:72` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/commerce-context.ts:72
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - customer:commerce:read
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      - name: limit
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: refresh
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/commerce-context/{conversationId}/actions:
    post:
      operationId: postCommerceContextConversationIdActions
      tags:
      - Integrations
      summary: Create Actions
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/commerce-context.ts:103` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/commerce-context.ts:103
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - customer:commerce:cancel
        - customer:commerce:refund
        - customer:commerce:return
        - customer:commerce:tag
        - customer:commerce:note
        - customer:commerce:notify
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/connectors/{slug}/config:
    post:
      operationId: postConnectorsSlugConfig
      tags:
      - Integrations
      summary: Create Config
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/connectors-admin.ts:161` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:161
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canManageSystems
      parameters:
      - name: slug
        in: path
        required: true
        schema:
          type: string
        description: "`slug` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/connectors/{slug}/connect:
    post:
      operationId: postConnectorsSlugConnect
      tags:
      - Integrations
      summary: Create Connect
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/connectors-admin.ts:226` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:226
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      parameters:
      - name: slug
        in: path
        required: true
        schema:
          type: string
        description: "`slug` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/connectors/{slug}/disconnect:
    post:
      operationId: postConnectorsSlugDisconnect
      tags:
      - Integrations
      summary: Create Disconnect
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/connectors-admin.ts:147` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:147
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canManageSystems
      parameters:
      - name: slug
        in: path
        required: true
        schema:
          type: string
        description: "`slug` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/connectors/{slug}/status:
    get:
      operationId: getConnectorsSlugStatus
      tags:
      - Integrations
      summary: Get Status
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/connectors-admin.ts:133` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:133
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canReadSystems
      parameters:
      - name: slug
        in: path
        required: true
        schema:
          type: string
        description: "`slug` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/airtable/mapping:
    get:
      operationId: getConnectorsAirtableMapping
      tags:
      - Integrations
      summary: List Mapping
      description: |-
        Read the mapping currently on the connection config - powers the
        post-onboarding "refresh fields / edit mapping" card. The onboarding wizard
        never needed this (it writes a fresh mapping), which is why editing the
        mapping after onboarding used to be impossible without reconnecting.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/connectors-admin.ts:826` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:826
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postConnectorsAirtableMapping
      tags:
      - Integrations
      summary: Create Mapping
      description: |-
        Save the mapping onto the connection config. Optionally auto-create the
        notes / idempotency columns we OWN (never identifier columns) when
        create_missing=true and the token carries schema.bases:write.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/connectors-admin.ts:853` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:853
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/connectors/airtable/meta/bases:
    get:
      operationId: getConnectorsAirtableMetaBases
      tags:
      - Integrations
      summary: List Bases
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/connectors-admin.ts:656` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:656
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      parameters:
      - name: pat
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/airtable/meta/tables/{baseId}:
    get:
      operationId: getConnectorsAirtableMetaTablesBaseId
      tags:
      - Integrations
      summary: Get Table
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/connectors-admin.ts:671` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:671
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      parameters:
      - name: baseId
        in: path
        required: true
        schema:
          type: string
        description: "`baseId` path segment."
      - name: pat
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/airtable/oauth/bases:
    get:
      operationId: getConnectorsAirtableOauthBases
      tags:
      - Integrations
      summary: List Bases
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/connectors-admin.ts:784` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:784
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/airtable/oauth/callback:
    get:
      operationId: getConnectorsAirtableOauthCallback
      tags:
      - Integrations
      summary: List Callback
      description: |-
        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/connectors-admin.ts:719` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:719
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/airtable/oauth/fields:
    get:
      operationId: getConnectorsAirtableOauthFields
      tags:
      - Integrations
      summary: List Fields
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/connectors-admin.ts:808` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:808
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      parameters:
      - name: baseId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: tableId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/airtable/oauth/init:
    get:
      operationId: getConnectorsAirtableOauthInit
      tags:
      - Integrations
      summary: List Init
      description: |-
        Airtable mandates PKCE (S256). Our other OAuth flows are plain auth-code;
        here we generate a code_verifier, send its S256 challenge on init, and carry
        the verifier inside the signed, short-lived state JWT so the callback can
        complete the token exchange. Confidential client → also HTTP Basic with the
        client secret. Reachable during onboarding (requireOnboardingOrActiveTenant).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/connectors-admin.ts:694` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:694
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      parameters:
      - name: flow
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/airtable/oauth/tables:
    get:
      operationId: getConnectorsAirtableOauthTables
      tags:
      - Integrations
      summary: List Tables
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/connectors-admin.ts:795` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:795
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      parameters:
      - name: baseId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/aws_rds/meta/tables:
    post:
      operationId: postConnectorsAwsRdsMetaTables
      tags:
      - Integrations
      summary: Create Tables
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/connectors-admin.ts:1381` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:1381
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/connectors/hubspot/oauth/callback:
    get:
      operationId: getConnectorsHubspotOauthCallback
      tags:
      - Integrations
      summary: List Callback
      description: |-
        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/connectors-admin.ts:391` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:391
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/hubspot/oauth/init:
    get:
      operationId: getConnectorsHubspotOauthInit
      tags:
      - Integrations
      summary: List Init
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/connectors-admin.ts:332` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:332
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      parameters:
      - name: flow
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/monday/meta/boards:
    get:
      operationId: getConnectorsMondayMetaBoards
      tags:
      - Integrations
      summary: List Boards
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/connectors-admin.ts:1231` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:1231
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/monday/oauth/callback:
    get:
      operationId: getConnectorsMondayOauthCallback
      tags:
      - Integrations
      summary: List Callback
      description: |-
        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/connectors-admin.ts:1167` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:1167
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/monday/oauth/init:
    get:
      operationId: getConnectorsMondayOauthInit
      tags:
      - Integrations
      summary: List Init
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/connectors-admin.ts:1139` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:1139
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      parameters:
      - name: flow
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/mongodb/meta/collections:
    post:
      operationId: postConnectorsMongodbMetaCollections
      tags:
      - Integrations
      summary: Create Collections
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/connectors-admin.ts:1320` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:1320
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/connectors/mongodb/meta/databases:
    post:
      operationId: postConnectorsMongodbMetaDatabases
      tags:
      - Integrations
      summary: Create Databases
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/connectors-admin.ts:1350` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:1350
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/connectors/postgres/meta/tables:
    post:
      operationId: postConnectorsPostgresMetaTables
      tags:
      - Integrations
      summary: Create Tables
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/connectors-admin.ts:1278` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:1278
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/connectors/salesforce/oauth/callback:
    get:
      operationId: getConnectorsSalesforceOauthCallback
      tags:
      - Integrations
      summary: List Callback
      description: |-
        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/connectors-admin.ts:1087` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:1087
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/salesforce/oauth/init:
    get:
      operationId: getConnectorsSalesforceOauthInit
      tags:
      - Integrations
      summary: List Init
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/connectors-admin.ts:1062` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:1062
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      parameters:
      - name: loginHost
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: flow
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/shopify/install:
    get:
      operationId: getConnectorsShopifyInstall
      tags:
      - Integrations
      summary: List Install
      description: |-
        Shopify's signed app-entry request. PUBLIC by requirement.

        No `authenticate`, no `resolveTenant`: a merchant installing from the App
        Store has no GOTCHA session yet, and requiring one here is precisely the
        "login screen before OAuth" that requirement 2.3.2 prohibits.

        Everything this route trusts comes from `verifyAppEntryHmac`, which
        refuses a missing, malformed, duplicated, stale or unsigned request. On
        success it hands back a shop domain in Shopify's exact canonical form, and
        that value - not anything from the query - is what builds the redirect.

        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/shopify-install.ts:201` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-install.ts:201
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
  /api/connectors/shopify/install/cancel:
    post:
      operationId: postConnectorsShopifyInstallCancel
      tags:
      - Integrations
      summary: Create Cancel
      description: |-
        Abandon an intent without installing.

        Small, but it keeps a stale intent from silently capturing a LATER install
        that the merchant meant for a different workspace.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/shopify-install.ts:381` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-install.ts:381
      x-operation-type: write
      x-publicly-exposed: true
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/connectors/shopify/install/claim:
    post:
      operationId: postConnectorsShopifyInstallClaim
      tags:
      - Integrations
      summary: Create Claim
      description: |-
        Bind a verified-but-unclaimed installation to the caller's workspace.

        The workspace is `req.tenantId` - resolved by the same middleware every
        other tenant write uses - and the permission is re-checked here rather than
        inherited from whoever started the install. Those are the two things that
        make this safe to expose: the claimant proves who they are and that they
        may connect integrations, and the handle proves only that an install
        happened.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/shopify-install.ts:301` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-install.ts:301
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/connectors/shopify/install/pending:
    get:
      operationId: getConnectorsShopifyInstallPending
      tags:
      - Integrations
      summary: List Pending
      description: |-
        What is waiting for this browser to claim, if anything.

        Returns the shop name only. The pending record also holds an access token,
        and no endpoint reachable from a browser returns it - not this one, not
        the claim below.

        `peek`, not consume: the claim screen may be reloaded, and losing a
        verified install to a refresh would mean reinstalling from Shopify.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/shopify-install.ts:275` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-install.ts:275
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      parameters:
      - name: handle
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/shopify/install/start:
    get:
      operationId: getConnectorsShopifyInstallStart
      tags:
      - Integrations
      summary: List Start
      description: |-
        "Connect Shopify", pressed by a signed-in user.

        Returns the Shopify-owned install URL and records a server-side intent so
        the callback knows which workspace started this. The intent handle goes
        back as an HttpOnly cookie, not in the URL: a handle in the URL travels
        through Shopify's redirect chain and into referrer headers and logs.

        There is deliberately NO `shop` parameter. Shopify picks the store.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/shopify-install.ts:135` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-install.ts:135
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      parameters:
      - name: flow
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "503":
          description: Temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/shopify/oauth/callback:
    get:
      operationId: getConnectorsShopifyOauthCallback
      tags:
      - Integrations
      summary: List Callback
      description: |-
        The OAuth callback. Public by necessity - Shopify calls it, not a browser
        we authenticated.

        Four checks, in this order, before anything is written:

          1. HMAC over the callback query. This was MISSING before: the callback
             trusted `shop` and `code` from an unsigned request, so anything that
             could guess a live `state` could have driven a token exchange against
             a host of its choosing.
          2. Single-use `state`, consumed exactly once (Redis SET NX, fail-closed).
          3. The shop Shopify returned must equal the shop bound INTO the state at
             install time. A mismatch means the state was moved to another store.
          4. Only then, the code exchange.

        After that the workspace question: an intent means we already knew it, no
        intent means the install started on Shopify and the merchant must sign in
        before anything is bound to a tenant.

        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/connectors-admin.ts:523` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:523
      x-operation-type: read
      x-publicly-exposed: true
      parameters:
      - name: code
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: state
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/shopify/oauth/init:
    get:
      operationId: getConnectorsShopifyOauthInit
      tags:
      - Integrations
      summary: List Init
      description: |-
        Installation NO LONGER STARTS HERE. The merchant-facing entry point is
        `/connectors/shopify/install/start` (routes/shopify-install.ts), which sends
        them to a Shopify-owned page; Shopify then calls our public install handler
        with a signed request and OAuth begins there.

        What remains here is the two halves that must stay:

          init      REAUTHORIZATION only. A workspace that already holds a Shopify
                    connection re-granting scopes, or replacing a revoked token.
                    The shop is read from the STORED connection, never from the
                    request - which is what lets this route keep existing without
                    reintroducing the typed-domain hole it used to be.

          callback  The single OAuth callback for every path. Shopify posts back
                    here for a fresh install, a reinstall and a reauthorization
                    alike, so the branching on "did we know the workspace?" lives
                    here rather than in three places.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/connectors-admin.ts:459` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:459
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      parameters:
      - name: flow
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/square/oauth/callback:
    get:
      operationId: getConnectorsSquareOauthCallback
      tags:
      - Integrations
      summary: List Callback
      description: |-
        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/connectors-admin.ts:1011` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:1011
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/square/oauth/init:
    get:
      operationId: getConnectorsSquareOauthInit
      tags:
      - Integrations
      summary: List Init
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/connectors-admin.ts:987` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:987
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      parameters:
      - name: environment
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/stripe/oauth/callback:
    get:
      operationId: getConnectorsStripeOauthCallback
      tags:
      - Integrations
      summary: List Callback
      description: |-
        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/connectors-admin.ts:281` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:281
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/stripe/oauth/init:
    get:
      operationId: getConnectorsStripeOauthInit
      tags:
      - Integrations
      summary: List Init
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/connectors-admin.ts:260` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:260
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/wix/oauth/callback:
    get:
      operationId: getConnectorsWixOauthCallback
      tags:
      - Integrations
      summary: List Callback
      description: |-
        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/connectors-admin.ts:935` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:935
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/connectors/wix/oauth/init:
    get:
      operationId: getConnectorsWixOauthInit
      tags:
      - Integrations
      summary: List Init
      description: |-
        CORRECT flow for "any Wix store owner connects their store to us":
          1. We send the user to https://www.wix.com/installer/install?appId=…
             &redirectUrl=…&state=… - Wix shows them a "Add to site" picker.
          2. After they pick a site + approve permissions, Wix redirects back to
             our callback with `?code=…&instanceId=<site-instance>&state=…`.
          3. We POST that code to https://www.wixapis.com/oauth/access
             (grant_type=authorization_code) for an access_token + refresh_token
             scoped to that instanceId.

        We persist `instanceId` on the integration so subsequent API calls can
        reference the right Wix site. Refresh tokens are long-lived (Wix handles
        expiry transparently - token expiry is ~5 minutes, refresh-token rotation
        is one-shot per refresh).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/connectors-admin.ts:915` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/connectors-admin.ts:915
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canConnectSystems
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/contacts:
    get:
      operationId: getContacts
      tags:
      - Contacts & CRM
      summary: List Contacts
      description: |-
        GET / - Search contacts in the connected CRM only.

        Query params:
          q       search across name / phone / email
          limit   max rows (default 20)

        CRM is the source of truth for people-data. The platform's local Contact
        table is a write-side cache populated by inbound conversations; it is
        not surfaced through this endpoint anymore. When no CRM is connected
        the endpoint returns an empty list.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/contacts.ts:33` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/contacts.ts:33
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - crm:contacts:read OR role ADMIN
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postContacts
      tags:
      - Contacts & CRM
      summary: Create Contacts
      description: |-
        POST / - Create contact

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/contacts.ts:350` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/contacts.ts:350
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - crm:contacts:read OR role ADMIN
      x-database:
      - contact.upsert
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/contacts/{id}:
    get:
      operationId: getContactsId
      tags:
      - Contacts & CRM
      summary: Get Contact
      description: |-
        GET /:id - Get single contact with conversation history count per channel

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/contacts.ts:223` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/contacts.ts:223
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - crm:contacts:read OR role ADMIN
      x-database:
      - contact.findFirst
      - conversation.groupBy
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchContactsId
      tags:
      - Contacts & CRM
      summary: Update Contact
      description: |-
        PATCH /:id - Update contact

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/contacts.ts:392` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/contacts.ts:392
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - crm:contacts:read OR role ADMIN
      x-database:
      - contact.findFirst
      - contact.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/contacts/{id}/opt-out:
    post:
      operationId: postContactsIdOptOut
      tags:
      - Contacts & CRM
      summary: Create Opt Out
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/contacts.ts:423` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/contacts.ts:423
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - crm:contacts:read OR role ADMIN
      x-database:
      - contact.findFirst
      - contact.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/contacts/initiate-conversation:
    post:
      operationId: postContactsInitiateConversation
      tags:
      - Contacts & CRM
      summary: Create Initiate Conversation
      description: |-
        POST /initiate-conversation - must be before /:id to avoid conflict

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Side effects: queue: outgoingMessageQueue.add.

        Implemented at `services/conversation/src/routes/contacts.ts:95` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/contacts.ts:95
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - crm:contacts:read OR role ADMIN
      x-database:
      - contact.findFirst
      - conversation.findFirst
      - conversation.create
      - conversation.update
      - message.create
      - messageTemplate.findFirst
      x-side-effects:
      - "queue: outgoingMessageQueue.add"
      x-async: Enqueues background work; the response returns before that work completes.
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/contacts/segment:
    post:
      operationId: postContactsSegment
      tags:
      - Contacts & CRM
      summary: Create Segment
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/contacts.ts:257` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/contacts.ts:257
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - crm:contacts:read OR role ADMIN
      x-database:
      - contact.count
      - contact.findMany
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/conversations:
    get:
      operationId: getConversations
      tags:
      - Conversations
      summary: List Conversations
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/conversations.ts:47` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/conversations.ts:47
      x-operation-type: read
      x-publicly-exposed: true
      x-service-calls:
      - conversationService.list
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/conversations/{conversationId}/messages:
    get:
      operationId: getConversationsConversationIdMessages
      tags:
      - Conversations
      summary: Get Messages
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/messages.ts:50` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/messages.ts:50
      x-operation-type: read
      x-publicly-exposed: true
      x-service-calls:
      - messageService.listByConversation
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postConversationsConversationIdMessages
      tags:
      - Conversations
      summary: Create Messages
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Side effects: queue: outgoingMessageQueue.add.

        Implemented at `services/conversation/src/routes/messages.ts:64` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/messages.ts:64
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - conversation.findFirst
      - message.findFirst
      - conversation.update
      x-side-effects:
      - "queue: outgoingMessageQueue.add"
      x-service-calls:
      - messageService.create
      x-async: Enqueues background work; the response returns before that work completes.
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      requestBody:
        required: true
        description: Validated by `sendMessageSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                body:
                  type: string
                  minLength: 1
                messageType:
                  type: string
                replyToMessageId:
                  type: string
                emailReplyMode:
                  type: string
                  enum:
                  - reply
                  - new
                subject:
                  type: string
                  maxLength: 200
              required:
              - body
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/conversations/{conversationId}/messages/{messageId}:
    delete:
      operationId: deleteConversationsConversationIdMessagesMessageId
      tags:
      - Conversations
      summary: Delete Message
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/messages.ts:318` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/messages.ts:318
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - message.findFirst
      - message.delete
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      - name: messageId
        in: path
        required: true
        schema:
          type: string
        description: "`messageId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/conversations/{conversationId}/messages/media:
    post:
      operationId: postConversationsConversationIdMessagesMedia
      tags:
      - Conversations
      summary: Create Media
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Side effects: queue: outgoingMessageQueue.add.

        Implemented at `services/conversation/src/routes/messages.ts:205` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/messages.ts:205
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - conversation.findFirst
      x-side-effects:
      - "queue: outgoingMessageQueue.add"
      x-service-calls:
      - messageService.create
      x-async: Enqueues background work; the response returns before that work completes.
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/conversations/{id}:
    delete:
      operationId: deleteConversationsId
      tags:
      - Conversations
      summary: Delete Conversation
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/conversations.ts:189` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/conversations.ts:189
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - conversation.findFirst
      - message.deleteMany
      - conversation.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: force
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: getConversationsId
      tags:
      - Conversations
      summary: Get Conversation
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/conversations.ts:102` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/conversations.ts:102
      x-operation-type: read
      x-publicly-exposed: true
      x-service-calls:
      - conversationService.getById
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/conversations/{id}/claim:
    post:
      operationId: postConversationsIdClaim
      tags:
      - Conversations
      summary: Create Claim
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/conversations.ts:113` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/conversations.ts:113
      x-operation-type: write
      x-publicly-exposed: true
      x-service-calls:
      - conversationService.claim
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/conversations/{id}/close:
    post:
      operationId: postConversationsIdClose
      tags:
      - Conversations
      summary: Create Close
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/conversations.ts:174` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/conversations.ts:174
      x-operation-type: write
      x-publicly-exposed: true
      x-service-calls:
      - conversationService.close
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/conversations/{id}/notes:
    get:
      operationId: getConversationsIdNotes
      tags:
      - Conversations
      summary: Get Notes
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/conversations.ts:91` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/conversations.ts:91
      x-operation-type: read
      x-publicly-exposed: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postConversationsIdNotes
      tags:
      - Conversations
      summary: Create Notes
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/conversations.ts:96` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/conversations.ts:96
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/conversations/{id}/reassign:
    post:
      operationId: postConversationsIdReassign
      tags:
      - Conversations
      summary: Create Reassign
      description: |-
        Allow both ADMIN and the assigned agent to transfer/reassign

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/conversations.ts:143` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/conversations.ts:143
      x-operation-type: write
      x-publicly-exposed: true
      x-service-calls:
      - conversationService.getById
      - conversationService.transferToDepartment
      - conversationService.reassign
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/conversations/{id}/release:
    post:
      operationId: postConversationsIdRelease
      tags:
      - Conversations
      summary: Create Release
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/conversations.ts:122` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/conversations.ts:122
      x-operation-type: write
      x-publicly-exposed: true
      x-service-calls:
      - conversationService.release
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/conversations/{id}/return-to-ai:
    post:
      operationId: postConversationsIdReturnToAi
      tags:
      - Conversations
      summary: Create Return To Ai
      description: |-
        Hand a conversation back to the AI employee (clears the one-way
        isHandedOver latch). 409 when no AI employee is bound.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/conversations.ts:133` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/conversations.ts:133
      x-operation-type: write
      x-publicly-exposed: true
      x-service-calls:
      - conversationService.returnToAi
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/conversations/history/{customerExternalId}:
    get:
      operationId: getConversationsHistoryCustomerExternalId
      tags:
      - Conversations
      summary: Get History
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/conversations.ts:71` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/conversations.ts:71
      x-operation-type: read
      x-publicly-exposed: true
      x-service-calls:
      - conversationService.getHistoryByCustomerExternalId
      parameters:
      - name: customerExternalId
        in: path
        required: true
        schema:
          type: string
        description: "`customerExternalId` path segment."
      - name: conversationId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/conversations/stats/workload:
    get:
      operationId: getConversationsStatsWorkload
      tags:
      - Conversations
      summary: List Workload
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/conversations.ts:37` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/conversations.ts:37
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-service-calls:
      - conversationService.getAgentWorkload
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/copilot/cue-outcome:
    post:
      operationId: postCopilotCueOutcome
      tags:
      - Internal
      summary: Create Cue Outcome
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/copilot-outcomes.ts:39` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/copilot-outcomes.ts:39
      x-operation-type: write
      x-publicly-exposed: false
      x-database:
      - conversation.findFirst
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/crm/auto-link-identifier:
    post:
      operationId: postCrmAutoLinkIdentifier
      tags:
      - Contacts & CRM
      summary: Create Auto Link Identifier
      description: |-
        POST /api/crm/auto-link-identifier
          body: { tenantId, conversationId, identifier: { type: "email"|"phone", value: string } }
          returns: { ok, outcome: "linked"|"created"|"needs_approval"|"not_found"|"error",
                     crmContactId?, crmObjectKind?, candidates?, reason? }

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/ai/src/routes/crm-auto-link.ts:160` (ai service).
      security:
      - internalServiceToken: []
      x-service: ai
      x-source: services/ai/src/routes/crm-auto-link.ts:160
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - conversation.findFirst
      - tenant.findUnique
      - contact.findFirst
      - contact.update
      - contact.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/crm/conversation/{id}/context:
    get:
      operationId: getCrmConversationIdContext
      tags:
      - Contacts & CRM
      summary: Get Context
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/crm-panel.ts:318` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/crm-panel.ts:318
      x-operation-type: read
      x-publicly-exposed: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/crm/conversation/{id}/create-lead:
    post:
      operationId: postCrmConversationIdCreateLead
      tags:
      - Contacts & CRM
      summary: Create Create Lead
      description: |-
        Used when the panel shows "unmapped" - operator clicks "Create lead in CRM".

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/crm-panel.ts:514` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/crm-panel.ts:514
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/crm/conversation/{id}/notes:
    post:
      operationId: postCrmConversationIdNotes
      tags:
      - Contacts & CRM
      summary: Create Notes
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/crm-panel.ts:667` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/crm-panel.ts:667
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/crm/conversation/{id}/pick-candidate:
    post:
      operationId: postCrmConversationIdPickCandidate
      tags:
      - Contacts & CRM
      summary: Create Pick Candidate
      description: |-
        Operator resolution path for the "needs_approval" state. When the identity
        service finds 2+ CRM candidates and can't safely merge, the panel surfaces
        the list; the operator picks one and we pin the linkage. No merge happens -
        the unselected duplicates stay in the CRM and the operator can clean them
        up in the vendor UI.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/crm-panel.ts:615` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/crm-panel.ts:615
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/crm/conversation/{id}/sync-close:
    post:
      operationId: postCrmConversationIdSyncClose
      tags:
      - Contacts & CRM
      summary: Create Sync Close
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/crm-panel.ts:929` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/crm-panel.ts:929
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/crm/resolve-identifiers:
    post:
      operationId: postCrmResolveIdentifiers
      tags:
      - Contacts & CRM
      summary: Create Resolve Identifiers
      description: |-
        POST /api/crm/resolve-identifiers
          body: { tenantId, conversationId }
          returns: { ok, phone?, email?, channelExternalIds: Record<channel, string>,
                     crmContactId?, crmObjectKind?, vendor? }

        Used by the conversation service's cross-platform History walk: the CRM is
        the source of truth for every identifier a person carries (phone, email,
        gotcha_psid_*, etc.). Returning them all lets the walk find conversations
        across every channel without depending on the sparse local Contact table.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/ai/src/routes/crm-auto-link.ts:65` (ai service).
      security:
      - internalServiceToken: []
      x-service: ai
      x-source: services/ai/src/routes/crm-auto-link.ts:65
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - conversation.findFirst
      - contact.findFirst
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/custom-api-tools:
    get:
      operationId: getCustomApiTools
      tags:
      - AI Studio
      summary: List Custom Api Tools
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/custom-api-admin.ts:31` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/custom-api-admin.ts:31
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postCustomApiTools
      tags:
      - AI Studio
      summary: Create Custom Api Tools
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/custom-api-admin.ts:39` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/custom-api-admin.ts:39
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/custom-api-tools/{id}:
    delete:
      operationId: deleteCustomApiToolsId
      tags:
      - AI Studio
      summary: Delete Custom Api Tool
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/custom-api-admin.ts:124` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/custom-api-admin.ts:124
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchCustomApiToolsId
      tags:
      - AI Studio
      summary: Update Custom Api Tool
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/custom-api-admin.ts:93` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/custom-api-admin.ts:93
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/custom-api-tools/{id}/test:
    post:
      operationId: postCustomApiToolsIdTest
      tags:
      - AI Studio
      summary: Create Test
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/custom-api-admin.ts:131` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/custom-api-admin.ts:131
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/custom-db-tools:
    get:
      operationId: getCustomDbTools
      tags:
      - AI Studio
      summary: List Custom Db Tools
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/custom-db-admin.ts:24` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/custom-db-admin.ts:24
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: provider
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postCustomDbTools
      tags:
      - AI Studio
      summary: Create Custom Db Tools
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/custom-db-admin.ts:34` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/custom-db-admin.ts:34
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/custom-db-tools/{id}:
    delete:
      operationId: deleteCustomDbToolsId
      tags:
      - AI Studio
      summary: Delete Custom Db Tool
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/custom-db-admin.ts:105` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/custom-db-admin.ts:105
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchCustomDbToolsId
      tags:
      - AI Studio
      summary: Update Custom Db Tool
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/custom-db-admin.ts:84` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/custom-db-admin.ts:84
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/custom-db-tools/{id}/test:
    post:
      operationId: postCustomDbToolsIdTest
      tags:
      - AI Studio
      summary: Create Test
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/custom-db-admin.ts:112` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/custom-db-admin.ts:112
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/customer-snapshot:
    get:
      operationId: getCustomerSnapshot
      tags:
      - Integrations
      summary: List Customer Snapshot
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/customer-snapshot.ts:17` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/customer-snapshot.ts:17
      x-operation-type: read
      x-publicly-exposed: true
      parameters:
      - name: conversationId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: identityKey
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/customer-summary:
    post:
      operationId: postCustomerSummary
      tags:
      - Integrations
      summary: Create Customer Summary
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/customer-summary.ts:33` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/customer-summary.ts:33
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - conversation.findUnique
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/decision-timeline/conversation/{id}:
    get:
      operationId: getDecisionTimelineConversationId
      tags:
      - AI Studio
      summary: Get Conversation
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/decision-timeline.ts:40` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/decision-timeline.ts:40
      x-operation-type: read
      x-publicly-exposed: false
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/departments:
    get:
      operationId: getDepartments
      tags:
      - Tenant Settings
      summary: List Departments
      description: |-
        GET / - List departments (any authenticated user)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/departments.ts:9` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/departments.ts:9
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        mfa: required
      x-database:
      - department.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postDepartments
      tags:
      - Tenant Settings
      summary: Create Departments
      description: |-
        Plan limit enforced before the create runs, not after.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/departments.ts:100` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/departments.ts:100
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        planLimits:
        - limit:departments
        mfa: required
      x-database:
      - department.findUnique
      - department.findFirst
      - department.create
      requestBody:
        required: true
        description: Validated by `createDepartmentSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                description:
                  type: string
                  maxLength: 500
                queueMode:
                  type: string
                  enum:
                  - CLAIM
                  - ROUND_ROBIN
                parentId:
                  type: string
              required:
              - name
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/departments/{id}:
    delete:
      operationId: deleteDepartmentsId
      tags:
      - Tenant Settings
      summary: Delete Department
      description: |-
        DELETE /:id - Delete department (ADMIN only)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/departments.ts:163` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/departments.ts:163
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      x-database:
      - department.findFirst
      - department.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchDepartmentsId
      tags:
      - Tenant Settings
      summary: Update Department
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/departments.ts:138` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/departments.ts:138
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      x-database:
      - department.findFirst
      - department.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: true
        description: Validated by `updateDepartmentSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                description:
                  type: string
                  maxLength: 500
                queueMode:
                  type: string
                  enum:
                  - CLAIM
                  - ROUND_ROBIN
                isActive:
                  type: boolean
                parentId:
                  type: string
                  nullable: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/departments/{id}/ai-employee:
    get:
      operationId: getDepartmentsIdAiEmployee
      tags:
      - Tenant Settings
      summary: Get Ai Employee
      description: |-
        GET /:id/ai-employee - Get assigned AI Employee for department

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/departments.ts:389` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/departments.ts:389
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requireDepartmentRole(MANAGER)
        mfa: required
      x-database:
      - department.findFirst
      - routerRule.findFirst
      - aIAgent.findUnique
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putDepartmentsIdAiEmployee
      tags:
      - Tenant Settings
      summary: Replace Ai Employee
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/departments.ts:427` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/departments.ts:427
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      x-database:
      - department.findFirst
      - routerRule.findFirst
      - routerRule.delete
      - aIAgent.findFirst
      - routerRule.update
      - routerRule.aggregate
      - routerRule.create
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: true
        description: Validated by `assignAIEmployeeSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                aiAgentId:
                  type: string
                  nullable: true
              required:
              - aiAgentId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/departments/{id}/ai-employees:
    get:
      operationId: getDepartmentsIdAiEmployees
      tags:
      - Tenant Settings
      summary: Get Ai Employees
      description: |-
        GET /:id/ai-employees - list ALL AI employees attached to the department

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/departments.ts:517` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/departments.ts:517
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requireDepartmentRole(MANAGER)
        mfa: required
      x-database:
      - department.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postDepartmentsIdAiEmployees
      tags:
      - Tenant Settings
      summary: Create Ai Employees
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/departments.ts:530` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/departments.ts:530
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:members:manage
        mfa: required
      x-database:
      - department.findFirst
      - aIAgent.findFirst
      - routerRule.findFirst
      - routerRule.update
      - routerRule.aggregate
      - routerRule.create
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: true
        description: Validated by `attachAIEmployeeSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                aiAgentId:
                  type: string
                  minLength: 1
              required:
              - aiAgentId
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/departments/{id}/ai-employees/{aiAgentId}:
    delete:
      operationId: deleteDepartmentsIdAiEmployeesAiAgentId
      tags:
      - Tenant Settings
      summary: Delete Ai Employee
      description: |-
        DELETE /:id/ai-employees/:aiAgentId - detach ONE AI employee, leaving the rest

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/departments.ts:575` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/departments.ts:575
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissions:
        - settings:members:manage
        mfa: required
      x-database:
      - department.findFirst
      - routerRule.deleteMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: aiAgentId
        in: path
        required: true
        schema:
          type: string
        description: "`aiAgentId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/departments/{id}/copilot:
    get:
      operationId: getDepartmentsIdCopilot
      tags:
      - Tenant Settings
      summary: Get Copilot
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/departments.ts:319` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/departments.ts:319
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        mfa: required
      x-database:
      - department.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putDepartmentsIdCopilot
      tags:
      - Tenant Settings
      summary: Replace Copilot
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/departments.ts:339` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/departments.ts:339
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      x-database:
      - department.findFirst
      - aIAgent.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: true
        description: Validated by `updateCopilotSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties: {}
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/departments/{id}/members:
    get:
      operationId: getDepartmentsIdMembers
      tags:
      - Tenant Settings
      summary: Get Members
      description: |-
        GET /:id/members - List department members (ADMIN or MANAGER)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/departments.ts:177` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/departments.ts:177
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requireDepartmentRole(MANAGER)
        mfa: required
      x-database:
      - department.findFirst
      - departmentMember.findMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postDepartmentsIdMembers
      tags:
      - Tenant Settings
      summary: Create Members
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/departments.ts:205` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/departments.ts:205
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      x-database:
      - department.findFirst
      - user.findFirst
      - departmentMember.findFirst
      - departmentMember.create
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: true
        description: Validated by `addMemberSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                userId:
                  type: string
                  minLength: 1
                departmentRole:
                  type: string
                  enum:
                  - AGENT
                  - MANAGER
              required:
              - userId
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/departments/{id}/members/{userId}:
    delete:
      operationId: deleteDepartmentsIdMembersUserId
      tags:
      - Tenant Settings
      summary: Delete Member
      description: |-
        DELETE /:id/members/:userId - Remove member (ADMIN only)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/departments.ts:261` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/departments.ts:261
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      x-database:
      - departmentMember.findFirst
      - departmentMember.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: userId
        in: path
        required: true
        schema:
          type: string
        description: "`userId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchDepartmentsIdMembersUserId
      tags:
      - Tenant Settings
      summary: Update Member
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        This is a write operation.

        Implemented at `services/auth/src/routes/departments.ts:241` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/departments.ts:241
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        mfa: required
      x-database:
      - departmentMember.findFirst
      - departmentMember.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: userId
        in: path
        required: true
        schema:
          type: string
        description: "`userId` path segment."
      requestBody:
        required: true
        description: Validated by `updateMemberSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                departmentRole:
                  type: string
                  enum:
                  - AGENT
                  - MANAGER
              required:
              - departmentRole
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/departments/tree:
    get:
      operationId: getDepartmentsTree
      tags:
      - Tenant Settings
      summary: List Tree
      description: |-
        GET /tree - Get departments as hierarchical tree

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. The caller must have MFA enrolled.

        Implemented at `services/auth/src/routes/departments.ts:34` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/departments.ts:34
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        mfa: required
      x-database:
      - department.findMany
      - aIAgent.findMany
      - routerRule.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/embedded-chat/bootstrap:
    post:
      operationId: postEmbeddedChatBootstrap
      tags:
      - Embedded Chat
      summary: Create Bootstrap
      description: |-
        POST /api/embedded-chat/init - Initialize a chat session (public, no auth)

        POST /bootstrap - everything the widget needs before it draws anything.

        This exists so a widget that no longer belongs to anyone draws NOTHING.
        Previously the script painted its launcher unconditionally and only
        called the server when a visitor opened it; delete the channel and the
        customer's site kept a chat button that opened onto silence. Asking
        first means a deleted widget simply is not there.

        The refusal is uniform on purpose. A deleted widget, a disabled one and
        an id that never existed all answer the same way: a public endpoint on
        someone else's website is not a place to explain our internal state.

        And it answers 200 with an empty body rather than 404. "Render nothing"
        is a legitimate answer to "what should I render", not a failure - and a
        404 puts a red line in the console of a tenant's own website that no
        amount of handling in the script can suppress. It also stops the status
        code from confirming whether a given widget id exists.

        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/ai/src/routes/embedded-chat.ts:107` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/embedded-chat.ts:107
      x-operation-type: write
      x-publicly-exposed: true
      x-rate-limit:
      - initLimiter
      x-database:
      - channelAccount.findFirst
      - channelAccount.update
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/embedded-chat/init:
    post:
      operationId: postEmbeddedChatInit
      tags:
      - Embedded Chat
      summary: Create Init
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/ai/src/routes/embedded-chat.ts:170` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/embedded-chat.ts:170
      x-operation-type: write
      x-publicly-exposed: true
      x-rate-limit:
      - initLimiter
      x-database:
      - channelAccount.findFirst
      - channelAccount.update
      - conversation.findFirst
      - conversation.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/embedded-chat/message:
    post:
      operationId: postEmbeddedChatMessage
      tags:
      - Embedded Chat
      summary: Create Message
      description: |-
        POST /api/embedded-chat/message - Send a message (public)

        No authentication middleware is applied to this route.

        This is a write operation.

        Side effects: queue: incomingMessageQueue.add.

        Implemented at `services/ai/src/routes/embedded-chat.ts:279` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/embedded-chat.ts:279
      x-operation-type: write
      x-publicly-exposed: true
      x-rate-limit:
      - messageLimiter
      x-database:
      - conversation.findUnique
      x-side-effects:
      - "queue: incomingMessageQueue.add"
      x-async: Enqueues background work; the response returns before that work completes.
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "413":
          description: Payload too large
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/embedded-chat/messages/{sessionId}:
    get:
      operationId: getEmbeddedChatMessagesSessionId
      tags:
      - Embedded Chat
      summary: Get Message
      description: |-
        GET /api/embedded-chat/messages/:sessionId - Get messages for a session (public)

        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/embedded-chat.ts:343` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/embedded-chat.ts:343
      x-operation-type: read
      x-publicly-exposed: true
      x-rate-limit:
      - pollLimiter
      x-database:
      - conversation.findUnique
      - message.findMany
      parameters:
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
        description: "`sessionId` path segment."
      - name: after
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/field-definitions:
    get:
      operationId: getFieldDefinitions
      tags:
      - Tenant Settings
      summary: List Field Definitions
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/field-definitions.ts:36` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/field-definitions.ts:36
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: scope
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postFieldDefinitions
      tags:
      - Tenant Settings
      summary: Create Field Definitions
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/field-definitions.ts:48` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/field-definitions.ts:48
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/field-definitions/{id}:
    delete:
      operationId: deleteFieldDefinitionsId
      tags:
      - Tenant Settings
      summary: Delete Field Definition
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/field-definitions.ts:92` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/field-definitions.ts:92
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putFieldDefinitionsId
      tags:
      - Tenant Settings
      summary: Replace Field Definition
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/field-definitions.ts:68` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/field-definitions.ts:68
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/flow-canvas:
    get:
      operationId: getFlowCanvas
      tags:
      - AI Studio
      summary: List Flow Canvas
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/flow-canvas.ts:9` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/flow-canvas.ts:9
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - flowCanvas.findUnique
      - channelAccount.findMany
      - routerRule.findMany
      - chatbotFlow.findMany
      - aIAgent.findMany
      - department.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putFlowCanvas
      tags:
      - AI Studio
      summary: Replace Flow Canvas
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/flow-canvas.ts:54` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/flow-canvas.ts:54
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - flowCanvas.upsert
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/flow-canvas/auto-generate:
    post:
      operationId: postFlowCanvasAutoGenerate
      tags:
      - AI Studio
      summary: Create Auto Generate
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/flow-canvas.ts:84` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/flow-canvas.ts:84
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - channelAccount.findMany
      - routerRule.findMany
      - chatbotFlow.findMany
      - aIAgent.findMany
      - department.findMany
      - flowCanvas.upsert
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/funnels:
    get:
      operationId: getFunnels
      tags:
      - AI Studio
      summary: List Funnels
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/funnel-admin.ts:29` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/funnel-admin.ts:29
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: departmentId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postFunnels
      tags:
      - AI Studio
      summary: Create Funnels
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/funnel-admin.ts:55` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/funnel-admin.ts:55
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/funnels/{id}:
    delete:
      operationId: deleteFunnelsId
      tags:
      - AI Studio
      summary: Delete Funnel
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/funnel-admin.ts:143` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/funnel-admin.ts:143
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: getFunnelsId
      tags:
      - AI Studio
      summary: Get Funnel
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/funnel-admin.ts:42` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/funnel-admin.ts:42
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchFunnelsId
      tags:
      - AI Studio
      summary: Update Funnel
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/funnel-admin.ts:95` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/funnel-admin.ts:95
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/funnels/{id}/activate:
    post:
      operationId: postFunnelsIdActivate
      tags:
      - AI Studio
      summary: Create Activate
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/funnel-admin.ts:119` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/funnel-admin.ts:119
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/gdpr-internal/purge-tenant-vectors:
    post:
      operationId: postGdprInternalPurgeTenantVectors
      tags:
      - GDPR & Compliance
      summary: Create Purge Tenant Vectors
      description: |-
        POST /api/gdpr-internal/purge-tenant-vectors
          body: { tenantId }
          Called by the auth service during tenant deletion to clean up this
          tenant's Qdrant vectors (KB embeddings are the only per-tenant data
          this service stores outside Postgres).

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/ai/src/routes/gdpr-internal.ts:25` (ai service).
      security:
      - internalServiceToken: []
      x-service: ai
      x-source: services/ai/src/routes/gdpr-internal.ts:25
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/gdpr-internal/run-retention-purge:
    post:
      operationId: postGdprInternalRunRetentionPurge
      tags:
      - GDPR & Compliance
      summary: Create Run Retention Purge
      description: |-
        POST /api/gdpr-internal/run-retention-purge
          Triggers a full data-retention purge run across all enabled
          DataRetentionPolicy rows. Invoked by cron or manually.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/ai/src/routes/gdpr-internal.ts:52` (ai service).
      security:
      - internalServiceToken: []
      x-service: ai
      x-source: services/ai/src/routes/gdpr-internal.ts:52
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/gdpr/consent:
    get:
      operationId: getGdprConsent
      tags:
      - GDPR & Compliance
      summary: List Consent
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/auth/src/routes/gdpr.ts:139` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/gdpr.ts:139
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - consentRecord.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postGdprConsent
      tags:
      - GDPR & Compliance
      summary: Create Consent
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/auth/src/routes/gdpr.ts:105` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/gdpr.ts:105
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - consentRecord.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/gdpr/contacts/{id}:
    delete:
      operationId: deleteGdprContactsId
      tags:
      - GDPR & Compliance
      summary: Delete Contact
      description: |-
        DELETE /:id - Right to erasure (Art. 17)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/gdpr-contacts.ts:124` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/gdpr-contacts.ts:124
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - contact.findFirst
      - contact.findMany
      - conversation.findMany
      - message.deleteMany
      - conversation.deleteMany
      - consentRecord.deleteMany
      - contact.deleteMany
      - customerHistoricalMemory.deleteMany
      - dataSubjectRequest.create
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/gdpr/contacts/{id}/consent:
    get:
      operationId: getGdprContactsIdConsent
      tags:
      - GDPR & Compliance
      summary: Get Consent
      description: |-
        GET /:id/consent - list consent records for the contact

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/gdpr-contacts.ts:279` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/gdpr-contacts.ts:279
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - contact.findFirst
      - consentRecord.findMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postGdprContactsIdConsent
      tags:
      - GDPR & Compliance
      summary: Create Consent
      description: |-
        POST /:id/consent - record consent (Art. 7 proof)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/gdpr-contacts.ts:229` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/gdpr-contacts.ts:229
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - contact.findFirst
      - consentRecord.create
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/gdpr/contacts/{id}/export:
    get:
      operationId: getGdprContactsIdExport
      tags:
      - GDPR & Compliance
      summary: Get Export
      description: |-
        GET /:id/export - Right of access/portability (Art. 15/20)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/gdpr-contacts.ts:34` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/gdpr-contacts.ts:34
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - contact.findFirst
      - conversation.findMany
      - message.findMany
      - consentRecord.findMany
      - dataSubjectRequest.create
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/gdpr/retention:
    get:
      operationId: getGdprRetention
      tags:
      - GDPR & Compliance
      summary: List Retention
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/auth/src/routes/gdpr.ts:165` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/gdpr.ts:165
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - dataRetentionPolicy.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putGdprRetention
      tags:
      - GDPR & Compliance
      summary: Replace Retention
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/auth/src/routes/gdpr.ts:182` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/gdpr.ts:182
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - dataRetentionPolicy.upsert
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/gdpr/tenants/{tenantId}:
    delete:
      operationId: deleteGdprTenantsTenantId
      tags:
      - GDPR & Compliance
      summary: Delete Tenant
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/auth/src/routes/gdpr.ts:88` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/gdpr.ts:88
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      parameters:
      - name: tenantId
        in: path
        required: true
        schema:
          type: string
        description: "`tenantId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/gdpr/tenants/{tenantId}/export:
    get:
      operationId: getGdprTenantsTenantIdExport
      tags:
      - GDPR & Compliance
      summary: Get Export
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/auth/src/routes/gdpr.ts:72` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/gdpr.ts:72
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      parameters:
      - name: tenantId
        in: path
        required: true
        schema:
          type: string
        description: "`tenantId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/gdpr/users/{userId}:
    delete:
      operationId: deleteGdprUsersUserId
      tags:
      - GDPR & Compliance
      summary: Delete User
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/auth/src/routes/gdpr.ts:46` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/gdpr.ts:46
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: userId
        in: path
        required: true
        schema:
          type: string
        description: "`userId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/gdpr/users/{userId}/export:
    get:
      operationId: getGdprUsersUserIdExport
      tags:
      - GDPR & Compliance
      summary: Get Export
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/auth/src/routes/gdpr.ts:30` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/gdpr.ts:30
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: userId
        in: path
        required: true
        schema:
          type: string
        description: "`userId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/historical-imports:
    get:
      operationId: getHistoricalImports
      tags:
      - Historical Import
      summary: List Historical Imports
      description: |-
        GET /api/historical-imports

        The channel card's poll. Deliberately small and cheap: it runs every few
        seconds while an import is in flight, and it must stay a single indexed read.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/historical-import.ts:56` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/historical-import.ts:56
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
      x-database:
      - historicalImport.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/historical-imports/{id}:
    get:
      operationId: getHistoricalImportsId
      tags:
      - Historical Import
      summary: Get Historical Import
      description: |-
        GET /api/historical-imports/:id - status plus the full persisted summary.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/historical-import.ts:71` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/historical-import.ts:71
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
      x-database:
      - historicalImport.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/historical-imports/{id}/bulk-approve:
    post:
      operationId: postHistoricalImportsIdBulkApprove
      tags:
      - Historical Import
      summary: Create Bulk Approve
      description: |-
        POST /api/historical-imports/:id/bulk-approve

        Only ever touches items the API itself classified as safe: no conflicts, and
        confidence at or above the shared threshold. The set is recomputed
        server-side rather than taken from the client, so a stale page cannot sweep
        in a candidate that has since been flagged.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/historical-import.ts:428` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/historical-import.ts:428
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
        gates:
        - writeGuard
      x-database:
      - knowledgeCandidate.findMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/historical-imports/{id}/bulk-reject:
    post:
      operationId: postHistoricalImportsIdBulkReject
      tags:
      - Historical Import
      summary: Create Bulk Reject
      description: |-
        POST /api/historical-imports/:id/bulk-reject

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/historical-import.ts:465` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/historical-import.ts:465
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
        gates:
        - writeGuard
      x-database:
      - knowledgeCandidate.updateMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/historical-imports/{id}/candidates:
    get:
      operationId: getHistoricalImportsIdCandidates
      tags:
      - Historical Import
      summary: Get Candidates
      description: |-
        GET /api/historical-imports/:id/candidates

        The review queue. Evidence is included but capped: the reviewer needs two or
        three examples to judge a suggestion, not the two hundred conversations
        behind it. The full count is reported so the card can say "found in 126
        conversations" truthfully while showing three.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/historical-import.ts:129` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/historical-import.ts:129
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
      x-database:
      - knowledgeCandidate.findMany
      - knowledgeCandidate.groupBy
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/historical-imports/{id}/customers:
    get:
      operationId: getHistoricalImportsIdCustomers
      tags:
      - Historical Import
      summary: Get Customers
      description: |-
        GET /api/historical-imports/:id/customers

        The imported customers and what we managed to link them to. Used by the
        results page and, more often, by an operator checking a real import.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/historical-import.ts:565` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/historical-import.ts:565
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
      x-database:
      - historicalCustomer.findMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/historical-imports/{id}/events:
    get:
      operationId: getHistoricalImportsIdEvents
      tags:
      - Historical Import
      summary: Get Events
      description: |-
        GET /api/historical-imports/:id/events

        The operator's view of what happened, for debugging a real customer's import.
        Counts and safe metadata only - the event rows never contain message bodies.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/historical-import.ts:97` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/historical-import.ts:97
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
      x-database:
      - historicalImport.findFirst
      - historicalImportEvent.findMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/historical-imports/{id}/rerun-intelligence:
    post:
      operationId: postHistoricalImportsIdRerunIntelligence
      tags:
      - Historical Import
      summary: Create Rerun Intelligence
      description: |-
        POST /api/historical-imports/:id/rerun-intelligence

        Wipe the derived artifacts (memories, candidates, vectors) and run the
        intelligence stages again over the already-imported conversations. For when
        the analysis was wrong but the data is fine - the imported messages are the
        one thing Meta will never send twice, and they are untouched.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/historical-import.ts:492` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/historical-import.ts:492
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
        gates:
        - writeGuard
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/historical-imports/candidates/{candidateId}/approve:
    post:
      operationId: postHistoricalImportsCandidatesCandidateIdApprove
      tags:
      - Historical Import
      summary: Create Approve
      description: |-
        POST /api/historical-imports/candidates/:candidateId/approve

        The only path from a suggestion into production knowledge, and it requires a
        human on the other end of it. The body may carry an edited answer, which is
        stored separately from the observed one so the original stays auditable.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/historical-import.ts:263` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/historical-import.ts:263
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
        gates:
        - writeGuard
      x-database:
      - knowledgeCandidate.findFirst
      - knowledgeCandidate.updateMany
      - knowledgeDocument.create
      parameters:
      - name: candidateId
        in: path
        required: true
        schema:
          type: string
        description: "`candidateId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/historical-imports/candidates/{candidateId}/evidence:
    get:
      operationId: getHistoricalImportsCandidatesCandidateIdEvidence
      tags:
      - Historical Import
      summary: Get Evidence
      description: |-
        GET /api/historical-imports/candidates/:candidateId/evidence

        Every conversation behind one suggestion, for the reviewer who wants to check
        rather than trust. Capped, and scoped to the tenant like everything else.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/historical-import.ts:224` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/historical-import.ts:224
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
      x-database:
      - knowledgeCandidate.findFirst
      - knowledgeCandidateEvidence.findMany
      parameters:
      - name: candidateId
        in: path
        required: true
        schema:
          type: string
        description: "`candidateId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/historical-imports/candidates/{candidateId}/reject:
    post:
      operationId: postHistoricalImportsCandidatesCandidateIdReject
      tags:
      - Historical Import
      summary: Create Reject
      description: |-
        POST /api/historical-imports/candidates/:candidateId/reject

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/historical-import.ts:399` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/historical-import.ts:399
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
        gates:
        - writeGuard
      x-database:
      - knowledgeCandidate.updateMany
      parameters:
      - name: candidateId
        in: path
        required: true
        schema:
          type: string
        description: "`candidateId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/historical-imports/conversations/{conversationId}/messages:
    get:
      operationId: getHistoricalImportsConversationsConversationIdMessages
      tags:
      - Historical Import
      summary: Get Messages
      description: |-
        GET /api/historical-imports/conversations/:conversationId/messages

        One imported thread. Explicitly opts out of the live-only default, because
        reading imported history is the entire purpose of the endpoint.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/historical-import.ts:631` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/historical-import.ts:631
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
      x-database:
      - conversation.findFirst
      - message.findMany
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/historical-imports/customer-context:
    get:
      operationId: getHistoricalImportsCustomerContext
      tags:
      - Historical Import
      summary: List Customer Context
      description: |-
        GET /api/historical-imports/customer-context?externalId=<phone>

        What we learned about THIS person, for the live inbox - keyed by the
        customer's own identifier rather than an import-internal id.

        The import already produced this (a summary and durable facts per customer)
        but only the AI could see it: the memory went into the bot prompt and
        nowhere else, so a human agent opening the same chat saw nothing and the
        import looked like it had done nothing. This is the endpoint the
        conversation panel reads.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/historical-import.ts:518` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/historical-import.ts:518
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
      x-database:
      - customerHistoricalMemory.findFirst
      parameters:
      - name: externalId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/historical-imports/customers/{customerId}/memory:
    get:
      operationId: getHistoricalImportsCustomersCustomerIdMemory
      tags:
      - Historical Import
      summary: Get Memory
      description: |-
        GET /api/historical-imports/customers/:customerId/memory

        What GOTCHA learned about one customer. No dedicated UI for this in V1 - it
        is here so an operator can verify that memory is durable rather than
        transient, which is the property most worth checking on the first real
        import.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/historical-import.ts:602` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/historical-import.ts:602
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
      x-database:
      - historicalCustomer.findFirst
      - customerHistoricalMemory.findFirst
      parameters:
      - name: customerId
        in: path
        required: true
        schema:
          type: string
        description: "`customerId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/identity/{id}/timeline:
    get:
      operationId: getIdentityIdTimeline
      tags:
      - Contacts & CRM
      summary: Get Timeline
      description: |-
        GET /:id/timeline - unified timeline of events across channels for the contact

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/identity.ts:202` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/identity.ts:202
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - contact.findFirst
      - conversation.findMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/identity/capture:
    post:
      operationId: postIdentityCapture
      tags:
      - Contacts & CRM
      summary: Create Capture
      description: |-
        Strong-signal identity capture. Called when a bot/flow/agent
        EXPLICITLY asked the customer for their email or phone and the
        customer responded with an identifier. Distinct from /link, which
        handles weak-signal LLM extraction.

        Behavior:
          1. Format-validate + normalize the identifier
          2. Mark it verified=true on the current contact (they volunteered it)
          3. Sibling check: does another contact in this tenant already have
             this identifier?
            - YES → assign the same personId to both. Auto-link across
              channels. No merge, no data loss.
            - NO  → set the identifier on the current contact. Seed planted
              for a future collision on another channel.

        Rate limit: max 3 captures per contact per 24h (reuses the
        identity-link suggestion count as a proxy since both record
        identity-related activity).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/identity.ts:422` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/identity.ts:422
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - contact.findFirst
      - identityLinkSuggestion.count
      - contact.update
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/identity/link:
    post:
      operationId: postIdentityLink
      tags:
      - Contacts & CRM
      summary: Create Link
      description: |-
        Progressive identity linker. Called by the AI tool executor when an
        email or phone is extracted from a customer message. NEVER merges -
        either attaches the identifier to the current contact (safe) or
        records a pending IdentityLinkSuggestion when the identifier already
        belongs to a DIFFERENT contact in the same tenant.

        Security rules:
          - verified identifier on current contact → never overwrite
          - new identifier for a contact that already has one (unverified) → keep
            existing, still create a suggestion so an admin can decide
          - rate-limited: max 3 new identifiers per contact per 24h
          - idempotent per messageId + identifier

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/identity.ts:274` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/identity.ts:274
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - contact.findFirst
      - identityLinkSuggestion.findFirst
      - identityLinkSuggestion.count
      - identityLinkSuggestion.create
      - contact.update
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/identity/merge:
    post:
      operationId: postIdentityMerge
      tags:
      - Contacts & CRM
      summary: Create Merge
      description: |-
        POST /merge - merge source contact into target contact

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/identity.ts:88` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/identity.ts:88
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - contact.findFirst
      - broadcastRecipient.updateMany
      - contact.update
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/identity/resolve:
    post:
      operationId: postIdentityResolve
      tags:
      - Contacts & CRM
      summary: Create Resolve
      description: |-
        POST /resolve - find existing contact by email/phone or heuristic metadata match

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/identity.ts:8` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/identity.ts:8
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - contact.findFirst
      - contact.findMany
      - contact.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/industry-packs:
    get:
      operationId: getIndustryPacks
      tags:
      - AI Studio
      summary: List Industry Packs
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/industry-packs.ts:24` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/industry-packs.ts:24
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/industry-packs/apply:
    post:
      operationId: postIndustryPacksApply
      tags:
      - AI Studio
      summary: Create Apply
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/industry-packs.ts:41` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/industry-packs.ts:41
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/integration-workspace:
    get:
      operationId: getIntegrationWorkspace
      tags:
      - Integrations
      summary: List Integration Workspace
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/integration-workspace.ts:83` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/integration-workspace.ts:83
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:tools:read OR role ADMIN
      x-database:
      - |-
          tenantIntegration.findMany({
                  where: { tenantId },
                  select: { status: true, config: true, integration: { select: { slug: true } } },
                })
      - |-
          knowledgeIntegration.findMany({
                  where: { tenantId },
                  select: { provider: true, isActive: true },
                })
      - knowledgeIntegration.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/integration-workspace/{id}:
    get:
      operationId: getIntegrationWorkspaceId
      tags:
      - Integrations
      summary: Get Integration Workspace
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/integration-workspace.ts:184` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/integration-workspace.ts:184
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:tools:read OR role ADMIN
      x-database:
      - |-
          channelAccount
                    .findMany({ where: { tenantId }, select: { channel: true, connectionStatus: true } })
                    
      - |-
          channelAccount
                    .findMany
      - tenantIntegration.findFirst
      - integrationCatalog.findUnique
      - catalogTool.count
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/integrations:
    get:
      operationId: getIntegrations
      tags:
      - Integrations
      summary: List Integrations
      description: |-
        GET / - List all published catalog integrations with tenant connection status

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/integrations.ts:29` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/integrations.ts:29
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canReadSystems
      x-database:
      - integrationCatalog.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/integrations/{slug}:
    get:
      operationId: getIntegrationsSlug
      tags:
      - Integrations
      summary: Get Integration
      description: |-
        GET /:slug - Get single catalog integration with connection status + tools

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/integrations.ts:288` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/integrations.ts:288
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canReadSystems
      x-database:
      - integrationCatalog.findUnique
      parameters:
      - name: slug
        in: path
        required: true
        schema:
          type: string
        description: "`slug` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/integrations/{slug}/audience-config:
    put:
      operationId: putIntegrationsSlugAudienceConfig
      tags:
      - Integrations
      summary: Replace Audience Config
      description: |-
        PUT /:slug/audience-config - persist leadsBoardId / contactsBoardId.

        For Monday tenants, this maps the abstract "Leads"/"Contacts" modules
        the audience builder uses onto concrete boards. The shared CRM client
        reads these from `tenant_integrations.config` when it dispatches
        describe/search calls.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/integrations.ts:885` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/integrations.ts:885
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canReadSystems
        - canManageSystems
      x-database:
      - integrationCatalog.findUnique
      - tenantIntegration.findFirst
      - tenantIntegration.update
      parameters:
      - name: slug
        in: path
        required: true
        schema:
          type: string
        description: "`slug` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/integrations/{slug}/connect:
    post:
      operationId: postIntegrationsSlugConnect
      tags:
      - Integrations
      summary: Create Connect
      description: |-
        POST /:slug/connect - Connect tenant to integration (API_KEY/BASIC_AUTH path)

        OAuth providers should hit /api/connectors/:slug/oauth/init instead - this
        endpoint stores credentials directly. Credentials are ENCRYPTED before
        persistence (the adapter framework decrypts on load). Existing connections
        are updated in-place rather than rejected, so the marketplace can re-bind
        credentials without forcing a disconnect first.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/integrations.ts:333` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/integrations.ts:333
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canReadSystems
        - canConnectSystems
      x-database:
      - integrationCatalog.findUnique
      - tenantIntegration.findFirst
      - tenantIntegration.update
      - tenantIntegration.create
      parameters:
      - name: slug
        in: path
        required: true
        schema:
          type: string
        description: "`slug` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/integrations/{slug}/credentials:
    put:
      operationId: putIntegrationsSlugCredentials
      tags:
      - Integrations
      summary: Replace Credentials
      description: |-
        PUT /:slug/credentials - Update credentials (encrypted, in place)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/integrations.ts:607` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/integrations.ts:607
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canReadSystems
        - canConnectSystems
      x-database:
      - integrationCatalog.findUnique
      - tenantIntegration.findFirst
      - tenantIntegration.update
      parameters:
      - name: slug
        in: path
        required: true
        schema:
          type: string
        description: "`slug` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/integrations/{slug}/crm-source:
    put:
      operationId: putIntegrationsSlugCrmSource
      tags:
      - Integrations
      summary: Replace Crm Source
      description: |-
        PUT /:slug/crm-source - Opt this integration in/out as the tenant's CRM
        source of truth. Today only Shopify supports this (it's an ECOMMERCE
        integration a tenant may elect as their customer system of record). Sets
        `config.useAsCrm` and invalidates the CRM resolver cache so the next bot
        turn reads customer context from Shopify instead of any CRM-category
        integration. Toggling OFF restores the default resolution order - it never
        disturbs tenants who don't opt in.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/integrations.ts:655` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/integrations.ts:655
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canReadSystems
        - canSelectSot
      x-database:
      - integrationCatalog.findUnique
      - tenantIntegration.findFirst
      - tenantIntegration.update
      parameters:
      - name: slug
        in: path
        required: true
        schema:
          type: string
        description: "`slug` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/integrations/{slug}/disconnect:
    post:
      operationId: postIntegrationsSlugDisconnect
      tags:
      - Integrations
      summary: Create Disconnect
      description: |-
        POST /:slug/disconnect - Disconnect and delete tenant tools (cascade handles child rows)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/integrations.ts:559` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/integrations.ts:559
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canReadSystems
        - canManageSystems
      x-database:
      - integrationCatalog.findUnique
      - tenantIntegration.findFirst
      - tenantTool.deleteMany
      - tenantIntegration.update
      parameters:
      - name: slug
        in: path
        required: true
        schema:
          type: string
        description: "`slug` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/integrations/{slug}/monday-boards:
    get:
      operationId: getIntegrationsSlugMondayBoards
      tags:
      - Integrations
      summary: Get Monday Boards
      description: |-
        Monday's "schema" lives on a board (columns), not on a global Lead/Contact
        object. The audience builder needs the operator to pick which board acts
        as Leads and which acts as Contacts; without these, the schema fetcher
        has nothing to describe and CRM-side filter rules can't run.

        GET /:slug/monday-boards - list the operator's boards for the picker UI.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/integrations.ts:856` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/integrations.ts:856
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canReadSystems
      parameters:
      - name: slug
        in: path
        required: true
        schema:
          type: string
        description: "`slug` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/integrations/{slug}/test:
    post:
      operationId: postIntegrationsSlugTest
      tags:
      - Integrations
      summary: Create Test
      description: |-
        POST /:slug/test - Live connection test.

        Routes by adapter slug:
          - If a registered adapter exposes a READ-only tool, we invoke its first
            READ tool with empty args - a real network roundtrip that validates
            credentials end-to-end.
          - If no adapter is registered (e.g. zoho via the legacy path), we fall
            back to validating required credential fields from authSchema.

        Marks the integration CONNECTED on success and ERROR (with lastError) on
        failure, so the marketplace UI reflects reality.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/integrations.ts:411` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/integrations.ts:411
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canReadSystems
        - canConnectSystems
      x-database:
      - integrationCatalog.findUnique
      - tenantIntegration.findFirst
      - tenantIntegration.update
      parameters:
      - name: slug
        in: path
        required: true
        schema:
          type: string
        description: "`slug` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/integrations/{slug}/tools:
    get:
      operationId: getIntegrationsSlugTools
      tags:
      - Integrations
      summary: Get Tools
      description: |-
        GET /:slug/tools?aiAgentId=… - List catalog tools with tenant activation
        status, plus (when ?aiAgentId is passed) the per-agent permission state.
        Used by the IntegrationDrawer in two contexts:
          - marketplace page (no aiAgentId) → toggles reflect TenantTool.isEnabled
          - agent edit page (aiAgentId set) → toggles reflect
            AgentToolPermission.isAllowed for that agent.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/integrations.ts:712` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/integrations.ts:712
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canReadSystems
      x-database:
      - integrationCatalog.findUnique
      - tenantIntegration.findFirst
      - catalogTool.findMany
      - aIAgent.findFirst
      - agentToolPermission.findMany
      parameters:
      - name: slug
        in: path
        required: true
        schema:
          type: string
        description: "`slug` path segment."
      - name: aiAgentId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/integrations/{slug}/tools/{toolSlug}:
    put:
      operationId: putIntegrationsSlugToolsToolSlug
      tags:
      - Integrations
      summary: Replace Tool
      description: |-
        PUT /:slug/tools/:toolSlug - Toggle tool enabled/disabled for tenant

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/integrations.ts:780` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/integrations.ts:780
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canReadSystems
        - canManageSystems
      x-database:
      - integrationCatalog.findUnique
      - tenantIntegration.findFirst
      - catalogTool.findFirst
      - tenantTool.findFirst
      - tenantTool.update
      - tenantTool.create
      parameters:
      - name: slug
        in: path
        required: true
        schema:
          type: string
        description: "`slug` path segment."
      - name: toolSlug
        in: path
        required: true
        schema:
          type: string
        description: "`toolSlug` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/integrations/calendar/disconnect:
    post:
      operationId: postIntegrationsCalendarDisconnect
      tags:
      - Integrations
      summary: Create Disconnect
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/calendar-oauth.ts:314` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/calendar-oauth.ts:314
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/integrations/oauth/calendly/callback:
    get:
      operationId: getIntegrationsOauthCalendlyCallback
      tags:
      - Integrations
      summary: List Callback
      description: |-
        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/calendar-oauth.ts:222` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/calendar-oauth.ts:222
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/integrations/oauth/calendly/init:
    get:
      operationId: getIntegrationsOauthCalendlyInit
      tags:
      - Integrations
      summary: List Init
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/calendar-oauth.ts:190` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/calendar-oauth.ts:190
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: aiAgentId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: flow
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/integrations/oauth/google_calendar/callback:
    get:
      operationId: getIntegrationsOauthGoogleCalendarCallback
      tags:
      - Integrations
      summary: List Callback
      description: |-
        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/calendar-oauth.ts:93` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/calendar-oauth.ts:93
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/integrations/oauth/google_calendar/init:
    get:
      operationId: getIntegrationsOauthGoogleCalendarInit
      tags:
      - Integrations
      summary: List Init
      description: |-
        The marketplace UI calls /api/integrations/oauth/{catalog_slug}/init -
        the catalog slug for this provider is `google_calendar` (underscore).
        We accept BOTH the marketplace path and the dashed alias so direct callers
        (Scheduler settings page) and the marketplace flow both work.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/calendar-oauth.ts:53` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/calendar-oauth.ts:53
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: aiAgentId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: flow
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/integrations/oauth/google-calendar/callback:
    get:
      operationId: getIntegrationsOauthGoogleCalendarCallback2
      tags:
      - Integrations
      summary: List Callback
      description: |-
        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/calendar-oauth.ts:93` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/calendar-oauth.ts:93
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/integrations/oauth/google-calendar/init:
    get:
      operationId: getIntegrationsOauthGoogleCalendarInit2
      tags:
      - Integrations
      summary: List Init
      description: |-
        The marketplace UI calls /api/integrations/oauth/{catalog_slug}/init -
        the catalog slug for this provider is `google_calendar` (underscore).
        We accept BOTH the marketplace path and the dashed alias so direct callers
        (Scheduler settings page) and the marketplace flow both work.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/calendar-oauth.ts:53` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/calendar-oauth.ts:53
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: aiAgentId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: flow
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/integrations/oauth/zoho_crm/callback:
    get:
      operationId: getIntegrationsOauthZohoCrmCallback
      tags:
      - Integrations
      summary: List Callback
      description: |-
        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/crm-oauth.ts:69` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/crm-oauth.ts:69
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - integrationCatalog.findUnique
      - tenantIntegration.findFirst
      - tenantIntegration.update
      - tenantIntegration.create
      - tenantTool.findMany
      - tenantTool.createMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/integrations/oauth/zoho_crm/init:
    get:
      operationId: getIntegrationsOauthZohoCrmInit
      tags:
      - Integrations
      summary: List Init
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/crm-oauth.ts:32` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/crm-oauth.ts:32
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: flow
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/integrations/source-of-truth:
    get:
      operationId: getIntegrationsSourceOfTruth
      tags:
      - Integrations
      summary: List Source Of Truth
      description: |-
        GET /source-of-truth - the tenant's ELECTED customer system of record as
        the AI-side resolver actually sees it, with its truthful capability set.
        This is what the UI must render (never frontend guesses): which system
        answers "who is this customer", whether back-office writes are available,
        and which operations the provider genuinely supports. Registered BEFORE
        /:slug so the literal path wins.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/integrations.ts:87` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/integrations.ts:87
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - canReadSystems
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/integrations/source-of-truth/customers:
    get:
      operationId: getIntegrationsSourceOfTruthCustomers
      tags:
      - Integrations
      summary: List Customers
      description: |-
        GET /source-of-truth/customers?q= - unified, tenant-scoped customer search
        for outbound calling. Searches THE resolved source of truth (Shopify-as-CRM
        included), so the dialer offers the same identities that answer "who is
        this customer" everywhere else - never another tenant's data, never a
        free-text number promoted to an identity.

        Query detection: phone-shaped and email-shaped inputs go through the
        vendor-neutral adapter's identifier lookup; everything else is a NAME
        search - adapter-native when the adapter implements searchByName (Shopify:
        default text search + first_name/last_name filters, `read_customers`
        enforced by the tool plane), shared CRM lead/contact search otherwise.

        The list is a PICKER: identifiers come back MASKED. Full contact data is
        served by /source-of-truth/customers/detail only after the human selects a
        candidate - a search never auto-resolves to "the first match".
        Registered BEFORE /:slug so the literal path wins.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/integrations.ts:130` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/integrations.ts:130
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - crm:contacts:read OR role ADMIN
        gates:
        - canReadSystems
      parameters:
      - name: q
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: limit
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/integrations/source-of-truth/customers/detail:
    get:
      operationId: getIntegrationsSourceOfTruthCustomersDetail
      tags:
      - Integrations
      summary: List Detail
      description: |-
        GET /source-of-truth/customers/detail?id=&kind= - full contact data for ONE
        explicitly selected candidate (the list above is masked). Same permission
        gate; the adapter resolves within the active tenant's own connection, so a
        foreign id can never cross tenants - it simply doesn't exist there.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/integrations.ts:250` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/integrations.ts:250
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - crm:contacts:read OR role ADMIN
        gates:
        - canReadSystems
      parameters:
      - name: id
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/intelligence-reviews:
    get:
      operationId: getIntelligenceReviews
      tags:
      - AI Studio
      summary: List Intelligence Reviews
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/intelligence-reviews.ts:30` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/intelligence-reviews.ts:30
      x-operation-type: read
      x-publicly-exposed: false
      x-authorization:
        roles:
        - ADMIN
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/intelligence-reviews/{id}/approve:
    post:
      operationId: postIntelligenceReviewsIdApprove
      tags:
      - AI Studio
      summary: Create Approve
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/intelligence-reviews.ts:40` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/intelligence-reviews.ts:40
      x-operation-type: write
      x-publicly-exposed: false
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/intelligence-reviews/{id}/reject:
    post:
      operationId: postIntelligenceReviewsIdReject
      tags:
      - AI Studio
      summary: Create Reject
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/intelligence-reviews.ts:48` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/intelligence-reviews.ts:48
      x-operation-type: write
      x-publicly-exposed: false
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/auth/payment-succeeded:
    post:
      operationId: postInternalAuthPaymentSucceeded
      tags:
      - Internal
      summary: Create Payment Succeeded
      description: |-
        A tenant's payment cleared: welcome them and hand over the way in.

        Called by billing at activation. The commercial facts travel in the request
        because they are billing's to state; auth supplies only the identity half.

        Answers 200 even when there is nothing to send. The money is already
        settled, and a failure here must read as "no email went out", never as a
        reason for the caller to retry a payment.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/auth/src/routes/internal.ts:31` (auth service).
      security:
      - internalServiceToken: []
      x-service: auth
      x-source: services/auth/src/routes/internal.ts:31
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/activate-manual-contract:
    post:
      operationId: postInternalBillingActivateManualContract
      tags:
      - Internal
      summary: Create Activate Manual Contract
      description: |-
        Activate an externally settled contract.

        No provider call, no Charge row, no claim that an iCount transaction
        occurred. Reuses the one activation boundary so the snapshot, the
        amount match and the once-only credit grant all still apply.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:373` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:373
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/auto-purchase:
    post:
      operationId: postInternalBillingAutoPurchase
      tags:
      - Internal
      summary: Create Auto Purchase
      description: |-
        Called by the AI runtime gate when balance crosses the auto-purchase threshold.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:57` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:57
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/chargeback:
    post:
      operationId: postInternalBillingChargeback
      tags:
      - Internal
      summary: Create Chargeback
      description: |-
        Record a chargeback the bank has already made.

        Deliberately operator-initiated. The webhook that used to call this was
        removed because it acted on invented event names from an unverified contract,
        and could be triggered by anyone able to reach it - a stranger could suspend
        a paying organization by posting JSON. Taking that away left no way to record
        a real chargeback at all, which is its own problem: dispute windows are short.

        So it lives here instead, behind the internal key, where a person invokes it
        after seeing the dispute in iCount. Same effect, no strangers.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:129` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:129
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/ensure-entity:
    post:
      operationId: postInternalBillingEnsureEntity
      tags:
      - Internal
      summary: Create Ensure Entity
      description: |-
        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:37` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:37
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/grandfather:
    post:
      operationId: postInternalBillingGrandfather
      tags:
      - Internal
      summary: Create Grandfather
      description: |-
        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:97` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:97
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/grant-credits:
    post:
      operationId: postInternalBillingGrantCredits
      tags:
      - Internal
      summary: Create Grant Credits
      description: |-
        SYSTEM_ADMIN console: top up credits (PURCHASED bucket - never expires).

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:206` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:206
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/manual-contracts/{tenantId}:
    get:
      operationId: getInternalBillingManualContractsTenantId
      tags:
      - Internal
      summary: Get Manual Contract
      description: |-
        Manual contracts on record for a tenant, for billing history.

        Service-to-service only, authenticated by the internal shared secret.

        Implemented at `services/billing/src/routes/internal.ts:395` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:395
      x-operation-type: read
      x-publicly-exposed: false
      parameters:
      - name: tenantId
        in: path
        required: true
        schema:
          type: string
        description: "`tenantId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
  /api/internal/billing/payg-accrue:
    post:
      operationId: postInternalBillingPaygAccrue
      tags:
      - Internal
      summary: Create Payg Accrue
      description: |-
        Usage the wallet could not cover, reported by the AI runtime.

        Separate from usage-threshold on purpose. That one fires only when a
        percentage boundary is crossed; a shortfall happens on every call once the
        balance is gone, and folding it into a threshold report would bill only the
        calls that happened to land on 80/90/95/100.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:88` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:88
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/poc-feature-domains:
    get:
      operationId: getInternalBillingPocFeatureDomains
      tags:
      - Internal
      summary: List Poc Feature Domains
      description: |-
        The domains a POC's feature areas may be chosen from.

        Service-to-service only, authenticated by the internal shared secret.

        Implemented at `services/billing/src/routes/internal.ts:191` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:191
      x-operation-type: read
      x-publicly-exposed: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
  /api/internal/billing/provision-paid-tenant:
    post:
      operationId: postInternalBillingProvisionPaidTenant
      tags:
      - Internal
      summary: Create Provision Paid Tenant
      description: |-
        Create the billing scaffolding for an already-created paid tenant.

        Returns the raw continuation token exactly once, for the caller to put in
        one email. It is never persisted and never returned again.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:264` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:264
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/refund:
    post:
      operationId: postInternalBillingRefund
      tags:
      - Internal
      summary: Create Refund
      description: |-
        Ops-initiated refund of a successful charge (claws back purchased Units).

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:108` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:108
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/refund-confirmation:
    post:
      operationId: postInternalBillingRefundConfirmation
      tags:
      - Internal
      summary: Create Refund Confirmation
      description: |-
        Record a refund the provider made outside GOTCHA.

        For a refund issued directly in iCount. Reverses our state without calling
        the provider again, which would attempt a second refund.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:144` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:144
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/resend-payment-link:
    post:
      operationId: postInternalBillingResendPaymentLink
      tags:
      - Internal
      summary: Create Resend Payment Link
      description: |-
        Issue a replacement continuation link.

        Reuses the SAME checkout and the SAME payment attempt: a resend is a new
        envelope for the existing offer, not a new offer. Issuing revokes the prior
        link inside one transaction, so at most one link is ever valid.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:296` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:296
      x-operation-type: write
      x-publicly-exposed: false
      x-database:
      - pendingCheckout.findFirst
      - paymentContinuationLink.findFirst
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/revoke-payment-links:
    post:
      operationId: postInternalBillingRevokePaymentLinks
      tags:
      - Internal
      summary: Create Revoke Payment Links
      description: |-
        Revoke every active link for a tenant's checkout, without reissuing.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:353` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:353
      x-operation-type: write
      x-publicly-exposed: false
      x-database:
      - pendingCheckout.findFirst
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/run-cycle:
    post:
      operationId: postInternalBillingRunCycle
      tags:
      - Internal
      summary: Create Run Cycle
      description: |-
        Ops/scheduler hook - process trials, renewals, pending changes, dunning.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:152` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:152
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/setup-poc:
    post:
      operationId: postInternalBillingSetupPoc
      tags:
      - Internal
      summary: Create Setup Poc
      description: |-
        Provision a card-less POC in full: an ENFORCED subscription, the operator's
        credit budget, the expiry, and an explicit entitlement row for every feature
        domain - enabled for the chosen ones, denied for the rest.

        The single POC entry point. Both callers - tenant creation and the per-tenant
        console action - come through here, so a POC cannot exist with its money set
        up and its features not, or the reverse.

        Idempotent: safe to call again for a repair or a correction.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:169` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:169
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/shopify/connected:
    post:
      operationId: postInternalBillingShopifyConnected
      tags:
      - Internal
      summary: Create Connected
      description: |-
        Called by services/ai, which owns the Shopify OAuth callback and the verified
        webhook handlers. The split is deliberate: services/ai decides WHO the store
        and workspace are (it holds the HMAC verification and the session), and
        billing decides what that means COMMERCIALLY. Neither reaches into the
        other's tables to answer the other's question.


        A Shopify store finished OAuth and was linked to a workspace.

        Returns what should happen next, and never an error the caller must act on:
        OAuth has already succeeded by the time this is called, so a billing problem
        here can change what the merchant is SHOWN and must never retract the
        installation. The one exception is a cross-tenant claim, which is a 409 the
        caller genuinely has to surface.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:428` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:428
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/shopify/state/{tenantId}:
    get:
      operationId: getInternalBillingShopifyStateTenantId
      tags:
      - Internal
      summary: Get State
      description: |-
        Current Shopify billing state for a workspace. Read-only.

        Service-to-service only, authenticated by the internal shared secret.

        Implemented at `services/billing/src/routes/internal.ts:491` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:491
      x-operation-type: read
      x-publicly-exposed: false
      parameters:
      - name: tenantId
        in: path
        required: true
        schema:
          type: string
        description: "`tenantId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
  /api/internal/billing/shopify/uninstalled:
    post:
      operationId: postInternalBillingShopifyUninstalled
      tags:
      - Internal
      summary: Create Uninstalled
      description: |-
        A verified `app/uninstalled` for a Shopify store.

        Idempotent: an unknown or already-disconnected shop answers 200 with
        `revoked: 0`. Shopify retries webhooks, and a retry must not be an error.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:459` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:459
      x-operation-type: write
      x-publicly-exposed: false
      x-database:
      - commerceConnection.findFirst
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/start-trial:
    post:
      operationId: postInternalBillingStartTrial
      tags:
      - Internal
      summary: Create Start Trial
      description: |-
        Signup completes here: card already tokenized → start the 14-day trial.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:45` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:45
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/summary:
    get:
      operationId: getInternalBillingSummary
      tags:
      - Internal
      summary: List Summary
      description: |-
        SYSTEM_ADMIN console: subscription + wallet snapshot for one tenant.

        Service-to-service only, authenticated by the internal shared secret.

        Implemented at `services/billing/src/routes/internal.ts:216` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:216
      x-operation-type: read
      x-publicly-exposed: false
      x-database:
      - subscription.findUnique
      parameters:
      - name: tenantId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/internal/billing/tenant-plan-audit:
    get:
      operationId: getInternalBillingTenantPlanAudit
      tags:
      - Internal
      summary: List Tenant Plan Audit
      description: |-
        Every tenant, grouped by how it holds access. Read-only by design: it
        assigns nothing and grants nothing, because a tenant with no plan needs an
        operator's decision, not an automatic one.

        Service-to-service only, authenticated by the internal shared secret.

        Implemented at `services/billing/src/routes/internal.ts:200` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:200
      x-operation-type: read
      x-publicly-exposed: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
  /api/internal/billing/usage-threshold:
    post:
      operationId: postInternalBillingUsageThreshold
      tags:
      - Internal
      summary: Create Usage Threshold
      description: |-
        Called by the AI runtime when Unit consumption crosses 80/90/95/100%.
        Emits owner alerts and attempts auto-purchase (which self-gates on policy +
        monthly ceiling). The AI gate already blocked the turn if balance hit zero.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:69` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:69
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/internal/billing/validate-paid-plan:
    post:
      operationId: postInternalBillingValidatePaidPlan
      tags:
      - Internal
      summary: Create Validate Paid Plan
      description: |-
        Validate a plan + volume selection WITHOUT creating anything.

        Called before the tenant is created, so a bad selection fails while there is
        still nothing to roll back. It also returns the server-computed commercial
        summary, which is the only figure the Sysadmin UI is allowed to display.

        Service-to-service only, authenticated by the internal shared secret.

        This is a write operation.

        Implemented at `services/billing/src/routes/internal.ts:241` (billing service).
      security:
      - internalServiceToken: []
      x-service: billing
      x-source: services/billing/src/routes/internal.ts:241
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/knowledge-bases:
    get:
      operationId: getKnowledgeBases
      tags:
      - Knowledge
      summary: List Knowledge Bases
      description: |-
        List knowledge bases

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/knowledge.ts:44` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge.ts:44
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
      x-database:
      - knowledgeBase.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postKnowledgeBases
      tags:
      - Knowledge
      summary: Create Knowledge Bases
      description: |-
        Create knowledge base

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/knowledge.ts:72` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge.ts:72
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
        - ai:knowledge:write OR role ADMIN
        entitlements:
        - ai.knowledge_base
        planLimits:
        - limit:knowledge_sources
      x-database:
      - knowledgeBase.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/knowledge-bases/{id}:
    delete:
      operationId: deleteKnowledgeBasesId
      tags:
      - Knowledge
      summary: Delete Knowledge Base
      description: |-
        Delete knowledge base

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/knowledge.ts:124` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge.ts:124
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
        - ai:knowledge:write OR role ADMIN
      x-database:
      - knowledgeBase.findFirst
      - knowledgeBase.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchKnowledgeBasesId
      tags:
      - Knowledge
      summary: Update Knowledge Base
      description: |-
        Update knowledge base

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/knowledge.ts:100` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge.ts:100
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
        - ai:knowledge:write OR role ADMIN
      x-database:
      - knowledgeBase.findFirst
      - knowledgeBase.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/knowledge-bases/{id}/documents:
    post:
      operationId: postKnowledgeBasesIdDocuments
      tags:
      - Knowledge
      summary: Create Documents
      description: |-
        Upload document to knowledge base

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/knowledge.ts:141` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge.ts:141
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
        - ai:knowledge:write OR role ADMIN
      x-database:
      - knowledgeBase.findFirst
      - knowledgeDocument.create
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/knowledge-bases/{id}/documents/{docId}:
    delete:
      operationId: deleteKnowledgeBasesIdDocumentsDocId
      tags:
      - Knowledge
      summary: Delete Document
      description: |-
        Delete document

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/knowledge.ts:281` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge.ts:281
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
        - ai:knowledge:write OR role ADMIN
      x-database:
      - knowledgeDocument.findFirst
      - knowledgeDocument.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: docId
        in: path
        required: true
        schema:
          type: string
        description: "`docId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putKnowledgeBasesIdDocumentsDocId
      tags:
      - Knowledge
      summary: Replace Document
      description: |-
        Update a document in place (content and/or provenance metadata).

        This route is what makes a website re-scan a REFRESH rather than an append.
        Without it the only way to reflect a changed page was to create a second
        document, so every refresh grew the knowledge base and retrieval started
        returning several stale copies of the same page ranked above the current
        one. Re-embedding is triggered only when the body actually changed, so an
        unchanged page costs nothing.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/knowledge.ts:223` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge.ts:223
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
        - ai:knowledge:write OR role ADMIN
      x-database:
      - knowledgeDocument.findFirst
      - knowledgeDocument.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: docId
        in: path
        required: true
        schema:
          type: string
        description: "`docId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/knowledge-bases/{id}/documents/{docId}/process:
    post:
      operationId: postKnowledgeBasesIdDocumentsDocIdProcess
      tags:
      - Knowledge
      summary: Create Process
      description: |-
        Trigger document processing (embedding generation)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/knowledge.ts:351` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge.ts:351
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
        - ai:knowledge:write OR role ADMIN
      x-database:
      - knowledgeDocument.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: docId
        in: path
        required: true
        schema:
          type: string
        description: "`docId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/knowledge-bases/{id}/documents/upload:
    post:
      operationId: postKnowledgeBasesIdDocumentsUpload
      tags:
      - Knowledge
      summary: Create Upload
      description: |-
        Upload file document to knowledge base

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/knowledge.ts:298` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge.ts:298
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:knowledge:read OR role ADMIN
        - ai:knowledge:write OR role ADMIN
      x-database:
      - knowledgeBase.findFirst
      - knowledgeDocument.create
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
              required:
              - file
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/knowledge/integrations/{intId}:
    delete:
      operationId: deleteKnowledgeIntegrationsIntId
      tags:
      - Knowledge
      summary: Delete Integration
      description: |-
        Delete (disconnect) integration.

        Two different requests hide behind one button, so they are two different
        calls: disconnecting stops the sync and keeps what was already learned, while
        `removeData=1` also deletes the documents and embeddings this integration
        produced. Defaulting to deletion would throw away a customer's knowledge base
        because they wanted to stop a sync.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/knowledge-oauth.ts:349` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge-oauth.ts:349
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - knowledgeIntegration.findFirst
      - knowledgeIntegration.delete
      x-service-calls:
      - googleDriveService.removeSourceData
      parameters:
      - name: intId
        in: path
        required: true
        schema:
          type: string
        description: "`intId` path segment."
      - name: removeData
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/knowledge/integrations/{intId}/auto-sync:
    patch:
      operationId: patchKnowledgeIntegrationsIntIdAutoSync
      tags:
      - Knowledge
      summary: Update Auto Sync
      description: |-
        Enable/disable the hourly background re-sync for one integration. Stored on
        config.autoSync; the scheduler only refreshes integrations where it's not
        explicitly false and that have a persisted selection (spaceKeys / fileIds).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/knowledge-oauth.ts:636` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge-oauth.ts:636
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - knowledgeIntegration.findFirst
      - knowledgeIntegration.update
      parameters:
      - name: intId
        in: path
        required: true
        schema:
          type: string
        description: "`intId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/knowledge/integrations/{intId}/confluence/spaces:
    get:
      operationId: getKnowledgeIntegrationsIntIdConfluenceSpaces
      tags:
      - Knowledge
      summary: Get Spaces
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/knowledge-oauth.ts:371` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge-oauth.ts:371
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - knowledgeIntegration.findFirst
      x-service-calls:
      - confluenceService.listSpaces
      parameters:
      - name: intId
        in: path
        required: true
        schema:
          type: string
        description: "`intId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/knowledge/integrations/{intId}/confluence/spaces/{key}/pages:
    get:
      operationId: getKnowledgeIntegrationsIntIdConfluenceSpacesKeyPages
      tags:
      - Knowledge
      summary: Get Pages
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/knowledge-oauth.ts:388` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge-oauth.ts:388
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - knowledgeIntegration.findFirst
      x-service-calls:
      - confluenceService.listPages
      parameters:
      - name: intId
        in: path
        required: true
        schema:
          type: string
        description: "`intId` path segment."
      - name: key
        in: path
        required: true
        schema:
          type: string
        description: "`key` path segment."
      - name: parentId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/knowledge/integrations/{intId}/confluence/sync:
    post:
      operationId: postKnowledgeIntegrationsIntIdConfluenceSync
      tags:
      - Knowledge
      summary: Create Sync
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/knowledge-oauth.ts:406` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge-oauth.ts:406
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - knowledgeIntegration.findFirst
      - knowledgeIntegration.update
      x-service-calls:
      - confluenceService.syncSpace
      parameters:
      - name: intId
        in: path
        required: true
        schema:
          type: string
        description: "`intId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/knowledge/integrations/{intId}/drive/files:
    get:
      operationId: getKnowledgeIntegrationsIntIdDriveFiles
      tags:
      - Knowledge
      summary: Get Files
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/knowledge-oauth.ts:456` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge-oauth.ts:456
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - knowledgeIntegration.findFirst
      x-service-calls:
      - googleDriveService.listFiles
      parameters:
      - name: intId
        in: path
        required: true
        schema:
          type: string
        description: "`intId` path segment."
      - name: folderId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: driveId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/knowledge/integrations/{intId}/drive/shared-drives:
    get:
      operationId: getKnowledgeIntegrationsIntIdDriveSharedDrives
      tags:
      - Knowledge
      summary: Get Shared Drives
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/knowledge-oauth.ts:439` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge-oauth.ts:439
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - knowledgeIntegration.findFirst
      x-service-calls:
      - googleDriveService.listSharedDrives
      parameters:
      - name: intId
        in: path
        required: true
        schema:
          type: string
        description: "`intId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/knowledge/integrations/{intId}/drive/sources:
    get:
      operationId: getKnowledgeIntegrationsIntIdDriveSources
      tags:
      - Knowledge
      summary: Get Sources
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/knowledge-oauth.ts:547` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge-oauth.ts:547
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - knowledgeIntegration.findFirst
      x-service-calls:
      - googleDriveService.normalizeSources
      parameters:
      - name: intId
        in: path
        required: true
        schema:
          type: string
        description: "`intId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/knowledge/integrations/{intId}/drive/sources/{sourceKey}:
    delete:
      operationId: deleteKnowledgeIntegrationsIntIdDriveSourcesSourceKey
      tags:
      - Knowledge
      summary: Delete Source
      description: |-
        `removeData=1` also deletes the knowledge that source produced. Without it
        the documents stay: unsubscribing from a folder is not the same request as
        throwing away everything the AI learned from it.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/knowledge-oauth.ts:598` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge-oauth.ts:598
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - knowledgeIntegration.findFirst
      - knowledgeIntegration.update
      x-service-calls:
      - googleDriveService.normalizeSources
      - googleDriveService.removeSourceData
      parameters:
      - name: intId
        in: path
        required: true
        schema:
          type: string
        description: "`intId` path segment."
      - name: sourceKey
        in: path
        required: true
        schema:
          type: string
        description: "`sourceKey` path segment."
      - name: removeData
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchKnowledgeIntegrationsIntIdDriveSourcesSourceKey
      tags:
      - Knowledge
      summary: Update Source
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/knowledge-oauth.ts:564` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge-oauth.ts:564
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - knowledgeIntegration.findFirst
      - knowledgeIntegration.update
      x-service-calls:
      - googleDriveService.normalizeSources
      parameters:
      - name: intId
        in: path
        required: true
        schema:
          type: string
        description: "`intId` path segment."
      - name: sourceKey
        in: path
        required: true
        schema:
          type: string
        description: "`sourceKey` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/knowledge/integrations/{intId}/drive/sync:
    post:
      operationId: postKnowledgeIntegrationsIntIdDriveSync
      tags:
      - Knowledge
      summary: Create Sync
      description: |-
        Accepts the structured `sources` list (files and/or folders) and still
        accepts the original bare `fileIds`, so an older client keeps working.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/knowledge-oauth.ts:478` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge-oauth.ts:478
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - knowledgeIntegration.findFirst
      - knowledgeIntegration.update
      x-service-calls:
      - googleDriveService.mergeSources
      - googleDriveService.normalizeSources
      - googleDriveService.syncIntegration
      parameters:
      - name: intId
        in: path
        required: true
        schema:
          type: string
        description: "`intId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/knowledge/integrations/{intId}/drive/sync-now:
    post:
      operationId: postKnowledgeIntegrationsIntIdDriveSyncNow
      tags:
      - Knowledge
      summary: Create Sync Now
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/knowledge-oauth.ts:524` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge-oauth.ts:524
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - knowledgeIntegration.findFirst
      x-service-calls:
      - googleDriveService.syncIntegration
      parameters:
      - name: intId
        in: path
        required: true
        schema:
          type: string
        description: "`intId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/knowledge/kb/{kbId}/integrations:
    get:
      operationId: getKnowledgeKbKbIdIntegrations
      tags:
      - Knowledge
      summary: Get Integrations
      description: |-
        List integrations for a knowledge base

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/knowledge-oauth.ts:315` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge-oauth.ts:315
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - knowledgeBase.findFirst
      - knowledgeIntegration.findMany
      parameters:
      - name: kbId
        in: path
        required: true
        schema:
          type: string
        description: "`kbId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/knowledge/oauth/confluence/callback:
    get:
      operationId: getKnowledgeOauthConfluenceCallback
      tags:
      - Knowledge
      summary: List Callback
      description: |-
        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/knowledge-oauth.ts:96` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge-oauth.ts:96
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/knowledge/oauth/confluence/init:
    get:
      operationId: getKnowledgeOauthConfluenceInit
      tags:
      - Knowledge
      summary: List Init
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/knowledge-oauth.ts:75` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge-oauth.ts:75
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: kbId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/knowledge/oauth/google-drive/callback:
    get:
      operationId: getKnowledgeOauthGoogleDriveCallback
      tags:
      - Knowledge
      summary: List Callback
      description: |-
        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/knowledge-oauth.ts:216` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge-oauth.ts:216
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - knowledgeIntegration.findFirst
      - knowledgeIntegration.update
      - knowledgeIntegration.create
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/knowledge/oauth/google-drive/init:
    get:
      operationId: getKnowledgeOauthGoogleDriveInit
      tags:
      - Knowledge
      summary: List Init
      description: |-
        PENDING_ONBOARDING allowed: onboarding Movement 6 connects Drive before the
        tenant flips ACTIVE. `flow=onboarding` rides the state so the callback can
        land back on /setup instead of the app's knowledge page.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be PENDING_ONBOARDING or ACTIVE.

        Implemented at `services/ai/src/routes/knowledge-oauth.ts:176` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/knowledge-oauth.ts:176
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - knowledgeIntegration.findFirst
      parameters:
      - name: kbId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: flow
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: reconnect
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/notification-preferences:
    get:
      operationId: getNotificationPreferences
      tags:
      - Notifications
      summary: List Notification Preferences
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/notifications/src/routes/notification-preferences-admin.ts:88` (notifications service).
      security:
      - bearerAuth: []
      x-service: notifications
      x-source: services/notifications/src/routes/notification-preferences-admin.ts:88
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: departmentId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: eventType
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postNotificationPreferences
      tags:
      - Notifications
      summary: Create Notification Preferences
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/notifications/src/routes/notification-preferences-admin.ts:103` (notifications service).
      security:
      - bearerAuth: []
      x-service: notifications
      x-source: services/notifications/src/routes/notification-preferences-admin.ts:103
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/notification-preferences/{id}:
    delete:
      operationId: deleteNotificationPreferencesId
      tags:
      - Notifications
      summary: Delete Notification Preference
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/notifications/src/routes/notification-preferences-admin.ts:186` (notifications service).
      security:
      - bearerAuth: []
      x-service: notifications
      x-source: services/notifications/src/routes/notification-preferences-admin.ts:186
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchNotificationPreferencesId
      tags:
      - Notifications
      summary: Update Notification Preference
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/notifications/src/routes/notification-preferences-admin.ts:148` (notifications service).
      security:
      - bearerAuth: []
      x-service: notifications
      x-source: services/notifications/src/routes/notification-preferences-admin.ts:148
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/notifications:
    get:
      operationId: getNotifications
      tags:
      - Notifications
      summary: List Notifications
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/notifications/src/routes/notifications.ts:23` (notifications service).
      security:
      - bearerAuth: []
      x-service: notifications
      x-source: services/notifications/src/routes/notifications.ts:23
      x-operation-type: read
      x-publicly-exposed: true
      parameters:
      - name: unreadOnly
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: limit
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: cursor
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/notifications/{id}/mark-read:
    post:
      operationId: postNotificationsIdMarkRead
      tags:
      - Notifications
      summary: Create Mark Read
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/notifications/src/routes/notifications.ts:68` (notifications service).
      security:
      - bearerAuth: []
      x-service: notifications
      x-source: services/notifications/src/routes/notifications.ts:68
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/notifications/mark-all-read:
    post:
      operationId: postNotificationsMarkAllRead
      tags:
      - Notifications
      summary: Create Mark All Read
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/notifications/src/routes/notifications.ts:90` (notifications service).
      security:
      - bearerAuth: []
      x-service: notifications
      x-source: services/notifications/src/routes/notifications.ts:90
      x-operation-type: write
      x-publicly-exposed: true
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/notifications/unread-count:
    get:
      operationId: getNotificationsUnreadCount
      tags:
      - Notifications
      summary: List Unread Count
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/notifications/src/routes/notifications.ts:53` (notifications service).
      security:
      - bearerAuth: []
      x-service: notifications
      x-source: services/notifications/src/routes/notifications.ts:53
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/agent-config/{departmentId}:
    get:
      operationId: getOnboardingAgentConfigDepartmentId
      tags:
      - Onboarding
      summary: Get Agent Config
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:1090` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1090
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - department.findFirst
      - routerRule.findFirst
      - aIAgent.findUnique
      parameters:
      - name: departmentId
        in: path
        required: true
        schema:
          type: string
        description: "`departmentId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/ai-chat:
    post:
      operationId: postOnboardingAiChat
      tags:
      - Onboarding
      summary: Create Ai Chat
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:736` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:736
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - businessProfile.findUnique
      - department.findMany
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/onboarding/analyze-domain:
    post:
      operationId: postOnboardingAnalyzeDomain
      tags:
      - Onboarding
      summary: Create Analyze Domain
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:1616` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1616
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - auditLog.create
      requestBody:
        required: true
        description: Validated by `analyzeDomainSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                domain:
                  type: string
                  minLength: 3
                  maxLength: 255
                locale:
                  type: string
                  minLength: 2
                  maxLength: 10
                  default: en
              required:
              - domain
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/business-profile:
    get:
      operationId: getOnboardingBusinessProfile
      tags:
      - Onboarding
      summary: List Business Profile
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:1061` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1061
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - businessProfile.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postOnboardingBusinessProfile
      tags:
      - Onboarding
      summary: Create Business Profile
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:542` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:542
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - tenant.findUnique
      - businessProfile.findUnique
      - businessProfile.upsert
      - tenant.update
      - tenantOnboarding.upsert
      requestBody:
        required: true
        description: Validated by `businessProfileSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                organizationName:
                  type: string
                  minLength: 1
                  maxLength: 200
                businessDescription:
                  type: string
                  minLength: 1
                  maxLength: 2000
                locale:
                  type: string
                  minLength: 2
                  maxLength: 10
                businessGoals:
                  type: array
                  items:
                    type: string
                    minLength: 1
                    maxLength: 64
                  maxItems: 6
                websiteDomain:
                  type: string
                  maxLength: 255
                industry:
                  type: string
                  maxLength: 120
                country:
                  type: string
                  maxLength: 120
                primaryLanguage:
                  type: string
                  maxLength: 10
              required:
              - organizationName
              - businessDescription
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/complete:
    post:
      operationId: postOnboardingComplete
      tags:
      - Onboarding
      summary: Create Complete
      description: |-
        The new 2-screen flow makes this the synchronous critical path:
          1. Verify profile exists
          2. Auto-create a default "General" department if none exists
          3. Flip tenant.status = ACTIVE (so AppLayout stops redirecting to /setup)
          4. Fire-and-forget the AI config generation - the user enters the app
             immediately; agents materialize within a few seconds in the background.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:859` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:859
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - tenant.findUnique
      - businessProfile.findUnique
      - |-
          businessDiscovery
                .findUnique({ where: { tenantId: req
      - |-
          businessDiscovery
                .findUnique
      - department.findMany
      - department.create
      - tenant.update
      - tenantOnboarding.upsert
      - knowledgeDocument.findMany
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/onboarding/core-system:
    post:
      operationId: postOnboardingCoreSystem
      tags:
      - Onboarding
      summary: Create Core System
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:1434` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1434
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - businessProfile.updateMany
      - tenantIntegration.findFirst
      - |-
          recommendation.updateMany({
                  where: { tenantId: req
      - recommendation.updateMany
      requestBody:
        required: true
        description: Validated by `coreSystemSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                slug:
                  type: string
                  enum: []
              required:
              - slug
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/departments:
    get:
      operationId: getOnboardingDepartments
      tags:
      - Onboarding
      summary: List Departments
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:1075` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1075
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - department.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postOnboardingDepartments
      tags:
      - Onboarding
      summary: Create Departments
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:661` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:661
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - tenant.findUnique
      - businessProfile.findUnique
      - department.deleteMany
      - department.create
      - tenantOnboarding.upsert
      requestBody:
        required: true
        description: Validated by `departmentConfigSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                departments:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        minLength: 1
                        maxLength: 100
                      description:
                        type: string
                        maxLength: 500
                      queueMode:
                        type: string
                        enum:
                        - CLAIM
                        - ROUND_ROBIN
                        default: CLAIM
                      workingHours:
                        type: object
                        properties:
                          timezone:
                            type: string
                            minLength: 1
                          schedule:
                            type: object
                            additionalProperties: true
                        required:
                        - timezone
                        - schedule
                      slaTarget:
                        type: integer
                        minimum: 1
                        maximum: 1440
                      escalationPolicy:
                        type: object
                        properties:
                          escalateOnBreach:
                            type: boolean
                            default: false
                          escalateTo:
                            type: string
                          maxWaitMinutes:
                            type: integer
                            minimum: 1
                      autoGreetingEnabled:
                        type: boolean
                        default: false
                      autoGreetingMessage:
                        type: string
                        maxLength: 1000
                      autoCloseMinutes:
                        type: integer
                        minimum: 1
                        maximum: 10080
                      escalateOnSlaBreach:
                        type: boolean
                        default: false
                      aiSuggestionsEnabled:
                        type: boolean
                        default: true
                      autoRepliesEnabled:
                        type: boolean
                        default: false
                    required:
                    - name
                    - slaTarget
                  minItems: 1
              required:
              - departments
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/discover:
    post:
      operationId: postOnboardingDiscover
      tags:
      - Onboarding
      summary: Create Discover
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:2130` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2130
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - auditLog.create
      - businessDiscovery.findUnique
      - |-
          businessDiscovery.update({
                  where: { tenantId: req
      - "businessDiscovery.update({ where: { tenantId: req"
      - |-
          businessDiscovery.update({
                where: { tenantId: req
      - |-
          channelAccount.findFirst({
                where: { tenantId: req
      - channelAccount.findFirst
      - |-
          businessProfile.findUnique({
                  where: { tenantId: req
      - businessProfile.findUnique
      requestBody:
        required: true
        description: Validated by `discoverSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                domain:
                  type: string
                  minLength: 3
                  maxLength: 255
                locale:
                  type: string
                  minLength: 2
                  maxLength: 10
                  default: en
                legalAccepted:
                  type: boolean
              required:
              - domain
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/discover/plan:
    post:
      operationId: postOnboardingDiscoverPlan
      tags:
      - Onboarding
      summary: Create Plan
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:2516` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2516
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      requestBody:
        required: true
        description: Validated by `planSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                domain:
                  type: string
                  minLength: 3
                  maxLength: 255
                locale:
                  type: string
                  minLength: 2
                  maxLength: 10
                  default: en
              required:
              - domain
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/discovery:
    get:
      operationId: getOnboardingDiscovery
      tags:
      - Onboarding
      summary: List Discovery
      description: |-
        GET /discovery - the persisted Business Intelligence Report (re-hydrates the
        review, health, and recommendation surfaces on refresh / OAuth return).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:2536` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2536
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - businessDiscovery.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchOnboardingDiscovery
      tags:
      - Onboarding
      summary: Update Discovery
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:2592` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2592
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - businessDiscovery.findUnique
      - businessDiscovery.update
      requestBody:
        required: true
        description: Validated by `discoveryPatchSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                employeeName:
                  type: string
                  minLength: 1
                  maxLength: 120
                business:
                  type: object
                  properties:
                    name:
                      type: string
                      maxLength: 200
                    industry:
                      type: string
                      maxLength: 120
                    country:
                      type: string
                      maxLength: 120
                    summary:
                      type: string
                      maxLength: 2000
                    valueProp:
                      type: string
                      maxLength: 2000
                    products:
                      type: array
                      items:
                        type: string
                        maxLength: 120
                      maxItems: 24
                brand:
                  type: object
                  properties:
                    voice:
                      type: string
                      maxLength: 200
                    tone:
                      type: string
                      maxLength: 200
                    languages:
                      type: array
                      items:
                        type: string
                        maxLength: 20
                      maxItems: 6
                communication:
                  type: object
                  properties:
                    channels:
                      type: array
                      items:
                        type: object
                        properties:
                          type:
                            type: string
                            minLength: 1
                            maxLength: 40
                          identifier:
                            type: string
                            maxLength: 200
                          purpose:
                            type: string
                            maxLength: 200
                          provider:
                            type: string
                            maxLength: 40
                          confidence:
                            type: string
                            maxLength: 40
                        required:
                        - type
                      maxItems: 24
                  required:
                  - channels
                progress:
                  type: string
                  enum: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/discovery/correct:
    post:
      operationId: postOnboardingDiscoveryCorrect
      tags:
      - Onboarding
      summary: Create Correct
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:2644` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2644
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - |-
          recommendation.updateMany({
                  where: { tenantId: req
      - recommendation.updateMany
      - businessDiscovery.update
      requestBody:
        required: true
        description: Validated by `correctSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                target:
                  type: string
                  enum:
                  - channel
                  - tool
                  - platform
                  - gap
                action:
                  type: string
                  enum:
                  - remove
                  - incorrect
                  - ignore
                key:
                  type: string
                  minLength: 1
                  maxLength: 200
              required:
              - target
              - action
              - key
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/employee-chat:
    post:
      operationId: postOnboardingEmployeeChat
      tags:
      - Onboarding
      summary: Create Employee Chat
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:3032` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:3032
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - |-
          businessDiscovery.update({
                    where: { tenantId: req
      - |-
          businessDiscovery.update({
                  where: { tenantId: req
      - businessDiscovery.update
      requestBody:
        required: true
        description: Validated by `employeeChatSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                persona:
                  type: object
                  properties:
                    tone:
                      type: string
                      maxLength: 40
                    personality:
                      type: string
                      maxLength: 400
                    focus:
                      type: string
                      maxLength: 200
                    goal:
                      type: string
                      maxLength: 600
                    successCriteria:
                      type: array
                      items:
                        type: string
                        maxLength: 300
                      maxItems: 8
                    instructions:
                      type: array
                      items:
                        type: string
                        maxLength: 400
                      maxItems: 20
                messages:
                  type: array
                  items:
                    type: object
                    properties:
                      role:
                        type: string
                        enum:
                        - user
                        - assistant
                      content:
                        type: string
                        minLength: 1
                        maxLength: 2000
                    required:
                    - role
                    - content
                  maxItems: 20
                locale:
                  type: string
                  minLength: 2
                  maxLength: 10
                personaOnly:
                  type: boolean
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/generate-configs:
    post:
      operationId: postOnboardingGenerateConfigs
      tags:
      - Onboarding
      summary: Create Generate Configs
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:796` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:796
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - businessProfile.findUnique
      - department.findMany
      - aIAgent.findMany
      - tenantOnboarding.upsert
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/onboarding/goal:
    post:
      operationId: postOnboardingGoal
      tags:
      - Onboarding
      summary: Create Goal
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:2927` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2927
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - |-
          businessProfile.updateMany({
                where: { tenantId: req
      - businessProfile.updateMany
      requestBody:
        required: true
        description: Validated by `goalSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                goals:
                  type: array
                  items:
                    type: string
                    minLength: 1
                    maxLength: 64
                  minItems: 1
                  maxItems: 4
                goal:
                  type: string
                  minLength: 1
                  maxLength: 64
                detail:
                  type: string
                  maxLength: 500
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/guides:
    get:
      operationId: getOnboardingGuides
      tags:
      - Onboarding
      summary: List Guides
      description: |-
        GET/PATCH /guides - per-user first-time guidance state for the persistent
        onboarding layer. Available to ALL roles (agents get guidance too), so no
        requireRole gate. State machine per feature: unseen -> snoozed | done.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:1523` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1523
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - user.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchOnboardingGuides
      tags:
      - Onboarding
      summary: Update Guides
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:1539` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1539
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - user.findUnique
      - user.update
      requestBody:
        required: true
        description: Validated by `guidePatchSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                feature:
                  type: string
                  minLength: 1
                  maxLength: 64
                state:
                  type: string
                  enum:
                  - unseen
                  - snoozed
                  - done
              required:
              - feature
              - state
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/health:
    get:
      operationId: getOnboardingHealth
      tags:
      - Onboarding
      summary: List Health
      description: |-
        GET /health - Business Health (Movement 3), recomputed against LIVE connection
        state so a channel/system connected after the scan flips its ⚠ to ✓.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:2687` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2687
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - |-
          channelAccount.findFirst({
                where: { tenantId: req
      - channelAccount.findFirst
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/invite-link:
    post:
      operationId: postOnboardingInviteLink
      tags:
      - Onboarding
      summary: Create Invite Link
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:3182` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:3182
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - tenant.findUnique
      - tenantInvite.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/onboarding/invite-team:
    post:
      operationId: postOnboardingInviteTeam
      tags:
      - Onboarding
      summary: Create Invite Team
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:3101` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:3101
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - user.findUnique
      - tenant.findUnique
      - tenantInvite.create
      requestBody:
        required: true
        description: Validated by `inviteTeamSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                emails:
                  type: array
                  items:
                    type: string
                    format: email
                  minItems: 1
                  maxItems: 25
                role:
                  type: string
                  enum:
                  - ADMIN
                  - AGENT
                  default: AGENT
              required:
              - emails
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/journey:
    get:
      operationId: getOnboardingJourney
      tags:
      - Onboarding
      summary: List Journey
      description: |-
        GET /journey - THE canonical setup-readiness state. The Getting Started
        page, the sidebar "Complete setup" panel, and the nav badge all derive from
        this one endpoint, so they can never contradict each other.

        Five canonical actions, every one DERIVED from persisted product state
        (never a visited-page flag, an opened modal, or a started OAuth):
          1. connect_source_of_truth  - a USABLE core system (CONNECTED and, for
             Airtable, field-mapped; see connectedCoreSystem).
          2. connect_channel          - at least one ChannelAccount CONNECTED + active.
             A tenant whose only channel turned ERROR/DISCONNECTED is NOT complete.
          3. connect_knowledge        - at least one ACTIVE knowledge base that has
             real content (a document or an active synced integration). An empty
             KB shell does not count.
          4. create_ai_employee       - an AIAgent in ACTIVE/PAUSED. A mid-wizard
             DRAFT counts as in_progress, never as done.
          5. create_process           - at least one persisted ChatbotFlow.

        Each item carries `state`: done | in_progress | attention | not_started.
        `attention` = a resource exists but is unhealthy (ERROR/DISCONNECTED) and
        nothing usable remains - the UI shows a fix/reconnect CTA for it.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:1243` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1243
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - |-
          aIAgent.findFirst({
                  where: { tenantId, status: "DRAFT" },
                  select: { id: true },
                })
      - |-
          tenantIntegration.findMany({
                  where: { tenantId, integration: { slug: { in: CORE_SYSTEM_SLUGS as unknown as string[] } } },
                  select: { status: true },
                })
      - |-
          channelAccount.findMany({
                  where: { tenantId },
                  select: { connectionStatus: true, isActive: true },
                })
      - |-
          businessDiscovery.findUnique({
                  where: { tenantId },
                  select: { communication: true, business: true },
                })
      - |-
          knowledgeBase.count({
                  where: {
                    tenantId,
                    isActive: true,
                    OR: [{ documents: { some: {} } }, { integrations: { some: { isActive: true } } }],
                  },
                })
      - |-
          flowCanvas.findUnique({
                  where: { tenantId },
                  select: { nodes: true, edges: true },
                })
      - "chatbotFlow.count({ where: { tenantId } })"
      - |-
          businessProfile.findUnique({
                  where: { tenantId },
                  select: { organizationName: true, industry: true },
                })
      - businessProfile.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/missions:
    get:
      operationId: getOnboardingMissions
      tags:
      - Onboarding
      summary: List Missions
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:1143` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1143
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - |-
          businessDiscovery.findUnique({
                  where: { tenantId },
                  select: { communication: true },
                })
      - |-
          recommendation.findMany({
                  where: { tenantId, status: "OPEN" },
                  select: { kind: true, dedupeKey: true },
                })
      - recommendation.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/notify-integration:
    post:
      operationId: postOnboardingNotifyIntegration
      tags:
      - Onboarding
      summary: Create Notify Integration
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:2964` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2964
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - "user.findUnique({ where: { id: req"
      - "businessDiscovery.findUnique({ where: { tenantId: req"
      - businessDiscovery.findUnique
      requestBody:
        required: true
        description: Validated by `notifyIntegrationSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                integration:
                  type: string
                  minLength: 1
                  maxLength: 120
                note:
                  type: string
                  maxLength: 500
                source:
                  type: string
                  maxLength: 60
              required:
              - integration
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/recommendation:
    get:
      operationId: getOnboardingRecommendation
      tags:
      - Onboarding
      summary: List Recommendation
      description: |-
        GET /recommendation - the first AI Recommendation (Movement 4), read straight
        off the persisted report.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:2712` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2712
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - businessDiscovery.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/recommendations:
    get:
      operationId: getOnboardingRecommendations
      tags:
      - Onboarding
      summary: List Recommendations
      description: |-
        GET /recommendations - the persistent living backlog (defaults to OPEN).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:2864` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2864
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/recommendations/{id}/{decision}:
    post:
      operationId: postOnboardingRecommendationsIdDecision
      tags:
      - Onboarding
      summary: Decision action
      description: |-
        POST /recommendations/:id/complete | /dismiss | /reopen.
        A resolved rec is never resurrected by a future re-scan; `reopen` is the
        explicit human undo (with confirmation in the UI) and restores OPEN.
        Every transition writes an audit event with the acting user.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:2882` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2882
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: decision
        in: path
        required: true
        schema:
          type: string
        description: "`decision` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/onboarding/setup-map:
    get:
      operationId: getOnboardingSetupMap
      tags:
      - Onboarding
      summary: List Setup Map
      description: |-
        GET /setup-map - the non-linear Setup Hub tiles + done flags, derived from
        real product signals (no manual check-offs). Tiles tagged stage:"later"
        (Channels, extra Integrations) are shown but excluded from the headline
        progress count.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:1475` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1475
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - "aIAgent.findFirst({ where: { tenantId }, select: { id: true, status: true } })"
      - "flowCanvas.findUnique({ where: { tenantId }, select: { nodes: true, edges: true } })"
      - "chatbotFlow.count({ where: { tenantId } })"
      - chatbotFlow.count
      - "channelAccount.findFirst({ where: { tenantId, connectionStatus: \"CONNECTED\" }, select: { id: true } })"
      - "tenantIntegration.count({ where: { tenantId, status: \"CONNECTED\" } })"
      - "businessProfile.findUnique({ where: { tenantId }, select: { primarySystem: true } })"
      - businessProfile.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/status:
    get:
      operationId: getOnboardingStatus
      tags:
      - Onboarding
      summary: List Status
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:461` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:461
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - tenant.findUnique
      - tenantOnboarding.findUnique
      - businessProfile.findUnique
      - department.findMany
      - aIAgent.count
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/onboarding/teach:
    post:
      operationId: postOnboardingTeach
      tags:
      - Onboarding
      summary: Create Teach
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:2755` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2755
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - knowledgeDocument.findFirst
      - knowledgeBase.findFirst
      - |-
          knowledgeDocument
                    .findMany({
                      where: { tenantId: req
      - |-
          knowledgeDocument
                    .findMany
      requestBody:
        required: true
        description: Validated by `teachSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                label:
                  type: string
                  minLength: 1
                  maxLength: 200
                method:
                  type: string
                  enum:
                  - text
                  - url
                  - file
                value:
                  type: string
                  minLength: 1
                  maxLength: 20000
              required:
              - label
              - method
              - value
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/permissions/catalog:
    get:
      operationId: getPermissionsCatalog
      tags:
      - Auth & Identity
      summary: List Catalog
      description: |-
        Drives the User Management UI: the permission picker + built-in roles +
        scope options. Hierarchical (feature:sub-feature:action), grouped by domain.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/permissions.ts:479` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/permissions.ts:479
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/permissions/features:
    get:
      operationId: getPermissionsFeatures
      tags:
      - Auth & Identity
      summary: List Features
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/permissions.ts:470` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/permissions.ts:470
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/permissions/me:
    get:
      operationId: getPermissionsMe
      tags:
      - Auth & Identity
      summary: List Me
      description: |-
        Returns the canonical hierarchical permission set + effective scope (the
        new RBAC surface the frontend reacts to) AND the legacy feature list
        (kept for back-compat with existing callers).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/permissions.ts:84` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/permissions.ts:84
      x-operation-type: read
      x-publicly-exposed: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/permissions/roles:
    get:
      operationId: getPermissionsRoles
      tags:
      - Auth & Identity
      summary: List Roles
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/permissions.ts:268` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/permissions.ts:268
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - tenantRole.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postPermissionsRoles
      tags:
      - Auth & Identity
      summary: Create Roles
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/permissions.ts:284` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/permissions.ts:284
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - tenantRole.create
      requestBody:
        required: true
        description: Validated by `createRoleSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 80
                description:
                  type: string
                  maxLength: 500
                features:
                  type: array
                  items:
                    type: string
              required:
              - name
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/permissions/roles/{roleId}:
    delete:
      operationId: deletePermissionsRolesRoleId
      tags:
      - Auth & Identity
      summary: Delete Role
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/permissions.ts:358` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/permissions.ts:358
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - tenantRole.delete
      parameters:
      - name: roleId
        in: path
        required: true
        schema:
          type: string
        description: "`roleId` path segment."
      responses:
        "204":
          description: No content
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchPermissionsRolesRoleId
      tags:
      - Auth & Identity
      summary: Update Role
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/permissions.ts:326` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/permissions.ts:326
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - tenantRole.update
      parameters:
      - name: roleId
        in: path
        required: true
        schema:
          type: string
        description: "`roleId` path segment."
      requestBody:
        required: true
        description: Validated by `updateRoleSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 80
                description:
                  type: string
                  maxLength: 500
                  nullable: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/permissions/roles/{roleId}/features:
    put:
      operationId: putPermissionsRolesRoleIdFeatures
      tags:
      - Auth & Identity
      summary: Replace Features
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/permissions.ts:386` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/permissions.ts:386
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - tenantRoleFeature.deleteMany
      - tenantRoleFeature.createMany
      - tenantRole.findUnique
      parameters:
      - name: roleId
        in: path
        required: true
        schema:
          type: string
        description: "`roleId` path segment."
      requestBody:
        required: true
        description: Validated by `replaceFeaturesSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                features:
                  type: array
                  items:
                    type: string
              required:
              - features
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/permissions/users:
    get:
      operationId: getPermissionsUsers
      tags:
      - Auth & Identity
      summary: List Users
      description: |-
        The unified User Management list. Unlike /api/agents (agent-only), this
        returns every member with the new built-in role assignment.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/permissions.ts:111` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/permissions.ts:111
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - user.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/permissions/users/{userId}:
    get:
      operationId: getPermissionsUsersUserId
      tags:
      - Auth & Identity
      summary: Get User
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/permissions.ts:162` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/permissions.ts:162
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - user.findFirst
      parameters:
      - name: userId
        in: path
        required: true
        schema:
          type: string
        description: "`userId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/permissions/users/{userId}/grants:
    get:
      operationId: getPermissionsUsersUserIdGrants
      tags:
      - Auth & Identity
      summary: Get Grants
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/permissions.ts:202` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/permissions.ts:202
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - userFeatureGrant.findMany
      parameters:
      - name: userId
        in: path
        required: true
        schema:
          type: string
        description: "`userId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/permissions/users/{userId}/grants/{feature}:
    delete:
      operationId: deletePermissionsUsersUserIdGrantsFeature
      tags:
      - Auth & Identity
      summary: Delete Grant
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/permissions.ts:248` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/permissions.ts:248
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - userFeatureGrant.deleteMany
      parameters:
      - name: userId
        in: path
        required: true
        schema:
          type: string
        description: "`userId` path segment."
      - name: feature
        in: path
        required: true
        schema:
          type: string
        description: "`feature` path segment."
      responses:
        "204":
          description: No content
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putPermissionsUsersUserIdGrantsFeature
      tags:
      - Auth & Identity
      summary: Replace Grant
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/permissions.ts:218` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/permissions.ts:218
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - userFeatureGrant.upsert
      parameters:
      - name: userId
        in: path
        required: true
        schema:
          type: string
        description: "`userId` path segment."
      - name: feature
        in: path
        required: true
        schema:
          type: string
        description: "`feature` path segment."
      requestBody:
        required: true
        description: Validated by `upsertGrantSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                granted:
                  type: boolean
                reason:
                  type: string
                  maxLength: 500
              required:
              - granted
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/permissions/users/{userId}/role:
    put:
      operationId: putPermissionsUsersUserIdRole
      tags:
      - Auth & Identity
      summary: Replace Role
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/permissions.ts:499` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/permissions.ts:499
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - userRoleAssignment.deleteMany
      - userRoleAssignment.create
      - userRoleAssignment.findFirst
      parameters:
      - name: userId
        in: path
        required: true
        schema:
          type: string
        description: "`userId` path segment."
      requestBody:
        required: true
        description: Validated by `setUserRoleSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                roleId:
                  type: string
                  minLength: 1
                scope:
                  type: string
                  enum:
                  - OWN
                  - TEAM
                  - DEPARTMENT
                  - WORKSPACE
                  nullable: true
              required:
              - roleId
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/permissions/users/{userId}/roles/{roleId}:
    delete:
      operationId: deletePermissionsUsersUserIdRolesRoleId
      tags:
      - Auth & Identity
      summary: Delete Role
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/permissions.ts:450` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/permissions.ts:450
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - userRoleAssignment.deleteMany
      parameters:
      - name: userId
        in: path
        required: true
        schema:
          type: string
        description: "`userId` path segment."
      - name: roleId
        in: path
        required: true
        schema:
          type: string
        description: "`roleId` path segment."
      responses:
        "204":
          description: No content
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postPermissionsUsersUserIdRolesRoleId
      tags:
      - Auth & Identity
      summary: RoleId action
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/permissions.ts:422` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/permissions.ts:422
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - userRoleAssignment.upsert
      parameters:
      - name: userId
        in: path
        required: true
        schema:
          type: string
        description: "`userId` path segment."
      - name: roleId
        in: path
        required: true
        schema:
          type: string
        description: "`roleId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/post-call/analyze:
    post:
      operationId: postPostCallAnalyze
      tags:
      - Voice
      summary: Create Analyze
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/post-call.ts:46` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/post-call.ts:46
      x-operation-type: write
      x-publicly-exposed: false
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "202":
          description: Accepted for asynchronous processing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/post-conversation-config:
    get:
      operationId: getPostConversationConfig
      tags:
      - AI Studio
      summary: List Post Conversation Config
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/post-conversation-config.ts:27` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/post-conversation-config.ts:27
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putPostConversationConfig
      tags:
      - AI Studio
      summary: Replace Post Conversation Config
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/post-conversation-config.ts:44` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/post-conversation-config.ts:44
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/public/onboarding/agent-config/{departmentId}:
    get:
      operationId: getPublicOnboardingAgentConfigDepartmentId
      tags:
      - Onboarding
      summary: Get Agent Config
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:1090` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1090
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - department.findFirst
      - routerRule.findFirst
      - aIAgent.findUnique
      parameters:
      - name: departmentId
        in: path
        required: true
        schema:
          type: string
        description: "`departmentId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/ai-chat:
    post:
      operationId: postPublicOnboardingAiChat
      tags:
      - Onboarding
      summary: Create Ai Chat
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:736` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:736
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - businessProfile.findUnique
      - department.findMany
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/public/onboarding/analyze-domain:
    post:
      operationId: postPublicOnboardingAnalyzeDomain
      tags:
      - Onboarding
      summary: Create Analyze Domain
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:1616` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1616
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - auditLog.create
      requestBody:
        required: true
        description: Validated by `analyzeDomainSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                domain:
                  type: string
                  minLength: 3
                  maxLength: 255
                locale:
                  type: string
                  minLength: 2
                  maxLength: 10
                  default: en
              required:
              - domain
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/business-profile:
    get:
      operationId: getPublicOnboardingBusinessProfile
      tags:
      - Onboarding
      summary: List Business Profile
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:1061` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1061
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - businessProfile.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postPublicOnboardingBusinessProfile
      tags:
      - Onboarding
      summary: Create Business Profile
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:542` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:542
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - tenant.findUnique
      - businessProfile.findUnique
      - businessProfile.upsert
      - tenant.update
      - tenantOnboarding.upsert
      requestBody:
        required: true
        description: Validated by `businessProfileSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                organizationName:
                  type: string
                  minLength: 1
                  maxLength: 200
                businessDescription:
                  type: string
                  minLength: 1
                  maxLength: 2000
                locale:
                  type: string
                  minLength: 2
                  maxLength: 10
                businessGoals:
                  type: array
                  items:
                    type: string
                    minLength: 1
                    maxLength: 64
                  maxItems: 6
                websiteDomain:
                  type: string
                  maxLength: 255
                industry:
                  type: string
                  maxLength: 120
                country:
                  type: string
                  maxLength: 120
                primaryLanguage:
                  type: string
                  maxLength: 10
              required:
              - organizationName
              - businessDescription
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/complete:
    post:
      operationId: postPublicOnboardingComplete
      tags:
      - Onboarding
      summary: Create Complete
      description: |-
        The new 2-screen flow makes this the synchronous critical path:
          1. Verify profile exists
          2. Auto-create a default "General" department if none exists
          3. Flip tenant.status = ACTIVE (so AppLayout stops redirecting to /setup)
          4. Fire-and-forget the AI config generation - the user enters the app
             immediately; agents materialize within a few seconds in the background.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:859` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:859
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - tenant.findUnique
      - businessProfile.findUnique
      - |-
          businessDiscovery
                .findUnique({ where: { tenantId: req
      - |-
          businessDiscovery
                .findUnique
      - department.findMany
      - department.create
      - tenant.update
      - tenantOnboarding.upsert
      - knowledgeDocument.findMany
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/public/onboarding/core-system:
    post:
      operationId: postPublicOnboardingCoreSystem
      tags:
      - Onboarding
      summary: Create Core System
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:1434` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1434
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - businessProfile.updateMany
      - tenantIntegration.findFirst
      - |-
          recommendation.updateMany({
                  where: { tenantId: req
      - recommendation.updateMany
      requestBody:
        required: true
        description: Validated by `coreSystemSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                slug:
                  type: string
                  enum: []
              required:
              - slug
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/departments:
    get:
      operationId: getPublicOnboardingDepartments
      tags:
      - Onboarding
      summary: List Departments
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:1075` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1075
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - department.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postPublicOnboardingDepartments
      tags:
      - Onboarding
      summary: Create Departments
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:661` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:661
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - tenant.findUnique
      - businessProfile.findUnique
      - department.deleteMany
      - department.create
      - tenantOnboarding.upsert
      requestBody:
        required: true
        description: Validated by `departmentConfigSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                departments:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        minLength: 1
                        maxLength: 100
                      description:
                        type: string
                        maxLength: 500
                      queueMode:
                        type: string
                        enum:
                        - CLAIM
                        - ROUND_ROBIN
                        default: CLAIM
                      workingHours:
                        type: object
                        properties:
                          timezone:
                            type: string
                            minLength: 1
                          schedule:
                            type: object
                            additionalProperties: true
                        required:
                        - timezone
                        - schedule
                      slaTarget:
                        type: integer
                        minimum: 1
                        maximum: 1440
                      escalationPolicy:
                        type: object
                        properties:
                          escalateOnBreach:
                            type: boolean
                            default: false
                          escalateTo:
                            type: string
                          maxWaitMinutes:
                            type: integer
                            minimum: 1
                      autoGreetingEnabled:
                        type: boolean
                        default: false
                      autoGreetingMessage:
                        type: string
                        maxLength: 1000
                      autoCloseMinutes:
                        type: integer
                        minimum: 1
                        maximum: 10080
                      escalateOnSlaBreach:
                        type: boolean
                        default: false
                      aiSuggestionsEnabled:
                        type: boolean
                        default: true
                      autoRepliesEnabled:
                        type: boolean
                        default: false
                    required:
                    - name
                    - slaTarget
                  minItems: 1
              required:
              - departments
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/discover:
    post:
      operationId: postPublicOnboardingDiscover
      tags:
      - Onboarding
      summary: Create Discover
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:2130` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2130
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - auditLog.create
      - businessDiscovery.findUnique
      - |-
          businessDiscovery.update({
                  where: { tenantId: req
      - "businessDiscovery.update({ where: { tenantId: req"
      - |-
          businessDiscovery.update({
                where: { tenantId: req
      - |-
          channelAccount.findFirst({
                where: { tenantId: req
      - channelAccount.findFirst
      - |-
          businessProfile.findUnique({
                  where: { tenantId: req
      - businessProfile.findUnique
      requestBody:
        required: true
        description: Validated by `discoverSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                domain:
                  type: string
                  minLength: 3
                  maxLength: 255
                locale:
                  type: string
                  minLength: 2
                  maxLength: 10
                  default: en
                legalAccepted:
                  type: boolean
              required:
              - domain
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/discover/plan:
    post:
      operationId: postPublicOnboardingDiscoverPlan
      tags:
      - Onboarding
      summary: Create Plan
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:2516` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2516
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      requestBody:
        required: true
        description: Validated by `planSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                domain:
                  type: string
                  minLength: 3
                  maxLength: 255
                locale:
                  type: string
                  minLength: 2
                  maxLength: 10
                  default: en
              required:
              - domain
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/discovery:
    get:
      operationId: getPublicOnboardingDiscovery
      tags:
      - Onboarding
      summary: List Discovery
      description: |-
        GET /discovery - the persisted Business Intelligence Report (re-hydrates the
        review, health, and recommendation surfaces on refresh / OAuth return).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:2536` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2536
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - businessDiscovery.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchPublicOnboardingDiscovery
      tags:
      - Onboarding
      summary: Update Discovery
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:2592` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2592
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - businessDiscovery.findUnique
      - businessDiscovery.update
      requestBody:
        required: true
        description: Validated by `discoveryPatchSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                employeeName:
                  type: string
                  minLength: 1
                  maxLength: 120
                business:
                  type: object
                  properties:
                    name:
                      type: string
                      maxLength: 200
                    industry:
                      type: string
                      maxLength: 120
                    country:
                      type: string
                      maxLength: 120
                    summary:
                      type: string
                      maxLength: 2000
                    valueProp:
                      type: string
                      maxLength: 2000
                    products:
                      type: array
                      items:
                        type: string
                        maxLength: 120
                      maxItems: 24
                brand:
                  type: object
                  properties:
                    voice:
                      type: string
                      maxLength: 200
                    tone:
                      type: string
                      maxLength: 200
                    languages:
                      type: array
                      items:
                        type: string
                        maxLength: 20
                      maxItems: 6
                communication:
                  type: object
                  properties:
                    channels:
                      type: array
                      items:
                        type: object
                        properties:
                          type:
                            type: string
                            minLength: 1
                            maxLength: 40
                          identifier:
                            type: string
                            maxLength: 200
                          purpose:
                            type: string
                            maxLength: 200
                          provider:
                            type: string
                            maxLength: 40
                          confidence:
                            type: string
                            maxLength: 40
                        required:
                        - type
                      maxItems: 24
                  required:
                  - channels
                progress:
                  type: string
                  enum: []
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/discovery/correct:
    post:
      operationId: postPublicOnboardingDiscoveryCorrect
      tags:
      - Onboarding
      summary: Create Correct
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:2644` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2644
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - |-
          recommendation.updateMany({
                  where: { tenantId: req
      - recommendation.updateMany
      - businessDiscovery.update
      requestBody:
        required: true
        description: Validated by `correctSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                target:
                  type: string
                  enum:
                  - channel
                  - tool
                  - platform
                  - gap
                action:
                  type: string
                  enum:
                  - remove
                  - incorrect
                  - ignore
                key:
                  type: string
                  minLength: 1
                  maxLength: 200
              required:
              - target
              - action
              - key
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/employee-chat:
    post:
      operationId: postPublicOnboardingEmployeeChat
      tags:
      - Onboarding
      summary: Create Employee Chat
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:3032` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:3032
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - |-
          businessDiscovery.update({
                    where: { tenantId: req
      - |-
          businessDiscovery.update({
                  where: { tenantId: req
      - businessDiscovery.update
      requestBody:
        required: true
        description: Validated by `employeeChatSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                persona:
                  type: object
                  properties:
                    tone:
                      type: string
                      maxLength: 40
                    personality:
                      type: string
                      maxLength: 400
                    focus:
                      type: string
                      maxLength: 200
                    goal:
                      type: string
                      maxLength: 600
                    successCriteria:
                      type: array
                      items:
                        type: string
                        maxLength: 300
                      maxItems: 8
                    instructions:
                      type: array
                      items:
                        type: string
                        maxLength: 400
                      maxItems: 20
                messages:
                  type: array
                  items:
                    type: object
                    properties:
                      role:
                        type: string
                        enum:
                        - user
                        - assistant
                      content:
                        type: string
                        minLength: 1
                        maxLength: 2000
                    required:
                    - role
                    - content
                  maxItems: 20
                locale:
                  type: string
                  minLength: 2
                  maxLength: 10
                personaOnly:
                  type: boolean
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/generate-configs:
    post:
      operationId: postPublicOnboardingGenerateConfigs
      tags:
      - Onboarding
      summary: Create Generate Configs
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:796` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:796
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - businessProfile.findUnique
      - department.findMany
      - aIAgent.findMany
      - tenantOnboarding.upsert
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/public/onboarding/goal:
    post:
      operationId: postPublicOnboardingGoal
      tags:
      - Onboarding
      summary: Create Goal
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:2927` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2927
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - |-
          businessProfile.updateMany({
                where: { tenantId: req
      - businessProfile.updateMany
      requestBody:
        required: true
        description: Validated by `goalSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                goals:
                  type: array
                  items:
                    type: string
                    minLength: 1
                    maxLength: 64
                  minItems: 1
                  maxItems: 4
                goal:
                  type: string
                  minLength: 1
                  maxLength: 64
                detail:
                  type: string
                  maxLength: 500
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/guides:
    get:
      operationId: getPublicOnboardingGuides
      tags:
      - Onboarding
      summary: List Guides
      description: |-
        GET/PATCH /guides - per-user first-time guidance state for the persistent
        onboarding layer. Available to ALL roles (agents get guidance too), so no
        requireRole gate. State machine per feature: unseen -> snoozed | done.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:1523` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1523
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - user.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchPublicOnboardingGuides
      tags:
      - Onboarding
      summary: Update Guides
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:1539` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1539
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - user.findUnique
      - user.update
      requestBody:
        required: true
        description: Validated by `guidePatchSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                feature:
                  type: string
                  minLength: 1
                  maxLength: 64
                state:
                  type: string
                  enum:
                  - unseen
                  - snoozed
                  - done
              required:
              - feature
              - state
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/health:
    get:
      operationId: getPublicOnboardingHealth
      tags:
      - Onboarding
      summary: List Health
      description: |-
        GET /health - Business Health (Movement 3), recomputed against LIVE connection
        state so a channel/system connected after the scan flips its ⚠ to ✓.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:2687` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2687
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - |-
          channelAccount.findFirst({
                where: { tenantId: req
      - channelAccount.findFirst
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/invite-link:
    post:
      operationId: postPublicOnboardingInviteLink
      tags:
      - Onboarding
      summary: Create Invite Link
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:3182` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:3182
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - tenant.findUnique
      - tenantInvite.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/public/onboarding/invite-team:
    post:
      operationId: postPublicOnboardingInviteTeam
      tags:
      - Onboarding
      summary: Create Invite Team
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:3101` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:3101
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - user.findUnique
      - tenant.findUnique
      - tenantInvite.create
      requestBody:
        required: true
        description: Validated by `inviteTeamSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                emails:
                  type: array
                  items:
                    type: string
                    format: email
                  minItems: 1
                  maxItems: 25
                role:
                  type: string
                  enum:
                  - ADMIN
                  - AGENT
                  default: AGENT
              required:
              - emails
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/journey:
    get:
      operationId: getPublicOnboardingJourney
      tags:
      - Onboarding
      summary: List Journey
      description: |-
        GET /journey - THE canonical setup-readiness state. The Getting Started
        page, the sidebar "Complete setup" panel, and the nav badge all derive from
        this one endpoint, so they can never contradict each other.

        Five canonical actions, every one DERIVED from persisted product state
        (never a visited-page flag, an opened modal, or a started OAuth):
          1. connect_source_of_truth  - a USABLE core system (CONNECTED and, for
             Airtable, field-mapped; see connectedCoreSystem).
          2. connect_channel          - at least one ChannelAccount CONNECTED + active.
             A tenant whose only channel turned ERROR/DISCONNECTED is NOT complete.
          3. connect_knowledge        - at least one ACTIVE knowledge base that has
             real content (a document or an active synced integration). An empty
             KB shell does not count.
          4. create_ai_employee       - an AIAgent in ACTIVE/PAUSED. A mid-wizard
             DRAFT counts as in_progress, never as done.
          5. create_process           - at least one persisted ChatbotFlow.

        Each item carries `state`: done | in_progress | attention | not_started.
        `attention` = a resource exists but is unhealthy (ERROR/DISCONNECTED) and
        nothing usable remains - the UI shows a fix/reconnect CTA for it.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:1243` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1243
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - |-
          aIAgent.findFirst({
                  where: { tenantId, status: "DRAFT" },
                  select: { id: true },
                })
      - |-
          tenantIntegration.findMany({
                  where: { tenantId, integration: { slug: { in: CORE_SYSTEM_SLUGS as unknown as string[] } } },
                  select: { status: true },
                })
      - |-
          channelAccount.findMany({
                  where: { tenantId },
                  select: { connectionStatus: true, isActive: true },
                })
      - |-
          businessDiscovery.findUnique({
                  where: { tenantId },
                  select: { communication: true, business: true },
                })
      - |-
          knowledgeBase.count({
                  where: {
                    tenantId,
                    isActive: true,
                    OR: [{ documents: { some: {} } }, { integrations: { some: { isActive: true } } }],
                  },
                })
      - |-
          flowCanvas.findUnique({
                  where: { tenantId },
                  select: { nodes: true, edges: true },
                })
      - "chatbotFlow.count({ where: { tenantId } })"
      - |-
          businessProfile.findUnique({
                  where: { tenantId },
                  select: { organizationName: true, industry: true },
                })
      - businessProfile.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/missions:
    get:
      operationId: getPublicOnboardingMissions
      tags:
      - Onboarding
      summary: List Missions
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:1143` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1143
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - |-
          businessDiscovery.findUnique({
                  where: { tenantId },
                  select: { communication: true },
                })
      - |-
          recommendation.findMany({
                  where: { tenantId, status: "OPEN" },
                  select: { kind: true, dedupeKey: true },
                })
      - recommendation.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/notify-integration:
    post:
      operationId: postPublicOnboardingNotifyIntegration
      tags:
      - Onboarding
      summary: Create Notify Integration
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:2964` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2964
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - "user.findUnique({ where: { id: req"
      - "businessDiscovery.findUnique({ where: { tenantId: req"
      - businessDiscovery.findUnique
      requestBody:
        required: true
        description: Validated by `notifyIntegrationSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                integration:
                  type: string
                  minLength: 1
                  maxLength: 120
                note:
                  type: string
                  maxLength: 500
                source:
                  type: string
                  maxLength: 60
              required:
              - integration
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/recommendation:
    get:
      operationId: getPublicOnboardingRecommendation
      tags:
      - Onboarding
      summary: List Recommendation
      description: |-
        GET /recommendation - the first AI Recommendation (Movement 4), read straight
        off the persisted report.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:2712` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2712
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - businessDiscovery.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/recommendations:
    get:
      operationId: getPublicOnboardingRecommendations
      tags:
      - Onboarding
      summary: List Recommendations
      description: |-
        GET /recommendations - the persistent living backlog (defaults to OPEN).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:2864` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2864
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/recommendations/{id}/{decision}:
    post:
      operationId: postPublicOnboardingRecommendationsIdDecision
      tags:
      - Onboarding
      summary: Decision action
      description: |-
        POST /recommendations/:id/complete | /dismiss | /reopen.
        A resolved rec is never resurrected by a future re-scan; `reopen` is the
        explicit human undo (with confirmation in the UI) and restores OPEN.
        Every transition writes an audit event with the acting user.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:2882` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2882
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: decision
        in: path
        required: true
        schema:
          type: string
        description: "`decision` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/public/onboarding/setup-map:
    get:
      operationId: getPublicOnboardingSetupMap
      tags:
      - Onboarding
      summary: List Setup Map
      description: |-
        GET /setup-map - the non-linear Setup Hub tiles + done flags, derived from
        real product signals (no manual check-offs). Tiles tagged stage:"later"
        (Channels, extra Integrations) are shown but excluded from the headline
        progress count.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:1475` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:1475
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - "aIAgent.findFirst({ where: { tenantId }, select: { id: true, status: true } })"
      - "flowCanvas.findUnique({ where: { tenantId }, select: { nodes: true, edges: true } })"
      - "chatbotFlow.count({ where: { tenantId } })"
      - chatbotFlow.count
      - "channelAccount.findFirst({ where: { tenantId, connectionStatus: \"CONNECTED\" }, select: { id: true } })"
      - "tenantIntegration.count({ where: { tenantId, status: \"CONNECTED\" } })"
      - "businessProfile.findUnique({ where: { tenantId }, select: { primarySystem: true } })"
      - businessProfile.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/status:
    get:
      operationId: getPublicOnboardingStatus
      tags:
      - Onboarding
      summary: List Status
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/onboarding.ts:461` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:461
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - tenant.findUnique
      - tenantOnboarding.findUnique
      - businessProfile.findUnique
      - department.findMany
      - aIAgent.count
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/onboarding/teach:
    post:
      operationId: postPublicOnboardingTeach
      tags:
      - Onboarding
      summary: Create Teach
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/onboarding.ts:2755` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/onboarding.ts:2755
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - knowledgeDocument.findFirst
      - knowledgeBase.findFirst
      - |-
          knowledgeDocument
                    .findMany({
                      where: { tenantId: req
      - |-
          knowledgeDocument
                    .findMany
      requestBody:
        required: true
        description: Validated by `teachSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                label:
                  type: string
                  minLength: 1
                  maxLength: 200
                method:
                  type: string
                  enum:
                  - text
                  - url
                  - file
                value:
                  type: string
                  minLength: 1
                  maxLength: 20000
              required:
              - label
              - method
              - value
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/public/pricing:
    get:
      operationId: getPublicPricing
      tags:
      - Billing
      summary: List Pricing
      description: |-
        The public catalog.

        404 when the flag is off, deliberately - a disabled pricing page should look
        like it does not exist rather than like a locked door, and a 404 leaks no
        configuration.

        No authentication middleware is applied to this route.

        Implemented at `services/billing/src/routes/public-pricing.ts:271` (billing service).
      security: []
      x-service: billing
      x-source: services/billing/src/routes/public-pricing.ts:271
      x-operation-type: read
      x-publicly-exposed: true
      parameters:
      - name: currency
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: locale
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "304":
          description: HTTP 304
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "503":
          description: Temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/reasoner-shadow/{id}:
    get:
      operationId: getReasonerShadowId
      tags:
      - AI Studio
      summary: Get Reasoner Shadow
      description: |-
        GET /:id - one full record (incl. replayable reasonerInput) for deep review.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/reasoner-shadow.ts:76` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/reasoner-shadow.ts:76
      x-operation-type: read
      x-publicly-exposed: false
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/reasoner-shadow/divergences:
    get:
      operationId: getReasonerShadowDivergences
      tags:
      - AI Studio
      summary: List Divergences
      description: |-
        GET /divergences - the review queue: recent disagreements, newest first.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/reasoner-shadow.ts:56` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/reasoner-shadow.ts:56
      x-operation-type: read
      x-publicly-exposed: false
      x-authorization:
        roles:
        - ADMIN
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/reasoner-shadow/summary:
    get:
      operationId: getReasonerShadowSummary
      tags:
      - AI Studio
      summary: List Summary
      description: |-
        GET /summary - parity headline, sliceable by provider/model/promptVersion/since.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/reasoner-shadow.ts:38` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/reasoner-shadow.ts:38
      x-operation-type: read
      x-publicly-exposed: false
      x-authorization:
        roles:
        - ADMIN
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/router-rules:
    get:
      operationId: getRouterRules
      tags:
      - Conversations
      summary: List Router Rules
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/router-rules.ts:18` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/router-rules.ts:18
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - routerRule.findMany
      - chatbotFlow.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postRouterRules
      tags:
      - Conversations
      summary: Create Router Rules
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/router-rules.ts:81` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/router-rules.ts:81
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - routerRule.aggregate
      - routerRule.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/router-rules/{id}:
    delete:
      operationId: deleteRouterRulesId
      tags:
      - Conversations
      summary: Delete Router Rule
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/router-rules.ts:323` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/router-rules.ts:323
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - routerRule.findFirst
      - routerRule.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: getRouterRulesId
      tags:
      - Conversations
      summary: Get Router Rule
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/router-rules.ts:59` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/router-rules.ts:59
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - routerRule.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchRouterRulesId
      tags:
      - Conversations
      summary: Update Router Rule
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/router-rules.ts:124` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/router-rules.ts:124
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - routerRule.findFirst
      - routerRule.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/router-rules/reorder:
    put:
      operationId: putRouterRulesReorder
      tags:
      - Conversations
      summary: Replace Reorder
      description: |-
        Client sends `ruleIds` in desired evaluation order. The server writes
        `position` = index + 1 for each. Default rules are automatically
        evaluated last by the live router regardless of their position value.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/router-rules.ts:168` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/router-rules.ts:168
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - routerRule.findMany
      - routerRule.update
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/router-rules/test:
    post:
      operationId: postRouterRulesTest
      tags:
      - Conversations
      summary: Create Test
      description: |-
        Body: { message: string, channel?: string, tags?: string[] }
        Returns: full routing trace + which rule (or fallback) would win.
        No DB mutation. Admin only.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/router-rules.ts:208` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/router-rules.ts:208
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - routerRule.findMany
      - tenant.findUnique
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/scheduled-messages:
    get:
      operationId: getScheduledMessages
      tags:
      - Conversations
      summary: List Scheduled Messages
      description: |-
        GET / - List scheduled messages for tenant

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/scheduled-messages.ts:8` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/scheduled-messages.ts:8
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        - AGENT
      x-database:
      - scheduledMessage.findMany
      - scheduledMessage.count
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postScheduledMessages
      tags:
      - Conversations
      summary: Create Scheduled Messages
      description: |-
        POST / - Create scheduled message

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/scheduled-messages.ts:51` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/scheduled-messages.ts:51
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        - AGENT
      x-database:
      - scheduledMessage.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/scheduled-messages/{id}:
    delete:
      operationId: deleteScheduledMessagesId
      tags:
      - Conversations
      summary: Delete Scheduled Message
      description: |-
        DELETE /:id - Cancel scheduled message

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/scheduled-messages.ts:147` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/scheduled-messages.ts:147
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        - AGENT
      x-database:
      - scheduledMessage.findFirst
      - scheduledMessage.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: getScheduledMessagesId
      tags:
      - Conversations
      summary: Get Scheduled Message
      description: |-
        GET /:id - Get single scheduled message

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/scheduled-messages.ts:37` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/scheduled-messages.ts:37
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        - AGENT
      x-database:
      - scheduledMessage.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchScheduledMessagesId
      tags:
      - Conversations
      summary: Update Scheduled Message
      description: |-
        PATCH /:id - Update scheduled message (only if PENDING)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/scheduled-messages.ts:109` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/scheduled-messages.ts:109
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        - AGENT
      x-database:
      - scheduledMessage.findFirst
      - scheduledMessage.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/scheduler/calendar-accounts:
    get:
      operationId: getSchedulerCalendarAccounts
      tags:
      - Integrations
      summary: List Calendar Accounts
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/scheduler-admin.ts:107` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/scheduler-admin.ts:107
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: aiAgentId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/scheduler/meeting-types:
    get:
      operationId: getSchedulerMeetingTypes
      tags:
      - Integrations
      summary: List Meeting Types
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/scheduler-admin.ts:25` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/scheduler-admin.ts:25
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postSchedulerMeetingTypes
      tags:
      - Integrations
      summary: Create Meeting Types
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/scheduler-admin.ts:33` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/scheduler-admin.ts:33
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/scheduler/meeting-types/{id}:
    delete:
      operationId: deleteSchedulerMeetingTypesId
      tags:
      - Integrations
      summary: Delete Meeting Type
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/scheduler-admin.ts:98` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/scheduler-admin.ts:98
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchSchedulerMeetingTypesId
      tags:
      - Integrations
      summary: Update Meeting Type
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/scheduler-admin.ts:76` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/scheduler-admin.ts:76
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/shopify-chat/bootstrap:
    post:
      operationId: postShopifyChatBootstrap
      tags:
      - Shopify
      summary: Create Bootstrap
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/ai/src/routes/shopify-chat-public.ts:244` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-chat-public.ts:244
      x-operation-type: write
      x-publicly-exposed: true
      x-rate-limit:
      - bootstrapLimiter
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/shopify-chat/cart/result:
    post:
      operationId: postShopifyChatCartResult
      tags:
      - Shopify
      summary: Create Result
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/ai/src/routes/shopify-chat-public.ts:723` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-chat-public.ts:723
      x-operation-type: write
      x-publicly-exposed: true
      x-rate-limit:
      - cartLimiter
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/shopify-chat/cart/validate:
    post:
      operationId: postShopifyChatCartValidate
      tags:
      - Shopify
      summary: Create Validate
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/ai/src/routes/shopify-chat-public.ts:669` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-chat-public.ts:669
      x-operation-type: write
      x-publicly-exposed: true
      x-rate-limit:
      - cartLimiter
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/shopify-chat/conversation:
    post:
      operationId: postShopifyChatConversation
      tags:
      - Shopify
      summary: Create Conversation
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/ai/src/routes/shopify-chat-public.ts:426` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-chat-public.ts:426
      x-operation-type: write
      x-publicly-exposed: true
      x-rate-limit:
      - conversationLimiter
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/shopify-chat/events:
    post:
      operationId: postShopifyChatEvents
      tags:
      - Shopify
      summary: Create Events
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/ai/src/routes/shopify-chat-public.ts:860` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-chat-public.ts:860
      x-operation-type: write
      x-publicly-exposed: true
      x-rate-limit:
      - eventLimiter
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/shopify-chat/handoff:
    post:
      operationId: postShopifyChatHandoff
      tags:
      - Shopify
      summary: Create Handoff
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/ai/src/routes/shopify-chat-public.ts:735` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-chat-public.ts:735
      x-operation-type: write
      x-publicly-exposed: true
      x-rate-limit:
      - conversationLimiter
      x-database:
      - conversation.update
      - message.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/shopify-chat/lead:
    post:
      operationId: postShopifyChatLead
      tags:
      - Shopify
      summary: Create Lead
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/ai/src/routes/shopify-chat-public.ts:779` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-chat-public.ts:779
      x-operation-type: write
      x-publicly-exposed: true
      x-rate-limit:
      - conversationLimiter
      x-database:
      - conversation.update
      - message.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/shopify-chat/message:
    post:
      operationId: postShopifyChatMessage
      tags:
      - Shopify
      summary: Create Message
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Side effects: queue: incomingMessageQueue.add.

        Implemented at `services/ai/src/routes/shopify-chat-public.ts:515` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-chat-public.ts:515
      x-operation-type: write
      x-publicly-exposed: true
      x-rate-limit:
      - messageLimiter
      x-side-effects:
      - "queue: incomingMessageQueue.add"
      x-async: Enqueues background work; the response returns before that work completes.
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "413":
          description: Payload too large
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/shopify-chat/messages:
    get:
      operationId: getShopifyChatMessages
      tags:
      - Shopify
      summary: List Messages
      description: |-
        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/shopify-chat-public.ts:586` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-chat-public.ts:586
      x-operation-type: read
      x-publicly-exposed: true
      x-rate-limit:
      - pollLimiter
      x-database:
      - conversation.findFirst
      parameters:
      - name: after
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/shopify-chat/proxy/identity:
    get:
      operationId: getShopifyChatProxyIdentity
      tags:
      - Shopify
      summary: List Identity
      description: |-
        Reached ONLY through Shopify's App Proxy: the browser calls the
        merchant's own origin (`https://shop.myshopify.com/apps/gotcha-chat/
        identity`) and Shopify forwards it here, appending
        `logged_in_customer_id` and a `signature` made with our app secret.

        That is what makes the answer trustworthy. The browser never holds the
        secret, so it cannot manufacture the signature - unlike Liquid's
        `customer.id`, which any shopper can edit before it reaches us.

        A logged-out shopper is not an error: the request is still validly
        signed, there is simply no id, and the widget carries on anonymously.

        No authentication middleware is applied to this route.

        Implemented at `services/ai/src/routes/shopify-chat-public.ts:384` (ai service).
      security: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-chat-public.ts:384
      x-operation-type: read
      x-publicly-exposed: true
      x-rate-limit:
      - bootstrapLimiter
      parameters:
      - name: shop
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/shopify-live-chat/channels:
    get:
      operationId: getShopifyLiveChatChannels
      tags:
      - Shopify
      summary: List Channels
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/shopify-live-chat.ts:102` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-live-chat.ts:102
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        featureFlags:
        - SHOPIFY_LIVE_CHAT
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postShopifyLiveChatChannels
      tags:
      - Shopify
      summary: Create Channels
      description: |-
        Create the channel.

        A connected store is mandatory: a Shopify Live Chat channel with no
        Shopify behind it is a support widget wearing a costume, and every
        downstream guarantee (product truth, store isolation, cart validation)
        depends on the binding made right here. The binding is written once and
        is not patchable afterwards.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/shopify-live-chat.ts:124` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-live-chat.ts:124
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        featureFlags:
        - SHOPIFY_LIVE_CHAT
      x-database:
      - channelAccount.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/shopify-live-chat/channels/{id}:
    delete:
      operationId: deleteShopifyLiveChatChannelsId
      tags:
      - Shopify
      summary: Delete Channel
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/shopify-live-chat.ts:231` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-live-chat.ts:231
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        featureFlags:
        - SHOPIFY_LIVE_CHAT
      x-database:
      - channelAccount.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: getShopifyLiveChatChannelsId
      tags:
      - Shopify
      summary: Get Channel
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/shopify-live-chat.ts:178` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-live-chat.ts:178
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        featureFlags:
        - SHOPIFY_LIVE_CHAT
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putShopifyLiveChatChannelsId
      tags:
      - Shopify
      summary: Replace Channel
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/shopify-live-chat.ts:191` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-live-chat.ts:191
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        featureFlags:
        - SHOPIFY_LIVE_CHAT
      x-database:
      - channelAccount.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/shopify-live-chat/channels/{id}/diagnostics:
    get:
      operationId: getShopifyLiveChatChannelsIdDiagnostics
      tags:
      - Shopify
      summary: Get Diagnostics
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/shopify-live-chat.ts:257` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-live-chat.ts:257
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        featureFlags:
        - SHOPIFY_LIVE_CHAT
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/shopify-live-chat/channels/{id}/install:
    get:
      operationId: getShopifyLiveChatChannelsIdInstall
      tags:
      - Shopify
      summary: Get Install
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/shopify-live-chat.ts:396` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-live-chat.ts:396
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        featureFlags:
        - SHOPIFY_LIVE_CHAT
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/shopify-live-chat/conversations/{conversationId}/products:
    post:
      operationId: postShopifyLiveChatConversationsConversationIdProducts
      tags:
      - Shopify
      summary: Create Products
      description: |-
        Send one product or a carousel into a conversation as the human agent.

        The product references are re-resolved from Shopify here; the browser's
        copy of the price is never persisted. The conversation is loaded
        tenant-scoped, and the channel binding decides which store is legal -
        an agent cannot send another workspace's product.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/shopify-live-chat.ts:474` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-live-chat.ts:474
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        featureFlags:
        - SHOPIFY_PRODUCT_MESSAGING
      x-database:
      - auditLog.create
      parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
        description: "`conversationId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/shopify-live-chat/products:
    get:
      operationId: getShopifyLiveChatProducts
      tags:
      - Shopify
      summary: List Products
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/shopify-live-chat.ts:437` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-live-chat.ts:437
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        featureFlags:
        - SHOPIFY_PRODUCT_MESSAGING
      parameters:
      - name: q
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: limit
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: includeUnpublished
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/shopify-live-chat/store:
    get:
      operationId: getShopifyLiveChatStore
      tags:
      - Shopify
      summary: List Store
      description: |-
        Theme Editor deep links are built from the CHAT app's identity, never
        the Core integration's: the App Embed belongs to the Chat app, and
        pointing `activateAppId` at the Core client id opens the editor with
        nothing selected.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/shopify-live-chat.ts:74` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/shopify-live-chat.ts:74
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        featureFlags:
        - SHOPIFY_LIVE_CHAT
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/stage-transitions/contacts/{id}:
    get:
      operationId: getStageTransitionsContactsId
      tags:
      - AI Studio
      summary: Get Contact
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/stage-admin.ts:226` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/stage-admin.ts:226
      x-operation-type: read
      x-publicly-exposed: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: limit
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/stage-transitions/conversations/{id}:
    get:
      operationId: getStageTransitionsConversationsId
      tags:
      - AI Studio
      summary: Get Conversation
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/stage-admin.ts:209` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/stage-admin.ts:209
      x-operation-type: read
      x-publicly-exposed: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: limit
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/stage-transitions/conversations/{id}/override:
    post:
      operationId: postStageTransitionsConversationsIdOverride
      tags:
      - AI Studio
      summary: Create Override
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/stage-admin.ts:48` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/stage-admin.ts:48
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - conversation.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/system-chat/ask:
    post:
      operationId: postSystemChatAsk
      tags:
      - AI Studio
      summary: Create Ask
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/ai/src/routes/system-chat.ts:231` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/system-chat.ts:231
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/system-chat/knowledge-bases:
    get:
      operationId: getSystemChatKnowledgeBases
      tags:
      - AI Studio
      summary: List Knowledge Bases
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/ai/src/routes/system-chat.ts:42` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/system-chat.ts:42
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      x-database:
      - knowledgeBase.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postSystemChatKnowledgeBases
      tags:
      - AI Studio
      summary: Create Knowledge Bases
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/ai/src/routes/system-chat.ts:61` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/system-chat.ts:61
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      x-database:
      - knowledgeBase.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/system-chat/knowledge-bases/{id}:
    delete:
      operationId: deleteSystemChatKnowledgeBasesId
      tags:
      - AI Studio
      summary: Delete Knowledge Base
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/ai/src/routes/system-chat.ts:99` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/system-chat.ts:99
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      x-database:
      - knowledgeBase.findFirst
      - knowledgeBase.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchSystemChatKnowledgeBasesId
      tags:
      - AI Studio
      summary: Update Knowledge Base
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/ai/src/routes/system-chat.ts:76` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/system-chat.ts:76
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      x-database:
      - knowledgeBase.findFirst
      - knowledgeBase.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/system-chat/knowledge-bases/{id}/documents:
    post:
      operationId: postSystemChatKnowledgeBasesIdDocuments
      tags:
      - AI Studio
      summary: Create Documents
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/ai/src/routes/system-chat.ts:116` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/system-chat.ts:116
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      x-database:
      - knowledgeBase.findFirst
      - knowledgeDocument.create
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/system-chat/knowledge-bases/{id}/documents/{docId}:
    delete:
      operationId: deleteSystemChatKnowledgeBasesIdDocumentsDocId
      tags:
      - AI Studio
      summary: Delete Document
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/ai/src/routes/system-chat.ts:195` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/system-chat.ts:195
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      x-database:
      - knowledgeDocument.findFirst
      - knowledgeDocument.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: docId
        in: path
        required: true
        schema:
          type: string
        description: "`docId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system-chat/knowledge-bases/{id}/documents/{docId}/process:
    post:
      operationId: postSystemChatKnowledgeBasesIdDocumentsDocIdProcess
      tags:
      - AI Studio
      summary: Create Process
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/ai/src/routes/system-chat.ts:211` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/system-chat.ts:211
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      x-database:
      - knowledgeDocument.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: docId
        in: path
        required: true
        schema:
          type: string
        description: "`docId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/system-chat/knowledge-bases/{id}/documents/upload:
    post:
      operationId: postSystemChatKnowledgeBasesIdDocumentsUpload
      tags:
      - AI Studio
      summary: Create Upload
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/ai/src/routes/system-chat.ts:144` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/system-chat.ts:144
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      x-database:
      - knowledgeBase.findFirst
      - knowledgeDocument.create
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
              required:
              - file
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/system-chat/token-usage:
    get:
      operationId: getSystemChatTokenUsage
      tags:
      - AI Studio
      summary: List Token Usage
      description: |-
        NOTE: these endpoints now read from `usage_logs` (the authoritative source)
        filtered to `type = "ai_tokens"`, using the denormalized `feature`, `model`,
        `promptTokens`, `completionTokens` columns added in the 20260414 migration.
        The legacy `token_logs` table is retired - nothing writes to it.

        Requires a valid Authentik access token.

        Implemented at `services/ai/src/routes/system-chat.ts:286` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/system-chat.ts:286
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      x-database:
      - usageLog.aggregate
      parameters:
      - name: tenantId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system-chat/token-usage/tenants:
    get:
      operationId: getSystemChatTokenUsageTenants
      tags:
      - AI Studio
      summary: List Tenants
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/ai/src/routes/system-chat.ts:394` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/system-chat.ts:394
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      x-database:
      - usageLog.groupBy
      - tenant.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/features:
    get:
      operationId: getSystemFeatures
      tags:
      - Platform Admin
      summary: List Features
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        Implemented at `services/auth/src/routes/system-features.ts:39` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system-features.ts:39
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/knowledge-backfill:
    post:
      operationId: postSystemKnowledgeBackfill
      tags:
      - Platform Admin
      summary: Create Knowledge Backfill
      description: |-
        POST /api/system/knowledge-backfill
        body: { dryRun?: boolean (default true), tenantIds?: string[], limit?: number }

        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/knowledge-backfill.ts:100` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/knowledge-backfill.ts:100
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - |-
          tenant.findUnique({
                  where: { id: disc
      - |-
          businessProfile.findUnique({
                  where: { tenantId: tenant
      - |-
          knowledgeDocument.findMany({
                    where: { tenantId: tenant
      - knowledgeDocument.findMany
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/system/onboarding-console:
    get:
      operationId: getSystemOnboardingConsole
      tags:
      - Platform Admin
      summary: List Onboarding Console
      description: |-
        One row per tenant with every onboarding milestone, derived progress,
        health, and the Next Recommended Action. The operational board.

        Requires a valid Authentik access token. Deliberately spans tenants.

        Implemented at `services/auth/src/routes/system.ts:1322` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1322
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/poc-feature-domains:
    get:
      operationId: getSystemPocFeatureDomains
      tags:
      - Platform Admin
      summary: List Poc Feature Domains
      description: |-
        The feature areas a POC may be scoped to.

        Served rather than hardcoded in the UI so the picker cannot fall behind the
        catalog - a domain missing from the picker is one an operator cannot grant,
        and, under default-ALLOW, one they cannot deny either.

        Requires a valid Authentik access token. Deliberately spans tenants.

        Implemented at `services/auth/src/routes/system.ts:1027` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1027
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/pricing/trends:
    get:
      operationId: getSystemPricingTrends
      tags:
      - Platform Admin
      summary: List Trends
      description: |-
        Daily cost-per-category buckets for the period. Used by the trends chart
        on /system/pricing. Returns dense buckets (zero-filled) so the chart x-axis
        never has gaps.

        Requires a valid Authentik access token. Deliberately spans tenants.

        Implemented at `services/auth/src/routes/system.ts:2186` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:2186
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      parameters:
      - name: days
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/pricing/unit-costs:
    get:
      operationId: getSystemPricingUnitCosts
      tags:
      - Platform Admin
      summary: List Unit Costs
      description: |-
        Powers /system/pricing. For each customer-facing AI surface (Autonomous
        Agent, Inbox Co-pilot, Call-pilot, Embedded Chat, etc) we return raw
        totals plus unit averages: $/call and $/conversation. The frontend turns
        those into a pricing calculator (markup % → suggested price per unit) and
        a trends chart.

        Notes:
         - Categorisation lives in @chatcenter/shared/lib/ai-feature-categories so
           backend SQL CASE and frontend display labels stay in sync.
         - We split prompt vs completion cost using AI_MODEL_PRICING because input
           and output are billed at very different rates (e.g. gpt-4o = $2.50 in /
           $10 out per 1M). The stored UsageLog.costUsd column already reflects
           cache discounts; we re-derive the split here without re-applying them.
         - Per-conversation averages count distinct `metadata.conversationId` -
           embeddings and one-shot classifications won't have one, so those
           categories return null for that metric.

        Requires a valid Authentik access token. Deliberately spans tenants.

        Implemented at `services/auth/src/routes/system.ts:1945` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1945
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      parameters:
      - name: days
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/seed:
    post:
      operationId: postSystemSeed
      tags:
      - Platform Admin
      summary: Create Seed
      description: |-
        No authentication middleware is applied to this route. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system.ts:1629` (auth service).
      security: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1629
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        crossTenant: true
      x-database:
      - user.findFirst
      - tenant.findUnique
      - tenant.create
      - identity.upsert
      - user.create
      requestBody:
        required: true
        description: Validated by `seedSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  format: email
                name:
                  type: string
                  minLength: 1
                setupSecret:
                  type: string
                  minLength: 1
              required:
              - email
              - name
              - setupSecret
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/stats:
    get:
      operationId: getSystemStats
      tags:
      - Platform Admin
      summary: List Stats
      description: |-
        REMOVED. System admins authenticate through Authentik like everyone else;
        there is no second password login. SYSTEM_ADMIN is a local RBAC role, so
        requireSystemAdmin() still gates every route below - the role is read from
        the database after Authentik proves the identity.

        Requires a valid Authentik access token. Deliberately spans tenants.

        Implemented at `services/auth/src/routes/system.ts:64` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:64
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.count
      - user.count
      - conversation.count
      - message.count
      - channelAccount.count
      - tenant.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/tenants:
    get:
      operationId: getSystemTenants
      tags:
      - Platform Admin
      summary: List Tenants
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        Implemented at `services/auth/src/routes/system.ts:100` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:100
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findMany
      - tenant.count
      parameters:
      - name: search
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: page
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: limit
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postSystemTenants
      tags:
      - Platform Admin
      summary: Create Tenants
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system.ts:347` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:347
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      - identity.upsert
      - tenant.create
      - user.create
      - tenantOnboarding.create
      requestBody:
        required: true
        description: Validated by `createTenantSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                slug:
                  type: string
                  minLength: 2
                  maxLength: 50
                adminEmail:
                  type: string
                  format: email
                adminName:
                  type: string
                  minLength: 1
                billing: {}
              required:
              - name
              - slug
              - adminEmail
              - adminName
              - billing
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/tenants-plan-audit:
    get:
      operationId: getSystemTenantsPlanAudit
      tags:
      - Platform Admin
      summary: List Tenants Plan Audit
      description: |-
        Every tenant, grouped by how it holds access.

        Read-only. A tenant with no plan is REPORTED, never repaired automatically:
        assigning a paid plan would invent a commercial agreement nobody made, and
        granting credits would just make the anomaly stop showing up.

        Requires a valid Authentik access token. Deliberately spans tenants.

        Implemented at `services/auth/src/routes/system.ts:1040` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1040
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/tenants/{id}:
    delete:
      operationId: deleteSystemTenantsId
      tags:
      - Platform Admin
      summary: Delete Tenant
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system.ts:1226` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1226
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: force
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: getSystemTenantsId
      tags:
      - Platform Admin
      summary: Get Tenant
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        Implemented at `services/auth/src/routes/system.ts:171` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:171
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      - user.findMany
      - channelAccount.findMany
      - department.findMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchSystemTenantsId
      tags:
      - Platform Admin
      summary: Update Tenant
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system.ts:1371` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1371
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      - tenant.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: true
        description: Validated by `updateTenantSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 100
                isActive:
                  type: boolean
                voiceCopilotEnabled:
                  type: boolean
                voiceInboxUiEnabled:
                  type: boolean
                voiceIncomingEnabled:
                  type: boolean
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/tenants/{id}/activate-manual-contract:
    post:
      operationId: postSystemTenantsIdActivateManualContract
      tags:
      - Platform Admin
      summary: Create Activate Manual Contract
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system.ts:681` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:681
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
        crossTenant: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/system/tenants/{id}/assign-paid-plan:
    post:
      operationId: postSystemTenantsIdAssignPaidPlan
      tags:
      - Platform Admin
      summary: Create Assign Paid Plan
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system.ts:909` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:909
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      - tenant.update
      - user.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/system/tenants/{id}/billing-provisioning:
    get:
      operationId: getSystemTenantsIdBillingProvisioning
      tags:
      - Platform Admin
      summary: Get Billing Provisioning
      description: |-
        Safe provisioning state for the Sysadmin tenant UI.

        Requires a valid Authentik access token. Deliberately spans tenants.

        Implemented at `services/auth/src/routes/system.ts:879` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:879
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/tenants/{id}/bot-config:
    patch:
      operationId: patchSystemTenantsIdBotConfig
      tags:
      - Platform Admin
      summary: Update Bot Config
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system.ts:1557` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1557
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      - tenant.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: true
        description: Validated by `botConfigSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                botEnabled:
                  type: boolean
                botType:
                  type: string
                  enum:
                  - CHATBOT_FLOW
                  - AUTONOMOUS_AI
              required:
              - botEnabled
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/tenants/{id}/first-take-care:
    patch:
      operationId: patchSystemTenantsIdFirstTakeCare
      tags:
      - Platform Admin
      summary: Update First Take Care
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system.ts:1594` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1594
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      - tenant.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/system/tenants/{id}/manual-contracts:
    get:
      operationId: getSystemTenantsIdManualContracts
      tags:
      - Platform Admin
      summary: Get Manual Contracts
      description: |-
        Manual contracts on record, for the Sysadmin billing history view.

        Requires a valid Authentik access token. Deliberately spans tenants.

        Implemented at `services/auth/src/routes/system.ts:733` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:733
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - requirePlatformPermission
        crossTenant: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/tenants/{id}/nudge:
    post:
      operationId: postSystemTenantsIdNudge
      tags:
      - Platform Admin
      summary: Create Nudge
      description: |-
        Admin-triggered manual nudge - arms + delivers immediately, returns outcome.

        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system.ts:1345` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1345
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/system/tenants/{id}/onboarding-snapshot:
    get:
      operationId: getSystemTenantsIdOnboardingSnapshot
      tags:
      - Platform Admin
      summary: Get Onboarding Snapshot
      description: |-
        Single-tenant snapshot (used by the tenant detail drawer).

        Requires a valid Authentik access token. Deliberately spans tenants.

        Implemented at `services/auth/src/routes/system.ts:1333` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1333
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/tenants/{id}/repair-billing-provisioning:
    post:
      operationId: postSystemTenantsIdRepairBillingProvisioning
      tags:
      - Platform Admin
      summary: Create Repair Billing Provisioning
      description: |-
        Finish billing setup for a tenant whose provisioning did not complete.

        Distinct from resend on purpose. Resend REUSES an existing checkout; repair
        CREATES the records that were never made. Letting one button do both would
        hide a broken provisioning behind an action that looks like it worked.

        Idempotent: billing converges on the request's deterministic key, so calling
        this repeatedly - or concurrently - yields one checkout, one initial payment
        attempt and one active link.

        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system.ts:756` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:756
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      - user.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/system/tenants/{id}/resend-onboarding:
    post:
      operationId: postSystemTenantsIdResendOnboarding
      tags:
      - Platform Admin
      summary: Create Resend Onboarding
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system.ts:1160` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1160
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      - user.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/system/tenants/{id}/resend-payment-link:
    post:
      operationId: postSystemTenantsIdResendPaymentLink
      tags:
      - Platform Admin
      summary: Create Resend Payment Link
      description: |-
        Issue a replacement continuation link for a tenant awaiting payment.

        Reuses the SAME checkout and the SAME payment attempt: a resend is a new
        envelope for the existing offer, never a new offer, so the customer cannot be
        re-priced by an administrator clicking a button. Issuing revokes the previous
        link, so at most one is ever valid.

        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system.ts:1059` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1059
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      - user.findFirst
      - plan.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/system/tenants/{id}/reset-onboarding:
    post:
      operationId: postSystemTenantsIdResetOnboarding
      tags:
      - Platform Admin
      summary: Create Reset Onboarding
      description: |-
        Returns a tenant to a brand-new onboarding state WITHOUT deleting the tenant
        or its people. Deterministic + idempotent.

        DELETED (everything onboarding created / derived):
          BusinessDiscovery (discovery, health, readiness, goals, recommendations,
          the narrated report), BusinessProfile (the confirmed understanding),
          TenantOnboarding tracker, the generated AI employee(s) + their
          AIAgentKnowledge links + RouterRules, onboarding departments + members,
          and all ScheduledNudges. Tenant.status → PENDING_ONBOARDING.

        PRESERVED (deliberately - not onboarding artifacts, and unsafe to destroy):
          the tenant row, its users, connected OAuth integrations + channels
          (revoking live tokens is irreversible), and imported KnowledgeBases
          (real customer content). The next login runs onboarding from scratch;
          any already-connected system is simply detected as already-connected.

        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system.ts:1278` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1278
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      - aIAgent.findMany
      - aIAgentKnowledge.deleteMany
      - routerRule.deleteMany
      - aIAgent.deleteMany
      - departmentMember.deleteMany
      - department.deleteMany
      - businessProfile.deleteMany
      - tenantOnboarding.deleteMany
      - tenant.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/system/tenants/{id}/users:
    post:
      operationId: postSystemTenantsIdUsers
      tags:
      - Platform Admin
      summary: Create Users
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system.ts:1417` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1417
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: true
        description: Validated by `createUserSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  format: email
                name:
                  type: string
                  minLength: 1
                role:
                  type: string
                  enum:
                  - ADMIN
                  - AGENT
                  default: AGENT
              required:
              - email
              - name
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/tenants/{id}/users/{userId}:
    delete:
      operationId: deleteSystemTenantsIdUsersUserId
      tags:
      - Platform Admin
      summary: Delete User
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system.ts:1514` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1514
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - user.findFirst
      - user.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: userId
        in: path
        required: true
        schema:
          type: string
        description: "`userId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchSystemTenantsIdUsersUserId
      tags:
      - Platform Admin
      summary: Update User
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system.ts:1449` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1449
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - user.findFirst
      - user.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: userId
        in: path
        required: true
        schema:
          type: string
        description: "`userId` path segment."
      requestBody:
        required: true
        description: Validated by `updateUserSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 120
                email:
                  type: string
                  format: email
                role:
                  type: string
                  enum:
                  - ADMIN
                  - AGENT
                isActive:
                  type: boolean
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/tenants/{tenantId}/credits:
    post:
      operationId: postSystemTenantsTenantIdCredits
      tags:
      - Platform Admin
      summary: Create Credits
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system-features.ts:312` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system-features.ts:312
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      parameters:
      - name: tenantId
        in: path
        required: true
        schema:
          type: string
        description: "`tenantId` path segment."
      requestBody:
        required: true
        description: Validated by `creditsSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                units:
                  type: number
                  exclusiveMinimum: 0
                  maximum: 1000000
              required:
              - units
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/tenants/{tenantId}/entitlements:
    get:
      operationId: getSystemTenantsTenantIdEntitlements
      tags:
      - Platform Admin
      summary: Get Entitlements
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        Implemented at `services/auth/src/routes/system-features.ts:183` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system-features.ts:183
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      - tenantFeature.findMany
      - tenantEntitlement.findMany
      parameters:
      - name: tenantId
        in: path
        required: true
        schema:
          type: string
        description: "`tenantId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/tenants/{tenantId}/entitlements/{key}:
    put:
      operationId: putSystemTenantsTenantIdEntitlementsKey
      tags:
      - Platform Admin
      summary: Replace Entitlement
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system-features.ts:218` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system-features.ts:218
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      parameters:
      - name: tenantId
        in: path
        required: true
        schema:
          type: string
        description: "`tenantId` path segment."
      - name: key
        in: path
        required: true
        schema:
          type: string
        description: "`key` path segment."
      requestBody:
        required: true
        description: Validated by `entitlementSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled:
                  type: boolean
              required:
              - enabled
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/tenants/{tenantId}/features:
    get:
      operationId: getSystemTenantsTenantIdFeatures
      tags:
      - Platform Admin
      summary: Get Features
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        Implemented at `services/auth/src/routes/system-features.ts:51` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system-features.ts:51
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      - tenantFeature.findMany
      parameters:
      - name: tenantId
        in: path
        required: true
        schema:
          type: string
        description: "`tenantId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/tenants/{tenantId}/features/{feature}:
    put:
      operationId: putSystemTenantsTenantIdFeaturesFeature
      tags:
      - Platform Admin
      summary: Replace Feature
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system-features.ts:97` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system-features.ts:97
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      - tenantFeature.upsert
      parameters:
      - name: tenantId
        in: path
        required: true
        schema:
          type: string
        description: "`tenantId` path segment."
      - name: feature
        in: path
        required: true
        schema:
          type: string
        description: "`feature` path segment."
      requestBody:
        required: true
        description: Validated by `updateFeatureSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled:
                  type: boolean
                config:
                  type: object
                  additionalProperties: true
                  nullable: true
              required:
              - enabled
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/tenants/{tenantId}/poc:
    post:
      operationId: postSystemTenantsTenantIdPoc
      tags:
      - Platform Admin
      summary: Create Poc
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        This is a write operation.

        Implemented at `services/auth/src/routes/system-features.ts:251` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system-features.ts:251
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - tenant.findUnique
      parameters:
      - name: tenantId
        in: path
        required: true
        schema:
          type: string
        description: "`tenantId` path segment."
      requestBody:
        required: true
        description: Validated by `pocSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                credits:
                  type: number
                  exclusiveMinimum: 0
                  maximum: 1000000
                expiresAt:
                  type: string
                  format: date-time
                features:
                  type: array
                  items:
                    type: string
                note:
                  type: string
                  maxLength: 2000
              required:
              - credits
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/usage/by-tenant:
    get:
      operationId: getSystemUsageByTenant
      tags:
      - Platform Admin
      summary: List By Tenant
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        Implemented at `services/auth/src/routes/system.ts:1810` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1810
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - usageLog.groupBy
      - tenant.findMany
      parameters:
      - name: days
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/system/usage/stats:
    get:
      operationId: getSystemUsageStats
      tags:
      - Platform Admin
      summary: List Stats
      description: |-
        Requires a valid Authentik access token. Deliberately spans tenants.

        Implemented at `services/auth/src/routes/system.ts:1692` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/system.ts:1692
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
        crossTenant: true
      x-database:
      - usageLog.groupBy
      - usageLog.aggregate
      - usageLog.count
      parameters:
      - name: days
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/templates:
    get:
      operationId: getTemplates
      tags:
      - Conversations
      summary: List Templates
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/templates.ts:261` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/templates.ts:261
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - messageTemplate.findMany
      - messageTemplate.count
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postTemplates
      tags:
      - Conversations
      summary: Create Templates
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/templates.ts:304` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/templates.ts:304
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - channelAccount.findFirst
      - messageTemplate.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/templates/{id}:
    delete:
      operationId: deleteTemplatesId
      tags:
      - Conversations
      summary: Delete Template
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/templates.ts:415` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/templates.ts:415
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - messageTemplate.findFirst
      - broadcast.count
      - broadcast.updateMany
      - messageTemplate.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: force
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: getTemplatesId
      tags:
      - Conversations
      summary: Get Template
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/templates.ts:291` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/templates.ts:291
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - messageTemplate.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchTemplatesId
      tags:
      - Conversations
      summary: Update Template
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/templates.ts:363` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/templates.ts:363
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - messageTemplate.findFirst
      - channelAccount.findFirst
      - messageTemplate.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/templates/{id}/duplicate:
    post:
      operationId: postTemplatesIdDuplicate
      tags:
      - Conversations
      summary: Create Duplicate
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/templates.ts:465` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/templates.ts:465
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - messageTemplate.findFirst
      - messageTemplate.create
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/templates/{id}/submit-to-meta:
    post:
      operationId: postTemplatesIdSubmitToMeta
      tags:
      - Conversations
      summary: Create Submit To Meta
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/templates.ts:497` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/templates.ts:497
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - messageTemplate.findFirst
      - messageTemplate.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/tenant-settings:
    get:
      operationId: getTenantSettings
      tags:
      - Tenant Settings
      summary: List Tenant Settings
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/tenant-settings.ts:23` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/tenant-settings.ts:23
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - tenant.findUnique
      - communicationChannel.count
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchTenantSettings
      tags:
      - Tenant Settings
      summary: Update Tenant Settings
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/tenant-settings.ts:59` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/tenant-settings.ts:59
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - tenant.update
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/tenant/security:
    get:
      operationId: getTenantSecurity
      tags:
      - Tenant Settings
      summary: List Security
      description: |-
        GET current policy + compliance. Any workspace admin may view.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/tenant-security.ts:114` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/tenant-security.ts:114
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - settings:workspace:update OR role ADMIN
      x-database:
      - tenant.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchTenantSecurity
      tags:
      - Tenant Settings
      summary: Update Security
      description: |-
        PATCH policy flags. Enabling never locks out already-enrolled members; the
        backfill stamps them compliant, and only the un-enrolled get gated.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        This is a write operation.

        Implemented at `services/auth/src/routes/tenant-security.ts:135` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/tenant-security.ts:135
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - settings:workspace:update OR role ADMIN
      x-database:
      - tenant.findUnique
      - tenant.update
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/tenant/security/review:
    get:
      operationId: getTenantSecurityReview
      tags:
      - Tenant Settings
      summary: List Review
      description: |-
        GET the per-member roster ("Review users" - who still needs MFA).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body.

        Implemented at `services/auth/src/routes/tenant-security.ts:169` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/tenant-security.ts:169
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - settings:workspace:update OR role ADMIN
      x-database:
      - tenant.findUnique
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/tool-permissions:
    get:
      operationId: getToolPermissions
      tags:
      - AI Studio
      summary: List Tool Permissions
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/tool-permissions.ts:77` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/tool-permissions.ts:77
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:tools:read OR role ADMIN
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/tool-permissions/{toolName}:
    put:
      operationId: putToolPermissionsToolName
      tags:
      - AI Studio
      summary: Replace Tool Permission
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/tool-permissions.ts:282` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/tool-permissions.ts:282
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:tools:read OR role ADMIN
        - ai:tools:manage OR role ADMIN
      parameters:
      - name: toolName
        in: path
        required: true
        schema:
          type: string
        description: "`toolName` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/tool-policy-migration:
    get:
      operationId: getToolPolicyMigration
      tags:
      - AI Studio
      summary: List Tool Policy Migration
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/tool-policy-migration.ts:157` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/tool-policy-migration.ts:157
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:tools:read OR role ADMIN
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/tool-policy-migration/apply:
    post:
      operationId: postToolPolicyMigrationApply
      tags:
      - AI Studio
      summary: Create Apply
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/tool-policy-migration.ts:168` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/tool-policy-migration.ts:168
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - ai:tools:read OR role ADMIN
        - ai:tools:manage OR role ADMIN
      x-database:
      - tenantToolPermission.updateMany
      - tenantTool.findFirst
      - tenantTool.updateMany
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/tools:
    get:
      operationId: getTools
      tags:
      - AI Studio
      summary: List Tools
      description: |-
        GET / - List all active TenantTools for the tenant (with catalog info)

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/tools.ts:9` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/tools.ts:9
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - tenantTool.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/tools/permissions/{departmentId}:
    get:
      operationId: getToolsPermissionsDepartmentId
      tags:
      - AI Studio
      summary: Get Permission
      description: |-
        GET /permissions/:departmentId - Get tool permissions for a department

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/tools.ts:31` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/tools.ts:31
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - department.findFirst
      - tenantTool.findMany
      parameters:
      - name: departmentId
        in: path
        required: true
        schema:
          type: string
        description: "`departmentId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putToolsPermissionsDepartmentId
      tags:
      - AI Studio
      summary: Replace Permission
      description: |-
        PUT /permissions/:departmentId - Batch update tool permissions for a department

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/ai/src/routes/tools.ts:63` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/tools.ts:63
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - department.findFirst
      - agentToolPermission.upsert
      parameters:
      - name: departmentId
        in: path
        required: true
        schema:
          type: string
        description: "`departmentId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/usage/daily:
    get:
      operationId: getUsageDaily
      tags:
      - Analytics
      summary: List Daily
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/usage.ts:37` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/usage.ts:37
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      parameters:
      - name: days
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/usage/logs:
    get:
      operationId: getUsageLogs
      tags:
      - Analytics
      summary: List Logs
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/usage.ts:69` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/usage.ts:69
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - usageLog.findMany
      - usageLog.count
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: offset
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: type
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/usage/stats:
    get:
      operationId: getUsageStats
      tags:
      - Analytics
      summary: List Stats
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/ai/src/routes/usage.ts:8` (ai service).
      security:
      - bearerAuth: []
      x-service: ai
      x-source: services/ai/src/routes/usage.ts:8
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - usageLog.groupBy
      parameters:
      - name: days
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/voice-channels:
    get:
      operationId: getVoiceChannels
      tags:
      - Voice
      summary: List Voice Channels
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/voice-channels.ts:325` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:325
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postVoiceChannels
      tags:
      - Voice
      summary: Create Voice Channels
      description: |-
        Create a BYO channel. The server uses the just-provided credentials to
        list `incomingPhoneNumbers` from Twilio and persists each as a child
        `VoiceChannelPhoneNumber` row (isActive=false). Admin activates a number
        in step 3 of the wizard via /numbers/:numberId/activate.
        apiKeySid / apiKeySecret / twimlAppSid are now auto-provisioned - legacy
        clients may still send them, but they are ignored.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-channels.ts:441` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:441
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
        entitlements:
        - voice.call_pilot
        planLimits:
        - limit:voice_channels
      x-database:
      - communicationChannel.create
      - voiceChannel.create
      - communicationChannel.update
      - communicationChannel.findUnique
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-channels/{id}:
    delete:
      operationId: deleteVoiceChannelsId
      tags:
      - Voice
      summary: Delete Voice Channel
      description: |-
        Also deactivates every active number for the channel and best-effort
        reverts `voiceUrl` on Twilio side to empty.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-channels.ts:1010` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:1010
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findUnique
      - communicationChannel.update
      - voiceChannelPhoneNumber.updateMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: getVoiceChannelsId
      tags:
      - Voice
      summary: Get Voice Channel
      description: |-
        Fetch a single channel with its numbers - used by the detail page.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/voice-channels.ts:540` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:540
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findUnique
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchVoiceChannelsId
      tags:
      - Voice
      summary: Update Voice Channel
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-channels.ts:558` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:558
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findUnique
      - communicationChannel.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-channels/{id}/ai-agent:
    get:
      operationId: getVoiceChannelsIdAiAgent
      tags:
      - Voice
      summary: Get Ai Agent
      description: |-
        Returns { aiAgentId } - the AI Employee bound to this voice channel.
        Phase 6 moved this off `copilot_config.aiAgentId` (JSONB) onto a real
        FK column. Empty body when nothing is configured; the live runner then
        falls back to the legacy per-channel copilot config (until Phase 7
        retires that path entirely).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/voice-channels.ts:672` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:672
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findUnique
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putVoiceChannelsIdAiAgent
      tags:
      - Voice
      summary: Replace Ai Agent
      description: |-
        Body: { aiAgentId: string | null }. Validates the agent belongs to the
        same tenant before writing - never trust the id from the client. Pass
        null/"" to detach the channel from any AI Employee (falls back to legacy
        channel config for the duration of Phase 6).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-channels.ts:695` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:695
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findUnique
      - aIAgent.findUnique
      - voiceChannel.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-channels/{id}/copilot-config:
    get:
      operationId: getVoiceChannelsIdCopilotConfig
      tags:
      - Voice
      summary: Get Copilot Config
      description: |-
        Returns the per-channel Live Call Copilot config (language, persona,
        goals, required questions, data collection fields). Empty `{}` means
        "use platform defaults".

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/voice-channels.ts:599` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:599
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findUnique
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putVoiceChannelsIdCopilotConfig
      tags:
      - Voice
      summary: Replace Copilot Config
      description: |-
        Replaces the per-channel copilot config. Validated against
        CopilotConfigSchema before persistence. Returns the persisted config.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-channels.ts:620` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:620
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findUnique
      - voiceChannel.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-channels/{id}/funnel:
    get:
      operationId: getVoiceChannelsIdFunnel
      tags:
      - Voice
      summary: Get Funnel
      description: |-
        Returns { funnelId } - the per-channel pipeline funnel override. Stored
        inside the copilot_config JSONB blob today (`copilot_config.funnelId`);
        Phase 7 will promote this to a real FK column. Null/empty means
        "fall back to the department-scoped funnel resolution".

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/voice-channels.ts:744` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:744
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findUnique
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putVoiceChannelsIdFunnel
      tags:
      - Voice
      summary: Replace Funnel
      description: |-
        Body: { funnelId: string | null }. Validates the funnel belongs to the
        same tenant before writing. Pass null/"" to clear the override so the
        channel falls back to the department-scoped funnel.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-channels.ts:770` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:770
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findUnique
      - tenantFunnel.findUnique
      - voiceChannel.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-channels/{id}/numbers:
    get:
      operationId: getVoiceChannelsIdNumbers
      tags:
      - Voice
      summary: Get Numbers
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/voice-channels.ts:1069` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:1069
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findUnique
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/voice-channels/{id}/numbers/{numberId}/activate:
    post:
      operationId: postVoiceChannelsIdNumbersNumberIdActivate
      tags:
      - Voice
      summary: Create Activate
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-channels.ts:1172` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:1172
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - voiceChannelPhoneNumber.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: numberId
        in: path
        required: true
        schema:
          type: string
        description: "`numberId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-channels/{id}/numbers/{numberId}/deactivate:
    post:
      operationId: postVoiceChannelsIdNumbersNumberIdDeactivate
      tags:
      - Voice
      summary: Create Deactivate
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-channels.ts:1208` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:1208
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - voiceChannelPhoneNumber.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: numberId
        in: path
        required: true
        schema:
          type: string
        description: "`numberId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-channels/{id}/numbers/refresh:
    post:
      operationId: postVoiceChannelsIdNumbersRefresh
      tags:
      - Voice
      summary: Create Refresh
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-channels.ts:1114` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:1114
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findUnique
      - voiceChannelPhoneNumber.findMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-channels/{id}/rotate-secrets:
    post:
      operationId: postVoiceChannelsIdRotateSecrets
      tags:
      - Voice
      summary: Create Rotate Secrets
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-channels.ts:1325` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:1325
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findUnique
      - communicationChannel.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-channels/{id}/routing:
    get:
      operationId: getVoiceChannelsIdRouting
      tags:
      - Voice
      summary: Get Routing
      description: |-
        Per-channel inbound routing: defaultAgentId, fallbackDepartmentId,
        ringTimeoutSeconds. Returns null IDs when nothing is configured.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/voice-channels.ts:827` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:827
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findUnique
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      operationId: putVoiceChannelsIdRouting
      tags:
      - Voice
      summary: Replace Routing
      description: |-
        Validates IDs belong to the tenant before persisting so a channel can
        never point at someone else's user / department.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-channels.ts:860` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:860
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findUnique
      - user.findUnique
      - department.findUnique
      - voiceChannel.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-channels/{id}/validate:
    post:
      operationId: postVoiceChannelsIdValidate
      tags:
      - Voice
      summary: Create Validate
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-channels.ts:1240` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-channels.ts:1240
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findUnique
      - communicationChannel.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-copilot/callbacks/click/{token}:
    get:
      operationId: getVoiceCopilotCallbacksClickToken
      tags:
      - Voice
      summary: Get Click
      description: |-
        Public endpoint reached by the customer tapping the URL button in
        the missed-call WhatsApp template. Verifies the signed token and
        initiates the AGENT_FIRST callback. Returns a tiny HTML page so
        the customer's browser shows a confirmation while we ring the
        agent server-side.

        No authentication middleware is applied to this route.

        Implemented at `services/voice-copilot/src/routes/voice-callback.ts:437` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/voice-callback.ts:437
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - communicationChannel.findUnique
      parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
        description: "`token` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/voice-copilot/callbacks/ensure-template:
    post:
      operationId: postVoiceCopilotCallbacksEnsureTemplate
      tags:
      - Voice
      summary: Create Ensure Template
      description: |-
        Make sure the tenant's WABA has the missed-call template approved.
        Idempotent: checks the existing list first via Graph API and only
        POSTs a create when the template name isn't already registered.
        Returns { exists, created } so the caller can show the admin
        whether anything new was submitted for review.

        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/voice-callback.ts:675` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/voice-callback.ts:675
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - channelAccount.findFirst
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-copilot/callbacks/initiate:
    post:
      operationId: postVoiceCopilotCallbacksInitiate
      tags:
      - Voice
      summary: Create Initiate
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/voice-callback.ts:112` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/voice-callback.ts:112
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - communicationChannel.findUnique
      - user.findFirst
      - conversation.create
      - |-
          voiceCallSession.update({
                  where: { id: session
      - voiceCallSession.update
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "503":
          description: Temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-copilot/callbacks/missed-template:
    post:
      operationId: postVoiceCopilotCallbacksMissedTemplate
      tags:
      - Voice
      summary: Create Missed Template
      description: |-
        Fires the WhatsApp "X tried to reach you" template to the customer
        of a MISSED voice session, with a URL button containing the
        pre-signed callback link. Idempotent per sessionId (records the
        outgoing message id under session.meta to prevent re-sends).

        No authentication middleware is applied to this route.

        This is a write operation.

        Side effects: queue: outgoingMessageQueue.add.

        Implemented at `services/voice-copilot/src/routes/voice-callback.ts:494` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/voice-callback.ts:494
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - voiceCallSession.findUnique
      - communicationChannel.findUnique
      - |-
          contact.findFirst({
                where: { tenantId: session
      - contact.findFirst
      - |-
          voiceCallSession.update({
                where: { id: session
      - voiceCallSession.update
      x-side-effects:
      - "queue: outgoingMessageQueue.add"
      x-async: Enqueues background work; the response returns before that work completes.
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-copilot/live:
    get:
      operationId: getVoiceCopilotLive
      tags:
      - Voice
      summary: List Live
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/voice-copilot/src/routes/live.ts:19` (voice-copilot service).
      security:
      - bearerAuth: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/live.ts:19
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        roles:
        - ADMIN
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/voice-copilot/sessions/{sessionId}/add-participant:
    post:
      operationId: postVoiceCopilotSessionsSessionIdAddParticipant
      tags:
      - Voice
      summary: Create Add Participant
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/voice-add-participant.ts:32` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/voice-add-participant.ts:32
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - voiceCallSession.findUnique
      - contact.findFirst
      - |-
          voiceSessionParticipant.update({
                  where: { id: participantId },
                  data: { status: "FAILED", leftAt: new Date(), endReason: "dial_failed" },
                })
      - voiceSessionParticipant.update
      parameters:
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
        description: "`sessionId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "503":
          description: Temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-copilot/sessions/{sessionId}/agent-leave:
    post:
      operationId: postVoiceCopilotSessionsSessionIdAgentLeave
      tags:
      - Voice
      summary: Create Agent Leave
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/voice-conference-control.ts:129` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/voice-conference-control.ts:129
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
        description: "`sessionId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "503":
          description: Temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-copilot/sessions/{sessionId}/customer-hold:
    post:
      operationId: postVoiceCopilotSessionsSessionIdCustomerHold
      tags:
      - Voice
      summary: Create Customer Hold
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/voice-conference-control.ts:71` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/voice-conference-control.ts:71
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
        description: "`sessionId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "503":
          description: Temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-copilot/sessions/{sessionId}/participants:
    get:
      operationId: getVoiceCopilotSessionsSessionIdParticipants
      tags:
      - Voice
      summary: Get Participants
      description: |-
        No authentication middleware is applied to this route.

        Implemented at `services/voice-copilot/src/routes/voice-participants.ts:76` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/voice-participants.ts:76
      x-operation-type: read
      x-publicly-exposed: true
      x-database:
      - voiceSessionParticipant.findMany
      parameters:
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
        description: "`sessionId` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/voice-copilot/sessions/{sessionId}/participants/{participantId}/hold:
    post:
      operationId: postVoiceCopilotSessionsSessionIdParticipantsParticipantIdHold
      tags:
      - Voice
      summary: Create Hold
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/voice-participants.ts:113` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/voice-participants.ts:113
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - |-
          voiceSessionParticipant.update({
                  where: { id: participantId },
                  data: { onHold: hold },
                })
      - voiceSessionParticipant.update
      parameters:
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
        description: "`sessionId` path segment."
      - name: participantId
        in: path
        required: true
        schema:
          type: string
        description: "`participantId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "503":
          description: Temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-copilot/sessions/{sessionId}/participants/{participantId}/kick:
    post:
      operationId: postVoiceCopilotSessionsSessionIdParticipantsParticipantIdKick
      tags:
      - Voice
      summary: Create Kick
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/voice-participants.ts:186` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/voice-participants.ts:186
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - voiceSessionParticipant.findUnique
      parameters:
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
        description: "`sessionId` path segment."
      - name: participantId
        in: path
        required: true
        schema:
          type: string
        description: "`participantId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "503":
          description: Temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-copilot/sessions/{sessionId}/terminate:
    post:
      operationId: postVoiceCopilotSessionsSessionIdTerminate
      tags:
      - Voice
      summary: Create Terminate
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/voice-terminate.ts:28` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/voice-terminate.ts:28
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: sessionId
        in: path
        required: true
        schema:
          type: string
        description: "`sessionId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-copilot/token:
    post:
      operationId: postVoiceCopilotToken
      tags:
      - Voice
      summary: Create Token
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/twilio-token.ts:23` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/twilio-token.ts:23
      x-operation-type: write
      x-publicly-exposed: true
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "503":
          description: Temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-copilot/twiml/callback-agent-status:
    post:
      operationId: postVoiceCopilotTwimlCallbackAgentStatus
      tags:
      - Voice
      summary: Create Callback Agent Status
      description: |-
        Status callback for the agent's mobile leg. Lets us mark the
        session MISSED/FAILED if the agent's phone never answered.

        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/voice-callback.ts:387` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/voice-callback.ts:387
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - voiceCallSession.update
      parameters:
      - name: tenantId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: sessionId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-copilot/twiml/callback-bridge:
    post:
      operationId: postVoiceCopilotTwimlCallbackBridge
      tags:
      - Voice
      summary: Create Callback Bridge
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/voice-callback.ts:342` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/voice-callback.ts:342
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: tenantId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: conferenceName
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-copilot/twiml/conference-status:
    post:
      operationId: postVoiceCopilotTwimlConferenceStatus
      tags:
      - Voice
      summary: Create Conference Status
      description: |-
        Tenant for this callback lives in the redis stash keyed by FriendlyName,
        so we resolve meta FIRST and only then build the provider + verify
        signature. (The signature middleware can't run upfront because Twilio
        doesn't echo our tenantId on conference callbacks.)

        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/twilio-twiml.ts:262` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/twilio-twiml.ts:262
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - voiceSessionParticipant.updateMany
      - voiceSessionParticipant.upsert
      - voiceSessionParticipant.count
      - voiceCallSession.findUnique
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "204":
          description: No content
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "503":
          description: Temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-copilot/twiml/customer-status:
    post:
      operationId: postVoiceCopilotTwimlCustomerStatus
      tags:
      - Voice
      summary: Create Customer Status
      description: |-
        Twilio POSTs here when the customer-side dial completes. The leg may
        never have joined the conference (no-answer/busy/canceled/failed) - in
        that case we transition the VoiceCallSession to MISSED/FAILED and hang
        up the agent leg so the agent isn't left alone in an empty conference.

        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/twilio-twiml.ts:597` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/twilio-twiml.ts:597
      x-operation-type: write
      x-publicly-exposed: true
      parameters:
      - name: friendlyName
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "204":
          description: No content
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "503":
          description: Temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-copilot/twiml/outbound:
    post:
      operationId: postVoiceCopilotTwimlOutbound
      tags:
      - Voice
      summary: Create Outbound
      description: |-
        Authenticated by provider signature, not by a user token.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/twilio-twiml.ts:110` (voice-copilot service).
      security:
      - providerSignature: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/twilio-twiml.ts:110
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - voiceCallSession.findUnique
      - voiceCallSession.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
      x-documentation-gap: request-body-unschematized
  /api/voice-copilot/twiml/outbound-status:
    post:
      operationId: postVoiceCopilotTwimlOutboundStatus
      tags:
      - Voice
      summary: Create Outbound Status
      description: |-
        TwiML-App-level StatusCallback registered during channel provisioning
        (see services/conversation/src/routes/voice-channels.ts: Applications.json
        StatusCallback URL). Twilio's payload here is the standard webhook shape
        (CallSid, CallStatus, ApplicationSid, AccountSid, From, To...) - there
        is no tenantId field, so the standard signature verifier can't run.
        The endpoint is logging-only; Twilio doesn't accept commands from the
        response, so an unverified 204 is safe and stops the 404 noise in logs.

        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/twilio-twiml.ts:737` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/twilio-twiml.ts:737
      x-operation-type: write
      x-publicly-exposed: true
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "204":
          description: No content
      x-documentation-gap: request-body-unschematized
  /api/voice-copilot/twiml/status:
    post:
      operationId: postVoiceCopilotTwimlStatus
      tags:
      - Voice
      summary: Create Status
      description: |-
        Authenticated by provider signature, not by a user token.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/twilio-twiml.ts:714` (voice-copilot service).
      security:
      - providerSignature: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/twilio-twiml.ts:714
      x-operation-type: write
      x-publicly-exposed: true
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "204":
          description: No content
      x-documentation-gap: request-body-unschematized
  /api/voice-sessions/{id}:
    get:
      operationId: getVoiceSessionsId
      tags:
      - Voice
      summary: Get Voice Session
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:364` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:364
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/voice-sessions/{id}/add-participant:
    post:
      operationId: postVoiceSessionsIdAddParticipant
      tags:
      - Voice
      summary: Create Add Participant
      description: |-
        Brings a third-party number into the live conference (3-way). Body:
        { to: "+972..." }. Only the assigned agent (or an ADMIN) can add; the
        session must be ACTIVE/HOLD. Proxies to voice-copilot which holds the
        Twilio credentials.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:610` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:610
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-sessions/{id}/agent-leave:
    post:
      operationId: postVoiceSessionsIdAgentLeave
      tags:
      - Voice
      summary: Create Agent Leave
      description: |-
        Cold transfer: drop the agent leg so the customer + the added 3rd
        party continue the call without the agent.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:699` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:699
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-sessions/{id}/answer:
    post:
      operationId: postVoiceSessionsIdAnswer
      tags:
      - Voice
      summary: Create Answer
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:501` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:501
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-sessions/{id}/context:
    get:
      operationId: getVoiceSessionsIdContext
      tags:
      - Voice
      summary: Get Context
      description: |-
        CRM enrichment for the right-panel cards. Returns prior conversations,
        any persisted summaries, and the contact record (if matched on phone).
        Heavy CRM lookups (lead_search/contact_search) are NOT triggered here -
        the AI service handles those at call-start via its existing prefetch.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:420` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:420
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      x-database:
      - contact.findUnique
      - callAnalysis.findUnique
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/voice-sessions/{id}/customer-hold:
    post:
      operationId: postVoiceSessionsIdCustomerHold
      tags:
      - Voice
      summary: Create Customer Hold
      description: |-
        Whisper/consult mode: toggle hold music on the customer's leg so the
        agent can talk privately with an added 3rd party. Body: { hold: bool }.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:664` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:664
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-sessions/{id}/decline:
    post:
      operationId: postVoiceSessionsIdDecline
      tags:
      - Voice
      summary: Create Decline
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:526` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:526
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "410":
          description: Gone - the resource expired or was superseded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-sessions/{id}/hangup:
    post:
      operationId: postVoiceSessionsIdHangup
      tags:
      - Voice
      summary: Create Hangup
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:559` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:559
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "410":
          description: Gone - the resource expired or was superseded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-sessions/{id}/participants:
    get:
      operationId: getVoiceSessionsIdParticipants
      tags:
      - Voice
      summary: Get Participants
      description: |-
        Live + already-left legs for the conference, with optional CRM
        enrichment (when the leg matched a Contact by phone at join time).
        Used by the voice workspace to render per-row controls (hold/kick).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:867` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:867
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      x-database:
      - voiceSessionParticipant.findMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/voice-sessions/{id}/participants/{participantId}/hold:
    post:
      operationId: postVoiceSessionsIdParticipantsParticipantIdHold
      tags:
      - Voice
      summary: Create Hold
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:900` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:900
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      x-database:
      - voiceSessionParticipant.findUnique
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: participantId
        in: path
        required: true
        schema:
          type: string
        description: "`participantId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-sessions/{id}/participants/{participantId}/kick:
    post:
      operationId: postVoiceSessionsIdParticipantsParticipantIdKick
      tags:
      - Voice
      summary: Create Kick
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:945` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:945
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      x-database:
      - voiceSessionParticipant.findUnique
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: participantId
        in: path
        required: true
        schema:
          type: string
        description: "`participantId` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-sessions/{id}/transcript:
    get:
      operationId: getVoiceSessionsIdTranscript
      tags:
      - Voice
      summary: Get Transcript
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:379` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:379
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      x-database:
      - message.findMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      - name: cursor
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: limit
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/voice-sessions/active:
    get:
      operationId: getVoiceSessionsActive
      tags:
      - Voice
      summary: List Active
      description: |-
        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:67` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:67
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      x-database:
      - voiceCallSession.findMany
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/voice-sessions/missed:
    get:
      operationId: getVoiceSessionsMissed
      tags:
      - Voice
      summary: List Missed
      description: |-
        Recent MISSED inbound calls for the agent's tenant. Enriched with the
        matching Contact row (if any) so the UI can render a name + tags
        without an extra round trip. Limited to inbound - outbound MISSED
        rows are a Twilio quirk (caller-decline) and don't belong in this
        inbox view.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:103` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:103
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      x-database:
      - voiceCallSession.findMany
      - contact.findMany
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/voice-sessions/missed/{id}/callback:
    post:
      operationId: postVoiceSessionsMissedIdCallback
      tags:
      - Voice
      summary: Create Callback
      description: |-
        Return-call entry point for the missed-calls inbox. Looks up the
        missed session, then proxies to voice-copilot's /callbacks/initiate
        endpoint with the channel's outboundMode policy applied. IN_PLATFORM
        mode is unsupported here (the agent would need to dial from the
        browser-side dialer - return a 409 so the UI can guide them).

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:153` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:153
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findUnique
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-sessions/missed/{id}/detail:
    get:
      operationId: getVoiceSessionsMissedIdDetail
      tags:
      - Voice
      summary: Get Detail
      description: |-
        Rich enrichment for the missed-call detail drawer: contact + CRM
        custom fields, the latest CustomerBrief (persistent behavioral
        summary), and the 3 most-recent prior conversations with their
        aiSummary. Read-only; missed-call list is cheap, but this endpoint
        pulls heavier data only when the agent actually opens the drawer.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:253` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:253
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      x-database:
      - contact.findFirst
      - customerBrief.findFirst
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/voice-sessions/missed/{id}/handle:
    post:
      operationId: postVoiceSessionsMissedIdHandle
      tags:
      - Voice
      summary: Create Handle
      description: |-
        Explicit "Mark as handled" from the inbox UI. Stamps `handledAt` on
        every MISSED inbound session for the same (tenant, customerNumber)
        - same cascade semantics as the auto-handle on ACTIVE. Idempotent.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:219` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:219
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      x-database:
      - voiceCallSession.updateMany
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-sessions/presence/heartbeat:
    post:
      operationId: postVoiceSessionsPresenceHeartbeat
      tags:
      - Voice
      summary: Create Heartbeat
      description: |-
        Agent client pings every 30s to bump lastSeenAt. Tenants where
        voiceCopilotEnabled = false already 404 via the gate above.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:988` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:988
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        gates:
        - voiceCopilotGate
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice-sessions/start-outbound:
    post:
      operationId: postVoiceSessionsStartOutbound
      tags:
      - Voice
      summary: Create Start Outbound
      description: |-
        Outbound-dial gate. The frontend always calls this BEFORE opening
        the browser dialer; the response decides which path to take based
        on the channel's `outboundMode`:

          IN_PLATFORM  → frontend dials customer via the Twilio browser SDK
                         (current behavior unchanged).
          AGENT_FIRST  → server REST-dials the calling agent's mobile via
                         voice-copilot `/callbacks/initiate`; once they
                         answer, the customer is bridged in. Frontend skips
                         device.connect() and just navigates to /voice/<id>.

        Returns 409 with hint codes when AGENT_FIRST is configured but
        can't run (agent missing phone, etc.) so the UI can show a real
        error instead of silently falling back.
        Permission, not Role==ADMIN: `outbound:calls:create` is held by the
        built-in admin/department-manager/agent roles (catalog), and custom roles
        can grant or withhold it. The ADMIN role fallback covers tenants whose
        role rows predate the key. The identity → Active Membership → tenant chain
        is enforced by authenticate + resolveTenant + requireActiveTenant above.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/conversation/src/routes/voice-sessions.ts:749` (conversation service).
      security:
      - bearerAuth: []
      x-service: conversation
      x-source: services/conversation/src/routes/voice-sessions.ts:749
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        permissionOrRole:
        - outbound:calls:create OR role ADMIN
        gates:
        - voiceCopilotGate
      x-database:
      - communicationChannel.findFirst
      - conversation.findFirst
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "502":
          description: Upstream provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice/incoming/forward-complete:
    post:
      operationId: postVoiceIncomingForwardComplete
      tags:
      - Voice
      summary: Create Forward Complete
      description: |-
        Action callback on the <Dial> emitted by FORWARD_TO_AGENT mode.
        Twilio posts DialCallStatus once the forwarded leg finishes
        (answered, busy, no-answer, failed, canceled). We use this to
        transition the session FSM - without it the session would stay
        RINGING because the conference-status events never fire.

        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/voice-incoming.ts:798` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/voice-incoming.ts:798
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - voiceCallSession.findUnique
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice/incoming/recording:
    post:
      operationId: postVoiceIncomingRecording
      tags:
      - Voice
      summary: Create Recording
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/voice-incoming.ts:870` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/voice-incoming.ts:870
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - voiceCallSession.updateMany
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "503":
          description: Temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice/incoming/status:
    post:
      operationId: postVoiceIncomingStatus
      tags:
      - Voice
      summary: Create Status
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/voice-incoming.ts:671` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/voice-incoming.ts:671
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - voiceCallSession.findUnique
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "422":
          description: Semantically invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "503":
          description: Temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/voice/incoming/voice:
    post:
      operationId: postVoiceIncomingVoice
      tags:
      - Voice
      summary: Create Voice
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/voice-copilot/src/routes/voice-incoming.ts:187` (voice-copilot service).
      security: []
      x-service: voice-copilot
      x-source: services/voice-copilot/src/routes/voice-incoming.ts:187
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - |-
          voiceCallSession.findFirst({
                      where: {
                        tenantId,
                        direction: "inbound",
                        state: "MISSED",
                        startedAt: { gte: cutoff },
                        OR: [
                          { assignedAgentId: matchingAgent
      - voiceCallSession.findFirst
      - conversation.create
      - voiceCallSession.upsert
      - channelAccount.findFirst
      - voiceCallSession.create
      - |-
          conversation.upsert({
                  where: {
                    id: `voice-inbound-placeholder-${callSid}`,
                  },
                  update: {},
                  create: {
                    id: `voice-inbound-${callSid}`,
                    tenantId,
                    channel: "VOICE",
                    channelAccountId: channelAccount?
      - conversation.upsert
      - voiceCallSession.findUnique
      - voiceCallSession.update
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "503":
          description: Temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/waitlist:
    post:
      operationId: postWaitlist
      tags:
      - Onboarding
      summary: Create Waitlist
      description: |-
        No authentication middleware is applied to this route.

        This is a write operation.

        Implemented at `services/auth/src/routes/waitlist.ts:36` (auth service).
      security: []
      x-service: auth
      x-source: services/auth/src/routes/waitlist.ts:36
      x-operation-type: write
      x-publicly-exposed: true
      x-database:
      - waitlistEntry.findUnique
      - waitlistEntry.findFirst
      - waitlistEntry.create
      - waitlistEntry.count
      requestBody:
        required: true
        description: Validated by `waitlistSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                firstName:
                  type: string
                  minLength: 1
                  maxLength: 100
                email:
                  type: string
                  format: email
                  maxLength: 255
                phone:
                  type: string
                  maxLength: 30
                  default: ""
                company:
                  type: string
                  maxLength: 200
                  default: ""
                companyDomain:
                  type: string
                  maxLength: 200
                  default: ""
                role:
                  type: string
                  maxLength: 100
                  default: ""
                companySize:
                  type: string
                  maxLength: 50
                  default: ""
                frustration:
                  type: string
                  maxLength: 500
                  default: ""
                source:
                  type: string
                  maxLength: 100
              required:
              - firstName
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict with current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/waitlist/leads:
    get:
      operationId: getWaitlistLeads
      tags:
      - Onboarding
      summary: List Leads
      description: |-
        ═══════════════════════════════════════════════════════════════
        Admin endpoints (SYSTEM_ADMIN only)
        ═══════════════════════════════════════════════════════════════

        Requires a valid Authentik access token.

        Implemented at `services/auth/src/routes/waitlist.ts:118` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/waitlist.ts:118
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      x-database:
      - waitlistEntry.findMany
      - waitlistEntry.count
      parameters:
      - name: search
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: sortBy
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: sortOrder
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: page
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: limit
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/waitlist/leads/{id}:
    delete:
      operationId: deleteWaitlistLeadsId
      tags:
      - Onboarding
      summary: Delete Lead
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/auth/src/routes/waitlist.ts:315` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/waitlist.ts:315
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      x-database:
      - waitlistEntry.findUnique
      - waitlistEntry.delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      operationId: getWaitlistLeadsId
      tags:
      - Onboarding
      summary: Get Lead
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/auth/src/routes/waitlist.ts:299` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/waitlist.ts:299
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      x-database:
      - waitlistEntry.findUnique
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      operationId: patchWaitlistLeadsId
      tags:
      - Onboarding
      summary: Update Lead
      description: |-
        Requires a valid Authentik access token.

        This is a write operation.

        Implemented at `services/auth/src/routes/waitlist.ts:267` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/waitlist.ts:267
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      x-database:
      - waitlistEntry.findUnique
      - waitlistEntry.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: true
        description: Validated by `updateLeadSchema` (Zod). Unlisted properties are stripped before the handler runs.
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum:
                  - NEW
                  - CONTACTED
                  - APPROVED
                  - REJECTED
                notes:
                  type: string
                  maxLength: 2000
                leadScore:
                  type: integer
                  minimum: 0
                  maximum: 100
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/waitlist/leads/export:
    get:
      operationId: getWaitlistLeadsExport
      tags:
      - Onboarding
      summary: List Export
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/auth/src/routes/waitlist.ts:210` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/waitlist.ts:210
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      x-database:
      - waitlistEntry.findMany
      parameters:
      - name: status
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/waitlist/leads/stats:
    get:
      operationId: getWaitlistLeadsStats
      tags:
      - Onboarding
      summary: List Stats
      description: |-
        Requires a valid Authentik access token.

        Implemented at `services/auth/src/routes/waitlist.ts:176` (auth service).
      security:
      - bearerAuth: []
      x-service: auth
      x-source: services/auth/src/routes/waitlist.ts:176
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        platformAdmin:
        - requireSystemAdmin
      x-database:
      - waitlistEntry.count
      - waitlistEntry.groupBy
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/webhook:
    get:
      operationId: getWebhook
      tags:
      - Webhooks
      summary: List Webhook
      description: |-
        Webhook verification (GET) - shared by WhatsApp and Messenger

        No authentication middleware is applied to this route. Deliberately spans tenants.

        Implemented at `services/webhook/src/routes/webhook.ts:94` (webhook service).
      security: []
      x-service: webhook
      x-source: services/webhook/src/routes/webhook.ts:94
      x-operation-type: read
      x-publicly-exposed: true
      x-authorization:
        crossTenant: true
      parameters:
      - name: hub.mode
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: hub.verify_token
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      - name: hub.challenge
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postWebhook
      tags:
      - Webhooks
      summary: Create Webhook
      description: |-
        Unified webhook handler (POST) - detects platform via adapter pattern

        No authentication middleware is applied to this route. Deliberately spans tenants.

        This is a write operation.

        Side effects: queue: incomingMessageQueue.add.

        Implemented at `services/webhook/src/routes/webhook.ts:119` (webhook service).
      security: []
      x-service: webhook
      x-source: services/webhook/src/routes/webhook.ts:119
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        crossTenant: true
      x-database:
      - channelAccount.findMany
      - messageTemplate.updateMany
      - channelAccount.findFirst
      x-side-effects:
      - "queue: incomingMessageQueue.add"
      x-async: Enqueues background work; the response returns before that work completes.
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/webhook-triggers:
    get:
      operationId: getWebhookTriggers
      tags:
      - Webhooks
      summary: List Webhook Triggers
      description: |-
        GET /api/webhook-triggers?workflowId=...
        Returns the trigger for a workflow (tenant-scoped), or { data: null } if the
        workflow has no trigger yet.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        Implemented at `services/webhook/src/routes/trigger-admin.ts:111` (webhook service).
      security:
      - bearerAuth: []
      x-service: webhook
      x-source: services/webhook/src/routes/trigger-admin.ts:111
      x-operation-type: read
      x-publicly-exposed: false
      x-database:
      - webhookTrigger.findFirst
      parameters:
      - name: workflowId
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      operationId: postWebhookTriggers
      tags:
      - Webhooks
      summary: Create Webhook Triggers
      description: |-
        POST /api/webhook-triggers  { workflowId }
        Idempotently provisions a trigger (token + secret) for a workflow the tenant
        owns. Returns the existing trigger if one already exists.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/webhook/src/routes/trigger-admin.ts:132` (webhook service).
      security:
      - bearerAuth: []
      x-service: webhook
      x-source: services/webhook/src/routes/trigger-admin.ts:132
      x-operation-type: write
      x-publicly-exposed: false
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - chatbotFlow.findFirst
      - webhookTrigger.findFirst
      - webhookTrigger.update
      - webhookTrigger.create
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/webhook-triggers/{id}:
    patch:
      operationId: patchWebhookTriggersId
      tags:
      - Webhooks
      summary: Update Webhook Trigger
      description: |-
        PATCH /api/webhook-triggers/:id  { enabled?, targetMode?, bodySchema? }
        Update the trigger's enabled flag, its target mode ("flow" | "connected"),
        and/or its declared body schema ([{ key, type }]). At least one field is
        required. A disabled trigger answers inbound POSTs with 403 (see
        routes/triggers.ts). bodySchema is declaration only - it is not enforced
        against inbound payloads.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/webhook/src/routes/trigger-admin.ts:243` (webhook service).
      security:
      - bearerAuth: []
      x-service: webhook
      x-source: services/webhook/src/routes/trigger-admin.ts:243
      x-operation-type: write
      x-publicly-exposed: false
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - webhookTrigger.findFirst
      - webhookTrigger.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "400":
          description: Validation failed or the request was malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/webhook-triggers/{id}/regenerate-secret:
    post:
      operationId: postWebhookTriggersIdRegenerateSecret
      tags:
      - Webhooks
      summary: Create Regenerate Secret
      description: |-
        POST /api/webhook-triggers/:id/regenerate-secret
        Rotates the shared secret. The token (and therefore the URL) is unchanged.

        Requires a valid Authentik access token. Tenant is resolved from the token, never from the request body. Tenant status must be ACTIVE.

        This is a write operation.

        Implemented at `services/webhook/src/routes/trigger-admin.ts:211` (webhook service).
      security:
      - bearerAuth: []
      x-service: webhook
      x-source: services/webhook/src/routes/trigger-admin.ts:211
      x-operation-type: write
      x-publicly-exposed: false
      x-authorization:
        roles:
        - ADMIN
      x-database:
      - webhookTrigger.findFirst
      - webhookTrigger.update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: "`id` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/webhook/email:
    post:
      operationId: postWebhookEmail
      tags:
      - Webhooks
      summary: Create Email
      description: |-
        No authentication middleware is applied to this route. Deliberately spans tenants.

        This is a write operation.

        Side effects: queue: incomingMessageQueue.add.

        Implemented at `services/webhook/src/routes/webhook.ts:651` (webhook service).
      security: []
      x-service: webhook
      x-source: services/webhook/src/routes/webhook.ts:651
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        crossTenant: true
      x-database:
      - channelAccount.findFirst
      x-side-effects:
      - "queue: incomingMessageQueue.add"
      x-async: Enqueues background work; the response returns before that work completes.
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/webhook/gmail:
    post:
      operationId: postWebhookGmail
      tags:
      - Webhooks
      summary: Create Gmail
      description: |-
        No authentication middleware is applied to this route. Deliberately spans tenants.

        This is a write operation.

        Side effects: queue: incomingMessageQueue.add.

        Implemented at `services/webhook/src/routes/webhook.ts:729` (webhook service).
      security: []
      x-service: webhook
      x-source: services/webhook/src/routes/webhook.ts:729
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        crossTenant: true
      x-database:
      - channelAccount.findFirst
      - channelAccount.update
      x-side-effects:
      - "queue: incomingMessageQueue.add"
      x-async: Enqueues background work; the response returns before that work completes.
      parameters:
      - name: token
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
          x-inferred: Status inferred from Express defaults; the handler calls no explicit res.status() for success.
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/webhook/outlook:
    post:
      operationId: postWebhookOutlook
      tags:
      - Webhooks
      summary: Create Outlook
      description: |-
        No authentication middleware is applied to this route. Deliberately spans tenants.

        This is a write operation.

        Side effects: queue: incomingMessageQueue.add.

        Implemented at `services/webhook/src/routes/webhook.ts:860` (webhook service).
      security: []
      x-service: webhook
      x-source: services/webhook/src/routes/webhook.ts:860
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        crossTenant: true
      x-database:
      - channelAccount.findMany
      x-side-effects:
      - "queue: incomingMessageQueue.add"
      x-async: Enqueues background work; the response returns before that work completes.
      parameters:
      - name: validationToken
        in: query
        required: false
        schema:
          type: string
        description: Read from `req.query` by the handler; type is not declared in source.
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /api/webhook/slack:
    post:
      operationId: postWebhookSlack
      tags:
      - Webhooks
      summary: Create Slack
      description: |-
        No authentication middleware is applied to this route. Deliberately spans tenants.

        This is a write operation.

        Side effects: queue: incomingMessageQueue.add.

        Implemented at `services/webhook/src/routes/webhook.ts:949` (webhook service).
      security: []
      x-service: webhook
      x-source: services/webhook/src/routes/webhook.ts:949
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        crossTenant: true
      x-database:
      - channelAccount.findFirst
      x-side-effects:
      - "queue: incomingMessageQueue.add"
      x-async: Enqueues background work; the response returns before that work completes.
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
  /webhooks/{token}:
    post:
      operationId: postWebhooksToken
      tags:
      - Webhooks
      summary: Token action
      description: |-
        POST /webhooks/:token

        Generic inbound webhook entrypoint that fronts the WebhookTrigger model.
        Authenticates the call and dispatches an event to incoming-worker over the
        existing BullMQ "incoming-messages" transport. Running the flow / variable
        injection happens downstream (ticket 3) - this route only authenticates and
        enqueues, forwarding the raw JSON body intact.

         - unknown token            → 404
         - missing / bad secret     → 401
         - trigger disabled         → 403
         - authenticated + enabled  → 200 (event enqueued)

        No authentication middleware is applied to this route. Deliberately spans tenants.

        This is a write operation.

        Side effects: queue: incomingMessageQueue.add.

        Implemented at `services/webhook/src/routes/triggers.ts:45` (webhook service).
      security: []
      x-service: webhook
      x-source: services/webhook/src/routes/triggers.ts:45
      x-operation-type: write
      x-publicly-exposed: true
      x-authorization:
        crossTenant: true
      x-database:
      - webhookTrigger.findUnique
      x-side-effects:
      - "queue: incomingMessageQueue.add"
      x-async: Enqueues background work; the response returns before that work completes.
      parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
        description: "`token` path segment."
      requestBody:
        required: false
        description: No Zod schema is declared for this route; the handler reads `req.body` directly. Shape is not verifiable from source.
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeUnknown"
        "401":
          description: Missing, invalid or expired access token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Authenticated, but not permitted to perform this operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found, or not visible to this tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Unexpected server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      x-documentation-gap: request-body-unschematized
