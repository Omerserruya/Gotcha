# GOTCHA API Execution Map

What happens inside the platform after an HTTP call arrives: the middleware that runs before the handler, the services it calls, the database rows it reads and writes, the providers it talks to, and the background work that outlives the response.

## How to read this, and how far to trust it

This file is **generated from the TypeScript AST**, not written by hand - see `scripts/api-docs/extract-routes.ts` and `scripts/api-docs/generate-execution-map.ts`. Regenerate with `npm run api:docs`. That matters because a hand-maintained map of 780 endpoints is stale within a sprint, and a stale map of *authorization* is actively dangerous.

| Claim | How it is established | Trust |
|---|---|---|
| Method, path, handler location | Read from the router AST | Exact |
| Authentication and authorization | Resolved middleware chain, including router-level, path-scoped and spread (`...ADMIN_ONLY`) middleware | Exact |
| Input validation | The Zod schema passed to `validate(...)` | Exact where a schema exists |
| Database operations | `prisma.<model>.<op>` calls in the handler body | Exact for direct calls; misses work done inside services |
| Service calls | Call expressions matching a service module | Exact for direct calls |
| Queues, events, outbound HTTP | Detected call patterns in the handler body | Exact for direct calls |
| Execution *order* | Source order of the AST | Indicative, not guaranteed - branches and early returns can skip steps |
| Response body shape | Not declared anywhere in this codebase | **Unknown - read the handler** |

The single most important caveat: step 7 lists Prisma calls made **in the handler itself**. When a handler delegates to a service, that service's own database work is not expanded here. The service is named in step 6 so it can be followed.

## Cross-cutting behaviour

- **Tenancy.** `resolveTenant` sets `req.tenantId` from the verified principal. No endpoint takes a tenant id from the caller. Endpoints that deliberately cross tenants are called out individually.
- **Authentication.** GOTCHA implements none of it. Authentik issues tokens; `authenticate()` verifies them against JWKS and maps `sub` → `User.authentikSubject`. It fails closed.
- **Validation.** `validate(schema)` replaces `req.body` with the parsed object. Properties the schema does not name are stripped before the handler sees them.
- **Async work.** Anything enqueued to BullMQ returns to the caller immediately; retries, backoff and failure handling belong to the worker, not the request.
- **Response envelope.** `{ data: ... }` on success, `{ error: ... }` on failure.

## Coverage

| Domain | Endpoints | Read | Write | Unauthenticated |
|---|---:|---:|---:|---:|
| [AI Studio](#ai-studio) | 161 | 63 | 98 | 0 |
| [Analytics](#analytics) | 21 | 18 | 3 | 0 |
| [Auth & Identity](#auth-identity) | 36 | 17 | 19 | 5 |
| [Billing](#billing) | 88 | 36 | 52 | 7 |
| [Channels](#channels) | 16 | 8 | 8 | 2 |
| [Contacts & CRM](#contacts-crm) | 19 | 4 | 15 | 0 |
| [Conversations](#conversations) | 49 | 16 | 33 | 0 |
| [Embedded Chat](#embedded-chat) | 4 | 1 | 3 | 4 |
| [GDPR & Compliance](#gdpr-compliance) | 14 | 6 | 8 | 0 |
| [Historical Import](#historical-import) | 14 | 9 | 5 | 0 |
| [Integrations](#integrations) | 71 | 48 | 23 | 13 |
| [Internal](#internal) | 31 | 6 | 25 | 0 |
| [Knowledge](#knowledge) | 26 | 11 | 15 | 2 |
| [Notifications](#notifications) | 8 | 3 | 5 | 0 |
| [Onboarding](#onboarding) | 69 | 28 | 41 | 1 |
| [Platform Admin](#platform-admin) | 37 | 16 | 21 | 1 |
| [Shopify](#shopify) | 20 | 8 | 12 | 10 |
| [Tenant Settings](#tenant-settings) | 28 | 11 | 17 | 0 |
| [Voice](#voice) | 63 | 17 | 46 | 21 |
| [Webhooks](#webhooks) | 11 | 2 | 9 | 7 |
| [WhatsApp](#whatsapp) | 11 | 3 | 8 | 0 |
| **Total** | **797** | **331** | **466** | **73** |

## AI Studio

161 endpoints.

### `GET /api/action-contracts`

- **Purpose:** Lists action contracts. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/action-contracts-admin.ts` line 26
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/action-contracts` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/action-contracts-admin.ts`.
2. **Handler.** `services/ai/src/routes/action-contracts-admin.ts:26` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/action-contracts
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/action-contracts-admin.ts:26
      → (responds directly; no downstream calls detected)
```

### `POST /api/action-contracts`

- **Purpose:** Creates an action contract. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/action-contracts-admin.ts` line 45
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/action-contracts` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/action-contracts-admin.ts`.
2. **Handler.** `services/ai/src/routes/action-contracts-admin.ts:45` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/action-contracts
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/action-contracts-admin.ts:45
      → (responds directly; no downstream calls detected)
```

### `DELETE /api/action-contracts/:id`

- **Purpose:** Deletes the referenced action contract. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/action-contracts-admin.ts` line 139
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/action-contracts` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/action-contracts-admin.ts`.
2. **Handler.** `services/ai/src/routes/action-contracts-admin.ts:139` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/action-contracts/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/action-contracts-admin.ts:139
      → (responds directly; no downstream calls detected)
```

### `GET /api/action-contracts/:id`

- **Purpose:** Reads a single action contract. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/action-contracts-admin.ts` line 34
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/action-contracts` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/action-contracts-admin.ts`.
2. **Handler.** `services/ai/src/routes/action-contracts-admin.ts:34` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/action-contracts/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/action-contracts-admin.ts:34
      → (responds directly; no downstream calls detected)
```

### `PATCH /api/action-contracts/:id`

- **Purpose:** Updates the referenced action contract. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/action-contracts-admin.ts` line 105
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/action-contracts` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/action-contracts-admin.ts`.
2. **Handler.** `services/ai/src/routes/action-contracts-admin.ts:105` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/action-contracts/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/action-contracts-admin.ts:105
      → (responds directly; no downstream calls detected)
```

### `GET /api/action-planner/approvals`

- **Purpose:** F4.2 - Approval queue: list pending high-risk actions that were blocked
- **Handler:** `services/ai/src/routes/action-planner.ts` line 249
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/action-planner` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/action-planner.ts`.
2. **Handler.** `services/ai/src/routes/action-planner.ts:249` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `auditLog.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/action-planner/approvals
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/action-planner.ts:249
      → db read   prisma.auditLog.findMany
```

### `POST /api/action-planner/classify`

- **Purpose:** POST /classify - decide chat vs execution mode for a prompt.
- **Handler:** `services/ai/src/routes/action-planner.ts` line 126
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/action-planner` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/action-planner.ts`.
2. **Handler.** `services/ai/src/routes/action-planner.ts:126` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/action-planner/classify
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/action-planner.ts:126
      → (responds directly; no downstream calls detected)
```

### `POST /api/action-planner/execute`

- **Purpose:** POST /execute - run a previously planned ExecutionPlan (F3 Action Engine)
- **Handler:** `services/ai/src/routes/action-planner.ts` line 273
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/action-planner` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/action-planner.ts`.
2. **Handler.** `services/ai/src/routes/action-planner.ts:273` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `auditLog.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/action-planner/execute
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/action-planner.ts:273
      → db read   prisma.auditLog.findFirst
```

### `POST /api/action-planner/plan`

- **Purpose:** POST /plan - produce an ExecutionPlan for a prompt (dry-run by default)
- **Handler:** `services/ai/src/routes/action-planner.ts` line 79
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/action-planner` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/action-planner.ts`.
2. **Handler.** `services/ai/src/routes/action-planner.ts:79` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/action-planner/plan
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/action-planner.ts:79
      → (responds directly; no downstream calls detected)
```

### `POST /api/action-planner/simulate`

- **Purpose:** POST /simulate - dual-mode. Classifies first: chat-mode returns a natural answer with no plan; execution-mode plans + dry-run executes as before.
- **Handler:** `services/ai/src/routes/action-planner.ts` line 177
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/action-planner` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/action-planner.ts`.
2. **Handler.** `services/ai/src/routes/action-planner.ts:177` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/action-planner/simulate
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/action-planner.ts:177
      → (responds directly; no downstream calls detected)
```

### `POST /api/agent-loop/execute-approved`

- **Purpose:** Creates an execute approved. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/agent-loop-resume.ts` line 29
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/agent-loop` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/agent-loop-resume.ts`.
2. **Handler.** `services/ai/src/routes/agent-loop-resume.ts:29` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `422`, `500`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/agent-loop/execute-approved
  → [internalAuth]
  → handler  services/ai/src/routes/agent-loop-resume.ts:29
      → (responds directly; no downstream calls detected)
```

### `GET /api/agent-scores`

- **Purpose:** GET / - get scores for all agents (admin)
- **Handler:** `services/ai/src/routes/agent-scores.ts` line 9
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/agent-scores` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/agent-scores.ts`.
2. **Handler.** `services/ai/src/routes/agent-scores.ts:9` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `agentScore.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/agent-scores
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/agent-scores.ts:9
      → db read   prisma.agentScore.findMany
```

### `GET /api/agent-scores/:agentId`

- **Purpose:** GET /:agentId - get overview for specific agent
- **Handler:** `services/ai/src/routes/agent-scores.ts` line 31
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/agent-scores` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/agent-scores.ts`.
2. **Handler.** `services/ai/src/routes/agent-scores.ts:31` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/agent-scores/:agentId
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/agent-scores.ts:31
      → db read   prisma.user.findFirst
```

### `GET /api/agent-scores/:agentId/history`

- **Purpose:** GET /:agentId/history - get score history
- **Handler:** `services/ai/src/routes/agent-scores.ts` line 60
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/agent-scores` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/agent-scores.ts`.
2. **Handler.** `services/ai/src/routes/agent-scores.ts:60` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findFirst`
   - `agentScore.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/agent-scores/:agentId/history
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/agent-scores.ts:60
      → db read   prisma.user.findFirst
      → db read   prisma.agentScore.findMany
```

### `POST /api/agent/clear`

- **Purpose:** Creates a clear. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/agent.ts` line 113
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/agent` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/agent.ts`.
2. **Handler.** `services/ai/src/routes/agent.ts:113` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `401`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/agent/clear
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/agent.ts:113
      → (responds directly; no downstream calls detected)
```

### `POST /api/agent/run`

- **Purpose:** Creates a run. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/agent.ts` line 27
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/agent` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/agent.ts`.
2. **Handler.** `services/ai/src/routes/agent.ts:27` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique({
    where: { id: tenantId },
    select: { name: true },
  })`
   - `tenant.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `401`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/agent/run
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/agent.ts:27
      → db read   prisma.tenant.findUnique({
    where: { id: tenantId },
    select: { name: true },
  })
      → db read   prisma.tenant.findUnique
```

### `GET /api/agents`

- **Purpose:** Lists agents. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 29
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:29` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/agents
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/agents.ts:29
      → db read   prisma.user.findMany
```

### `POST /api/agents`

- **Purpose:** Seat limit. Counts ACTIVE members only, so deactivating someone frees their seat rather than permanently consuming it.
- **Handler:** `services/auth/src/routes/agents.ts` line 109
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** `settings:members:manage` **or** role `ADMIN`; Plan limit: `limit:users`
- **Input validation:** `validate(inviteAgentSchema)` (Zod). Accepts: `email`, `name`, `departmentIds`. Required: `email`, `name`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:109` - inline `async (req, res)` handler.
3. **Validation.** `validate(inviteAgentSchema)` (Zod). Accepts: `email`, `name`, `departmentIds`. Required: `email`, `name`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** `settings:members:manage` **or** role `ADMIN`; Plan limit: `limit:users`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findMany`
   _Writes:_
   - `departmentMember.createMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/agents
  → [authenticate → resolveTenant → validate(inviteAgentSchema)]
  → handler  services/auth/src/routes/agents.ts:109
      → db read   prisma.department.findMany
      → db write  prisma.departmentMember.createMany
```

### `DELETE /api/agents/:id`

- **Purpose:** Delete agent (admin only)
- **Handler:** `services/auth/src/routes/agents.ts` line 236
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** `settings:members:manage` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:236` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** `settings:members:manage` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findFirst`
   _Writes:_
   - `conversation.updateMany`
   - `user.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/agents/:id
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/agents.ts:236
      → db read   prisma.user.findFirst
      → db write  prisma.conversation.updateMany
      → db write  prisma.user.delete
```

### `PATCH /api/agents/:id`

- **Purpose:** Updates the referenced agent. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 166
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** `settings:members:manage` **or** role `ADMIN`
- **Input validation:** `validate(updateAgentSchema)` (Zod). Accepts: `name`, `isActive`, `phoneNumber`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:166` - inline `async (req, res)` handler.
3. **Validation.** `validate(updateAgentSchema)` (Zod). Accepts: `name`, `isActive`, `phoneNumber`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** `settings:members:manage` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findFirst`
   _Writes:_
   - `user.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/agents/:id
  → [authenticate → resolveTenant → validate(updateAgentSchema)]
  → handler  services/auth/src/routes/agents.ts:166
      → db read   prisma.user.findFirst
      → db write  prisma.user.update
```

### `POST /api/agents/:id/reset-password`

- **Purpose:** Re-issue an agent's password-setup link (admin only).
- **Handler:** `services/auth/src/routes/agents.ts` line 216
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** `settings:members:manage` **or** role `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:216` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** `settings:members:manage` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/agents/:id/reset-password
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/agents.ts:216
      → db read   prisma.user.findFirst
```

### `GET /api/agents/login-status`

- **Purpose:** Login/invitation status for the tenant's members (admin only, lazy-loaded by the Users page so the main list stays fast). For each user we ask Authentik for last_login in one call and derive a status:   disabled       - GOTCHA isActive=false   invited        - active, never signed in (invite pending / link unused)   active         - active, has signed in (returns lastLogin)
- **Handler:** `services/auth/src/routes/agents.ts` line 73
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** `settings:members:manage` **or** role `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:73` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** `settings:members:manage` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/agents/login-status
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/agents.ts:73
      → db read   prisma.user.findMany
```

### `GET /api/agents/me/locale`

- **Purpose:** Lists locale. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 753
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:753` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/agents/me/locale
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/agents.ts:753
      → (responds directly; no downstream calls detected)
```

### `PUT /api/agents/me/locale`

- **Purpose:** Updates the referenced locale. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 773
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** `validate(localeSchema)` (Zod). Accepts: `locale`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:773` - inline `async (req, res)` handler.
3. **Validation.** `validate(localeSchema)` (Zod). Accepts: `locale`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `user.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/agents/me/locale
  → [authenticate → resolveTenant → validate(localeSchema)]
  → handler  services/auth/src/routes/agents.ts:773
      → db write  prisma.user.update
```

### `GET /api/agents/settings/approval-recipient`

- **Purpose:** Lists approval recipient. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 461
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:461` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/agents/settings/approval-recipient
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/agents.ts:461
      → (responds directly; no downstream calls detected)
```

### `PUT /api/agents/settings/approval-recipient`

- **Purpose:** Updates the referenced approval recipient. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 474
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(approvalRecipientSchema)` (Zod). Accepts: `userId`, `phone`, `enabled`, `maxRiskLevel`. Required: `userId`, `phone`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:474` - inline `async (req, res)` handler.
3. **Validation.** `validate(approvalRecipientSchema)` (Zod). Accepts: `userId`, `phone`, `enabled`, `maxRiskLevel`. Required: `userId`, `phone`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/agents/settings/approval-recipient
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(approvalRecipientSchema)]
  → handler  services/auth/src/routes/agents.ts:474
      → db read   prisma.user.findFirst
```

### `DELETE /api/agents/settings/approval-recipient/:userId`

- **Purpose:** Deletes the referenced approval recipient. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 518
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:518` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/agents/settings/approval-recipient/:userId
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/agents.ts:518
      → (responds directly; no downstream calls detected)
```

### `GET /api/agents/settings/auto-greeting`

- **Purpose:** Auto-greeting settings
- **Handler:** `services/auth/src/routes/agents.ts` line 270
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:270` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/agents/settings/auto-greeting
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/agents.ts:270
      → (responds directly; no downstream calls detected)
```

### `PUT /api/agents/settings/auto-greeting`

- **Purpose:** Updates the referenced auto greeting. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 281
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:281` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/agents/settings/auto-greeting
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/agents.ts:281
      → (responds directly; no downstream calls detected)
```

### `GET /api/agents/settings/bot-config`

- **Purpose:** Lists bot config. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 713
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:713` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/agents/settings/bot-config
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/agents.ts:713
      → db read   prisma.tenant.findUnique
```

### `GET /api/agents/settings/business-hours`

- **Purpose:** Lists business hours. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 561
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:561` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/agents/settings/business-hours
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/agents.ts:561
      → (responds directly; no downstream calls detected)
```

### `PUT /api/agents/settings/business-hours`

- **Purpose:** Updates the referenced business hour. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 590
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(businessHoursSchema)` (Zod). Accepts: `enabled`, `timezone`, `autoResponse`, `aiOutsideHours`, `outsideHoursHandoffMessage`, `schedule`. Required: `enabled`, `timezone`, `schedule`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:590` - inline `async (req, res)` handler.
3. **Validation.** `validate(businessHoursSchema)` (Zod). Accepts: `enabled`, `timezone`, `autoResponse`, `aiOutsideHours`, `outsideHoursHandoffMessage`, `schedule`. Required: `enabled`, `timezone`, `schedule`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/agents/settings/business-hours
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(businessHoursSchema)]
  → handler  services/auth/src/routes/agents.ts:590
      → (responds directly; no downstream calls detected)
```

### `GET /api/agents/settings/channel-config`

- **Purpose:** Lists channel config. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 410
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:410` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenantChannelConfig.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/agents/settings/channel-config
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/agents.ts:410
      → db read   prisma.tenantChannelConfig.findUnique
```

### `PUT /api/agents/settings/channel-config`

- **Purpose:** Updates the referenced channel config. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 430
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(channelConfigSchema)` (Zod). Accepts: `botFlowMode`. Required: `botFlowMode`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:430` - inline `async (req, res)` handler.
3. **Validation.** `validate(channelConfigSchema)` (Zod). Accepts: `botFlowMode`. Required: `botFlowMode`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `tenantChannelConfig.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/agents/settings/channel-config
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(channelConfigSchema)]
  → handler  services/auth/src/routes/agents.ts:430
      → db write  prisma.tenantChannelConfig.upsert
```

### `GET /api/agents/settings/channels`

- **Purpose:** Lists channels. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 299
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:299` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/agents/settings/channels
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/agents.ts:299
      → db read   prisma.channelAccount.findMany
```

### `POST /api/agents/settings/channels`

- **Purpose:** Creates a channel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 328
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(createChannelSchema)` (Zod). Accepts: `channel`, `externalId`, `displayName`, `credentials`. Required: `channel`, `externalId`, `displayName`, `credentials`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:328` - inline `async (req, res)` handler.
3. **Validation.** `validate(createChannelSchema)` (Zod). Accepts: `channel`, `externalId`, `displayName`, `credentials`. Required: `channel`, `externalId`, `displayName`, `credentials`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
   _Writes:_
   - `channelAccount.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/agents/settings/channels
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(createChannelSchema)]
  → handler  services/auth/src/routes/agents.ts:328
      → db read   prisma.channelAccount.findFirst
      → db write  prisma.channelAccount.create
```

### `DELETE /api/agents/settings/channels/:id`

- **Purpose:** Deletes the referenced channel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 393
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:393` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
   _Writes:_
   - `channelAccount.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/agents/settings/channels/:id
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/agents.ts:393
      → db read   prisma.channelAccount.findFirst
      → db write  prisma.channelAccount.delete
```

### `PATCH /api/agents/settings/channels/:id`

- **Purpose:** Updates the referenced channel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 367
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(updateChannelSchema)` (Zod). Accepts: `displayName`, `credentials`, `isActive`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:367` - inline `async (req, res)` handler.
3. **Validation.** `validate(updateChannelSchema)` (Zod). Accepts: `displayName`, `credentials`, `isActive`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
   _Writes:_
   - `channelAccount.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/agents/settings/channels/:id
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(updateChannelSchema)]
  → handler  services/auth/src/routes/agents.ts:367
      → db read   prisma.channelAccount.findFirst
      → db write  prisma.channelAccount.update
```

### `GET /api/agents/settings/copilot`

- **Purpose:** Legacy copilot/first-take-care settings - now managed via AI Employees in AI Studio
- **Handler:** `services/auth/src/routes/agents.ts` line 727
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:727` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `410`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/agents/settings/copilot
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/agents.ts:727
      → (responds directly; no downstream calls detected)
```

### `PUT /api/agents/settings/copilot`

- **Purpose:** Updates the referenced copilot. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 731
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:731` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `410`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/agents/settings/copilot
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/agents.ts:731
      → (responds directly; no downstream calls detected)
```

### `GET /api/agents/settings/first-take-care`

- **Purpose:** Lists first take care. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 735
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:735` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `410`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/agents/settings/first-take-care
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/agents.ts:735
      → (responds directly; no downstream calls detected)
```

### `PUT /api/agents/settings/first-take-care`

- **Purpose:** Updates the referenced first take care. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 739
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:739` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `410`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/agents/settings/first-take-care
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/agents.ts:739
      → (responds directly; no downstream calls detected)
```

### `GET /api/agents/settings/idle-automation`

- **Purpose:** Lists idle automation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 678
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:678` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/agents/settings/idle-automation
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/agents.ts:678
      → (responds directly; no downstream calls detected)
```

### `PUT /api/agents/settings/idle-automation`

- **Purpose:** Updates the referenced idle automation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 700
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(idleAutomationSchema)` (Zod). Accepts: `reminderEnabled`, `reminderDelayMinutes`, `reminderMessage`, `autoCloseEnabled`, `autoCloseDelayMinutes`, `autoCloseMessage`. Required: `reminderEnabled`, `autoCloseEnabled`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:700` - inline `async (req, res)` handler.
3. **Validation.** `validate(idleAutomationSchema)` (Zod). Accepts: `reminderEnabled`, `reminderDelayMinutes`, `reminderMessage`, `autoCloseEnabled`, `autoCloseDelayMinutes`, `autoCloseMessage`. Required: `reminderEnabled`, `autoCloseEnabled`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/agents/settings/idle-automation
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(idleAutomationSchema)]
  → handler  services/auth/src/routes/agents.ts:700
      → (responds directly; no downstream calls detected)
```

### `PUT /api/agents/settings/locale`

- **Purpose:** Updates the referenced locale. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 801
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(localeSchema)` (Zod). Accepts: `locale`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:801` - inline `async (req, res)` handler.
3. **Validation.** `validate(localeSchema)` (Zod). Accepts: `locale`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `tenant.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/agents/settings/locale
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(localeSchema)]
  → handler  services/auth/src/routes/agents.ts:801
      → db write  prisma.tenant.update
```

### `GET /api/agents/settings/sla`

- **Purpose:** Lists sla. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 608
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:608` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/agents/settings/sla
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/agents.ts:608
      → (responds directly; no downstream calls detected)
```

### `PUT /api/agents/settings/sla`

- **Purpose:** Updates the referenced sla. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 623
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(slaSettingsSchema)` (Zod). Accepts: `enabled`, `slaMinutes`, `warningThreshold`. Required: `enabled`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:623` - inline `async (req, res)` handler.
3. **Validation.** `validate(slaSettingsSchema)` (Zod). Accepts: `enabled`, `slaMinutes`, `warningThreshold`. Required: `enabled`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/agents/settings/sla
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(slaSettingsSchema)]
  → handler  services/auth/src/routes/agents.ts:623
      → (responds directly; no downstream calls detected)
```

### `GET /api/agents/settings/sla/department/:departmentId`

- **Purpose:** Department-level SLA override
- **Handler:** `services/auth/src/routes/agents.ts` line 635
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:635` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/agents/settings/sla/department/:departmentId
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/agents.ts:635
      → (responds directly; no downstream calls detected)
```

### `PUT /api/agents/settings/sla/department/:departmentId`

- **Purpose:** Updates the referenced department. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/agents.ts` line 650
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(slaSettingsSchema)` (Zod). Accepts: `enabled`, `slaMinutes`, `warningThreshold`. Required: `enabled`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/agents` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/agents.ts`.
2. **Handler.** `services/auth/src/routes/agents.ts:650` - inline `async (req, res)` handler.
3. **Validation.** `validate(slaSettingsSchema)` (Zod). Accepts: `enabled`, `slaMinutes`, `warningThreshold`. Required: `enabled`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/agents/settings/sla/department/:departmentId
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(slaSettingsSchema)]
  → handler  services/auth/src/routes/agents.ts:650
      → db read   prisma.department.findFirst
```

### `GET /api/ai-agents`

- **Purpose:** Lists ai agents. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-agents.ts` line 63
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `ai:employees:read` **or** role `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agents.ts`.
2. **Handler.** `services/ai/src/routes/ai-agents.ts:63` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `ai:employees:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findMany({
      where: { tenantId: req`
   - `department.findMany`
   - `conversation.findFirst({
        where: {
          tenantId: req`
   - `conversation.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-agents
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-agents.ts:63
      → db read   prisma.department.findMany({
      where: { tenantId: req
      → db read   prisma.department.findMany
      → db read   prisma.conversation.findFirst({
        where: {
          tenantId: req
      → db read   prisma.conversation.findFirst
```

### `POST /api/ai-agents`

- **Purpose:** Plan gates run BEFORE the handler:   requireEntitlement("ai.employee")     - is this capability sold on the plan?   requireCapacity("limit:ai_employees") - is there headroom under the limit? Both return a structured 402 the frontend can act on. Hiding the "New employee" button is not enforcement; this is.
- **Handler:** `services/ai/src/routes/ai-agents.ts` line 344
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `ai:employees:create` **or** role `ADMIN`; Entitlement: `ai.employee`; Plan limit: `limit:ai_employees`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agents.ts`.
2. **Handler.** `services/ai/src/routes/ai-agents.ts:344` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `ai:employees:create` **or** role `ADMIN`; Entitlement: `ai.employee`; Plan limit: `limit:ai_employees`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `aIAgent.create`
   - `aIAgentKnowledge.createMany`
   - `agentToolPermission.createMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `422`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-agents
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-agents.ts:344
      → db write  prisma.aIAgent.create
      → db write  prisma.aIAgentKnowledge.createMany
      → db write  prisma.agentToolPermission.createMany
```

### `DELETE /api/ai-agents/:id`

- **Purpose:** Deletes the referenced ai agent. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-agents.ts` line 910
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `ai:employees:delete` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agents.ts`.
2. **Handler.** `services/ai/src/routes/ai-agents.ts:910` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `ai:employees:delete` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst`
   _Writes:_
   - `routerRule.deleteMany`
   - `aIAgent.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/ai-agents/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-agents.ts:910
      → db read   prisma.aIAgent.findFirst
      → db write  prisma.routerRule.deleteMany
      → db write  prisma.aIAgent.delete
```

### `GET /api/ai-agents/:id`

- **Purpose:** Reads a single ai agent. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-agents.ts` line 250
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `ai:employees:read` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agents.ts`.
2. **Handler.** `services/ai/src/routes/ai-agents.ts:250` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `ai:employees:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst`
   - `agentToolPermission.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-agents/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-agents.ts:250
      → db read   prisma.aIAgent.findFirst
      → db read   prisma.agentToolPermission.findMany
```

### `PATCH /api/ai-agents/:id`

- **Purpose:** Updates the referenced ai agent. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-agents.ts` line 482
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `ai:employees:update` **or** role `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agents.ts`.
2. **Handler.** `services/ai/src/routes/ai-agents.ts:482` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `ai:employees:update` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst`
   - `aIAgentKnowledge.count`
   - `routerRule.updateMany({
        where: { tenantId: req`
   _Writes:_
   - `routerRule.updateMany`
   - `aIAgentKnowledge.deleteMany`
   - `aIAgentKnowledge.createMany`
   - `agentToolPermission.deleteMany`
   - `agentToolPermission.createMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `422`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/ai-agents/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-agents.ts:482
      → db read   prisma.aIAgent.findFirst
      → db read   prisma.aIAgentKnowledge.count
      → db read   prisma.routerRule.updateMany({
        where: { tenantId: req
      → db write  prisma.routerRule.updateMany
      → db write  prisma.aIAgentKnowledge.deleteMany
      → db write  prisma.aIAgentKnowledge.createMany
      → db write  prisma.agentToolPermission.deleteMany
      → db write  prisma.agentToolPermission.createMany
```

### `GET /api/ai-agents/:id/effective-permissions`

- **Purpose:** The SINGLE source of truth for "what can this employee actually do right now": the runtime AND-rule - an operation is EFFECTIVE only when its capability is live (CONNECTED / bookable / KB attached) AND (for tool-governed domains) an AgentToolPermission grants it. Reuses the exact permissions bridge + capability world the kernel uses, so the UI never disagrees with the runtime.
- **Handler:** `services/ai/src/routes/ai-agents.ts` line 810
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `ai:employees:read` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agents.ts`.
2. **Handler.** `services/ai/src/routes/ai-agents.ts:810` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `ai:employees:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-agents/:id/effective-permissions
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-agents.ts:810
      → db read   prisma.aIAgent.findFirst
```

### `GET /api/ai-agents/:id/reachability`

- **Purpose:** Runtime routing is exclusively the FlowCanvas graph: an ACTIVE employee with no agent-node targeting it NEVER receives a conversation. This endpoint tells the UI the truth so "go live" can't be a broken promise.
- **Handler:** `services/ai/src/routes/ai-agents.ts` line 778
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `ai:employees:read` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agents.ts`.
2. **Handler.** `services/ai/src/routes/ai-agents.ts:778` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `ai:employees:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-agents/:id/reachability
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-agents.ts:778
      → db read   prisma.aIAgent.findFirst
```

### `PUT /api/ai-agents/:id/tools/:tenantToolId`

- **Purpose:** Used by the IntegrationDrawer when opened from the agent page so that flipping one tool switch persists immediately (creates/updates a single `AgentToolPermission` row) instead of requiring the caller to send the full toolIds array via the PATCH path. The drawer also continues to hit `PUT /api/integrations/:slug/tools/:slug` to ensure the underlying `TenantTool` is enabled - both must be true for the bot to see the tool.
- **Handler:** `services/ai/src/routes/ai-agents.ts` line 705
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `ai:tools:assign` **or** role `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agents.ts`.
2. **Handler.** `services/ai/src/routes/ai-agents.ts:705` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `ai:tools:assign` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst`
   - `tenantTool.findFirst`
   - `agentToolPermission.findFirst`
   _Writes:_
   - `agentToolPermission.update`
   - `agentToolPermission.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/ai-agents/:id/tools/:tenantToolId
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-agents.ts:705
      → db read   prisma.aIAgent.findFirst
      → db read   prisma.tenantTool.findFirst
      → db read   prisma.agentToolPermission.findFirst
      → db write  prisma.agentToolPermission.update
      → db write  prisma.agentToolPermission.create
```

### `POST /api/ai-agents/builder/:id/complete`

- **Purpose:** Called when the wizard hands the admin off to the editor (readiness panel "Save live" / "Skip to editor"). Clears the resumable progress pointer so the editor renders instead of re-entering the builder, and promotes the finished employee DRAFT → ACTIVE. PAUSED/ACTIVE agents keep their status (only the step pointer is cleared) so this can't reactivate a paused agent.
- **Handler:** `services/ai/src/routes/ai-agent-builder.ts` line 404
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agent-builder.ts`.
2. **Handler.** `services/ai/src/routes/ai-agent-builder.ts:404` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst`
   _Writes:_
   - `aIAgent.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `422`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-agents/builder/:id/complete
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-agent-builder.ts:404
      → db read   prisma.aIAgent.findFirst
      → db write  prisma.aIAgent.update
```

### `GET /api/ai-agents/builder/:id/draft`

- **Purpose:** Reads a single draft for the referenced builder. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-agent-builder.ts` line 453
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agent-builder.ts`.
2. **Handler.** `services/ai/src/routes/ai-agent-builder.ts:453` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-agents/builder/:id/draft
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-agent-builder.ts:453
      → (responds directly; no downstream calls detected)
```

### `POST /api/ai-agents/builder/:id/finalize`

- **Purpose:** Performs the `finalize` action on the referenced builder. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-agent-builder.ts` line 469
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agent-builder.ts`.
2. **Handler.** `services/ai/src/routes/ai-agent-builder.ts:469` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-agents/builder/:id/finalize
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-agent-builder.ts:469
      → (responds directly; no downstream calls detected)
```

### `POST /api/ai-agents/builder/:id/knowledge`

- **Purpose:** POST /:id/knowledge - attach/detach one knowledge base to the draft.
- **Handler:** `services/ai/src/routes/ai-agent-builder.ts` line 287
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agent-builder.ts`.
2. **Handler.** `services/ai/src/routes/ai-agent-builder.ts:287` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst`
   - `knowledgeBase.findFirst`
   _Writes:_
   - `aIAgentKnowledge.createMany`
   - `aIAgentKnowledge.deleteMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-agents/builder/:id/knowledge
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-agent-builder.ts:287
      → db read   prisma.aIAgent.findFirst
      → db read   prisma.knowledgeBase.findFirst
      → db write  prisma.aIAgentKnowledge.createMany
      → db write  prisma.aIAgentKnowledge.deleteMany
```

### `GET /api/ai-agents/builder/:id/options`

- **Purpose:** Returns the tenant's available KBs + connected action tools with an `attached` flag for this draft, so the panel can render selectable cards instead of forcing the admin to dictate them over chat.
- **Handler:** `services/ai/src/routes/ai-agent-builder.ts` line 180
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agent-builder.ts`.
2. **Handler.** `services/ai/src/routes/ai-agent-builder.ts:180` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst`
   - `tenantTool.findMany`
   - `knowledgeBase.findMany`
   - `agentToolPermission.findMany`
   - `aIAgentKnowledge.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-agents/builder/:id/options
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-agent-builder.ts:180
      → db read   prisma.aIAgent.findFirst
      → db read   prisma.tenantTool.findMany
      → db read   prisma.knowledgeBase.findMany
      → db read   prisma.agentToolPermission.findMany
      → db read   prisma.aIAgentKnowledge.findMany
```

### `POST /api/ai-agents/builder/:id/readiness-test`

- **Purpose:** Generates the realistic customer questions this employee will face, scores how well it's covered (knowledge + tools), and lists concrete gap fixes.
- **Handler:** `services/ai/src/routes/ai-agent-builder.ts` line 487
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agent-builder.ts`.
2. **Handler.** `services/ai/src/routes/ai-agent-builder.ts:487` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst`
   _Writes:_
   - `aIAgent.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-agents/builder/:id/readiness-test
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-agent-builder.ts:487
      → db read   prisma.aIAgent.findFirst
      → db write  prisma.aIAgent.update
```

### `POST /api/ai-agents/builder/:id/refinements`

- **Purpose:** POST /:id/refinements - save the optional creation-wizard refinements (display name, conversation flow, custom guardrails) DETERMINISTICALLY, from the dedicated wizard step - instead of relying on the conversational builder to volunteer them. Send only the fields you're changing; an empty array clears that field. Returns the refreshed draft snapshot.
- **Handler:** `services/ai/src/routes/ai-agent-builder.ts` line 314
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agent-builder.ts`.
2. **Handler.** `services/ai/src/routes/ai-agent-builder.ts:314` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst`
   _Writes:_
   - `aIAgent.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-agents/builder/:id/refinements
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-agent-builder.ts:314
      → db read   prisma.aIAgent.findFirst
      → db write  prisma.aIAgent.update
```

### `POST /api/ai-agents/builder/:id/step`

- **Purpose:** Performs the `step` action on the referenced builder. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-agent-builder.ts` line 377
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agent-builder.ts`.
2. **Handler.** `services/ai/src/routes/ai-agent-builder.ts:377` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst`
   _Writes:_
   - `aIAgent.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-agents/builder/:id/step
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-agent-builder.ts:377
      → db read   prisma.aIAgent.findFirst
      → db write  prisma.aIAgent.update
```

### `POST /api/ai-agents/builder/:id/tool`

- **Purpose:** POST /:id/tool - attach/detach one tool to the draft (checkbox toggle).
- **Handler:** `services/ai/src/routes/ai-agent-builder.ts` line 223
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agent-builder.ts`.
2. **Handler.** `services/ai/src/routes/ai-agent-builder.ts:223` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst`
   - `tenantTool.findFirst`
   - `agentToolPermission.findFirst`
   _Writes:_
   - `agentToolPermission.update`
   - `agentToolPermission.create`
   - `agentToolPermission.deleteMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-agents/builder/:id/tool
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-agent-builder.ts:223
      → db read   prisma.aIAgent.findFirst
      → db read   prisma.tenantTool.findFirst
      → db read   prisma.agentToolPermission.findFirst
      → db write  prisma.agentToolPermission.update
      → db write  prisma.agentToolPermission.create
      → db write  prisma.agentToolPermission.deleteMany
```

### `POST /api/ai-agents/builder/:id/tools/bulk`

- **Purpose:** POST /:id/tools/bulk - attach/detach many tools at once (Select all / per-category select-all in the wizard). Validates ids against the tenant, then flips them in two queries instead of one round-trip per tool.
- **Handler:** `services/ai/src/routes/ai-agent-builder.ts` line 250
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agent-builder.ts`.
2. **Handler.** `services/ai/src/routes/ai-agent-builder.ts:250` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst`
   - `tenantTool.findMany`
   _Writes:_
   - `agentToolPermission.createMany`
   - `agentToolPermission.updateMany`
   - `agentToolPermission.deleteMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-agents/builder/:id/tools/bulk
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-agent-builder.ts:250
      → db read   prisma.aIAgent.findFirst
      → db read   prisma.tenantTool.findMany
      → db write  prisma.agentToolPermission.createMany
      → db write  prisma.agentToolPermission.updateMany
      → db write  prisma.agentToolPermission.deleteMany
```

### `POST /api/ai-agents/builder/run`

- **Purpose:** Creates a run. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-agent-builder.ts` line 518
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agent-builder.ts`.
2. **Handler.** `services/ai/src/routes/ai-agent-builder.ts:518` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `401`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-agents/builder/run
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-agent-builder.ts:518
      → db read   prisma.aIAgent.findFirst
```

### `POST /api/ai-agents/builder/start`

- **Purpose:** Creates a start. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-agent-builder.ts` line 104
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agent-builder.ts`.
2. **Handler.** `services/ai/src/routes/ai-agent-builder.ts:104` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessProfile.findUnique`
   - `aIAgent.findFirst`
   _Writes:_
   - `aIAgent.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-agents/builder/start
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-agent-builder.ts:104
      → db read   prisma.businessProfile.findUnique
      → db read   prisma.aIAgent.findFirst
      → db write  prisma.aIAgent.create
```

### `POST /api/ai-agents/generate`

- **Purpose:** Creates a generate. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-agents.ts` line 115
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `ai:employees:update` **or** role `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-agents` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-agents.ts`.
2. **Handler.** `services/ai/src/routes/ai-agents.ts:115` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `ai:employees:update` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-agents/generate
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-agents.ts:115
      → (responds directly; no downstream calls detected)
```

### `POST /api/ai-assist/:conversationId/adapter-tools/execute`

- **Purpose:** Bridge endpoint for adapter-framework providers (HubSpot, Salesforce, Monday, etc.) - they aren't HTTP-catalog tools, so the shared CRM client in `packages/shared/lib/crm.ts` reaches them through this endpoint.
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 708
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:708` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-assist/:conversationId/adapter-tools/execute
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-assist.ts:708
      → (responds directly; no downstream calls detected)
```

### `POST /api/ai-assist/:conversationId/analyze`

- **Purpose:** Performs the `analyze` action on the referenced ai assist. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 651
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:651` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-assist/:conversationId/analyze
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-assist.ts:651
      → (responds directly; no downstream calls detected)
```

### `POST /api/ai-assist/:conversationId/chat`

- **Purpose:** Performs the `chat` action on the referenced ai assist. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 543
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:543` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls**, in source order:
   - `aiService.getEffectiveCopilotConfig()`
   - `aiService.chatWithAgent()`
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findFirst`
   - `message.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-assist/:conversationId/chat
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-assist.ts:543
      → aiService.getEffectiveCopilotConfig()
      → aiService.chatWithAgent()
      → db read   prisma.conversation.findFirst
      → db read   prisma.message.findMany
```

### `POST /api/ai-assist/:conversationId/crm/find`

- **Purpose:** Search the connected system-of-record by email/phone through the CRMAdapter.findCustomer interface. Unlike the per-vendor catalog `lead_search`/`contact_search` tools (which only Zoho-style providers register), this covers EVERY source-of-truth integration connectable at onboarding - HubSpot, Salesforce, Zoho, Shopify, Fireberry, Airtable - through one code path. Shared's searchLeads/searchContacts fall back here for providers that don't expose the catalog search tools.
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 741
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:741` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-assist/:conversationId/crm/find
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-assist.ts:741
      → (responds directly; no downstream calls detected)
```

### `POST /api/ai-assist/:conversationId/followup`

- **Purpose:** F6.3 - generate a contextual follow-up for a stale conversation
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 842
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:842` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-assist/:conversationId/followup
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-assist.ts:842
      → (responds directly; no downstream calls detected)
```

### `GET /api/ai-assist/:conversationId/intelligence`

- **Purpose:** Reads a single intelligence for the referenced ai assist. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 643
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:643` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-assist/:conversationId/intelligence
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-assist.ts:643
      → (responds directly; no downstream calls detected)
```

### `GET /api/ai-assist/:conversationId/replay`

- **Purpose:** Reads a single replay for the referenced ai assist. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 659
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:659` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-assist/:conversationId/replay
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-assist.ts:659
      → (responds directly; no downstream calls detected)
```

### `GET /api/ai-assist/:conversationId/score`

- **Purpose:** Reads a single score for the referenced ai assist. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 765
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:765` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-assist/:conversationId/score
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-assist.ts:765
      → (responds directly; no downstream calls detected)
```

### `POST /api/ai-assist/:conversationId/score`

- **Purpose:** Performs the `score` action on the referenced ai assist. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 780
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:780` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-assist/:conversationId/score
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-assist.ts:780
      → (responds directly; no downstream calls detected)
```

### `GET /api/ai-assist/:conversationId/suggestions`

- **Purpose:** Chat Copilot is sold separately from the core inbox, so the suggestion endpoint is gated rather than merely hidden in the UI.
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 366
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Entitlement: `ai.copilot`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:366` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Entitlement: `ai.copilot`
6. **Service calls**, in source order:
   - `aiService.getEffectiveCopilotConfig()`
   - `aiService.getSuggestions()`
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findFirst`
   - `message.findMany`
   - `department.findUnique`
   - `user.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-assist/:conversationId/suggestions
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-assist.ts:366
      → aiService.getEffectiveCopilotConfig()
      → aiService.getSuggestions()
      → db read   prisma.conversation.findFirst
      → db read   prisma.message.findMany
      → db read   prisma.department.findUnique
      → db read   prisma.user.findUnique
```

### `GET /api/ai-assist/:conversationId/summary`

- **Purpose:** Gated on communication.crm_summaries - the SAME key as the background pipeline, and deliberately NOT ai.copilot. Foundation denies Copilot and grants summaries; gating this route on ai.copilot would break exactly the plan combination the product sells.
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 500
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Entitlement: `communication.crm_summaries`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:500` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Entitlement: `communication.crm_summaries`
6. **Service calls**, in source order:
   - `aiService.getEffectiveCopilotConfig()`
   - `aiService.summarizeConversation()`
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findFirst`
   - `message.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-assist/:conversationId/summary
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-assist.ts:500
      → aiService.getEffectiveCopilotConfig()
      → aiService.summarizeConversation()
      → db read   prisma.conversation.findFirst
      → db read   prisma.message.findMany
```

### `GET /api/ai-assist/:conversationId/tools`

- **Purpose:** Reads a single tool for the referenced ai assist. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 669
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:669` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-assist/:conversationId/tools
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-assist.ts:669
      → (responds directly; no downstream calls detected)
```

### `POST /api/ai-assist/:conversationId/tools/execute`

- **Purpose:** Creates an execute for the referenced tool. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 677
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:677` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-assist/:conversationId/tools/execute
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-assist.ts:677
      → (responds directly; no downstream calls detected)
```

### `POST /api/ai-assist/compose`

- **Purpose:** Drafts a single outbound message from a natural-language instruction. Used by: template editor, scheduled-message composer, inbox input, and the Command Center. Returns plain text (no JSON).
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 274
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:274` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findFirst`
   - `message.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-assist/compose
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-assist.ts:274
      → db read   prisma.conversation.findFirst
      → db read   prisma.message.findMany
```

### `GET /api/ai-assist/config`

- **Purpose:** Static routes BEFORE parameterized routes
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 263
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:263` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls**, in source order:
   - `aiService.getTenantCopilotConfig()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-assist/config
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-assist.ts:263
      → aiService.getTenantCopilotConfig()
```

### `GET /api/ai-assist/customer-state/:contactId`

- **Purpose:** F7.2/F7.5 - compact customer state object (decisions, summaries, tags)
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 824
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:824` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-assist/customer-state/:contactId
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-assist.ts:824
      → (responds directly; no downstream calls detected)
```

### `POST /api/ai-assist/discover-business`

- **Purpose:** The deep 5-domain website scan that produces the Business Intelligence Report + first recommendation (Bible Part II). The auth onboarding route fetches the pages + detects signals (no LLM) and calls this with the admin JWT forwarded - this is the one place onboarding is allowed to make an LLM call. Best-effort: returns { ok:false } on a scan miss so the caller can fall back to the shallow understanding and never block onboarding.
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 41
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:41` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-assist/discover-business
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-assist.ts:41
      → (responds directly; no downstream calls detected)
```

### `POST /api/ai-assist/generate-config/:departmentId`

- **Purpose:** Creates a generate config. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 132
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:132` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-assist/generate-config/:departmentId
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-assist.ts:132
      → db read   prisma.department.findFirst
```

### `POST /api/ai-assist/generate-configs`

- **Purpose:** Creates a generate config. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 104
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:104` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-assist/generate-configs
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-assist.ts:104
      → db read   prisma.aIAgent.findMany
```

### `POST /api/ai-assist/intent`

- **Purpose:** Creates an intent. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 152
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:152` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/ai-assist/intent
  → [internalAuth]
  → handler  services/ai/src/routes/ai-assist.ts:152
      → (responds directly; no downstream calls detected)
```

### `POST /api/ai-assist/onboarding-employee-chat`

- **Purpose:** Onboarding Movement 8 - chat with the recommended employee before deploy.
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 74
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:74` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/ai-assist/onboarding-employee-chat
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-assist.ts:74
      → (responds directly; no downstream calls detected)
```

### `GET /api/ai-assist/policy`

- **Purpose:** F8.4 - business policy admin API (UI-neutral)
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 796
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:796` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-assist/policy
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-assist.ts:796
      → (responds directly; no downstream calls detected)
```

### `PUT /api/ai-assist/policy`

- **Purpose:** Updates the referenced policy. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 806
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:806` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/ai-assist/policy
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-assist.ts:806
      → (responds directly; no downstream calls detected)
```

### `GET /api/ai-assist/prompt/:departmentId`

- **Purpose:** Get effective copilot config for a department (SYSTEM_ADMIN only)
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 607
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `SYSTEM_ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:607` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `SYSTEM_ADMIN`
6. **Service calls**, in source order:
   - `aiService.getEffectiveCopilotConfig()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-assist/prompt/:departmentId
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(SYSTEM_ADMIN)]
  → handler  services/ai/src/routes/ai-assist.ts:607
      → aiService.getEffectiveCopilotConfig()
```

### `GET /api/ai-assist/tools/registry`

- **Purpose:** Tools registry - MUST be before /:conversationId routes
- **Handler:** `services/ai/src/routes/ai-assist.ts` line 255
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-assist` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-assist.ts`.
2. **Handler.** `services/ai/src/routes/ai-assist.ts:255` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-assist/tools/registry
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/ai-assist.ts:255
      → (responds directly; no downstream calls detected)
```

### `GET /api/ai-skills`

- **Purpose:** GET /api/ai-skills
- **Handler:** `services/ai/src/routes/ai-skills.ts` line 30
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-skills` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-skills.ts`.
2. **Handler.** `services/ai/src/routes/ai-skills.ts:30` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/ai-skills
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/ai-skills.ts:30
      → (responds directly; no downstream calls detected)
```

### `GET /api/approvals`

- **Purpose:** GET /api/approvals List approval requests for this tenant. Supports:   - ?status=PENDING       default   - ?conversationId=xxx   scope to one conversation   - ?contactId=xxx        scope to one contact
- **Handler:** `services/conversation/src/routes/approvals.ts` line 896
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/approvals` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/approvals.ts`.
2. **Handler.** `services/conversation/src/routes/approvals.ts:896` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/approvals
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/approvals.ts:896
      → db read   prisma.user.findMany
```

### `GET /api/approvals/:id`

- **Purpose:** GET /api/approvals/:id Full record + enrichment for the rich in-conversation approval card:   - conversation + recent messages (last 5)   - contact snapshot
- **Handler:** `services/conversation/src/routes/approvals.ts` line 951
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/approvals` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/approvals.ts`.
2. **Handler.** `services/conversation/src/routes/approvals.ts:951` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findFirst`
   - `contact.findFirst`
   - `message.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/approvals/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/approvals.ts:951
      → db read   prisma.conversation.findFirst
      → db read   prisma.contact.findFirst
      → db read   prisma.message.findMany
```

### `POST /api/approvals/:id/approve`

- **Purpose:** POST /api/approvals/:id/approve Body: { decisionReason?, modifiedParams? }
- **Handler:** `services/conversation/src/routes/approvals.ts` line 1005
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/approvals` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/approvals.ts`.
2. **Handler.** `services/conversation/src/routes/approvals.ts:1005` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findFirst`
   _Writes:_
   - `conversation.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `401`, `403`, `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/approvals/:id/approve
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/approvals.ts:1005
      → db read   prisma.conversation.findFirst
      → db write  prisma.conversation.update
```

### `POST /api/approvals/:id/dispatch-approved`

- **Purpose:** Called by the incoming-worker after a manager approved on WhatsApp. The decision was already recorded there (through the same atomic CAS the UI uses); this endpoint only runs the action, so both channels converge on ONE execution path. Service-to-service auth ONLY - registered above the user auth middleware because there is no user session behind a WhatsApp tap.
- **Handler:** `services/conversation/src/routes/approvals.ts` line 805
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/approvals` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/approvals.ts`.
2. **Handler.** `services/conversation/src/routes/approvals.ts:805` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `500`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/approvals/:id/dispatch-approved
  → [internalAuth]
  → handler  services/conversation/src/routes/approvals.ts:805
      → (responds directly; no downstream calls detected)
```

### `POST /api/approvals/:id/dispatch-rejected`

- **Purpose:** POST /api/approvals/:id/dispatch-rejected  (internal, no user session)
- **Handler:** `services/conversation/src/routes/approvals.ts` line 853
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/approvals` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/approvals.ts`.
2. **Handler.** `services/conversation/src/routes/approvals.ts:853` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `500`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/approvals/:id/dispatch-rejected
  → [internalAuth]
  → handler  services/conversation/src/routes/approvals.ts:853
      → (responds directly; no downstream calls detected)
```

### `POST /api/approvals/:id/reject`

- **Purpose:** POST /api/approvals/:id/reject Body: { decisionReason } Rejection reason is REQUIRED - no silent "just no". The bot resume worker uses it to craft the fallback customer message.
- **Handler:** `services/conversation/src/routes/approvals.ts` line 1204
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/approvals` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/approvals.ts`.
2. **Handler.** `services/conversation/src/routes/approvals.ts:1204` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findFirst`
   _Writes:_
   - `conversation.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `401`, `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/approvals/:id/reject
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/approvals.ts:1204
      → db read   prisma.conversation.findFirst
      → db write  prisma.conversation.update
```

### `POST /api/approvals/:id/retry-execution`

- **Purpose:** POST /api/approvals/:id/retry-execution
- **Handler:** `services/conversation/src/routes/approvals.ts` line 1155
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/approvals` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/approvals.ts`.
2. **Handler.** `services/conversation/src/routes/approvals.ts:1155` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `401`, `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/approvals/:id/retry-execution
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/approvals.ts:1155
      → (responds directly; no downstream calls detected)
```

### `GET /api/chatbot-flows`

- **Purpose:** Lists chatbot flows. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/chatbot/src/routes/chatbot.ts` line 21
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `chatbot` service mounts this router at `/api/chatbot-flows` in `services/chatbot/src/index.ts`; the route is declared in `services/chatbot/src/routes/chatbot.ts`.
2. **Handler.** `services/chatbot/src/routes/chatbot.ts:21` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `chatbotFlow.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/chatbot-flows
  → [authenticate → resolveTenant]
  → handler  services/chatbot/src/routes/chatbot.ts:21
      → db read   prisma.chatbotFlow.findMany
```

### `POST /api/chatbot-flows`

- **Purpose:** Creates a chatbot flow. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/chatbot/src/routes/chatbot.ts` line 43
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** `ai:workflows:update` **or** role `ADMIN`; Entitlement: `communication.automations`; Plan limit: `limit:workflows`
- **Input validation:** `validate(flowSchema)` (Zod). Accepts: `name`, `description`, `channel`, `nodes`, `edges`, `isActive`, `expectedUpdatedAt`. Required: `name`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `chatbot` service mounts this router at `/api/chatbot-flows` in `services/chatbot/src/index.ts`; the route is declared in `services/chatbot/src/routes/chatbot.ts`.
2. **Handler.** `services/chatbot/src/routes/chatbot.ts:43` - inline `async (req, res)` handler.
3. **Validation.** `validate(flowSchema)` (Zod). Accepts: `name`, `description`, `channel`, `nodes`, `edges`, `isActive`, `expectedUpdatedAt`. Required: `name`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** `ai:workflows:update` **or** role `ADMIN`; Entitlement: `communication.automations`; Plan limit: `limit:workflows`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `chatbotFlow.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/chatbot-flows
  → [authenticate → resolveTenant → validate(flowSchema)]
  → handler  services/chatbot/src/routes/chatbot.ts:43
      → db write  prisma.chatbotFlow.create
```

### `DELETE /api/chatbot-flows/:id`

- **Purpose:** Deletes the referenced chatbot flow. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/chatbot/src/routes/chatbot.ts` line 78
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** `ai:workflows:update` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `chatbot` service mounts this router at `/api/chatbot-flows` in `services/chatbot/src/index.ts`; the route is declared in `services/chatbot/src/routes/chatbot.ts`.
2. **Handler.** `services/chatbot/src/routes/chatbot.ts:78` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** `ai:workflows:update` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `chatbotFlow.findFirst`
   _Writes:_
   - `chatbotFlow.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/chatbot-flows/:id
  → [authenticate → resolveTenant]
  → handler  services/chatbot/src/routes/chatbot.ts:78
      → db read   prisma.chatbotFlow.findFirst
      → db write  prisma.chatbotFlow.delete
```

### `GET /api/chatbot-flows/:id`

- **Purpose:** Reads a single chatbot flow. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/chatbot/src/routes/chatbot.ts` line 35
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `chatbot` service mounts this router at `/api/chatbot-flows` in `services/chatbot/src/index.ts`; the route is declared in `services/chatbot/src/routes/chatbot.ts`.
2. **Handler.** `services/chatbot/src/routes/chatbot.ts:35` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `chatbotFlow.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/chatbot-flows/:id
  → [authenticate → resolveTenant]
  → handler  services/chatbot/src/routes/chatbot.ts:35
      → db read   prisma.chatbotFlow.findFirst
```

### `PUT /api/chatbot-flows/:id`

- **Purpose:** Updates the referenced chatbot flow. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/chatbot/src/routes/chatbot.ts` line 59
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** `ai:workflows:update` **or** role `ADMIN`
- **Input validation:** `validate(flowSchema)` (Zod). Accepts: `name`, `description`, `channel`, `nodes`, `edges`, `isActive`, `expectedUpdatedAt`. Required: `name`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `chatbot` service mounts this router at `/api/chatbot-flows` in `services/chatbot/src/index.ts`; the route is declared in `services/chatbot/src/routes/chatbot.ts`.
2. **Handler.** `services/chatbot/src/routes/chatbot.ts:59` - inline `async (req, res)` handler.
3. **Validation.** `validate(flowSchema)` (Zod). Accepts: `name`, `description`, `channel`, `nodes`, `edges`, `isActive`, `expectedUpdatedAt`. Required: `name`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** `ai:workflows:update` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `chatbotFlow.findFirst`
   _Writes:_
   - `chatbotFlow.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/chatbot-flows/:id
  → [authenticate → resolveTenant → validate(flowSchema)]
  → handler  services/chatbot/src/routes/chatbot.ts:59
      → db read   prisma.chatbotFlow.findFirst
      → db write  prisma.chatbotFlow.update
```

### `POST /api/chatbot-flows/:id/activate`

- **Purpose:** Performs the `activate` action on the referenced chatbot flow. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/chatbot/src/routes/chatbot.ts` line 87
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** `ai:workflows:publish` **or** role `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `chatbot` service mounts this router at `/api/chatbot-flows` in `services/chatbot/src/index.ts`; the route is declared in `services/chatbot/src/routes/chatbot.ts`.
2. **Handler.** `services/chatbot/src/routes/chatbot.ts:87` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** `ai:workflows:publish` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `chatbotFlow.findFirst`
   _Writes:_
   - `chatbotFlow.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `422`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/chatbot-flows/:id/activate
  → [authenticate → resolveTenant]
  → handler  services/chatbot/src/routes/chatbot.ts:87
      → db read   prisma.chatbotFlow.findFirst
      → db write  prisma.chatbotFlow.update
```

### `POST /api/chatbot-flows/:id/validate`

- **Purpose:** Advisory validation for the editor's validation panel - same authoritative rules, without mutating anything.
- **Handler:** `services/chatbot/src/routes/chatbot.ts` line 105
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `chatbot` service mounts this router at `/api/chatbot-flows` in `services/chatbot/src/index.ts`; the route is declared in `services/chatbot/src/routes/chatbot.ts`.
2. **Handler.** `services/chatbot/src/routes/chatbot.ts:105` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `chatbotFlow.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/chatbot-flows/:id/validate
  → [authenticate → resolveTenant]
  → handler  services/chatbot/src/routes/chatbot.ts:105
      → db read   prisma.chatbotFlow.findFirst
```

### `GET /api/custom-api-tools`

- **Purpose:** Lists custom api tools. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/custom-api-admin.ts` line 31
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/custom-api-tools` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/custom-api-admin.ts`.
2. **Handler.** `services/ai/src/routes/custom-api-admin.ts:31` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/custom-api-tools
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/custom-api-admin.ts:31
      → (responds directly; no downstream calls detected)
```

### `POST /api/custom-api-tools`

- **Purpose:** Creates a custom api tool. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/custom-api-admin.ts` line 39
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/custom-api-tools` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/custom-api-admin.ts`.
2. **Handler.** `services/ai/src/routes/custom-api-admin.ts:39` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/custom-api-tools
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/custom-api-admin.ts:39
      → (responds directly; no downstream calls detected)
```

### `DELETE /api/custom-api-tools/:id`

- **Purpose:** Deletes the referenced custom api tool. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/custom-api-admin.ts` line 124
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/custom-api-tools` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/custom-api-admin.ts`.
2. **Handler.** `services/ai/src/routes/custom-api-admin.ts:124` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/custom-api-tools/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/custom-api-admin.ts:124
      → (responds directly; no downstream calls detected)
```

### `PATCH /api/custom-api-tools/:id`

- **Purpose:** Updates the referenced custom api tool. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/custom-api-admin.ts` line 93
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/custom-api-tools` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/custom-api-admin.ts`.
2. **Handler.** `services/ai/src/routes/custom-api-admin.ts:93` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/custom-api-tools/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/custom-api-admin.ts:93
      → (responds directly; no downstream calls detected)
```

### `POST /api/custom-api-tools/:id/test`

- **Purpose:** Performs the `test` action on the referenced custom api tool. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/custom-api-admin.ts` line 131
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/custom-api-tools` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/custom-api-admin.ts`.
2. **Handler.** `services/ai/src/routes/custom-api-admin.ts:131` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/custom-api-tools/:id/test
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/custom-api-admin.ts:131
      → (responds directly; no downstream calls detected)
```

### `GET /api/custom-db-tools`

- **Purpose:** Lists custom db tools. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/custom-db-admin.ts` line 24
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/custom-db-tools` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/custom-db-admin.ts`.
2. **Handler.** `services/ai/src/routes/custom-db-admin.ts:24` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/custom-db-tools
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/custom-db-admin.ts:24
      → (responds directly; no downstream calls detected)
```

### `POST /api/custom-db-tools`

- **Purpose:** Creates a custom db tool. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/custom-db-admin.ts` line 34
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/custom-db-tools` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/custom-db-admin.ts`.
2. **Handler.** `services/ai/src/routes/custom-db-admin.ts:34` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/custom-db-tools
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/custom-db-admin.ts:34
      → (responds directly; no downstream calls detected)
```

### `DELETE /api/custom-db-tools/:id`

- **Purpose:** Deletes the referenced custom db tool. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/custom-db-admin.ts` line 105
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/custom-db-tools` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/custom-db-admin.ts`.
2. **Handler.** `services/ai/src/routes/custom-db-admin.ts:105` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/custom-db-tools/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/custom-db-admin.ts:105
      → (responds directly; no downstream calls detected)
```

### `PATCH /api/custom-db-tools/:id`

- **Purpose:** Updates the referenced custom db tool. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/custom-db-admin.ts` line 84
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/custom-db-tools` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/custom-db-admin.ts`.
2. **Handler.** `services/ai/src/routes/custom-db-admin.ts:84` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/custom-db-tools/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/custom-db-admin.ts:84
      → (responds directly; no downstream calls detected)
```

### `POST /api/custom-db-tools/:id/test`

- **Purpose:** Performs the `test` action on the referenced custom db tool. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/custom-db-admin.ts` line 112
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/custom-db-tools` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/custom-db-admin.ts`.
2. **Handler.** `services/ai/src/routes/custom-db-admin.ts:112` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/custom-db-tools/:id/test
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/custom-db-admin.ts:112
      → (responds directly; no downstream calls detected)
```

### `GET /api/decision-timeline/conversation/:id`

- **Purpose:** Reads a single conversation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/decision-timeline.ts` line 40
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/decision-timeline` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/decision-timeline.ts`.
2. **Handler.** `services/ai/src/routes/decision-timeline.ts:40` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/decision-timeline/conversation/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/decision-timeline.ts:40
      → (responds directly; no downstream calls detected)
```

### `GET /api/flow-canvas`

- **Purpose:** Lists flow canvas. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/flow-canvas.ts` line 9
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/flow-canvas` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/flow-canvas.ts`.
2. **Handler.** `services/ai/src/routes/flow-canvas.ts:9` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `flowCanvas.findUnique`
   - `channelAccount.findMany`
   - `routerRule.findMany`
   - `chatbotFlow.findMany`
   - `aIAgent.findMany`
   - `department.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/flow-canvas
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/flow-canvas.ts:9
      → db read   prisma.flowCanvas.findUnique
      → db read   prisma.channelAccount.findMany
      → db read   prisma.routerRule.findMany
      → db read   prisma.chatbotFlow.findMany
      → db read   prisma.aIAgent.findMany
      → db read   prisma.department.findMany
```

### `PUT /api/flow-canvas`

- **Purpose:** Updates the referenced flow canva. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/flow-canvas.ts` line 54
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/flow-canvas` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/flow-canvas.ts`.
2. **Handler.** `services/ai/src/routes/flow-canvas.ts:54` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `flowCanvas.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/flow-canvas
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/flow-canvas.ts:54
      → db write  prisma.flowCanvas.upsert
```

### `POST /api/flow-canvas/auto-generate`

- **Purpose:** Creates an auto generate. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/flow-canvas.ts` line 84
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/flow-canvas` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/flow-canvas.ts`.
2. **Handler.** `services/ai/src/routes/flow-canvas.ts:84` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findMany`
   - `routerRule.findMany`
   - `chatbotFlow.findMany`
   - `aIAgent.findMany`
   - `department.findMany`
   _Writes:_
   - `flowCanvas.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/flow-canvas/auto-generate
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/flow-canvas.ts:84
      → db read   prisma.channelAccount.findMany
      → db read   prisma.routerRule.findMany
      → db read   prisma.chatbotFlow.findMany
      → db read   prisma.aIAgent.findMany
      → db read   prisma.department.findMany
      → db write  prisma.flowCanvas.upsert
```

### `GET /api/funnels`

- **Purpose:** Lists funnels. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/funnel-admin.ts` line 29
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/funnels` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/funnel-admin.ts`.
2. **Handler.** `services/ai/src/routes/funnel-admin.ts:29` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/funnels
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/funnel-admin.ts:29
      → (responds directly; no downstream calls detected)
```

### `POST /api/funnels`

- **Purpose:** Creates a funnel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/funnel-admin.ts` line 55
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/funnels` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/funnel-admin.ts`.
2. **Handler.** `services/ai/src/routes/funnel-admin.ts:55` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/funnels
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/funnel-admin.ts:55
      → (responds directly; no downstream calls detected)
```

### `DELETE /api/funnels/:id`

- **Purpose:** Deletes the referenced funnel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/funnel-admin.ts` line 143
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/funnels` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/funnel-admin.ts`.
2. **Handler.** `services/ai/src/routes/funnel-admin.ts:143` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/funnels/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/funnel-admin.ts:143
      → (responds directly; no downstream calls detected)
```

### `GET /api/funnels/:id`

- **Purpose:** Reads a single funnel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/funnel-admin.ts` line 42
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/funnels` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/funnel-admin.ts`.
2. **Handler.** `services/ai/src/routes/funnel-admin.ts:42` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/funnels/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/funnel-admin.ts:42
      → (responds directly; no downstream calls detected)
```

### `PATCH /api/funnels/:id`

- **Purpose:** Updates the referenced funnel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/funnel-admin.ts` line 95
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/funnels` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/funnel-admin.ts`.
2. **Handler.** `services/ai/src/routes/funnel-admin.ts:95` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/funnels/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/funnel-admin.ts:95
      → (responds directly; no downstream calls detected)
```

### `POST /api/funnels/:id/activate`

- **Purpose:** Performs the `activate` action on the referenced funnel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/funnel-admin.ts` line 119
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/funnels` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/funnel-admin.ts`.
2. **Handler.** `services/ai/src/routes/funnel-admin.ts:119` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/funnels/:id/activate
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/funnel-admin.ts:119
      → (responds directly; no downstream calls detected)
```

### `GET /api/industry-packs`

- **Purpose:** Lists industry packs. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/industry-packs.ts` line 24
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/industry-packs` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/industry-packs.ts`.
2. **Handler.** `services/ai/src/routes/industry-packs.ts:24` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/industry-packs
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/industry-packs.ts:24
      → (responds directly; no downstream calls detected)
```

### `POST /api/industry-packs/apply`

- **Purpose:** Creates an apply. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/industry-packs.ts` line 41
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/industry-packs` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/industry-packs.ts`.
2. **Handler.** `services/ai/src/routes/industry-packs.ts:41` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/industry-packs/apply
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/industry-packs.ts:41
      → (responds directly; no downstream calls detected)
```

### `GET /api/intelligence-reviews`

- **Purpose:** Lists intelligence reviews. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/intelligence-reviews.ts` line 30
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/intelligence-reviews` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/intelligence-reviews.ts`.
2. **Handler.** `services/ai/src/routes/intelligence-reviews.ts:30` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/intelligence-reviews
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/intelligence-reviews.ts:30
      → (responds directly; no downstream calls detected)
```

### `POST /api/intelligence-reviews/:id/approve`

- **Purpose:** Performs the `approve` action on the referenced intelligence review. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/intelligence-reviews.ts` line 40
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/intelligence-reviews` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/intelligence-reviews.ts`.
2. **Handler.** `services/ai/src/routes/intelligence-reviews.ts:40` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/intelligence-reviews/:id/approve
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/intelligence-reviews.ts:40
      → (responds directly; no downstream calls detected)
```

### `POST /api/intelligence-reviews/:id/reject`

- **Purpose:** Performs the `reject` action on the referenced intelligence review. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/intelligence-reviews.ts` line 48
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/intelligence-reviews` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/intelligence-reviews.ts`.
2. **Handler.** `services/ai/src/routes/intelligence-reviews.ts:48` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/intelligence-reviews/:id/reject
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/intelligence-reviews.ts:48
      → (responds directly; no downstream calls detected)
```

### `GET /api/post-conversation-config`

- **Purpose:** Lists post conversation config. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/post-conversation-config.ts` line 27
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/post-conversation-config` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/post-conversation-config.ts`.
2. **Handler.** `services/ai/src/routes/post-conversation-config.ts:27` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/post-conversation-config
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/post-conversation-config.ts:27
      → (responds directly; no downstream calls detected)
```

### `PUT /api/post-conversation-config`

- **Purpose:** Updates the referenced post conversation config. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/post-conversation-config.ts` line 44
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/post-conversation-config` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/post-conversation-config.ts`.
2. **Handler.** `services/ai/src/routes/post-conversation-config.ts:44` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/post-conversation-config
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/post-conversation-config.ts:44
      → (responds directly; no downstream calls detected)
```

### `GET /api/reasoner-shadow/:id`

- **Purpose:** GET /:id - one full record (incl. replayable reasonerInput) for deep review.
- **Handler:** `services/ai/src/routes/reasoner-shadow.ts` line 76
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/reasoner-shadow` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/reasoner-shadow.ts`.
2. **Handler.** `services/ai/src/routes/reasoner-shadow.ts:76` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/reasoner-shadow/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/reasoner-shadow.ts:76
      → (responds directly; no downstream calls detected)
```

### `GET /api/reasoner-shadow/divergences`

- **Purpose:** GET /divergences - the review queue: recent disagreements, newest first.
- **Handler:** `services/ai/src/routes/reasoner-shadow.ts` line 56
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/reasoner-shadow` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/reasoner-shadow.ts`.
2. **Handler.** `services/ai/src/routes/reasoner-shadow.ts:56` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/reasoner-shadow/divergences
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/reasoner-shadow.ts:56
      → (responds directly; no downstream calls detected)
```

### `GET /api/reasoner-shadow/summary`

- **Purpose:** GET /summary - parity headline, sliceable by provider/model/promptVersion/since.
- **Handler:** `services/ai/src/routes/reasoner-shadow.ts` line 38
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/reasoner-shadow` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/reasoner-shadow.ts`.
2. **Handler.** `services/ai/src/routes/reasoner-shadow.ts:38` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/reasoner-shadow/summary
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/reasoner-shadow.ts:38
      → (responds directly; no downstream calls detected)
```

### `GET /api/stage-transitions/contacts/:id`

- **Purpose:** Reads a single contact. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/stage-admin.ts` line 226
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/stage-transitions` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/stage-admin.ts`.
2. **Handler.** `services/ai/src/routes/stage-admin.ts:226` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/stage-transitions/contacts/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/stage-admin.ts:226
      → (responds directly; no downstream calls detected)
```

### `GET /api/stage-transitions/conversations/:id`

- **Purpose:** Reads a single conversation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/stage-admin.ts` line 209
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/stage-transitions` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/stage-admin.ts`.
2. **Handler.** `services/ai/src/routes/stage-admin.ts:209` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/stage-transitions/conversations/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/stage-admin.ts:209
      → (responds directly; no downstream calls detected)
```

### `POST /api/stage-transitions/conversations/:id/override`

- **Purpose:** Performs the `override` action on the referenced conversation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/stage-admin.ts` line 48
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/stage-transitions` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/stage-admin.ts`.
2. **Handler.** `services/ai/src/routes/stage-admin.ts:48` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/stage-transitions/conversations/:id/override
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/stage-admin.ts:48
      → db read   prisma.conversation.findFirst
```

### `POST /api/system-chat/ask`

- **Purpose:** Creates an ask. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/system-chat.ts` line 231
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/system-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/system-chat.ts`.
2. **Handler.** `services/ai/src/routes/system-chat.ts:231` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/system-chat/ask
  → [authenticate → requireSystemAdmin]
  → handler  services/ai/src/routes/system-chat.ts:231
      → (responds directly; no downstream calls detected)
```

### `GET /api/system-chat/knowledge-bases`

- **Purpose:** Lists knowledge bases. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/system-chat.ts` line 42
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/system-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/system-chat.ts`.
2. **Handler.** `services/ai/src/routes/system-chat.ts:42` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeBase.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system-chat/knowledge-bases
  → [authenticate → requireSystemAdmin]
  → handler  services/ai/src/routes/system-chat.ts:42
      → db read   prisma.knowledgeBase.findMany
```

### `POST /api/system-chat/knowledge-bases`

- **Purpose:** Creates a knowledge base. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/system-chat.ts` line 61
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/system-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/system-chat.ts`.
2. **Handler.** `services/ai/src/routes/system-chat.ts:61` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `knowledgeBase.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/system-chat/knowledge-bases
  → [authenticate → requireSystemAdmin]
  → handler  services/ai/src/routes/system-chat.ts:61
      → db write  prisma.knowledgeBase.create
```

### `DELETE /api/system-chat/knowledge-bases/:id`

- **Purpose:** Deletes the referenced knowledge base. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/system-chat.ts` line 99
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/system-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/system-chat.ts`.
2. **Handler.** `services/ai/src/routes/system-chat.ts:99` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeBase.findFirst`
   _Writes:_
   - `knowledgeBase.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/system-chat/knowledge-bases/:id
  → [authenticate → requireSystemAdmin]
  → handler  services/ai/src/routes/system-chat.ts:99
      → db read   prisma.knowledgeBase.findFirst
      → db write  prisma.knowledgeBase.delete
```

### `PATCH /api/system-chat/knowledge-bases/:id`

- **Purpose:** Updates the referenced knowledge base. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/system-chat.ts` line 76
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/system-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/system-chat.ts`.
2. **Handler.** `services/ai/src/routes/system-chat.ts:76` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeBase.findFirst`
   _Writes:_
   - `knowledgeBase.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/system-chat/knowledge-bases/:id
  → [authenticate → requireSystemAdmin]
  → handler  services/ai/src/routes/system-chat.ts:76
      → db read   prisma.knowledgeBase.findFirst
      → db write  prisma.knowledgeBase.update
```

### `POST /api/system-chat/knowledge-bases/:id/documents`

- **Purpose:** Performs the `documents` action on the referenced knowledge base. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/system-chat.ts` line 116
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/system-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/system-chat.ts`.
2. **Handler.** `services/ai/src/routes/system-chat.ts:116` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeBase.findFirst`
   _Writes:_
   - `knowledgeDocument.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/system-chat/knowledge-bases/:id/documents
  → [authenticate → requireSystemAdmin]
  → handler  services/ai/src/routes/system-chat.ts:116
      → db read   prisma.knowledgeBase.findFirst
      → db write  prisma.knowledgeDocument.create
```

### `DELETE /api/system-chat/knowledge-bases/:id/documents/:docId`

- **Purpose:** Deletes the referenced document. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/system-chat.ts` line 195
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/system-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/system-chat.ts`.
2. **Handler.** `services/ai/src/routes/system-chat.ts:195` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeDocument.findFirst`
   _Writes:_
   - `knowledgeDocument.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/system-chat/knowledge-bases/:id/documents/:docId
  → [authenticate → requireSystemAdmin]
  → handler  services/ai/src/routes/system-chat.ts:195
      → db read   prisma.knowledgeDocument.findFirst
      → db write  prisma.knowledgeDocument.delete
```

### `POST /api/system-chat/knowledge-bases/:id/documents/:docId/process`

- **Purpose:** Performs the `process` action on the referenced document. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/system-chat.ts` line 211
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/system-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/system-chat.ts`.
2. **Handler.** `services/ai/src/routes/system-chat.ts:211` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeDocument.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/system-chat/knowledge-bases/:id/documents/:docId/process
  → [authenticate → requireSystemAdmin]
  → handler  services/ai/src/routes/system-chat.ts:211
      → db read   prisma.knowledgeDocument.findFirst
```

### `POST /api/system-chat/knowledge-bases/:id/documents/upload`

- **Purpose:** Creates an upload for the referenced document. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/system-chat.ts` line 144
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** `multer` handles a `multipart/form-data` upload. No Zod schema.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/system-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/system-chat.ts`.
2. **Handler.** `services/ai/src/routes/system-chat.ts:144` - inline `async (req, res)` handler.
3. **Validation.** `multer` handles a `multipart/form-data` upload. No Zod schema.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeBase.findFirst`
   _Writes:_
   - `knowledgeDocument.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/system-chat/knowledge-bases/:id/documents/upload
  → [authenticate → requireSystemAdmin]
  → handler  services/ai/src/routes/system-chat.ts:144
      → db read   prisma.knowledgeBase.findFirst
      → db write  prisma.knowledgeDocument.create
```

### `GET /api/system-chat/token-usage`

- **Purpose:** NOTE: these endpoints now read from `usage_logs` (the authoritative source) filtered to `type = "ai_tokens"`, using the denormalized `feature`, `model`, `promptTokens`, `completionTokens` columns added in the 20260414 migration. The legacy `token_logs` table is retired - nothing writes to it.
- **Handler:** `services/ai/src/routes/system-chat.ts` line 286
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/system-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/system-chat.ts`.
2. **Handler.** `services/ai/src/routes/system-chat.ts:286` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `usageLog.aggregate`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system-chat/token-usage
  → [authenticate → requireSystemAdmin]
  → handler  services/ai/src/routes/system-chat.ts:286
      → db read   prisma.usageLog.aggregate
```

### `GET /api/system-chat/token-usage/tenants`

- **Purpose:** Lists tenants. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/system-chat.ts` line 394
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/system-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/system-chat.ts`.
2. **Handler.** `services/ai/src/routes/system-chat.ts:394` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `usageLog.groupBy`
   - `tenant.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system-chat/token-usage/tenants
  → [authenticate → requireSystemAdmin]
  → handler  services/ai/src/routes/system-chat.ts:394
      → db read   prisma.usageLog.groupBy
      → db read   prisma.tenant.findMany
```

### `GET /api/tool-permissions`

- **Purpose:** Lists tool permissions. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/tool-permissions.ts` line 77
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `ai:tools:read` **or** role `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/tool-permissions` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/tool-permissions.ts`.
2. **Handler.** `services/ai/src/routes/tool-permissions.ts:77` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `ai:tools:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/tool-permissions
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/tool-permissions.ts:77
      → (responds directly; no downstream calls detected)
```

### `PUT /api/tool-permissions/:toolName`

- **Purpose:** Updates the referenced tool permission. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/tool-permissions.ts` line 282
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `ai:tools:read` **or** role `ADMIN`; `ai:tools:manage` **or** role `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/tool-permissions` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/tool-permissions.ts`.
2. **Handler.** `services/ai/src/routes/tool-permissions.ts:282` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `ai:tools:read` **or** role `ADMIN`; `ai:tools:manage` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/tool-permissions/:toolName
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/tool-permissions.ts:282
      → (responds directly; no downstream calls detected)
```

### `GET /api/tool-policy-migration`

- **Purpose:** Lists tool policy migration. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/tool-policy-migration.ts` line 157
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `ai:tools:read` **or** role `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/tool-policy-migration` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/tool-policy-migration.ts`.
2. **Handler.** `services/ai/src/routes/tool-policy-migration.ts:157` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `ai:tools:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/tool-policy-migration
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/tool-policy-migration.ts:157
      → (responds directly; no downstream calls detected)
```

### `POST /api/tool-policy-migration/apply`

- **Purpose:** Creates an apply. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/tool-policy-migration.ts` line 168
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `ai:tools:read` **or** role `ADMIN`; `ai:tools:manage` **or** role `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/tool-policy-migration` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/tool-policy-migration.ts`.
2. **Handler.** `services/ai/src/routes/tool-policy-migration.ts:168` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `ai:tools:read` **or** role `ADMIN`; `ai:tools:manage` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenantTool.findFirst`
   _Writes:_
   - `tenantToolPermission.updateMany`
   - `tenantTool.updateMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/tool-policy-migration/apply
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/tool-policy-migration.ts:168
      → db read   prisma.tenantTool.findFirst
      → db write  prisma.tenantToolPermission.updateMany
      → db write  prisma.tenantTool.updateMany
```

### `GET /api/tools`

- **Purpose:** GET / - List all active TenantTools for the tenant (with catalog info)
- **Handler:** `services/ai/src/routes/tools.ts` line 9
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/tools` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/tools.ts`.
2. **Handler.** `services/ai/src/routes/tools.ts:9` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenantTool.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/tools
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/tools.ts:9
      → db read   prisma.tenantTool.findMany
```

### `GET /api/tools/permissions/:departmentId`

- **Purpose:** GET /permissions/:departmentId - Get tool permissions for a department
- **Handler:** `services/ai/src/routes/tools.ts` line 31
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/tools` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/tools.ts`.
2. **Handler.** `services/ai/src/routes/tools.ts:31` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findFirst`
   - `tenantTool.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/tools/permissions/:departmentId
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/tools.ts:31
      → db read   prisma.department.findFirst
      → db read   prisma.tenantTool.findMany
```

### `PUT /api/tools/permissions/:departmentId`

- **Purpose:** PUT /permissions/:departmentId - Batch update tool permissions for a department
- **Handler:** `services/ai/src/routes/tools.ts` line 63
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/tools` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/tools.ts`.
2. **Handler.** `services/ai/src/routes/tools.ts:63` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findFirst`
   _Writes:_
   - `agentToolPermission.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/tools/permissions/:departmentId
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/tools.ts:63
      → db read   prisma.department.findFirst
      → db write  prisma.agentToolPermission.upsert
```

## Analytics

21 endpoints.

### `POST /api/admin/analytics/backfill`

- **Purpose:** Backfill aggregates for conversations that closed before aggregation existed.
- **Handler:** `services/billing/src/routes/admin-analytics.ts` line 148
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-analytics.ts`.
2. **Handler.** `services/billing/src/routes/admin-analytics.ts:148` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversationUsageAggregate.findMany`
   - `conversation.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/analytics/backfill
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-analytics.ts:148
      → db read   prisma.conversationUsageAggregate.findMany
      → db read   prisma.conversation.findMany
```

### `GET /api/admin/analytics/conversation-costs`

- **Purpose:** Global or filtered actual conversation cost.
- **Handler:** `services/billing/src/routes/admin-analytics.ts` line 62
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-analytics.ts`.
2. **Handler.** `services/billing/src/routes/admin-analytics.ts:62` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/analytics/conversation-costs
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-analytics.ts:62
      → (responds directly; no downstream calls detected)
```

### `GET /api/admin/analytics/conversation-costs/by-tenant`

- **Purpose:** Per-organization breakdown. Each row is that organization's own weighted average.
- **Handler:** `services/billing/src/routes/admin-analytics.ts` line 83
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-analytics.ts`.
2. **Handler.** `services/billing/src/routes/admin-analytics.ts:83` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/analytics/conversation-costs/by-tenant
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-analytics.ts:83
      → db read   prisma.tenant.findMany
```

### `GET /api/admin/analytics/estimate-vs-actual`

- **Purpose:** Configured public estimate vs actual internal average.
- **Handler:** `services/billing/src/routes/admin-analytics.ts` line 106
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-analytics.ts`.
2. **Handler.** `services/billing/src/routes/admin-analytics.ts:106` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/analytics/estimate-vs-actual
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-analytics.ts:106
      → (responds directly; no downstream calls detected)
```

### `POST /api/admin/analytics/settle`

- **Purpose:** Force settlement of due conversations (ops). Idempotent.
- **Handler:** `services/billing/src/routes/admin-analytics.ts` line 137
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-analytics.ts`.
2. **Handler.** `services/billing/src/routes/admin-analytics.ts:137` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/analytics/settle
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-analytics.ts:137
      → (responds directly; no downstream calls detected)
```

### `GET /api/analytics/agents`

- **Purpose:** Lists agents. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/analytics/src/routes/analytics.ts` line 50
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `analytics` service mounts this router at `/api/analytics` in `services/analytics/src/index.ts`; the route is declared in `services/analytics/src/routes/analytics.ts`.
2. **Handler.** `services/analytics/src/routes/analytics.ts:50` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `analyticsService.getAgentStats()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/analytics/agents
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/analytics/src/routes/analytics.ts:50
      → analyticsService.getAgentStats()
```

### `GET /api/analytics/ai-performance`

- **Purpose:** Lists ai performance. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/analytics/src/routes/analytics.ts` line 153
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `analytics` service mounts this router at `/api/analytics` in `services/analytics/src/index.ts`; the route is declared in `services/analytics/src/routes/analytics.ts`.
2. **Handler.** `services/analytics/src/routes/analytics.ts:153` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `analyticsService.getAIPerformance()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/analytics/ai-performance
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/analytics/src/routes/analytics.ts:153
      → analyticsService.getAIPerformance()
```

### `GET /api/analytics/channel-performance`

- **Purpose:** Lists channel performance. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/analytics/src/routes/analytics.ts` line 129
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `analytics` service mounts this router at `/api/analytics` in `services/analytics/src/index.ts`; the route is declared in `services/analytics/src/routes/analytics.ts`.
2. **Handler.** `services/analytics/src/routes/analytics.ts:129` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `analyticsService.getChannelPerformance()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/analytics/channel-performance
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/analytics/src/routes/analytics.ts:129
      → analyticsService.getChannelPerformance()
```

### `GET /api/analytics/daily`

- **Purpose:** Lists daily. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/analytics/src/routes/analytics.ts` line 71
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `analytics` service mounts this router at `/api/analytics` in `services/analytics/src/index.ts`; the route is declared in `services/analytics/src/routes/analytics.ts`.
2. **Handler.** `services/analytics/src/routes/analytics.ts:71` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `analyticsService.getDailyVolume()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/analytics/daily
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/analytics/src/routes/analytics.ts:71
      → analyticsService.getDailyVolume()
```

### `GET /api/analytics/dashboard`

- **Purpose:** Lists dashboard. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/analytics/src/routes/analytics.ts` line 40
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `analytics` service mounts this router at `/api/analytics` in `services/analytics/src/index.ts`; the route is declared in `services/analytics/src/routes/analytics.ts`.
2. **Handler.** `services/analytics/src/routes/analytics.ts:40` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `analyticsService.getDashboardStats()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/analytics/dashboard
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/analytics/src/routes/analytics.ts:40
      → analyticsService.getDashboardStats()
```

### `GET /api/analytics/department-performance`

- **Purpose:** Lists department performance. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/analytics/src/routes/analytics.ts` line 141
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `analytics` service mounts this router at `/api/analytics` in `services/analytics/src/index.ts`; the route is declared in `services/analytics/src/routes/analytics.ts`.
2. **Handler.** `services/analytics/src/routes/analytics.ts:141` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `analyticsService.getDepartmentPerformance()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/analytics/department-performance
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/analytics/src/routes/analytics.ts:141
      → analyticsService.getDepartmentPerformance()
```

### `POST /api/analytics/events`

- **Purpose:** Creates an event. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/analytics/src/routes/analytics.ts` line 16
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `analytics` service mounts this router at `/api/analytics` in `services/analytics/src/index.ts`; the route is declared in `services/analytics/src/routes/analytics.ts`.
2. **Handler.** `services/analytics/src/routes/analytics.ts:16` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/analytics/events
  → [authenticate → resolveTenant]
  → handler  services/analytics/src/routes/analytics.ts:16
      → (responds directly; no downstream calls detected)
```

### `GET /api/analytics/hourly`

- **Purpose:** Lists hourly. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/analytics/src/routes/analytics.ts` line 60
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `analytics` service mounts this router at `/api/analytics` in `services/analytics/src/index.ts`; the route is declared in `services/analytics/src/routes/analytics.ts`.
2. **Handler.** `services/analytics/src/routes/analytics.ts:60` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `analyticsService.getHourlyVolume()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/analytics/hourly
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/analytics/src/routes/analytics.ts:60
      → analyticsService.getHourlyVolume()
```

### `GET /api/analytics/insights`

- **Purpose:** Lists insights. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/analytics/src/routes/analytics.ts` line 165
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `analytics` service mounts this router at `/api/analytics` in `services/analytics/src/index.ts`; the route is declared in `services/analytics/src/routes/analytics.ts`.
2. **Handler.** `services/analytics/src/routes/analytics.ts:165` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `analyticsService.getAIInsights()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/analytics/insights
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/analytics/src/routes/analytics.ts:165
      → analyticsService.getAIInsights()
```

### `GET /api/analytics/overview`

- **Purpose:** Lists overview. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/analytics/src/routes/analytics.ts` line 92
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `analytics` service mounts this router at `/api/analytics` in `services/analytics/src/index.ts`; the route is declared in `services/analytics/src/routes/analytics.ts`.
2. **Handler.** `services/analytics/src/routes/analytics.ts:92` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `analyticsService.getOverview()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/analytics/overview
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/analytics/src/routes/analytics.ts:92
      → analyticsService.getOverview()
```

### `GET /api/analytics/queue`

- **Purpose:** Lists queue. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/analytics/src/routes/analytics.ts` line 82
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `analytics` service mounts this router at `/api/analytics` in `services/analytics/src/index.ts`; the route is declared in `services/analytics/src/routes/analytics.ts`.
2. **Handler.** `services/analytics/src/routes/analytics.ts:82` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `analyticsService.getQueueDepth()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/analytics/queue
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/analytics/src/routes/analytics.ts:82
      → analyticsService.getQueueDepth()
```

### `GET /api/analytics/tool-usage`

- **Purpose:** Lists tool usage. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/analytics/src/routes/analytics.ts` line 117
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `analytics` service mounts this router at `/api/analytics` in `services/analytics/src/index.ts`; the route is declared in `services/analytics/src/routes/analytics.ts`.
2. **Handler.** `services/analytics/src/routes/analytics.ts:117` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `analyticsService.getToolUsageStats()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/analytics/tool-usage
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/analytics/src/routes/analytics.ts:117
      → analyticsService.getToolUsageStats()
```

### `GET /api/analytics/top-questions`

- **Purpose:** Lists top questions. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/analytics/src/routes/analytics.ts` line 104
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `analytics` service mounts this router at `/api/analytics` in `services/analytics/src/index.ts`; the route is declared in `services/analytics/src/routes/analytics.ts`.
2. **Handler.** `services/analytics/src/routes/analytics.ts:104` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `analyticsService.getTopQuestions()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/analytics/top-questions
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/analytics/src/routes/analytics.ts:104
      → analyticsService.getTopQuestions()
```

### `GET /api/usage/daily`

- **Purpose:** Lists daily. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/usage.ts` line 37
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/usage` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/usage.ts`.
2. **Handler.** `services/ai/src/routes/usage.ts:37` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/usage/daily
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/usage.ts:37
      → (responds directly; no downstream calls detected)
```

### `GET /api/usage/logs`

- **Purpose:** Lists logs. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/usage.ts` line 69
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/usage` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/usage.ts`.
2. **Handler.** `services/ai/src/routes/usage.ts:69` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `usageLog.findMany`
   - `usageLog.count`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/usage/logs
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/usage.ts:69
      → db read   prisma.usageLog.findMany
      → db read   prisma.usageLog.count
```

### `GET /api/usage/stats`

- **Purpose:** Lists stats. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/usage.ts` line 8
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/usage` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/usage.ts`.
2. **Handler.** `services/ai/src/routes/usage.ts:8` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `usageLog.groupBy`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/usage/stats
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/usage.ts:8
      → db read   prisma.usageLog.groupBy
```

## Auth & Identity

36 endpoints.

### `GET /api/account`

- **Purpose:** Lists account. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/account.ts` line 57
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/account` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/account.ts`.
2. **Handler.** `services/auth/src/routes/account.ts:57` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findUnique`
   - `departmentMember.findFirst`
   - `tenant.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/account
  → [authenticate]
  → handler  services/auth/src/routes/account.ts:57
      → db read   prisma.user.findUnique
      → db read   prisma.departmentMember.findFirst
      → db read   prisma.tenant.findUnique
```

### `PATCH /api/account`

- **Purpose:** Update own profile: name, phone, locale. (Email is a verified flow - separate.)
- **Handler:** `services/auth/src/routes/account.ts` line 84
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/account` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/account.ts`.
2. **Handler.** `services/auth/src/routes/account.ts:84` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findUnique`
   _Writes:_
   - `user.update`
   - `identity.update`
   - `user.updateMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/account
  → [authenticate]
  → handler  services/auth/src/routes/account.ts:84
      → db read   prisma.user.findUnique
      → db write  prisma.user.update
      → db write  prisma.identity.update
      → db write  prisma.user.updateMany
```

### `POST /api/account/email-change`

- **Purpose:** Creates an email change. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/account.ts` line 402
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/account` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/account.ts`.
2. **Handler.** `services/auth/src/routes/account.ts:402` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findUnique`
   - `identity.findFirst`
   - `user.findMany`
   - `user.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/account/email-change
  → [authenticate]
  → handler  services/auth/src/routes/account.ts:402
      → db read   prisma.user.findUnique
      → db read   prisma.identity.findFirst
      → db read   prisma.user.findMany
      → db read   prisma.user.findFirst
```

### `POST /api/account/email-change/verify`

- **Purpose:** Creates a verify. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/account.ts` line 448
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/account` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/account.ts`.
2. **Handler.** `services/auth/src/routes/account.ts:448` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findUnique`
   - `identity.findFirst`
   - `user.findMany`
   - `user.findFirst`
   _Writes:_
   - `identity.update`
   - `user.updateMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, `404`, `409`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/account/email-change/verify
  → [authenticate]
  → handler  services/auth/src/routes/account.ts:448
      → db read   prisma.user.findUnique
      → db read   prisma.identity.findFirst
      → db read   prisma.user.findMany
      → db read   prisma.user.findFirst
      → db write  prisma.identity.update
      → db write  prisma.user.updateMany
```

### `GET /api/account/login-history`

- **Purpose:** Lists login history. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/account.ts` line 363
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/account` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/account.ts`.
2. **Handler.** `services/auth/src/routes/account.ts:363` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/account/login-history
  → [authenticate]
  → handler  services/auth/src/routes/account.ts:363
      → (responds directly; no downstream calls detected)
```

### `GET /api/account/mfa-gate`

- **Purpose:** Is this caller required to have MFA, and have they enrolled?
- **Handler:** `services/auth/src/routes/account.ts` line 176
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/account` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/account.ts`.
2. **Handler.** `services/auth/src/routes/account.ts:176` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findUnique`
   _Writes:_
   - `identity.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/account/mfa-gate
  → [authenticate]
  → handler  services/auth/src/routes/account.ts:176
      → db read   prisma.user.findUnique
      → db write  prisma.identity.update
```

### `POST /api/account/password-link`

- **Purpose:** One-time link into Authentik's password-change flow, for the caller.
- **Handler:** `services/auth/src/routes/account.ts` line 149
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/account` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/account.ts`.
2. **Handler.** `services/auth/src/routes/account.ts:149` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `409`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/account/password-link
  → [authenticate]
  → handler  services/auth/src/routes/account.ts:149
      → (responds directly; no downstream calls detected)
```

### `GET /api/account/security`

- **Purpose:** Lists security. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/account.ts` line 136
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/account` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/account.ts`.
2. **Handler.** `services/auth/src/routes/account.ts:136` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/account/security
  → [authenticate]
  → handler  services/auth/src/routes/account.ts:136
      → (responds directly; no downstream calls detected)
```

### `DELETE /api/account/security/device/:type/:id`

- **Purpose:** Remove one of the caller's own authenticator devices (TOTP / passkey / recovery set).
- **Handler:** `services/auth/src/routes/account.ts` line 262
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/account` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/account.ts`.
2. **Handler.** `services/auth/src/routes/account.ts:262` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findUnique`
   _Writes:_
   - `identity.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/account/security/device/:type/:id
  → [authenticate]
  → handler  services/auth/src/routes/account.ts:262
      → db read   prisma.user.findUnique
      → db write  prisma.identity.update
```

### `DELETE /api/account/sessions`

- **Purpose:** Deletes the referenced session. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/account.ts` line 333
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/account` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/account.ts`.
2. **Handler.** `services/auth/src/routes/account.ts:333` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `409`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/account/sessions
  → [authenticate]
  → handler  services/auth/src/routes/account.ts:333
      → (responds directly; no downstream calls detected)
```

### `GET /api/account/sessions`

- **Purpose:** Lists sessions. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/account.ts` line 297
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/account` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/account.ts`.
2. **Handler.** `services/auth/src/routes/account.ts:297` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/account/sessions
  → [authenticate]
  → handler  services/auth/src/routes/account.ts:297
      → (responds directly; no downstream calls detected)
```

### `DELETE /api/account/sessions/:id`

- **Purpose:** Deletes the referenced session. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/account.ts` line 314
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/account` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/account.ts`.
2. **Handler.** `services/auth/src/routes/account.ts:314` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/account/sessions/:id
  → [authenticate]
  → handler  services/auth/src/routes/account.ts:314
      → (responds directly; no downstream calls detected)
```

### `GET /api/auth/callback`

- **Purpose:** GET /api/auth/callback → complete the flow, create the session, set the cookie.
- **Handler:** `services/auth/src/routes/session-auth.ts` line 83
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/auth` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/session-auth.ts`.
2. **Handler.** `services/auth/src/routes/session-auth.ts:83` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `oidcLoginState.findUnique`
   - `identity.findUnique`
   _Writes:_
   - `oidcLoginState.update`
   - `userSession.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
GET /api/auth/callback
  → handler  services/auth/src/routes/session-auth.ts:83
      → db read   prisma.oidcLoginState.findUnique
      → db read   prisma.identity.findUnique
      → db write  prisma.oidcLoginState.update
      → db write  prisma.userSession.create
```

### `GET /api/auth/login`

- **Purpose:** GET /api/auth/login → start the server-side Authorization-Code + PKCE flow.
- **Handler:** `services/auth/src/routes/session-auth.ts` line 52
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/auth` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/session-auth.ts`.
2. **Handler.** `services/auth/src/routes/session-auth.ts:52` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `oidcLoginState.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
GET /api/auth/login
  → handler  services/auth/src/routes/session-auth.ts:52
      → db write  prisma.oidcLoginState.create
```

### `POST /api/auth/logout`

- **Purpose:** POST /api/auth/logout → revoke the current session + clear the cookie. (Minimal here; logout-all / session-list / IdP end-session are commit 9.)
- **Handler:** `services/auth/src/routes/session-auth.ts` line 152
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/auth` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/session-auth.ts`.
2. **Handler.** `services/auth/src/routes/session-auth.ts:152` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `userSession.updateMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
POST /api/auth/logout
  → handler  services/auth/src/routes/session-auth.ts:152
      → db write  prisma.userSession.updateMany
```

### `GET /api/auth/me`

- **Purpose:** Lists me. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/auth.ts` line 51
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/auth` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/auth.ts`.
2. **Handler.** `services/auth/src/routes/auth.ts:51` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findUnique`
   - `departmentMember.findFirst`
   - `tenant.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/auth/me
  → [authenticate]
  → handler  services/auth/src/routes/auth.ts:51
      → db read   prisma.user.findUnique
      → db read   prisma.departmentMember.findFirst
      → db read   prisma.tenant.findUnique
```

### `GET /api/auth/me/memberships`

- **Purpose:** The caller's memberships, standalone - used by the post-login tenant picker BEFORE an active tenant is chosen. authenticate() resolves a default membership even without an X-Tenant-Id header, so this endpoint is reachable exactly when the picker needs it.
- **Handler:** `services/auth/src/routes/auth.ts` line 116
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/auth` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/auth.ts`.
2. **Handler.** `services/auth/src/routes/auth.ts:116` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/auth/me/memberships
  → [authenticate]
  → handler  services/auth/src/routes/auth.ts:116
      → db read   prisma.user.findUnique
```

### `POST /api/auth/me/switch-tenant`

- **Purpose:** Switch the ACTIVE tenant. Validates the target against the caller's memberships (never trusts the id), stamps last-used so future hint-less requests and the picker default follow the person, and returns the target membership. The client then re-issues requests with X-Tenant-Id set - no re-login: the Authentik session/token identifies the PERSON, not the tenant.
- **Handler:** `services/auth/src/routes/auth.ts` line 144
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/auth` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/auth.ts`.
2. **Handler.** `services/auth/src/routes/auth.ts:144` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findUnique`
   _Writes:_
   - `user.update`
   - `identity.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/auth/me/switch-tenant
  → [authenticate]
  → handler  services/auth/src/routes/auth.ts:144
      → db read   prisma.user.findUnique
      → db write  prisma.user.update
      → db write  prisma.identity.update
```

### `GET /api/auth/setup/:token`

- **Purpose:** Redeem a setup link.
- **Handler:** `services/auth/src/routes/setup-link.ts` line 55
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/auth` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/setup-link.ts`.
2. **Handler.** `services/auth/src/routes/setup-link.ts:55` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `429` from `redeemLimiter`.

#### Call graph

```text
GET /api/auth/setup/:token
  → handler  services/auth/src/routes/setup-link.ts:55
      → (responds directly; no downstream calls detected)
```

### `POST /api/auth/setup/resend`

- **Purpose:** Ask for a replacement link.
- **Handler:** `services/auth/src/routes/setup-link.ts` line 117
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/auth` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/setup-link.ts`.
2. **Handler.** `services/auth/src/routes/setup-link.ts:117` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `429` from `resendLimiter`.

#### Call graph

```text
POST /api/auth/setup/resend
  → handler  services/auth/src/routes/setup-link.ts:117
      → (responds directly; no downstream calls detected)
```

### `GET /api/permissions/catalog`

- **Purpose:** Drives the User Management UI: the permission picker + built-in roles + scope options. Hierarchical (feature:sub-feature:action), grouped by domain.
- **Handler:** `services/auth/src/routes/permissions.ts` line 479
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/permissions` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/permissions.ts`.
2. **Handler.** `services/auth/src/routes/permissions.ts:479` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/permissions/catalog
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/permissions.ts:479
      → (responds directly; no downstream calls detected)
```

### `GET /api/permissions/features`

- **Purpose:** Lists features. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/permissions.ts` line 470
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/permissions` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/permissions.ts`.
2. **Handler.** `services/auth/src/routes/permissions.ts:470` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/permissions/features
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/permissions.ts:470
      → (responds directly; no downstream calls detected)
```

### `GET /api/permissions/me`

- **Purpose:** Returns the canonical hierarchical permission set + effective scope (the new RBAC surface the frontend reacts to) AND the legacy feature list (kept for back-compat with existing callers).
- **Handler:** `services/auth/src/routes/permissions.ts` line 84
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/permissions` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/permissions.ts`.
2. **Handler.** `services/auth/src/routes/permissions.ts:84` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/permissions/me
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/permissions.ts:84
      → (responds directly; no downstream calls detected)
```

### `GET /api/permissions/roles`

- **Purpose:** Lists roles. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/permissions.ts` line 268
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/permissions` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/permissions.ts`.
2. **Handler.** `services/auth/src/routes/permissions.ts:268` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenantRole.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/permissions/roles
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/permissions.ts:268
      → db read   prisma.tenantRole.findMany
```

### `POST /api/permissions/roles`

- **Purpose:** Creates a role. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/permissions.ts` line 284
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(createRoleSchema)` (Zod). Accepts: `name`, `description`, `features`. Required: `name`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/permissions` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/permissions.ts`.
2. **Handler.** `services/auth/src/routes/permissions.ts:284` - inline `async (req, res)` handler.
3. **Validation.** `validate(createRoleSchema)` (Zod). Accepts: `name`, `description`, `features`. Required: `name`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `tenantRole.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `409`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/permissions/roles
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(createRoleSchema)]
  → handler  services/auth/src/routes/permissions.ts:284
      → db write  prisma.tenantRole.create
```

### `DELETE /api/permissions/roles/:roleId`

- **Purpose:** Deletes the referenced role. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/permissions.ts` line 358
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/permissions` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/permissions.ts`.
2. **Handler.** `services/auth/src/routes/permissions.ts:358` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `tenantRole.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `204`, answering the `{ data: ... }` envelope. Error paths: `403`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/permissions/roles/:roleId
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/permissions.ts:358
      → db write  prisma.tenantRole.delete
```

### `PATCH /api/permissions/roles/:roleId`

- **Purpose:** Updates the referenced role. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/permissions.ts` line 326
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(updateRoleSchema)` (Zod). Accepts: `name`, `description`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/permissions` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/permissions.ts`.
2. **Handler.** `services/auth/src/routes/permissions.ts:326` - inline `async (req, res)` handler.
3. **Validation.** `validate(updateRoleSchema)` (Zod). Accepts: `name`, `description`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `tenantRole.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `403`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/permissions/roles/:roleId
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(updateRoleSchema)]
  → handler  services/auth/src/routes/permissions.ts:326
      → db write  prisma.tenantRole.update
```

### `PUT /api/permissions/roles/:roleId/features`

- **Purpose:** Updates the referenced feature for the referenced role. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/permissions.ts` line 386
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(replaceFeaturesSchema)` (Zod). Accepts: `features`. Required: `features`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/permissions` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/permissions.ts`.
2. **Handler.** `services/auth/src/routes/permissions.ts:386` - inline `async (req, res)` handler.
3. **Validation.** `validate(replaceFeaturesSchema)` (Zod). Accepts: `features`. Required: `features`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenantRole.findUnique`
   _Writes:_
   - `tenantRoleFeature.deleteMany`
   - `tenantRoleFeature.createMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/permissions/roles/:roleId/features
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(replaceFeaturesSchema)]
  → handler  services/auth/src/routes/permissions.ts:386
      → db read   prisma.tenantRole.findUnique
      → db write  prisma.tenantRoleFeature.deleteMany
      → db write  prisma.tenantRoleFeature.createMany
```

### `GET /api/permissions/users`

- **Purpose:** The unified User Management list. Unlike /api/agents (agent-only), this returns every member with the new built-in role assignment.
- **Handler:** `services/auth/src/routes/permissions.ts` line 111
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/permissions` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/permissions.ts`.
2. **Handler.** `services/auth/src/routes/permissions.ts:111` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/permissions/users
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/permissions.ts:111
      → db read   prisma.user.findMany
```

### `GET /api/permissions/users/:userId`

- **Purpose:** Reads a single user. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/permissions.ts` line 162
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/permissions` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/permissions.ts`.
2. **Handler.** `services/auth/src/routes/permissions.ts:162` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/permissions/users/:userId
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/permissions.ts:162
      → db read   prisma.user.findFirst
```

### `GET /api/permissions/users/:userId/grants`

- **Purpose:** Reads a single grant for the referenced user. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/permissions.ts` line 202
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/permissions` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/permissions.ts`.
2. **Handler.** `services/auth/src/routes/permissions.ts:202` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `userFeatureGrant.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/permissions/users/:userId/grants
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/permissions.ts:202
      → db read   prisma.userFeatureGrant.findMany
```

### `DELETE /api/permissions/users/:userId/grants/:feature`

- **Purpose:** Deletes the referenced grant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/permissions.ts` line 248
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/permissions` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/permissions.ts`.
2. **Handler.** `services/auth/src/routes/permissions.ts:248` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `userFeatureGrant.deleteMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `204`, answering the `{ data: ... }` envelope. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/permissions/users/:userId/grants/:feature
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/permissions.ts:248
      → db write  prisma.userFeatureGrant.deleteMany
```

### `PUT /api/permissions/users/:userId/grants/:feature`

- **Purpose:** Updates the referenced grant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/permissions.ts` line 218
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(upsertGrantSchema)` (Zod). Accepts: `granted`, `reason`. Required: `granted`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/permissions` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/permissions.ts`.
2. **Handler.** `services/auth/src/routes/permissions.ts:218` - inline `async (req, res)` handler.
3. **Validation.** `validate(upsertGrantSchema)` (Zod). Accepts: `granted`, `reason`. Required: `granted`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `userFeatureGrant.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/permissions/users/:userId/grants/:feature
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(upsertGrantSchema)]
  → handler  services/auth/src/routes/permissions.ts:218
      → db write  prisma.userFeatureGrant.upsert
```

### `PUT /api/permissions/users/:userId/role`

- **Purpose:** Updates the referenced role for the referenced user. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/permissions.ts` line 499
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(setUserRoleSchema)` (Zod). Accepts: `roleId`, `scope`. Required: `roleId`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/permissions` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/permissions.ts`.
2. **Handler.** `services/auth/src/routes/permissions.ts:499` - inline `async (req, res)` handler.
3. **Validation.** `validate(setUserRoleSchema)` (Zod). Accepts: `roleId`, `scope`. Required: `roleId`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `userRoleAssignment.findFirst`
   _Writes:_
   - `userRoleAssignment.deleteMany`
   - `userRoleAssignment.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/permissions/users/:userId/role
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(setUserRoleSchema)]
  → handler  services/auth/src/routes/permissions.ts:499
      → db read   prisma.userRoleAssignment.findFirst
      → db write  prisma.userRoleAssignment.deleteMany
      → db write  prisma.userRoleAssignment.create
```

### `DELETE /api/permissions/users/:userId/roles/:roleId`

- **Purpose:** Deletes the referenced role. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/permissions.ts` line 450
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/permissions` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/permissions.ts`.
2. **Handler.** `services/auth/src/routes/permissions.ts:450` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `userRoleAssignment.deleteMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `204`, answering the `{ data: ... }` envelope. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/permissions/users/:userId/roles/:roleId
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/permissions.ts:450
      → db write  prisma.userRoleAssignment.deleteMany
```

### `POST /api/permissions/users/:userId/roles/:roleId`

- **Purpose:** Creates a role. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/permissions.ts` line 422
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/permissions` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/permissions.ts`.
2. **Handler.** `services/auth/src/routes/permissions.ts:422` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `userRoleAssignment.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/permissions/users/:userId/roles/:roleId
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/permissions.ts:422
      → db write  prisma.userRoleAssignment.upsert
```

## Billing

88 endpoints.

### `GET /api/admin/billing/enforcement-preview`

- **Purpose:** Who would stop being served if enforcement were switched on.
- **Handler:** `services/billing/src/routes/admin-exchange-rates.ts` line 297
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** Platform staff gate: `requirePlatformAdmin`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-exchange-rates.ts`.
2. **Handler.** `services/billing/src/routes/admin-exchange-rates.ts:297` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** Platform staff gate: `requirePlatformAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/admin/billing/enforcement-preview
  → [requirePlatformAdmin]
  → handler  services/billing/src/routes/admin-exchange-rates.ts:297
      → (responds directly; no downstream calls detected)
```

### `GET /api/admin/billing/exchange-rates`

- **Purpose:** Current state plus history, so the effect of a change is visible before it.
- **Handler:** `services/billing/src/routes/admin-exchange-rates.ts` line 106
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** Platform staff gate: `requirePlatformAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-exchange-rates.ts`.
2. **Handler.** `services/billing/src/routes/admin-exchange-rates.ts:106` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** Platform staff gate: `requirePlatformAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
GET /api/admin/billing/exchange-rates
  → [requirePlatformAdmin]
  → handler  services/billing/src/routes/admin-exchange-rates.ts:106
      → (responds directly; no downstream calls detected)
```

### `POST /api/admin/billing/exchange-rates`

- **Purpose:** Propose a MANUAL override. Created as DRAFT: proposing changes nothing.
- **Handler:** `services/billing/src/routes/admin-exchange-rates.ts` line 142
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** Platform staff gate: `requirePlatformAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-exchange-rates.ts`.
2. **Handler.** `services/billing/src/routes/admin-exchange-rates.ts:142` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** Platform staff gate: `requirePlatformAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/admin/billing/exchange-rates
  → [requirePlatformAdmin]
  → handler  services/billing/src/routes/admin-exchange-rates.ts:142
      → (responds directly; no downstream calls detected)
```

### `POST /api/admin/billing/exchange-rates/:id/approve`

- **Purpose:** Approve a draft, making it THE rate.
- **Handler:** `services/billing/src/routes/admin-exchange-rates.ts` line 209
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** Platform staff gate: `requirePlatformAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-exchange-rates.ts`.
2. **Handler.** `services/billing/src/routes/admin-exchange-rates.ts:209` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** Platform staff gate: `requirePlatformAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
POST /api/admin/billing/exchange-rates/:id/approve
  → [requirePlatformAdmin]
  → handler  services/billing/src/routes/admin-exchange-rates.ts:209
      → (responds directly; no downstream calls detected)
```

### `POST /api/admin/billing/exchange-rates/:id/retire`

- **Purpose:** Retire a rate without replacing it.
- **Handler:** `services/billing/src/routes/admin-exchange-rates.ts` line 229
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** Platform staff gate: `requirePlatformAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-exchange-rates.ts`.
2. **Handler.** `services/billing/src/routes/admin-exchange-rates.ts:229` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** Platform staff gate: `requirePlatformAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
POST /api/admin/billing/exchange-rates/:id/retire
  → [requirePlatformAdmin]
  → handler  services/billing/src/routes/admin-exchange-rates.ts:229
      → (responds directly; no downstream calls detected)
```

### `POST /api/admin/billing/exchange-rates/preview`

- **Purpose:** Preview a conversion without saving anything.
- **Handler:** `services/billing/src/routes/admin-exchange-rates.ts` line 247
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** Platform staff gate: `requirePlatformAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-exchange-rates.ts`.
2. **Handler.** `services/billing/src/routes/admin-exchange-rates.ts:247` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** Platform staff gate: `requirePlatformAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/admin/billing/exchange-rates/preview
  → [requirePlatformAdmin]
  → handler  services/billing/src/routes/admin-exchange-rates.ts:247
      → (responds directly; no downstream calls detected)
```

### `POST /api/admin/billing/fx-refresh`

- **Purpose:** Fetch the official rate now.
- **Handler:** `services/billing/src/routes/admin-exchange-rates.ts` line 192
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** Platform staff gate: `requirePlatformAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-exchange-rates.ts`.
2. **Handler.** `services/billing/src/routes/admin-exchange-rates.ts:192` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** Platform staff gate: `requirePlatformAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
POST /api/admin/billing/fx-refresh
  → [requirePlatformAdmin]
  → handler  services/billing/src/routes/admin-exchange-rates.ts:192
      → (responds directly; no downstream calls detected)
```

### `GET /api/admin/billing/fx-status`

- **Purpose:** The official rate feed: what it says, how old it is, and whether charging is possible right now.
- **Handler:** `services/billing/src/routes/admin-exchange-rates.ts` line 177
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** Platform staff gate: `requirePlatformAdmin`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-exchange-rates.ts`.
2. **Handler.** `services/billing/src/routes/admin-exchange-rates.ts:177` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** Platform staff gate: `requirePlatformAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/admin/billing/fx-status
  → [requirePlatformAdmin]
  → handler  services/billing/src/routes/admin-exchange-rates.ts:177
      → (responds directly; no downstream calls detected)
```

### `GET /api/admin/billing/payment-readiness`

- **Purpose:** Lists payment readiness. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/admin-payment-readiness.ts` line 39
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-payment-readiness.ts`.
2. **Handler.** `services/billing/src/routes/admin-payment-readiness.ts:39` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/billing/payment-readiness
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-payment-readiness.ts:39
      → (responds directly; no downstream calls detected)
```

### `GET /api/admin/billing/reconciliations`

- **Purpose:** Charges nobody could settle automatically.
- **Handler:** `services/billing/src/routes/admin-exchange-rates.ts` line 269
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** Platform staff gate: `requirePlatformAdmin`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-exchange-rates.ts`.
2. **Handler.** `services/billing/src/routes/admin-exchange-rates.ts:269` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** Platform staff gate: `requirePlatformAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
GET /api/admin/billing/reconciliations
  → [requirePlatformAdmin]
  → handler  services/billing/src/routes/admin-exchange-rates.ts:269
      → (responds directly; no downstream calls detected)
```

### `POST /api/admin/billing/reconciliations/sweep`

- **Purpose:** Run the sweep now.
- **Handler:** `services/billing/src/routes/admin-exchange-rates.ts` line 280
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** Platform staff gate: `requirePlatformAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-exchange-rates.ts`.
2. **Handler.** `services/billing/src/routes/admin-exchange-rates.ts:280` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** Platform staff gate: `requirePlatformAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/admin/billing/reconciliations/sweep
  → [requirePlatformAdmin]
  → handler  services/billing/src/routes/admin-exchange-rates.ts:280
      → (responds directly; no downstream calls detected)
```

### `DELETE /api/admin/billing/shopify/grandfather/:tenantId`

- **Purpose:** Withdraw a grant. SYSTEM_ADMIN only, attributable, and never silent.
- **Handler:** `services/billing/src/routes/shopify-billing.ts` line 372
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/shopify-billing.ts`.
2. **Handler.** `services/billing/src/routes/shopify-billing.ts:372` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `401`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/admin/billing/shopify/grandfather/:tenantId
  → [authenticate → requireSystemAdmin]
  → handler  services/billing/src/routes/shopify-billing.ts:372
      → (responds directly; no downstream calls detected)
```

### `GET /api/admin/billing/shopify/grandfather/:tenantId`

- **Purpose:** What the rules say, without acting on it.
- **Handler:** `services/billing/src/routes/shopify-billing.ts` line 333
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/shopify-billing.ts`.
2. **Handler.** `services/billing/src/routes/shopify-billing.ts:333` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/billing/shopify/grandfather/:tenantId
  → [authenticate → requireSystemAdmin]
  → handler  services/billing/src/routes/shopify-billing.ts:333
      → (responds directly; no downstream calls detected)
```

### `POST /api/admin/billing/shopify/grandfather/:tenantId`

- **Purpose:** Grant grandfathering on an admin's authority.
- **Handler:** `services/billing/src/routes/shopify-billing.ts` line 354
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/shopify-billing.ts`.
2. **Handler.** `services/billing/src/routes/shopify-billing.ts:354` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `401`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/billing/shopify/grandfather/:tenantId
  → [authenticate → requireSystemAdmin]
  → handler  services/billing/src/routes/shopify-billing.ts:354
      → (responds directly; no downstream calls detected)
```

### `GET /api/admin/coupons`

- **Purpose:** Lists coupons. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/admin-coupons.ts` line 62
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-coupons.ts`.
2. **Handler.** `services/billing/src/routes/admin-coupons.ts:62` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/coupons
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-coupons.ts:62
      → (responds directly; no downstream calls detected)
```

### `POST /api/admin/coupons`

- **Purpose:** Creates a coupon. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/admin-coupons.ts` line 71
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-coupons.ts`.
2. **Handler.** `services/billing/src/routes/admin-coupons.ts:71` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/coupons
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-coupons.ts:71
      → (responds directly; no downstream calls detected)
```

### `POST /api/admin/coupons/:id/active`

- **Purpose:** Performs the `active` action on the referenced coupon. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/admin-coupons.ts` line 92
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-coupons.ts`.
2. **Handler.** `services/billing/src/routes/admin-coupons.ts:92` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/coupons/:id/active
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-coupons.ts:92
      → (responds directly; no downstream calls detected)
```

### `GET /api/admin/coupons/assignable-tenants`

- **Purpose:** Organizations to assign a coupon to, for the sysadmin picker. Name + id only: this is a chooser, not a tenant directory.
- **Handler:** `services/billing/src/routes/admin-coupons.ts` line 179
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-coupons.ts`.
2. **Handler.** `services/billing/src/routes/admin-coupons.ts:179` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/coupons/assignable-tenants
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-coupons.ts:179
      → db read   prisma.tenant.findMany
```

### `DELETE /api/admin/coupons/assignments/:assignmentId`

- **Purpose:** Deletes the referenced assignment. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/admin-coupons.ts` line 160
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-coupons.ts`.
2. **Handler.** `services/billing/src/routes/admin-coupons.ts:160` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/admin/coupons/assignments/:assignmentId
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-coupons.ts:160
      → (responds directly; no downstream calls detected)
```

### `GET /api/admin/coupons/tenants/:tenantId`

- **Purpose:** Every assignment for one organization, live ones flagged.
- **Handler:** `services/billing/src/routes/admin-coupons.ts` line 119
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-coupons.ts`.
2. **Handler.** `services/billing/src/routes/admin-coupons.ts:119` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/coupons/tenants/:tenantId
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-coupons.ts:119
      → (responds directly; no downstream calls detected)
```

### `POST /api/admin/coupons/tenants/:tenantId`

- **Purpose:** Creates a tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/admin-coupons.ts` line 128
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-coupons.ts`.
2. **Handler.** `services/billing/src/routes/admin-coupons.ts:128` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/coupons/tenants/:tenantId
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-coupons.ts:128
      → (responds directly; no downstream calls detected)
```

### `POST /api/admin/evaluation`

- **Purpose:** Provision evaluation access. Sysadmin only - there is no customer self-activation path anywhere in the system.
- **Handler:** `services/billing/src/routes/admin-analytics.ts` line 195
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-analytics.ts`.
2. **Handler.** `services/billing/src/routes/admin-analytics.ts:195` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/evaluation
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-analytics.ts:195
      → (responds directly; no downstream calls detected)
```

### `POST /api/admin/evaluation/convert`

- **Purpose:** Convert an evaluation into a paid plan. Manual, never automatic.
- **Handler:** `services/billing/src/routes/admin-analytics.ts` line 219
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-analytics.ts`.
2. **Handler.** `services/billing/src/routes/admin-analytics.ts:219` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/evaluation/convert
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-analytics.ts:219
      → (responds directly; no downstream calls detected)
```

### `GET /api/admin/evaluation/templates`

- **Purpose:** Lists templates. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/admin-analytics.ts` line 187
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-analytics.ts`.
2. **Handler.** `services/billing/src/routes/admin-analytics.ts:187` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/evaluation/templates
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-analytics.ts:187
      → (responds directly; no downstream calls detected)
```

### `GET /api/admin/pricing/currency`

- **Purpose:** Lists currency. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 910
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:910` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `fxRateSnapshot.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/pricing/currency
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:910
      → db read   prisma.fxRateSnapshot.findFirst
```

### `PUT /api/admin/pricing/currency`

- **Purpose:** Updates the referenced currency. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 922
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:922` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `pricingCurrencyConfig.findFirst`
   _Writes:_
   - `pricingCurrencyConfig.update`
   - `pricingCurrencyConfig.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/admin/pricing/currency
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:922
      → db read   prisma.pricingCurrencyConfig.findFirst
      → db write  prisma.pricingCurrencyConfig.update
      → db write  prisma.pricingCurrencyConfig.create
```

### `POST /api/admin/pricing/currency/refresh-fx`

- **Purpose:** Creates a refresh fx. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 944
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:944` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/pricing/currency/refresh-fx
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:944
      → (responds directly; no downstream calls detected)
```

### `POST /api/admin/pricing/custom-plans`

- **Purpose:** Build an organization-specific negotiated plan.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 959
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:959` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `plan.findFirst`
   _Writes:_
   - `plan.create`
   - `planEntitlement.create`
   - `publicEstimationConfig.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `422`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/pricing/custom-plans
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:959
      → db read   prisma.tenant.findUnique
      → db read   prisma.plan.findFirst
      → db write  prisma.plan.create
      → db write  prisma.planEntitlement.create
      → db write  prisma.publicEstimationConfig.create
```

### `POST /api/admin/pricing/custom-plans/:id/approve`

- **Purpose:** Approve and publish a custom plan so the organization can be moved onto it.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 1045
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:1045` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `plan.findUnique`
   - `plan.findFirst`
   _Writes:_
   - `plan.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/pricing/custom-plans/:id/approve
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:1045
      → db read   prisma.plan.findUnique
      → db read   prisma.plan.findFirst
      → db write  prisma.plan.update
```

### `GET /api/admin/pricing/estimation`

- **Purpose:** Lists estimation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 670
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:670` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `publicEstimationConfig.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/pricing/estimation
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:670
      → db read   prisma.publicEstimationConfig.findMany
```

### `POST /api/admin/pricing/estimation`

- **Purpose:** Publish a new estimation version.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 768
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:768` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `publicEstimationConfig.findFirst`
   _Writes:_
   - `publicEstimationConfig.update`
   - `publicEstimationConfig.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/pricing/estimation
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:768
      → db read   prisma.publicEstimationConfig.findFirst
      → db write  prisma.publicEstimationConfig.update
      → db write  prisma.publicEstimationConfig.create
```

### `POST /api/admin/pricing/estimation/preview`

- **Purpose:** Preview a proposed ratio BEFORE publishing it.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 695
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:695` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `plan.findMany`
   - `subscription.count`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/pricing/estimation/preview
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:695
      → db read   prisma.plan.findMany
      → db read   prisma.subscription.count
```

### `GET /api/admin/pricing/features`

- **Purpose:** The feature catalog, including which keys are catalogued but not built.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 183
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:183` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/pricing/features
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:183
      → (responds directly; no downstream calls detected)
```

### `GET /api/admin/pricing/packages`

- **Purpose:** Lists packages. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 814
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:814` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `creditPackage.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/pricing/packages
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:814
      → db read   prisma.creditPackage.findMany
```

### `DELETE /api/admin/pricing/packages/:key`

- **Purpose:** Remove a credit package from the catalog.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 877
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:877` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `creditPackage.findUnique`
   - `autoPurchasePolicy.count`
   - `aiUnitLot.count`
   _Writes:_
   - `creditPackage.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/admin/pricing/packages/:key
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:877
      → db read   prisma.creditPackage.findUnique
      → db read   prisma.autoPurchasePolicy.count
      → db read   prisma.aiUnitLot.count
      → db write  prisma.creditPackage.delete
```

### `PUT /api/admin/pricing/packages/:key`

- **Purpose:** Updates the referenced package. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 830
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:830` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `creditPackage.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/admin/pricing/packages/:key
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:830
      → db write  prisma.creditPackage.upsert
```

### `GET /api/admin/pricing/permissions`

- **Purpose:** Lists permissions. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 203
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:203` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/pricing/permissions
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:203
      → (responds directly; no downstream calls detected)
```

### `GET /api/admin/pricing/plans`

- **Purpose:** Lists plans. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 111
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:111` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `plan.findMany`
   - `subscription.groupBy`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/pricing/plans
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:111
      → db read   prisma.plan.findMany
      → db read   prisma.subscription.groupBy
```

### `POST /api/admin/pricing/plans`

- **Purpose:** Create a plan that does not exist yet, of a chosen kind.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 410
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:410` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `plan.findFirst`
   _Writes:_
   - `plan.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `409`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/pricing/plans
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:410
      → db read   prisma.plan.findFirst
      → db write  prisma.plan.create
```

### `DELETE /api/admin/pricing/plans/:id`

- **Purpose:** Discard a DRAFT plan version.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 372
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:372` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `plan.findUnique`
   - `subscription.count`
   _Writes:_
   - `plan.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/admin/pricing/plans/:id
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:372
      → db read   prisma.plan.findUnique
      → db read   prisma.subscription.count
      → db write  prisma.plan.delete
```

### `PATCH /api/admin/pricing/plans/:id`

- **Purpose:** Edit a DRAFT. An ACTIVE version is immutable by design.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 306
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:306` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `plan.findUnique`
   _Writes:_
   - `plan.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/admin/pricing/plans/:id
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:306
      → db read   prisma.plan.findUnique
      → db write  prisma.plan.update
```

### `PUT /api/admin/pricing/plans/:id/entitlements`

- **Purpose:** Set feature entitlements and numeric limits on a DRAFT.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 461
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:461` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `plan.findUnique`
   _Writes:_
   - `planEntitlement.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `422`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/admin/pricing/plans/:id/entitlements
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:461
      → db read   prisma.plan.findUnique
      → db write  prisma.planEntitlement.upsert
```

### `GET /api/admin/pricing/plans/:id/preview`

- **Purpose:** What publishing this draft would actually do.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 529
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:529` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `plan.findUnique`
   - `plan.findFirst`
   - `subscription.count`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/pricing/plans/:id/preview
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:529
      → db read   prisma.plan.findUnique
      → db read   prisma.plan.findFirst
      → db read   prisma.subscription.count
```

### `POST /api/admin/pricing/plans/:id/publish`

- **Purpose:** Publish a draft: it becomes the ACTIVE version, the previous one is RETIRED.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 591
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:591` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `plan.findUnique`
   - `plan.findFirst`
   _Writes:_
   - `plan.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/pricing/plans/:id/publish
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:591
      → db read   prisma.plan.findUnique
      → db read   prisma.plan.findFirst
      → db write  prisma.plan.update
```

### `PUT /api/admin/pricing/plans/:id/volume-options`

- **Purpose:** Create or update a volume option on a DRAFT.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 486
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:486` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `plan.findUnique`
   _Writes:_
   - `planVolumeOption.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/admin/pricing/plans/:id/volume-options
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:486
      → db read   prisma.plan.findUnique
      → db write  prisma.planVolumeOption.upsert
```

### `POST /api/admin/pricing/plans/:key/versions`

- **Purpose:** Create a new DRAFT version of a plan key, cloned from an existing version.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 215
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:215` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `plan.findFirst`
   _Writes:_
   - `plan.create`
   - `planEntitlement.create`
   - `planVolumeOption.create`
   - `publicEstimationConfig.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/pricing/plans/:key/versions
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:215
      → db read   prisma.plan.findFirst
      → db write  prisma.plan.create
      → db write  prisma.planEntitlement.create
      → db write  prisma.planVolumeOption.create
      → db write  prisma.publicEstimationConfig.create
```

### `POST /api/admin/pricing/plans/order`

- **Purpose:** Reorder the public catalog and set the recommended plan.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 627
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:627` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `plan.findFirst`
   _Writes:_
   - `plan.updateMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/pricing/plans/order
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:627
      → db read   prisma.plan.findFirst
      → db write  prisma.plan.updateMany
```

### `GET /api/admin/pricing/preview-customer`

- **Purpose:** Exactly what a customer of this organization would see on the pricing page.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 1067
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:1067` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/pricing/preview-customer
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:1067
      → (responds directly; no downstream calls detected)
```

### `GET /api/admin/pricing/provisioning/plans`

- **Purpose:** Nested under /admin/pricing so it inherits the existing gateway location.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 1085
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:1085` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `plan.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/admin/pricing/provisioning/plans
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:1085
      → db read   prisma.plan.findMany
```

### `POST /api/admin/pricing/provisioning/quote`

- **Purpose:** The authoritative commercial summary for a selection.
- **Handler:** `services/billing/src/routes/admin-pricing.ts` line 1125
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/admin` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/admin-pricing.ts`.
2. **Handler.** `services/billing/src/routes/admin-pricing.ts:1125` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/admin/pricing/provisioning/quote
  → [authenticate → requirePlatformPermission]
  → handler  services/billing/src/routes/admin-pricing.ts:1125
      → (responds directly; no downstream calls detected)
```

### `GET /api/billing/auto-purchase`

- **Purpose:** Lists auto purchase. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/credits.ts` line 210
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/credits.ts`.
2. **Handler.** `services/billing/src/routes/credits.ts:210` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `autoPurchasePolicy.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/billing/auto-purchase
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/credits.ts:210
      → db read   prisma.autoPurchasePolicy.findUnique
```

### `PUT /api/billing/auto-purchase`

- **Purpose:** Updates the referenced auto purchase. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/credits.ts` line 227
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/credits.ts`.
2. **Handler.** `services/billing/src/routes/credits.ts:227` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `plan.findUnique`
   _Writes:_
   - `autoPurchasePolicy.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `402`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/billing/auto-purchase
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/credits.ts:227
      → db read   prisma.plan.findUnique
      → db write  prisma.autoPurchasePolicy.upsert
```

### `GET /api/billing/credit-summary`

- **Purpose:** Canonical customer-facing credit contract - ONE source for every number the Usage/Billing UI shows. Credits (not model tokens) are the unit throughout. Composes: the subscription's included allowance, the real ledger balance (getBalance), and the auto-purchase money-spend window. The two are kept strictly separate: `usage` is plan-CREDIT consumption; `usageCredits` is MONEY spent on automatic top-ups. Read-only for any tenant member.
- **Handler:** `services/billing/src/routes/credits.ts` line 37
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/credits.ts`.
2. **Handler.** `services/billing/src/routes/credits.ts:37` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `autoPurchasePolicy.findUnique`
   - `plan.findUnique`
   - `aiUnitLot.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/billing/credit-summary
  → [authenticate → resolveTenant]
  → handler  services/billing/src/routes/credits.ts:37
      → db read   prisma.autoPurchasePolicy.findUnique
      → db read   prisma.plan.findUnique
      → db read   prisma.aiUnitLot.findMany
```

### `GET /api/billing/credits/balance`

- **Purpose:** Separate INCLUDED (resets) vs PURCHASED (permanent) balances for the UI.
- **Handler:** `services/billing/src/routes/credits.ts` line 162
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/credits.ts`.
2. **Handler.** `services/billing/src/routes/credits.ts:162` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/billing/credits/balance
  → [authenticate → resolveTenant]
  → handler  services/billing/src/routes/credits.ts:162
      → (responds directly; no downstream calls detected)
```

### `POST /api/billing/credits/buy`

- **Purpose:** Creates a buy. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/credits.ts` line 188
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/credits.ts`.
2. **Handler.** `services/billing/src/routes/credits.ts:188` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `409`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/billing/credits/buy
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/credits.ts:188
      → (responds directly; no downstream calls detected)
```

### `GET /api/billing/credits/packages`

- **Purpose:** Lists packages. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/credits.ts` line 176
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/credits.ts`.
2. **Handler.** `services/billing/src/routes/credits.ts:176` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `creditPackage.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/billing/credits/packages
  → [authenticate → resolveTenant]
  → handler  services/billing/src/routes/credits.ts:176
      → db read   prisma.creditPackage.findMany
```

### `GET /api/billing/invoices`

- **Purpose:** Lists invoices. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/invoices.ts` line 18
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/invoices.ts`.
2. **Handler.** `services/billing/src/routes/invoices.ts:18` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `invoice.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/billing/invoices
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/invoices.ts:18
      → db read   prisma.invoice.findMany
```

### `GET /api/billing/payment-methods`

- **Purpose:** Lists payment methods. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/payment-methods.ts` line 78
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/payment-methods.ts`.
2. **Handler.** `services/billing/src/routes/payment-methods.ts:78` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `billableEntityTenant.findUnique`
   - `billingProfile.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/billing/payment-methods
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/payment-methods.ts:78
      → db read   prisma.billableEntityTenant.findUnique
      → db read   prisma.billingProfile.findUnique
```

### `DELETE /api/billing/payment-methods/:id`

- **Purpose:** Deletes the referenced payment method. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/payment-methods.ts` line 189
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/payment-methods.ts`.
2. **Handler.** `services/billing/src/routes/payment-methods.ts:189` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `billableEntityTenant.findUnique`
   - `billingProfile.findUnique`
   - `paymentMethod.count`
   - `subscription.findUnique`
   - `plan.findFirst`
   _Writes:_
   - `paymentMethod.updateMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `409`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/billing/payment-methods/:id
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/payment-methods.ts:189
      → db read   prisma.billableEntityTenant.findUnique
      → db read   prisma.billingProfile.findUnique
      → db read   prisma.paymentMethod.count
      → db read   prisma.subscription.findUnique
      → db read   prisma.plan.findFirst
      → db write  prisma.paymentMethod.updateMany
```

### `POST /api/billing/payment-methods/confirm`

- **Purpose:** Confirm that a card was stored.
- **Handler:** `services/billing/src/routes/payment-methods.ts` line 132
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/payment-methods.ts`.
2. **Handler.** `services/billing/src/routes/payment-methods.ts:132` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tokenizationSession.findUnique`
   - `paymentMethod.findUnique`
   _Writes:_
   - `paymentMethod.updateMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/billing/payment-methods/confirm
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/payment-methods.ts:132
      → db read   prisma.tokenizationSession.findUnique
      → db read   prisma.paymentMethod.findUnique
      → db write  prisma.paymentMethod.updateMany
```

### `POST /api/billing/payment-methods/session`

- **Purpose:** Start adding (or replacing) a card.
- **Handler:** `services/billing/src/routes/payment-methods.ts` line 91
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/payment-methods.ts`.
2. **Handler.** `services/billing/src/routes/payment-methods.ts:91` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/billing/payment-methods/session
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/payment-methods.ts:91
      → (responds directly; no downstream calls detected)
```

### `GET /api/billing/plans`

- **Purpose:** Tenant-scoped so an organization sees its OWN custom plan alongside the public catalog - and never another organization's negotiated terms.
- **Handler:** `services/billing/src/routes/subscription.ts` line 16
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/subscription.ts`.
2. **Handler.** `services/billing/src/routes/subscription.ts:16` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/billing/plans
  → [authenticate → resolveTenant]
  → handler  services/billing/src/routes/subscription.ts:16
      → (responds directly; no downstream calls detected)
```

### `GET /api/billing/pricing`

- **Purpose:** The pricing page payload: the plan catalog, the display-currency options and the FX attribution behind an ILS rendering.
- **Handler:** `services/billing/src/routes/pricing.ts` line 44
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/pricing.ts`.
2. **Handler.** `services/billing/src/routes/pricing.ts:44` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/billing/pricing
  → [authenticate → resolveTenant]
  → handler  services/billing/src/routes/pricing.ts:44
      → (responds directly; no downstream calls detected)
```

### `POST /api/billing/pricing/change`

- **Purpose:** Apply a plan / volume change. Owner-gated, provider-confirmed, and priced from the catalog rather than from anything the client sent.
- **Handler:** `services/billing/src/routes/pricing.ts` line 185
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/pricing.ts`.
2. **Handler.** `services/billing/src/routes/pricing.ts:185` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/billing/pricing/change
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/pricing.ts:185
      → (responds directly; no downstream calls detected)
```

### `GET /api/billing/pricing/current`

- **Purpose:** The current subscription rendered from its stored snapshot, so a published plan change never restates what an existing customer sees.
- **Handler:** `services/billing/src/routes/pricing.ts` line 115
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/pricing.ts`.
2. **Handler.** `services/billing/src/routes/pricing.ts:115` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/billing/pricing/current
  → [authenticate → resolveTenant]
  → handler  services/billing/src/routes/pricing.ts:115
      → (responds directly; no downstream calls detected)
```

### `GET /api/billing/pricing/packages`

- **Purpose:** The purchasable credit packages for THIS organization's plan.
- **Handler:** `services/billing/src/routes/pricing.ts` line 135
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/pricing.ts`.
2. **Handler.** `services/billing/src/routes/pricing.ts:135` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `plan.findUnique`
   - `creditPackage.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/billing/pricing/packages
  → [authenticate → resolveTenant]
  → handler  services/billing/src/routes/pricing.ts:135
      → db read   prisma.plan.findUnique
      → db read   prisma.creditPackage.findMany
```

### `POST /api/billing/pricing/quote`

- **Purpose:** Price a concrete selection. The body carries KEYS only; price, credits and estimate are all recomputed server-side from the catalog.
- **Handler:** `services/billing/src/routes/pricing.ts` line 79
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/pricing.ts`.
2. **Handler.** `services/billing/src/routes/pricing.ts:79` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/billing/pricing/quote
  → [authenticate → resolveTenant]
  → handler  services/billing/src/routes/pricing.ts:79
      → (responds directly; no downstream calls detected)
```

### `GET /api/billing/profile`

- **Purpose:** The identity a tax document is made out to.
- **Handler:** `services/billing/src/routes/payment-methods.ts` line 235
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/payment-methods.ts`.
2. **Handler.** `services/billing/src/routes/payment-methods.ts:235` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/billing/profile
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/payment-methods.ts:235
      → (responds directly; no downstream calls detected)
```

### `PUT /api/billing/profile`

- **Purpose:** Updates the referenced profile. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/payment-methods.ts` line 239
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/payment-methods.ts`.
2. **Handler.** `services/billing/src/routes/payment-methods.ts:239` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `billableEntityTenant.findUnique`
   - `billingProfile.findUnique`
   _Writes:_
   - `billingProfile.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `409`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/billing/profile
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/payment-methods.ts:239
      → db read   prisma.billableEntityTenant.findUnique
      → db read   prisma.billingProfile.findUnique
      → db write  prisma.billingProfile.update
```

### `POST /api/billing/providers/icount/ipn`

- **Purpose:** Creates an ipn. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/icount-ipn.ts` line 88
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/icount-ipn.ts`.
2. **Handler.** `services/billing/src/routes/icount-ipn.ts:88` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `billingWebhookEvent.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `413`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/billing/providers/icount/ipn
  → handler  services/billing/src/routes/icount-ipn.ts:88
      → db write  prisma.billingWebhookEvent.create
```

### `POST /api/billing/shopify/complete`

- **Purpose:** Called by `/integrations/shopify/billing/complete` after Shopify sends the merchant back.
- **Handler:** `services/billing/src/routes/shopify-billing.ts` line 215
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/shopify-billing.ts`.
2. **Handler.** `services/billing/src/routes/shopify-billing.ts:215` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `409`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/billing/shopify/complete
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/shopify-billing.ts:215
      → (responds directly; no downstream calls detected)
```

### `POST /api/billing/shopify/grandfather/evaluate`

- **Purpose:** Evaluate and, if earned, record grandfathered eligibility.
- **Handler:** `services/billing/src/routes/shopify-billing.ts` line 307
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/shopify-billing.ts`.
2. **Handler.** `services/billing/src/routes/shopify-billing.ts:307` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/billing/shopify/grandfather/evaluate
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/shopify-billing.ts:307
      → (responds directly; no downstream calls detected)
```

### `POST /api/billing/shopify/plan-selection`

- **Purpose:** Where to send the merchant to choose and approve a plan.
- **Handler:** `services/billing/src/routes/shopify-billing.ts` line 129
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/shopify-billing.ts`.
2. **Handler.** `services/billing/src/routes/shopify-billing.ts:129` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `409`, `503`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/billing/shopify/plan-selection
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/shopify-billing.ts:129
      → (responds directly; no downstream calls detected)
```

### `GET /api/billing/shopify/plans`

- **Purpose:** The plans this store could be offered.
- **Handler:** `services/billing/src/routes/shopify-billing.ts` line 107
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/shopify-billing.ts`.
2. **Handler.** `services/billing/src/routes/shopify-billing.ts:107` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/billing/shopify/plans
  → [authenticate → resolveTenant]
  → handler  services/billing/src/routes/shopify-billing.ts:107
      → (responds directly; no downstream calls detected)
```

### `GET /api/billing/shopify/state`

- **Purpose:** Everything the UI needs to render a Shopify billing state.
- **Handler:** `services/billing/src/routes/shopify-billing.ts` line 95
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/shopify-billing.ts`.
2. **Handler.** `services/billing/src/routes/shopify-billing.ts:95` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/billing/shopify/state
  → [authenticate → resolveTenant]
  → handler  services/billing/src/routes/shopify-billing.ts:95
      → (responds directly; no downstream calls detected)
```

### `GET /api/billing/subscription`

- **Purpose:** Lists subscription. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/subscription.ts` line 20
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/subscription.ts`.
2. **Handler.** `services/billing/src/routes/subscription.ts:20` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/billing/subscription
  → [authenticate → resolveTenant]
  → handler  services/billing/src/routes/subscription.ts:20
      → (responds directly; no downstream calls detected)
```

### `POST /api/billing/subscription/cancel`

- **Purpose:** Creates a cancel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/subscription.ts` line 60
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:cancel`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/subscription.ts`.
2. **Handler.** `services/billing/src/routes/subscription.ts:60` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:cancel`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/billing/subscription/cancel
  → [authenticate → resolveTenant → requirePermission(settings:billing:cancel)]
  → handler  services/billing/src/routes/subscription.ts:60
      → (responds directly; no downstream calls detected)
```

### `POST /api/billing/subscription/change-plan`

- **Purpose:** Creates a change plan. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/subscription.ts` line 25
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/subscription.ts`.
2. **Handler.** `services/billing/src/routes/subscription.ts:25` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/billing/subscription/change-plan
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/subscription.ts:25
      → (responds directly; no downstream calls detected)
```

### `POST /api/billing/subscription/change-volume`

- **Purpose:** Change only the chat/voice volume selection, staying on the same plan.
- **Handler:** `services/billing/src/routes/subscription.ts` line 45
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/subscription.ts`.
2. **Handler.** `services/billing/src/routes/subscription.ts:45` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/billing/subscription/change-volume
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/subscription.ts:45
      → (responds directly; no downstream calls detected)
```

### `POST /api/billing/subscription/migrate`

- **Purpose:** Creates a migrate. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/subscription.ts` line 78
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/subscription.ts`.
2. **Handler.** `services/billing/src/routes/subscription.ts:78` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/billing/subscription/migrate
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/subscription.ts:78
      → (responds directly; no downstream calls detected)
```

### `POST /api/billing/subscription/resume`

- **Purpose:** Creates a resume. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/subscription.ts` line 69
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `settings:billing:manage`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/subscription.ts`.
2. **Handler.** `services/billing/src/routes/subscription.ts:69` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `settings:billing:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/billing/subscription/resume
  → [authenticate → resolveTenant → requirePermission(settings:billing:manage)]
  → handler  services/billing/src/routes/subscription.ts:69
      → (responds directly; no downstream calls detected)
```

### `POST /api/billing/webhooks/icount`

- **Purpose:** Creates an icount. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/webhooks.ts` line 54
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/billing` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/webhooks.ts`.
2. **Handler.** `services/billing/src/routes/webhooks.ts:54` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `billingWebhookEvent.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `401`, `413`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/billing/webhooks/icount
  → handler  services/billing/src/routes/webhooks.ts:54
      → db write  prisma.billingWebhookEvent.create
```

### `POST /api/checkout/:reference/advance`

- **Purpose:** Ask the server to re-check and move the checkout forward.
- **Handler:** `services/billing/src/routes/checkout-session.ts` line 111
- **Authentication / tenant resolution:** `optionalAuth` - identifies the caller when a token is present, otherwise proceeds anonymously.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/checkout` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/checkout-session.ts`.
2. **Handler.** `services/billing/src/routes/checkout-session.ts:111` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `optionalAuth` - identifies the caller when a token is present, otherwise proceeds anonymously.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
POST /api/checkout/:reference/advance
  → handler  services/billing/src/routes/checkout-session.ts:111
      → (responds directly; no downstream calls detected)
```

### `POST /api/checkout/:reference/payment-session`

- **Purpose:** Start (or resume) payment setup.
- **Handler:** `services/billing/src/routes/checkout-session.ts` line 84
- **Authentication / tenant resolution:** `optionalAuth` - identifies the caller when a token is present, otherwise proceeds anonymously.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/checkout` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/checkout-session.ts`.
2. **Handler.** `services/billing/src/routes/checkout-session.ts:84` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `optionalAuth` - identifies the caller when a token is present, otherwise proceeds anonymously.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
POST /api/checkout/:reference/payment-session
  → handler  services/billing/src/routes/checkout-session.ts:84
      → (responds directly; no downstream calls detected)
```

### `POST /api/checkout/:reference/resend-welcome`

- **Purpose:** Send the post-payment welcome email again.
- **Handler:** `services/billing/src/routes/checkout-session.ts` line 137
- **Authentication / tenant resolution:** `optionalAuth` - identifies the caller when a token is present, otherwise proceeds anonymously.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/checkout` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/checkout-session.ts`.
2. **Handler.** `services/billing/src/routes/checkout-session.ts:137` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `optionalAuth` - identifies the caller when a token is present, otherwise proceeds anonymously.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `plan.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
POST /api/checkout/:reference/resend-welcome
  → handler  services/billing/src/routes/checkout-session.ts:137
      → db read   prisma.tenant.findUnique
      → db read   prisma.plan.findFirst
```

### `GET /api/checkout/:reference/status`

- **Purpose:** Reads a single statu for the referenced checkout. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/checkout.ts` line 216
- **Authentication / tenant resolution:** `optionalAuth` - identifies the caller when a token is present, otherwise proceeds anonymously.
- **Authorization:** None.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/checkout` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/checkout.ts`.
2. **Handler.** `services/billing/src/routes/checkout.ts:216` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `optionalAuth` - identifies the caller when a token is present, otherwise proceeds anonymously.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `pendingCheckout.findUnique`
   - `paymentAttempt.findFirst`
   - `tenant.findUnique`
   - `plan.findFirst`
   - `tokenizationSession.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `429`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/checkout/:reference/status
  → handler  services/billing/src/routes/checkout.ts:216
      → db read   prisma.pendingCheckout.findUnique
      → db read   prisma.paymentAttempt.findFirst
      → db read   prisma.tenant.findUnique
      → db read   prisma.plan.findFirst
      → db read   prisma.tokenizationSession.findFirst
```

### `GET /api/checkout/mine`

- **Purpose:** Safe summary + status.
- **Handler:** `services/billing/src/routes/checkout.ts` line 183
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Gate: `requirePaymentSetupAccess`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/checkout` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/checkout.ts`.
2. **Handler.** `services/billing/src/routes/checkout.ts:183` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Gate: `requirePaymentSetupAccess`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `pendingCheckout.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/checkout/mine
  → [authenticate → resolveTenant → requirePaymentSetupAccess]
  → handler  services/billing/src/routes/checkout.ts:183
      → db read   prisma.pendingCheckout.findFirst
```

### `GET /api/public/pricing`

- **Purpose:** The public catalog.
- **Handler:** `services/billing/src/routes/public-pricing.ts` line 271
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/public` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/public-pricing.ts`.
2. **Handler.** `services/billing/src/routes/public-pricing.ts:271` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `304`, answering the `{ data: ... }` envelope. Error paths: `404`, `503`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/public/pricing
  → handler  services/billing/src/routes/public-pricing.ts:271
      → (responds directly; no downstream calls detected)
```

## Channels

16 endpoints.

### `GET /api/channels`

- **Purpose:** Lists channels. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/channels.ts` line 147
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:read`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/channels.ts`.
2. **Handler.** `services/auth/src/routes/channels.ts:147` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:read`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/channels
  → [authenticate → resolveTenant → requirePermission(channels:manage:read)]
  → handler  services/auth/src/routes/channels.ts:147
      → db read   prisma.channelAccount.findMany
```

### `DELETE /api/channels/:id`

- **Purpose:** Deletes the referenced channel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/channels.ts` line 1757
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:update`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/channels.ts`.
2. **Handler.** `services/auth/src/routes/channels.ts:1757` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:update`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
   _Writes:_
   - `message.deleteMany`
   - `conversation.deleteMany`
   - `channelAccount.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/channels/:id
  → [authenticate → resolveTenant → requirePermission(channels:manage:update)]
  → handler  services/auth/src/routes/channels.ts:1757
      → db read   prisma.channelAccount.findFirst
      → db write  prisma.message.deleteMany
      → db write  prisma.conversation.deleteMany
      → db write  prisma.channelAccount.delete
```

### `POST /api/channels/:id/disconnect`

- **Purpose:** Performs the `disconnect` action on the referenced channel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/channels.ts` line 1667
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:update`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/channels.ts`.
2. **Handler.** `services/auth/src/routes/channels.ts:1667` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:update`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
   _Writes:_
   - `channelAccount.update`
8. **External integrations.**
   - outbound HTTP (axios.delete)
   - outbound HTTP (axios.post)
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/channels/:id/disconnect
  → [authenticate → resolveTenant → requirePermission(channels:manage:update)]
  → handler  services/auth/src/routes/channels.ts:1667
      → db read   prisma.channelAccount.findFirst
      → db write  prisma.channelAccount.update
      → external  outbound HTTP (axios.delete)
      → external  outbound HTTP (axios.post)
```

### `GET /api/channels/:id/posts`

- **Purpose:** Returns a normalized list of the page's own posts so flow authors can select which post a Comment Trigger should listen on, instead of pasting raw IDs. Group posts intentionally NOT supported - the Meta Groups API was deprecated April 2024; for those, the trigger UI exposes a manual post-URL paste field that the runtime later matches via the page webhook.
- **Handler:** `services/auth/src/routes/channels.ts` line 207
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:read`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/channels.ts`.
2. **Handler.** `services/auth/src/routes/channels.ts:207` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:read`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
8. **External integrations.**
   - outbound HTTP (axios.get)
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, `404`, `409`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/channels/:id/posts
  → [authenticate → resolveTenant → requirePermission(channels:manage:read)]
  → handler  services/auth/src/routes/channels.ts:207
      → db read   prisma.channelAccount.findFirst
      → external  outbound HTTP (axios.get)
```

### `PATCH /api/channels/:id/response-mode`

- **Purpose:** Updates the referenced response mode for the referenced channel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/channels.ts` line 1636
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:update`
- **Input validation:** `validate(responseModeSchema)` (Zod). Accepts: `responseMode`. Required: `responseMode`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/channels.ts`.
2. **Handler.** `services/auth/src/routes/channels.ts:1636` - inline `async (req, res)` handler.
3. **Validation.** `validate(responseModeSchema)` (Zod). Accepts: `responseMode`. Required: `responseMode`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:update`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
   _Writes:_
   - `channelAccount.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/channels/:id/response-mode
  → [authenticate → resolveTenant → requirePermission(channels:manage:update) → validate(responseModeSchema)]
  → handler  services/auth/src/routes/channels.ts:1636
      → db read   prisma.channelAccount.findFirst
      → db write  prisma.channelAccount.update
```

### `GET /api/channels/:id/status`

- **Purpose:** Reads a single statu for the referenced channel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/channels.ts` line 1800
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:read`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/channels.ts`.
2. **Handler.** `services/auth/src/routes/channels.ts:1800` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:read`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
   _Writes:_
   - `channelAccount.update`
8. **External integrations.**
   - outbound HTTP (axios.get)
   - outbound HTTP (axios.post)
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/channels/:id/status
  → [authenticate → resolveTenant → requirePermission(channels:manage:read)]
  → handler  services/auth/src/routes/channels.ts:1800
      → db read   prisma.channelAccount.findFirst
      → db write  prisma.channelAccount.update
      → external  outbound HTTP (axios.get)
      → external  outbound HTTP (axios.post)
```

### `GET /api/channels/config`

- **Purpose:** Lists config. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/channels.ts` line 2044
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:read`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/channels.ts`.
2. **Handler.** `services/auth/src/routes/channels.ts:2044` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:read`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/channels/config
  → [authenticate → resolveTenant → requirePermission(channels:manage:read)]
  → handler  services/auth/src/routes/channels.ts:2044
      → (responds directly; no downstream calls detected)
```

### `POST /api/channels/connect/email`

- **Purpose:** Creates an email. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/channels.ts` line 1913
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:update`; Plan limit: `limit:channels`
- **Input validation:** `validate(connectEmailSchema)` (Zod). Accepts: `emailAddress`, `displayName`, `smtpHost`, `smtpPort`, `smtpUser`, `smtpPass`, `imapHost`, `imapPort`, `imapUser`, `imapPass`. Required: `emailAddress`, `displayName`, `smtpHost`, `smtpPort`, `smtpUser`, `smtpPass`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/channels.ts`.
2. **Handler.** `services/auth/src/routes/channels.ts:1913` - inline `async (req, res)` handler.
3. **Validation.** `validate(connectEmailSchema)` (Zod). Accepts: `emailAddress`, `displayName`, `smtpHost`, `smtpPort`, `smtpUser`, `smtpPass`, `imapHost`, `imapPort`, `imapUser`, `imapPass`. Required: `emailAddress`, `displayName`, `smtpHost`, `smtpPort`, `smtpUser`, `smtpPass`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:update`; Plan limit: `limit:channels`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
   _Writes:_
   - `channelAccount.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/channels/connect/email
  → [authenticate → resolveTenant → requirePermission(channels:manage:update) → validate(connectEmailSchema)]
  → handler  services/auth/src/routes/channels.ts:1913
      → db read   prisma.channelAccount.findFirst
      → db write  prisma.channelAccount.create
```

### `POST /api/channels/connect/whatsapp`

- **Purpose:** Creates a whatsapp. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/channels.ts` line 301
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:update`; Plan limit: `limit:channels`
- **Input validation:** `validate(whatsappConnectSchema)` (Zod). Accepts: `code`, `wabaId`, `phoneNumberId`. Required: `code`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/channels.ts`.
2. **Handler.** `services/auth/src/routes/channels.ts:301` - inline `async (req, res)` handler.
3. **Validation.** `validate(whatsappConnectSchema)` (Zod). Accepts: `code`, `wabaId`, `phoneNumberId`. Required: `code`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:update`; Plan limit: `limit:channels`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findUnique`
   _Writes:_
   - `channelAccount.update`
   - `channelAccount.create`
8. **External integrations.**
   - outbound HTTP (axios.get)
   - outbound HTTP (axios.post)
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/channels/connect/whatsapp
  → [authenticate → resolveTenant → requirePermission(channels:manage:update) → validate(whatsappConnectSchema)]
  → handler  services/auth/src/routes/channels.ts:301
      → db read   prisma.channelAccount.findUnique
      → db write  prisma.channelAccount.update
      → db write  prisma.channelAccount.create
      → external  outbound HTTP (axios.get)
      → external  outbound HTTP (axios.post)
```

### `POST /api/channels/connect/whatsapp-session`

- **Purpose:** Creates a whatsapp session. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/channels.ts` line 536
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:update`; Plan limit: `limit:channels`
- **Input validation:** `validate(whatsappSessionSchema)` (Zod). Accepts: `wabaId`, `phoneNumberId`. Required: `wabaId`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/channels.ts`.
2. **Handler.** `services/auth/src/routes/channels.ts:536` - inline `async (req, res)` handler.
3. **Validation.** `validate(whatsappSessionSchema)` (Zod). Accepts: `wabaId`, `phoneNumberId`. Required: `wabaId`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:update`; Plan limit: `limit:channels`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findUnique`
   _Writes:_
   - `channelAccount.update`
   - `channelAccount.create`
8. **External integrations.**
   - outbound HTTP (axios.post)
   - outbound HTTP (axios.get)
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/channels/connect/whatsapp-session
  → [authenticate → resolveTenant → requirePermission(channels:manage:update) → validate(whatsappSessionSchema)]
  → handler  services/auth/src/routes/channels.ts:536
      → db read   prisma.channelAccount.findUnique
      → db write  prisma.channelAccount.update
      → db write  prisma.channelAccount.create
      → external  outbound HTTP (axios.post)
      → external  outbound HTTP (axios.get)
```

### `GET /api/channels/oauth/callback`

- **Purpose:** Lists callback. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/channels.ts` line 786
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/channels.ts`.
2. **Handler.** `services/auth/src/routes/channels.ts:786` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findUnique`
   _Writes:_
   - `channelAccount.update`
   - `channelAccount.create`
8. **External integrations.**
   - outbound HTTP (axios.get)
   - outbound HTTP (axios.post)
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
GET /api/channels/oauth/callback
  → handler  services/auth/src/routes/channels.ts:786
      → db read   prisma.channelAccount.findUnique
      → db write  prisma.channelAccount.update
      → db write  prisma.channelAccount.create
      → external  outbound HTTP (axios.get)
      → external  outbound HTTP (axios.post)
```

### `GET /api/channels/oauth/init`

- **Purpose:** Lists init. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/channels.ts` line 625
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/channels.ts`.
2. **Handler.** `services/auth/src/routes/channels.ts:625` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `401`, `403`, `500`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/channels/oauth/init
  → handler  services/auth/src/routes/channels.ts:625
      → (responds directly; no downstream calls detected)
```

### `GET /api/channels/summary`

- **Purpose:** The Inbox/History empty states need to know WHY a list is empty ("no channel connected" vs "channel broken" vs "no conversations yet"), and agents see those pages too - the full listing above is ADMIN-only. This returns health COUNTS only: no tokens, external ids, or error details.
- **Handler:** `services/auth/src/routes/channels.ts` line 182
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/channels.ts`.
2. **Handler.** `services/auth/src/routes/channels.ts:182` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/channels/summary
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/channels.ts:182
      → db read   prisma.channelAccount.findMany
```

### `GET /api/channels/webchat/:id/settings`

- **Purpose:** Reads a single setting for the referenced webchat. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/channels.ts` line 2026
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/channels.ts`.
2. **Handler.** `services/auth/src/routes/channels.ts:2026` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/channels/webchat/:id/settings
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/channels.ts:2026
      → db read   prisma.channelAccount.findFirst
```

### `PUT /api/channels/webchat/:id/settings`

- **Purpose:** Updates the referenced setting for the referenced webchat. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/channels.ts` line 1994
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/channels.ts`.
2. **Handler.** `services/auth/src/routes/channels.ts:1994` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
   _Writes:_
   - `channelAccount.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/channels/webchat/:id/settings
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/channels.ts:1994
      → db read   prisma.channelAccount.findFirst
      → db write  prisma.channelAccount.update
```

### `POST /api/channels/webchat/create`

- **Purpose:** Creates a create. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/channels.ts` line 1954
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:update`; Plan limit: `limit:channels`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/channels.ts`.
2. **Handler.** `services/auth/src/routes/channels.ts:1954` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:update`; Plan limit: `limit:channels`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
   _Writes:_
   - `channelAccount.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/channels/webchat/create
  → [authenticate → resolveTenant → requirePermission(channels:manage:update)]
  → handler  services/auth/src/routes/channels.ts:1954
      → db read   prisma.channelAccount.findFirst
      → db write  prisma.channelAccount.create
```

## Contacts & CRM

19 endpoints.

### `GET /api/contacts`

- **Purpose:** GET / - Search contacts in the connected CRM only.
- **Handler:** `services/conversation/src/routes/contacts.ts` line 33
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `crm:contacts:read` **or** role `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/contacts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/contacts.ts`.
2. **Handler.** `services/conversation/src/routes/contacts.ts:33` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `crm:contacts:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/contacts
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/contacts.ts:33
      → (responds directly; no downstream calls detected)
```

### `POST /api/contacts`

- **Purpose:** POST / - Create contact
- **Handler:** `services/conversation/src/routes/contacts.ts` line 350
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `crm:contacts:read` **or** role `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/contacts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/contacts.ts`.
2. **Handler.** `services/conversation/src/routes/contacts.ts:350` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `crm:contacts:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `contact.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/contacts
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/contacts.ts:350
      → db write  prisma.contact.upsert
```

### `GET /api/contacts/:id`

- **Purpose:** GET /:id - Get single contact with conversation history count per channel
- **Handler:** `services/conversation/src/routes/contacts.ts` line 223
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `crm:contacts:read` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/contacts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/contacts.ts`.
2. **Handler.** `services/conversation/src/routes/contacts.ts:223` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `crm:contacts:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `contact.findFirst`
   - `conversation.groupBy`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/contacts/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/contacts.ts:223
      → db read   prisma.contact.findFirst
      → db read   prisma.conversation.groupBy
```

### `PATCH /api/contacts/:id`

- **Purpose:** PATCH /:id - Update contact
- **Handler:** `services/conversation/src/routes/contacts.ts` line 392
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `crm:contacts:read` **or** role `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/contacts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/contacts.ts`.
2. **Handler.** `services/conversation/src/routes/contacts.ts:392` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `crm:contacts:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `contact.findFirst`
   _Writes:_
   - `contact.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/contacts/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/contacts.ts:392
      → db read   prisma.contact.findFirst
      → db write  prisma.contact.update
```

### `POST /api/contacts/:id/opt-out`

- **Purpose:** Performs the `opt out` action on the referenced contact. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/contacts.ts` line 423
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `crm:contacts:read` **or** role `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/contacts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/contacts.ts`.
2. **Handler.** `services/conversation/src/routes/contacts.ts:423` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `crm:contacts:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `contact.findFirst`
   _Writes:_
   - `contact.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/contacts/:id/opt-out
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/contacts.ts:423
      → db read   prisma.contact.findFirst
      → db write  prisma.contact.update
```

### `POST /api/contacts/initiate-conversation`

- **Purpose:** POST /initiate-conversation - must be before /:id to avoid conflict
- **Handler:** `services/conversation/src/routes/contacts.ts` line 95
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `crm:contacts:read` **or** role `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/contacts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/contacts.ts`.
2. **Handler.** `services/conversation/src/routes/contacts.ts:95` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `crm:contacts:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `contact.findFirst`
   - `conversation.findFirst`
   - `messageTemplate.findFirst`
   _Writes:_
   - `conversation.create`
   - `conversation.update`
   - `message.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.**
   - Enqueues `outgoingMessageQueue.add` (BullMQ). The response returns before this work runs; retries and backoff are the queue's.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/contacts/initiate-conversation
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/contacts.ts:95
      → db read   prisma.contact.findFirst
      → db read   prisma.conversation.findFirst
      → db read   prisma.messageTemplate.findFirst
      → db write  prisma.conversation.create
      → db write  prisma.conversation.update
      → db write  prisma.message.create
      → queue     outgoingMessageQueue.add  (async, outlives the response)
```

### `POST /api/contacts/segment`

- **Purpose:** Creates a segment. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/contacts.ts` line 257
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `crm:contacts:read` **or** role `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/contacts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/contacts.ts`.
2. **Handler.** `services/conversation/src/routes/contacts.ts:257` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `crm:contacts:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `contact.count`
   - `contact.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/contacts/segment
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/contacts.ts:257
      → db read   prisma.contact.count
      → db read   prisma.contact.findMany
```

### `POST /api/crm/auto-link-identifier`

- **Purpose:** POST /api/crm/auto-link-identifier   body: { tenantId, conversationId, identifier: { type: "email"|"phone", value: string } }   returns: { ok, outcome: "linked"|"created"|"needs_approval"|"not_found"|"error",              crmContactId?, crmObjectKind?, candidates?, reason? }
- **Handler:** `services/ai/src/routes/crm-auto-link.ts` line 160
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/crm` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/crm-auto-link.ts`.
2. **Handler.** `services/ai/src/routes/crm-auto-link.ts:160` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findFirst`
   - `tenant.findUnique`
   - `contact.findFirst`
   _Writes:_
   - `contact.update`
   - `contact.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/crm/auto-link-identifier
  → [internalAuth]
  → handler  services/ai/src/routes/crm-auto-link.ts:160
      → db read   prisma.conversation.findFirst
      → db read   prisma.tenant.findUnique
      → db read   prisma.contact.findFirst
      → db write  prisma.contact.update
      → db write  prisma.contact.create
```

### `GET /api/crm/conversation/:id/context`

- **Purpose:** Reads a single context for the referenced conversation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/crm-panel.ts` line 318
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/crm` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/crm-panel.ts`.
2. **Handler.** `services/ai/src/routes/crm-panel.ts:318` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/crm/conversation/:id/context
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/crm-panel.ts:318
      → (responds directly; no downstream calls detected)
```

### `POST /api/crm/conversation/:id/create-lead`

- **Purpose:** Used when the panel shows "unmapped" - operator clicks "Create lead in CRM".
- **Handler:** `services/ai/src/routes/crm-panel.ts` line 514
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/crm` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/crm-panel.ts`.
2. **Handler.** `services/ai/src/routes/crm-panel.ts:514` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/crm/conversation/:id/create-lead
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/crm-panel.ts:514
      → (responds directly; no downstream calls detected)
```

### `POST /api/crm/conversation/:id/notes`

- **Purpose:** Performs the `notes` action on the referenced conversation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/crm-panel.ts` line 667
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/crm` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/crm-panel.ts`.
2. **Handler.** `services/ai/src/routes/crm-panel.ts:667` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/crm/conversation/:id/notes
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/crm-panel.ts:667
      → (responds directly; no downstream calls detected)
```

### `POST /api/crm/conversation/:id/pick-candidate`

- **Purpose:** Operator resolution path for the "needs_approval" state. When the identity service finds 2+ CRM candidates and can't safely merge, the panel surfaces the list; the operator picks one and we pin the linkage. No merge happens - the unselected duplicates stay in the CRM and the operator can clean them up in the vendor UI.
- **Handler:** `services/ai/src/routes/crm-panel.ts` line 615
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/crm` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/crm-panel.ts`.
2. **Handler.** `services/ai/src/routes/crm-panel.ts:615` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/crm/conversation/:id/pick-candidate
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/crm-panel.ts:615
      → (responds directly; no downstream calls detected)
```

### `POST /api/crm/conversation/:id/sync-close`

- **Purpose:** Performs the `sync close` action on the referenced conversation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/crm-panel.ts` line 929
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/crm` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/crm-panel.ts`.
2. **Handler.** `services/ai/src/routes/crm-panel.ts:929` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/crm/conversation/:id/sync-close
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/crm-panel.ts:929
      → (responds directly; no downstream calls detected)
```

### `POST /api/crm/resolve-identifiers`

- **Purpose:** POST /api/crm/resolve-identifiers   body: { tenantId, conversationId }   returns: { ok, phone?, email?, channelExternalIds: Record<channel, string>,              crmContactId?, crmObjectKind?, vendor? }
- **Handler:** `services/ai/src/routes/crm-auto-link.ts` line 65
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/crm` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/crm-auto-link.ts`.
2. **Handler.** `services/ai/src/routes/crm-auto-link.ts:65` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findFirst`
   - `contact.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/crm/resolve-identifiers
  → [internalAuth]
  → handler  services/ai/src/routes/crm-auto-link.ts:65
      → db read   prisma.conversation.findFirst
      → db read   prisma.contact.findFirst
```

### `GET /api/identity/:id/timeline`

- **Purpose:** GET /:id/timeline - unified timeline of events across channels for the contact
- **Handler:** `services/conversation/src/routes/identity.ts` line 202
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/identity` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/identity.ts`.
2. **Handler.** `services/conversation/src/routes/identity.ts:202` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `contact.findFirst`
   - `conversation.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/identity/:id/timeline
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/identity.ts:202
      → db read   prisma.contact.findFirst
      → db read   prisma.conversation.findMany
```

### `POST /api/identity/capture`

- **Purpose:** Strong-signal identity capture. Called when a bot/flow/agent EXPLICITLY asked the customer for their email or phone and the customer responded with an identifier. Distinct from /link, which handles weak-signal LLM extraction.
- **Handler:** `services/conversation/src/routes/identity.ts` line 422
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/identity` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/identity.ts`.
2. **Handler.** `services/conversation/src/routes/identity.ts:422` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `contact.findFirst`
   - `identityLinkSuggestion.count`
   _Writes:_
   - `contact.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `429`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/identity/capture
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/identity.ts:422
      → db read   prisma.contact.findFirst
      → db read   prisma.identityLinkSuggestion.count
      → db write  prisma.contact.update
```

### `POST /api/identity/link`

- **Purpose:** Progressive identity linker. Called by the AI tool executor when an email or phone is extracted from a customer message. NEVER merges - either attaches the identifier to the current contact (safe) or records a pending IdentityLinkSuggestion when the identifier already belongs to a DIFFERENT contact in the same tenant.
- **Handler:** `services/conversation/src/routes/identity.ts` line 274
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/identity` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/identity.ts`.
2. **Handler.** `services/conversation/src/routes/identity.ts:274` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `contact.findFirst`
   - `identityLinkSuggestion.findFirst`
   - `identityLinkSuggestion.count`
   _Writes:_
   - `identityLinkSuggestion.create`
   - `contact.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `429`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/identity/link
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/identity.ts:274
      → db read   prisma.contact.findFirst
      → db read   prisma.identityLinkSuggestion.findFirst
      → db read   prisma.identityLinkSuggestion.count
      → db write  prisma.identityLinkSuggestion.create
      → db write  prisma.contact.update
```

### `POST /api/identity/merge`

- **Purpose:** POST /merge - merge source contact into target contact
- **Handler:** `services/conversation/src/routes/identity.ts` line 88
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/identity` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/identity.ts`.
2. **Handler.** `services/conversation/src/routes/identity.ts:88` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `contact.findFirst`
   _Writes:_
   - `broadcastRecipient.updateMany`
   - `contact.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/identity/merge
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/identity.ts:88
      → db read   prisma.contact.findFirst
      → db write  prisma.broadcastRecipient.updateMany
      → db write  prisma.contact.update
```

### `POST /api/identity/resolve`

- **Purpose:** POST /resolve - find existing contact by email/phone or heuristic metadata match
- **Handler:** `services/conversation/src/routes/identity.ts` line 8
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/identity` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/identity.ts`.
2. **Handler.** `services/conversation/src/routes/identity.ts:8` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `contact.findFirst`
   - `contact.findMany`
   _Writes:_
   - `contact.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/identity/resolve
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/identity.ts:8
      → db read   prisma.contact.findFirst
      → db read   prisma.contact.findMany
      → db write  prisma.contact.create
```

## Conversations

49 endpoints.

### `POST /api/audiences/preview`

- **Purpose:** Creates a preview. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/audiences.ts` line 35
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/audiences` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/audiences.ts`.
2. **Handler.** `services/conversation/src/routes/audiences.ts:35` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/audiences/preview
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/audiences.ts:35
      → (responds directly; no downstream calls detected)
```

### `POST /api/audiences/resolve`

- **Purpose:** Creates a resolve. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/audiences.ts` line 97
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/audiences` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/audiences.ts`.
2. **Handler.** `services/conversation/src/routes/audiences.ts:97` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/audiences/resolve
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/audiences.ts:97
      → (responds directly; no downstream calls detected)
```

### `GET /api/audiences/schema`

- **Purpose:** Schema discovery for the audience builder. Returns CRM fields only - the platform's local Contact fields are not surfaced here anymore (the platform contact table is a write-side cache, not a source of truth for audience targeting).
- **Handler:** `services/conversation/src/routes/audiences.ts` line 62
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/audiences` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/audiences.ts`.
2. **Handler.** `services/conversation/src/routes/audiences.ts:62` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/audiences/schema
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/audiences.ts:62
      → (responds directly; no downstream calls detected)
```

### `GET /api/broadcasts`

- **Purpose:** Lists broadcasts. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/broadcasts.ts` line 263
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/broadcasts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/broadcasts.ts`.
2. **Handler.** `services/conversation/src/routes/broadcasts.ts:263` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `broadcast.findMany`
   - `broadcastRecipient.groupBy`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/broadcasts
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/broadcasts.ts:263
      → db read   prisma.broadcast.findMany
      → db read   prisma.broadcastRecipient.groupBy
```

### `POST /api/broadcasts`

- **Purpose:** Broadcasts are a sold capability, so the gate is server-side. Hiding the Broadcasts nav item is presentation, not enforcement.
- **Handler:** `services/conversation/src/routes/broadcasts.ts` line 380
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Entitlement: `communication.broadcasts`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/broadcasts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/broadcasts.ts`.
2. **Handler.** `services/conversation/src/routes/broadcasts.ts:380` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Entitlement: `communication.broadcasts`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `broadcast.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/broadcasts
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/broadcasts.ts:380
      → db write  prisma.broadcast.create
```

### `DELETE /api/broadcasts/:id`

- **Purpose:** Deletes the referenced broadcast. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/broadcasts.ts` line 473
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/broadcasts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/broadcasts.ts`.
2. **Handler.** `services/conversation/src/routes/broadcasts.ts:473` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `broadcast.findFirst`
   _Writes:_
   - `broadcastRecipient.deleteMany`
   - `broadcast.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/broadcasts/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/broadcasts.ts:473
      → db read   prisma.broadcast.findFirst
      → db write  prisma.broadcastRecipient.deleteMany
      → db write  prisma.broadcast.delete
```

### `GET /api/broadcasts/:id`

- **Purpose:** Reads a single broadcast. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/broadcasts.ts` line 310
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/broadcasts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/broadcasts.ts`.
2. **Handler.** `services/conversation/src/routes/broadcasts.ts:310` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `broadcast.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/broadcasts/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/broadcasts.ts:310
      → db read   prisma.broadcast.findFirst
```

### `PATCH /api/broadcasts/:id`

- **Purpose:** Updates the referenced broadcast. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/broadcasts.ts` line 417
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/broadcasts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/broadcasts.ts`.
2. **Handler.** `services/conversation/src/routes/broadcasts.ts:417` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `broadcast.findFirst`
   _Writes:_
   - `broadcast.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/broadcasts/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/broadcasts.ts:417
      → db read   prisma.broadcast.findFirst
      → db write  prisma.broadcast.update
```

### `POST /api/broadcasts/:id/cancel`

- **Purpose:** Performs the `cancel` action on the referenced broadcast. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/broadcasts.ts` line 804
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/broadcasts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/broadcasts.ts`.
2. **Handler.** `services/conversation/src/routes/broadcasts.ts:804` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `broadcast.findFirst`
   _Writes:_
   - `broadcastRecipient.updateMany`
   - `broadcast.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/broadcasts/:id/cancel
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/broadcasts.ts:804
      → db read   prisma.broadcast.findFirst
      → db write  prisma.broadcastRecipient.updateMany
      → db write  prisma.broadcast.update
```

### `GET /api/broadcasts/:id/recipients`

- **Purpose:** Reads a single recipient for the referenced broadcast. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/broadcasts.ts` line 341
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/broadcasts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/broadcasts.ts`.
2. **Handler.** `services/conversation/src/routes/broadcasts.ts:341` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `broadcast.findFirst`
   - `broadcastRecipient.findMany`
   - `broadcastRecipient.count`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/broadcasts/:id/recipients
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/broadcasts.ts:341
      → db read   prisma.broadcast.findFirst
      → db read   prisma.broadcastRecipient.findMany
      → db read   prisma.broadcastRecipient.count
```

### `POST /api/broadcasts/:id/recipients`

- **Purpose:** Performs the `recipients` action on the referenced broadcast. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/broadcasts.ts` line 509
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/broadcasts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/broadcasts.ts`.
2. **Handler.** `services/conversation/src/routes/broadcasts.ts:509` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `broadcast.findFirst`
   - `broadcastRecipient.count`
   _Writes:_
   - `broadcastRecipient.createMany`
   - `broadcast.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/broadcasts/:id/recipients
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/broadcasts.ts:509
      → db read   prisma.broadcast.findFirst
      → db read   prisma.broadcastRecipient.count
      → db write  prisma.broadcastRecipient.createMany
      → db write  prisma.broadcast.update
```

### `POST /api/broadcasts/:id/resend`

- **Purpose:** Performs the `resend` action on the referenced broadcast. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/broadcasts.ts` line 853
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/broadcasts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/broadcasts.ts`.
2. **Handler.** `services/conversation/src/routes/broadcasts.ts:853` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `broadcast.findFirst`
   _Writes:_
   - `broadcast.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/broadcasts/:id/resend
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/broadcasts.ts:853
      → db read   prisma.broadcast.findFirst
      → db write  prisma.broadcast.create
```

### `POST /api/broadcasts/:id/send`

- **Purpose:** Performs the `send` action on the referenced broadcast. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/broadcasts.ts` line 554
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/broadcasts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/broadcasts.ts`.
2. **Handler.** `services/conversation/src/routes/broadcasts.ts:554` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `broadcast.findFirst`
   - `broadcastRecipient.count`
   - `broadcastRecipient.findMany`
   _Writes:_
   - `broadcast.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.**
   - Enqueues `broadcastQueue.add` (BullMQ). The response returns before this work runs; retries and backoff are the queue's.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/broadcasts/:id/send
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/broadcasts.ts:554
      → db read   prisma.broadcast.findFirst
      → db read   prisma.broadcastRecipient.count
      → db read   prisma.broadcastRecipient.findMany
      → db write  prisma.broadcast.update
      → queue     broadcastQueue.add  (async, outlives the response)
```

### `POST /api/broadcasts/:id/validate`

- **Purpose:** Performs the `validate` action on the referenced broadcast. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/broadcasts.ts` line 674
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/broadcasts` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/broadcasts.ts`.
2. **Handler.** `services/conversation/src/routes/broadcasts.ts:674` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `broadcast.findFirst`
   - `broadcastRecipient.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/broadcasts/:id/validate
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/broadcasts.ts:674
      → db read   prisma.broadcast.findFirst
      → db read   prisma.broadcastRecipient.findMany
```

### `GET /api/conversations`

- **Purpose:** Lists conversations. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/conversations.ts` line 47
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/conversations` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/conversations.ts`.
2. **Handler.** `services/conversation/src/routes/conversations.ts:47` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls**, in source order:
   - `conversationService.list()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/conversations
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/conversations.ts:47
      → conversationService.list()
```

### `GET /api/conversations/:conversationId/messages`

- **Purpose:** Reads a single message for the referenced conversation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/messages.ts` line 50
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/conversations` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/messages.ts`.
2. **Handler.** `services/conversation/src/routes/messages.ts:50` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls**, in source order:
   - `messageService.listByConversation()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/conversations/:conversationId/messages
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/messages.ts:50
      → messageService.listByConversation()
```

### `POST /api/conversations/:conversationId/messages`

- **Purpose:** Performs the `messages` action on the referenced conversation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/messages.ts` line 64
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** `validate(sendMessageSchema)` (Zod). Accepts: `body`, `messageType`, `replyToMessageId`, `emailReplyMode`, `subject`. Required: `body`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/conversations` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/messages.ts`.
2. **Handler.** `services/conversation/src/routes/messages.ts:64` - inline `async (req, res)` handler.
3. **Validation.** `validate(sendMessageSchema)` (Zod). Accepts: `body`, `messageType`, `replyToMessageId`, `emailReplyMode`, `subject`. Required: `body`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls**, in source order:
   - `messageService.create()`
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findFirst`
   - `message.findFirst`
   _Writes:_
   - `conversation.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.**
   - Enqueues `outgoingMessageQueue.add` (BullMQ). The response returns before this work runs; retries and backoff are the queue's.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/conversations/:conversationId/messages
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → validate(sendMessageSchema)]
  → handler  services/conversation/src/routes/messages.ts:64
      → messageService.create()
      → db read   prisma.conversation.findFirst
      → db read   prisma.message.findFirst
      → db write  prisma.conversation.update
      → queue     outgoingMessageQueue.add  (async, outlives the response)
```

### `DELETE /api/conversations/:conversationId/messages/:messageId`

- **Purpose:** Deletes the referenced message. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/messages.ts` line 318
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/conversations` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/messages.ts`.
2. **Handler.** `services/conversation/src/routes/messages.ts:318` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `message.findFirst`
   _Writes:_
   - `message.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `403`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/conversations/:conversationId/messages/:messageId
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/messages.ts:318
      → db read   prisma.message.findFirst
      → db write  prisma.message.delete
```

### `POST /api/conversations/:conversationId/messages/media`

- **Purpose:** Creates a media for the referenced message. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/messages.ts` line 205
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/conversations` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/messages.ts`.
2. **Handler.** `services/conversation/src/routes/messages.ts:205` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls**, in source order:
   - `messageService.create()`
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.**
   - Enqueues `outgoingMessageQueue.add` (BullMQ). The response returns before this work runs; retries and backoff are the queue's.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/conversations/:conversationId/messages/media
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/messages.ts:205
      → messageService.create()
      → db read   prisma.conversation.findFirst
      → queue     outgoingMessageQueue.add  (async, outlives the response)
```

### `DELETE /api/conversations/:id`

- **Purpose:** Deletes the referenced conversation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/conversations.ts` line 189
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/conversations` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/conversations.ts`.
2. **Handler.** `services/conversation/src/routes/conversations.ts:189` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findFirst`
   _Writes:_
   - `message.deleteMany`
   - `conversation.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/conversations/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/conversations.ts:189
      → db read   prisma.conversation.findFirst
      → db write  prisma.message.deleteMany
      → db write  prisma.conversation.delete
```

### `GET /api/conversations/:id`

- **Purpose:** Reads a single conversation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/conversations.ts` line 102
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/conversations` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/conversations.ts`.
2. **Handler.** `services/conversation/src/routes/conversations.ts:102` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls**, in source order:
   - `conversationService.getById()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/conversations/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/conversations.ts:102
      → conversationService.getById()
```

### `POST /api/conversations/:id/claim`

- **Purpose:** Performs the `claim` action on the referenced conversation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/conversations.ts` line 113
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/conversations` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/conversations.ts`.
2. **Handler.** `services/conversation/src/routes/conversations.ts:113` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls**, in source order:
   - `conversationService.claim()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/conversations/:id/claim
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/conversations.ts:113
      → conversationService.claim()
```

### `POST /api/conversations/:id/close`

- **Purpose:** Performs the `close` action on the referenced conversation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/conversations.ts` line 174
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/conversations` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/conversations.ts`.
2. **Handler.** `services/conversation/src/routes/conversations.ts:174` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls**, in source order:
   - `conversationService.close()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/conversations/:id/close
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/conversations.ts:174
      → conversationService.close()
```

### `GET /api/conversations/:id/notes`

- **Purpose:** Reads a single note for the referenced conversation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/conversations.ts` line 91
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/conversations` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/conversations.ts`.
2. **Handler.** `services/conversation/src/routes/conversations.ts:91` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/conversations/:id/notes
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/conversations.ts:91
      → (responds directly; no downstream calls detected)
```

### `POST /api/conversations/:id/notes`

- **Purpose:** Performs the `notes` action on the referenced conversation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/conversations.ts` line 96
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/conversations` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/conversations.ts`.
2. **Handler.** `services/conversation/src/routes/conversations.ts:96` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/conversations/:id/notes
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/conversations.ts:96
      → (responds directly; no downstream calls detected)
```

### `POST /api/conversations/:id/reassign`

- **Purpose:** Allow both ADMIN and the assigned agent to transfer/reassign
- **Handler:** `services/conversation/src/routes/conversations.ts` line 143
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/conversations` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/conversations.ts`.
2. **Handler.** `services/conversation/src/routes/conversations.ts:143` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls**, in source order:
   - `conversationService.getById()`
   - `conversationService.transferToDepartment()`
   - `conversationService.reassign()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/conversations/:id/reassign
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/conversations.ts:143
      → conversationService.getById()
      → conversationService.transferToDepartment()
      → conversationService.reassign()
```

### `POST /api/conversations/:id/release`

- **Purpose:** Performs the `release` action on the referenced conversation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/conversations.ts` line 122
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/conversations` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/conversations.ts`.
2. **Handler.** `services/conversation/src/routes/conversations.ts:122` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls**, in source order:
   - `conversationService.release()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/conversations/:id/release
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/conversations.ts:122
      → conversationService.release()
```

### `POST /api/conversations/:id/return-to-ai`

- **Purpose:** Hand a conversation back to the AI employee (clears the one-way isHandedOver latch). 409 when no AI employee is bound.
- **Handler:** `services/conversation/src/routes/conversations.ts` line 133
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/conversations` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/conversations.ts`.
2. **Handler.** `services/conversation/src/routes/conversations.ts:133` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls**, in source order:
   - `conversationService.returnToAi()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/conversations/:id/return-to-ai
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/conversations.ts:133
      → conversationService.returnToAi()
```

### `GET /api/conversations/history/:customerExternalId`

- **Purpose:** Reads a single history. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/conversations.ts` line 71
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/conversations` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/conversations.ts`.
2. **Handler.** `services/conversation/src/routes/conversations.ts:71` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls**, in source order:
   - `conversationService.getHistoryByCustomerExternalId()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/conversations/history/:customerExternalId
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/conversations.ts:71
      → conversationService.getHistoryByCustomerExternalId()
```

### `GET /api/conversations/stats/workload`

- **Purpose:** Lists workload. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/conversations.ts` line 37
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/conversations` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/conversations.ts`.
2. **Handler.** `services/conversation/src/routes/conversations.ts:37` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `conversationService.getAgentWorkload()`
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/conversations/stats/workload
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/conversations.ts:37
      → conversationService.getAgentWorkload()
```

### `GET /api/router-rules`

- **Purpose:** Lists router rules. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/router-rules.ts` line 18
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/router-rules` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/router-rules.ts`.
2. **Handler.** `services/ai/src/routes/router-rules.ts:18` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `routerRule.findMany`
   - `chatbotFlow.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/router-rules
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/router-rules.ts:18
      → db read   prisma.routerRule.findMany
      → db read   prisma.chatbotFlow.findMany
```

### `POST /api/router-rules`

- **Purpose:** Creates a router rule. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/router-rules.ts` line 81
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/router-rules` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/router-rules.ts`.
2. **Handler.** `services/ai/src/routes/router-rules.ts:81` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `routerRule.aggregate`
   _Writes:_
   - `routerRule.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/router-rules
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/router-rules.ts:81
      → db read   prisma.routerRule.aggregate
      → db write  prisma.routerRule.create
```

### `DELETE /api/router-rules/:id`

- **Purpose:** Deletes the referenced router rule. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/router-rules.ts` line 323
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/router-rules` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/router-rules.ts`.
2. **Handler.** `services/ai/src/routes/router-rules.ts:323` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `routerRule.findFirst`
   _Writes:_
   - `routerRule.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/router-rules/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/router-rules.ts:323
      → db read   prisma.routerRule.findFirst
      → db write  prisma.routerRule.delete
```

### `GET /api/router-rules/:id`

- **Purpose:** Reads a single router rule. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/router-rules.ts` line 59
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/router-rules` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/router-rules.ts`.
2. **Handler.** `services/ai/src/routes/router-rules.ts:59` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `routerRule.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/router-rules/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/router-rules.ts:59
      → db read   prisma.routerRule.findFirst
```

### `PATCH /api/router-rules/:id`

- **Purpose:** Updates the referenced router rule. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/router-rules.ts` line 124
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/router-rules` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/router-rules.ts`.
2. **Handler.** `services/ai/src/routes/router-rules.ts:124` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `routerRule.findFirst`
   _Writes:_
   - `routerRule.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/router-rules/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/router-rules.ts:124
      → db read   prisma.routerRule.findFirst
      → db write  prisma.routerRule.update
```

### `PUT /api/router-rules/reorder`

- **Purpose:** Client sends `ruleIds` in desired evaluation order. The server writes `position` = index + 1 for each. Default rules are automatically evaluated last by the live router regardless of their position value.
- **Handler:** `services/ai/src/routes/router-rules.ts` line 168
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/router-rules` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/router-rules.ts`.
2. **Handler.** `services/ai/src/routes/router-rules.ts:168` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `routerRule.findMany`
   _Writes:_
   - `routerRule.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/router-rules/reorder
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/router-rules.ts:168
      → db read   prisma.routerRule.findMany
      → db write  prisma.routerRule.update
```

### `POST /api/router-rules/test`

- **Purpose:** Body: { message: string, channel?: string, tags?: string[] } Returns: full routing trace + which rule (or fallback) would win. No DB mutation. Admin only.
- **Handler:** `services/ai/src/routes/router-rules.ts` line 208
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/router-rules` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/router-rules.ts`.
2. **Handler.** `services/ai/src/routes/router-rules.ts:208` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `routerRule.findMany`
   - `tenant.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/router-rules/test
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/router-rules.ts:208
      → db read   prisma.routerRule.findMany
      → db read   prisma.tenant.findUnique
```

### `GET /api/scheduled-messages`

- **Purpose:** GET / - List scheduled messages for tenant
- **Handler:** `services/conversation/src/routes/scheduled-messages.ts` line 8
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`, `AGENT`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/scheduled-messages` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/scheduled-messages.ts`.
2. **Handler.** `services/conversation/src/routes/scheduled-messages.ts:8` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`, `AGENT`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `scheduledMessage.findMany`
   - `scheduledMessage.count`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/scheduled-messages
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → requireRole(AGENT)]
  → handler  services/conversation/src/routes/scheduled-messages.ts:8
      → db read   prisma.scheduledMessage.findMany
      → db read   prisma.scheduledMessage.count
```

### `POST /api/scheduled-messages`

- **Purpose:** POST / - Create scheduled message
- **Handler:** `services/conversation/src/routes/scheduled-messages.ts` line 51
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`, `AGENT`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/scheduled-messages` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/scheduled-messages.ts`.
2. **Handler.** `services/conversation/src/routes/scheduled-messages.ts:51` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`, `AGENT`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `scheduledMessage.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/scheduled-messages
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → requireRole(AGENT)]
  → handler  services/conversation/src/routes/scheduled-messages.ts:51
      → db write  prisma.scheduledMessage.create
```

### `DELETE /api/scheduled-messages/:id`

- **Purpose:** DELETE /:id - Cancel scheduled message
- **Handler:** `services/conversation/src/routes/scheduled-messages.ts` line 147
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`, `AGENT`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/scheduled-messages` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/scheduled-messages.ts`.
2. **Handler.** `services/conversation/src/routes/scheduled-messages.ts:147` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`, `AGENT`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `scheduledMessage.findFirst`
   _Writes:_
   - `scheduledMessage.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/scheduled-messages/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → requireRole(AGENT)]
  → handler  services/conversation/src/routes/scheduled-messages.ts:147
      → db read   prisma.scheduledMessage.findFirst
      → db write  prisma.scheduledMessage.update
```

### `GET /api/scheduled-messages/:id`

- **Purpose:** GET /:id - Get single scheduled message
- **Handler:** `services/conversation/src/routes/scheduled-messages.ts` line 37
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`, `AGENT`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/scheduled-messages` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/scheduled-messages.ts`.
2. **Handler.** `services/conversation/src/routes/scheduled-messages.ts:37` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`, `AGENT`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `scheduledMessage.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/scheduled-messages/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → requireRole(AGENT)]
  → handler  services/conversation/src/routes/scheduled-messages.ts:37
      → db read   prisma.scheduledMessage.findFirst
```

### `PATCH /api/scheduled-messages/:id`

- **Purpose:** PATCH /:id - Update scheduled message (only if PENDING)
- **Handler:** `services/conversation/src/routes/scheduled-messages.ts` line 109
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`, `AGENT`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/scheduled-messages` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/scheduled-messages.ts`.
2. **Handler.** `services/conversation/src/routes/scheduled-messages.ts:109` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`, `AGENT`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `scheduledMessage.findFirst`
   _Writes:_
   - `scheduledMessage.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/scheduled-messages/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → requireRole(AGENT)]
  → handler  services/conversation/src/routes/scheduled-messages.ts:109
      → db read   prisma.scheduledMessage.findFirst
      → db write  prisma.scheduledMessage.update
```

### `GET /api/templates`

- **Purpose:** Lists templates. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/templates.ts` line 261
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/templates` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/templates.ts`.
2. **Handler.** `services/conversation/src/routes/templates.ts:261` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `messageTemplate.findMany`
   - `messageTemplate.count`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/templates
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/templates.ts:261
      → db read   prisma.messageTemplate.findMany
      → db read   prisma.messageTemplate.count
```

### `POST /api/templates`

- **Purpose:** Creates a template. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/templates.ts` line 304
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/templates` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/templates.ts`.
2. **Handler.** `services/conversation/src/routes/templates.ts:304` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
   _Writes:_
   - `messageTemplate.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/templates
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/templates.ts:304
      → db read   prisma.channelAccount.findFirst
      → db write  prisma.messageTemplate.create
```

### `DELETE /api/templates/:id`

- **Purpose:** Deletes the referenced template. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/templates.ts` line 415
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/templates` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/templates.ts`.
2. **Handler.** `services/conversation/src/routes/templates.ts:415` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `messageTemplate.findFirst`
   - `broadcast.count`
   _Writes:_
   - `broadcast.updateMany`
   - `messageTemplate.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/templates/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/templates.ts:415
      → db read   prisma.messageTemplate.findFirst
      → db read   prisma.broadcast.count
      → db write  prisma.broadcast.updateMany
      → db write  prisma.messageTemplate.delete
```

### `GET /api/templates/:id`

- **Purpose:** Reads a single template. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/templates.ts` line 291
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/templates` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/templates.ts`.
2. **Handler.** `services/conversation/src/routes/templates.ts:291` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `messageTemplate.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/templates/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/templates.ts:291
      → db read   prisma.messageTemplate.findFirst
```

### `PATCH /api/templates/:id`

- **Purpose:** Updates the referenced template. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/templates.ts` line 363
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/templates` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/templates.ts`.
2. **Handler.** `services/conversation/src/routes/templates.ts:363` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `messageTemplate.findFirst`
   - `channelAccount.findFirst`
   _Writes:_
   - `messageTemplate.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/templates/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/templates.ts:363
      → db read   prisma.messageTemplate.findFirst
      → db read   prisma.channelAccount.findFirst
      → db write  prisma.messageTemplate.update
```

### `POST /api/templates/:id/duplicate`

- **Purpose:** Performs the `duplicate` action on the referenced template. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/templates.ts` line 465
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/templates` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/templates.ts`.
2. **Handler.** `services/conversation/src/routes/templates.ts:465` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `messageTemplate.findFirst`
   _Writes:_
   - `messageTemplate.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/templates/:id/duplicate
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/templates.ts:465
      → db read   prisma.messageTemplate.findFirst
      → db write  prisma.messageTemplate.create
```

### `POST /api/templates/:id/submit-to-meta`

- **Purpose:** Performs the `submit to meta` action on the referenced template. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/templates.ts` line 497
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/templates` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/templates.ts`.
2. **Handler.** `services/conversation/src/routes/templates.ts:497` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `messageTemplate.findFirst`
   _Writes:_
   - `messageTemplate.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/templates/:id/submit-to-meta
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/templates.ts:497
      → db read   prisma.messageTemplate.findFirst
      → db write  prisma.messageTemplate.update
```

## Embedded Chat

4 endpoints.

### `POST /api/embedded-chat/bootstrap`

- **Purpose:** POST /api/embedded-chat/init - Initialize a chat session (public, no auth)
- **Handler:** `services/ai/src/routes/embedded-chat.ts` line 107
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/embedded-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/embedded-chat.ts`.
2. **Handler.** `services/ai/src/routes/embedded-chat.ts:107` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
   _Writes:_
   - `channelAccount.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `429` from `initLimiter`.

#### Call graph

```text
POST /api/embedded-chat/bootstrap
  → handler  services/ai/src/routes/embedded-chat.ts:107
      → db read   prisma.channelAccount.findFirst
      → db write  prisma.channelAccount.update
```

### `POST /api/embedded-chat/init`

- **Purpose:** Creates an init. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/embedded-chat.ts` line 170
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/embedded-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/embedded-chat.ts`.
2. **Handler.** `services/ai/src/routes/embedded-chat.ts:170` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
   - `conversation.findFirst`
   _Writes:_
   - `channelAccount.update`
   - `conversation.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `429` from `initLimiter`.

#### Call graph

```text
POST /api/embedded-chat/init
  → handler  services/ai/src/routes/embedded-chat.ts:170
      → db read   prisma.channelAccount.findFirst
      → db read   prisma.conversation.findFirst
      → db write  prisma.channelAccount.update
      → db write  prisma.conversation.create
```

### `POST /api/embedded-chat/message`

- **Purpose:** POST /api/embedded-chat/message - Send a message (public)
- **Handler:** `services/ai/src/routes/embedded-chat.ts` line 279
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/embedded-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/embedded-chat.ts`.
2. **Handler.** `services/ai/src/routes/embedded-chat.ts:279` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.**
   - Enqueues `incomingMessageQueue.add` (BullMQ). The response returns before this work runs; retries and backoff are the queue's.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `413`, `500`, answering `{ error: ... }`. `429` from `messageLimiter`.

#### Call graph

```text
POST /api/embedded-chat/message
  → handler  services/ai/src/routes/embedded-chat.ts:279
      → db read   prisma.conversation.findUnique
      → queue     incomingMessageQueue.add  (async, outlives the response)
```

### `GET /api/embedded-chat/messages/:sessionId`

- **Purpose:** GET /api/embedded-chat/messages/:sessionId - Get messages for a session (public)
- **Handler:** `services/ai/src/routes/embedded-chat.ts` line 343
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/embedded-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/embedded-chat.ts`.
2. **Handler.** `services/ai/src/routes/embedded-chat.ts:343` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findUnique`
   - `message.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `429` from `pollLimiter`.

#### Call graph

```text
GET /api/embedded-chat/messages/:sessionId
  → handler  services/ai/src/routes/embedded-chat.ts:343
      → db read   prisma.conversation.findUnique
      → db read   prisma.message.findMany
```

## GDPR & Compliance

14 endpoints.

### `POST /api/gdpr-internal/purge-tenant-vectors`

- **Purpose:** POST /api/gdpr-internal/purge-tenant-vectors   body: { tenantId }   Called by the auth service during tenant deletion to clean up this   tenant's Qdrant vectors (KB embeddings are the only per-tenant data   this service stores outside Postgres).
- **Handler:** `services/ai/src/routes/gdpr-internal.ts` line 25
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/gdpr-internal` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/gdpr-internal.ts`.
2. **Handler.** `services/ai/src/routes/gdpr-internal.ts:25` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/gdpr-internal/purge-tenant-vectors
  → [internalAuth]
  → handler  services/ai/src/routes/gdpr-internal.ts:25
      → (responds directly; no downstream calls detected)
```

### `POST /api/gdpr-internal/run-retention-purge`

- **Purpose:** POST /api/gdpr-internal/run-retention-purge   Triggers a full data-retention purge run across all enabled   DataRetentionPolicy rows. Invoked by cron or manually.
- **Handler:** `services/ai/src/routes/gdpr-internal.ts` line 52
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/gdpr-internal` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/gdpr-internal.ts`.
2. **Handler.** `services/ai/src/routes/gdpr-internal.ts:52` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/gdpr-internal/run-retention-purge
  → [internalAuth]
  → handler  services/ai/src/routes/gdpr-internal.ts:52
      → (responds directly; no downstream calls detected)
```

### `GET /api/gdpr/consent`

- **Purpose:** Lists consent. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/gdpr.ts` line 139
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/gdpr` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/gdpr.ts`.
2. **Handler.** `services/auth/src/routes/gdpr.ts:139` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `consentRecord.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/gdpr/consent
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/auth/src/routes/gdpr.ts:139
      → db read   prisma.consentRecord.findMany
```

### `POST /api/gdpr/consent`

- **Purpose:** Creates a consent. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/gdpr.ts` line 105
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/gdpr` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/gdpr.ts`.
2. **Handler.** `services/auth/src/routes/gdpr.ts:105` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `consentRecord.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/gdpr/consent
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/auth/src/routes/gdpr.ts:105
      → db write  prisma.consentRecord.create
```

### `DELETE /api/gdpr/contacts/:id`

- **Purpose:** DELETE /:id - Right to erasure (Art. 17)
- **Handler:** `services/conversation/src/routes/gdpr-contacts.ts` line 124
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/gdpr` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/gdpr-contacts.ts`.
2. **Handler.** `services/conversation/src/routes/gdpr-contacts.ts:124` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `contact.findFirst`
   - `contact.findMany`
   - `conversation.findMany`
   _Writes:_
   - `message.deleteMany`
   - `conversation.deleteMany`
   - `consentRecord.deleteMany`
   - `contact.deleteMany`
   - `customerHistoricalMemory.deleteMany`
   - `dataSubjectRequest.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/gdpr/contacts/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/gdpr-contacts.ts:124
      → db read   prisma.contact.findFirst
      → db read   prisma.contact.findMany
      → db read   prisma.conversation.findMany
      → db write  prisma.message.deleteMany
      → db write  prisma.conversation.deleteMany
      → db write  prisma.consentRecord.deleteMany
      → db write  prisma.contact.deleteMany
      → db write  prisma.customerHistoricalMemory.deleteMany
      → db write  prisma.dataSubjectRequest.create
```

### `GET /api/gdpr/contacts/:id/consent`

- **Purpose:** GET /:id/consent - list consent records for the contact
- **Handler:** `services/conversation/src/routes/gdpr-contacts.ts` line 279
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/gdpr` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/gdpr-contacts.ts`.
2. **Handler.** `services/conversation/src/routes/gdpr-contacts.ts:279` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `contact.findFirst`
   - `consentRecord.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/gdpr/contacts/:id/consent
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/gdpr-contacts.ts:279
      → db read   prisma.contact.findFirst
      → db read   prisma.consentRecord.findMany
```

### `POST /api/gdpr/contacts/:id/consent`

- **Purpose:** POST /:id/consent - record consent (Art. 7 proof)
- **Handler:** `services/conversation/src/routes/gdpr-contacts.ts` line 229
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/gdpr` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/gdpr-contacts.ts`.
2. **Handler.** `services/conversation/src/routes/gdpr-contacts.ts:229` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `contact.findFirst`
   _Writes:_
   - `consentRecord.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/gdpr/contacts/:id/consent
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/gdpr-contacts.ts:229
      → db read   prisma.contact.findFirst
      → db write  prisma.consentRecord.create
```

### `GET /api/gdpr/contacts/:id/export`

- **Purpose:** GET /:id/export - Right of access/portability (Art. 15/20)
- **Handler:** `services/conversation/src/routes/gdpr-contacts.ts` line 34
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/gdpr` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/gdpr-contacts.ts`.
2. **Handler.** `services/conversation/src/routes/gdpr-contacts.ts:34` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `contact.findFirst`
   - `conversation.findMany`
   - `message.findMany`
   - `consentRecord.findMany`
   _Writes:_
   - `dataSubjectRequest.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/gdpr/contacts/:id/export
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/gdpr-contacts.ts:34
      → db read   prisma.contact.findFirst
      → db read   prisma.conversation.findMany
      → db read   prisma.message.findMany
      → db read   prisma.consentRecord.findMany
      → db write  prisma.dataSubjectRequest.create
```

### `GET /api/gdpr/retention`

- **Purpose:** Lists retention. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/gdpr.ts` line 165
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/gdpr` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/gdpr.ts`.
2. **Handler.** `services/auth/src/routes/gdpr.ts:165` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `dataRetentionPolicy.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/gdpr/retention
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/auth/src/routes/gdpr.ts:165
      → db read   prisma.dataRetentionPolicy.findMany
```

### `PUT /api/gdpr/retention`

- **Purpose:** Updates the referenced retention. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/gdpr.ts` line 182
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/gdpr` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/gdpr.ts`.
2. **Handler.** `services/auth/src/routes/gdpr.ts:182` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `dataRetentionPolicy.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/gdpr/retention
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/auth/src/routes/gdpr.ts:182
      → db write  prisma.dataRetentionPolicy.upsert
```

### `DELETE /api/gdpr/tenants/:tenantId`

- **Purpose:** Deletes the referenced tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/gdpr.ts` line 88
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/gdpr` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/gdpr.ts`.
2. **Handler.** `services/auth/src/routes/gdpr.ts:88` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/gdpr/tenants/:tenantId
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/gdpr.ts:88
      → (responds directly; no downstream calls detected)
```

### `GET /api/gdpr/tenants/:tenantId/export`

- **Purpose:** Reads a single export for the referenced tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/gdpr.ts` line 72
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/gdpr` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/gdpr.ts`.
2. **Handler.** `services/auth/src/routes/gdpr.ts:72` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/gdpr/tenants/:tenantId/export
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/gdpr.ts:72
      → (responds directly; no downstream calls detected)
```

### `DELETE /api/gdpr/users/:userId`

- **Purpose:** Deletes the referenced user. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/gdpr.ts` line 46
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/gdpr` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/gdpr.ts`.
2. **Handler.** `services/auth/src/routes/gdpr.ts:46` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/gdpr/users/:userId
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/auth/src/routes/gdpr.ts:46
      → (responds directly; no downstream calls detected)
```

### `GET /api/gdpr/users/:userId/export`

- **Purpose:** Reads a single export for the referenced user. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/gdpr.ts` line 30
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/gdpr` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/gdpr.ts`.
2. **Handler.** `services/auth/src/routes/gdpr.ts:30` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/gdpr/users/:userId/export
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/auth/src/routes/gdpr.ts:30
      → (responds directly; no downstream calls detected)
```

## Historical Import

14 endpoints.

### `GET /api/historical-imports`

- **Purpose:** GET /api/historical-imports
- **Handler:** `services/ai/src/routes/historical-import.ts` line 56
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/historical-imports` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/historical-import.ts`.
2. **Handler.** `services/ai/src/routes/historical-import.ts:56` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `historicalImport.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/historical-imports
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/historical-import.ts:56
      → db read   prisma.historicalImport.findMany
```

### `GET /api/historical-imports/:id`

- **Purpose:** GET /api/historical-imports/:id - status plus the full persisted summary.
- **Handler:** `services/ai/src/routes/historical-import.ts` line 71
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/historical-imports` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/historical-import.ts`.
2. **Handler.** `services/ai/src/routes/historical-import.ts:71` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `historicalImport.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/historical-imports/:id
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/historical-import.ts:71
      → db read   prisma.historicalImport.findFirst
```

### `POST /api/historical-imports/:id/bulk-approve`

- **Purpose:** POST /api/historical-imports/:id/bulk-approve
- **Handler:** `services/ai/src/routes/historical-import.ts` line 428
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`; Gate: `writeGuard`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/historical-imports` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/historical-import.ts`.
2. **Handler.** `services/ai/src/routes/historical-import.ts:428` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`; Gate: `writeGuard`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeCandidate.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/historical-imports/:id/bulk-approve
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → writeGuard]
  → handler  services/ai/src/routes/historical-import.ts:428
      → db read   prisma.knowledgeCandidate.findMany
```

### `POST /api/historical-imports/:id/bulk-reject`

- **Purpose:** POST /api/historical-imports/:id/bulk-reject
- **Handler:** `services/ai/src/routes/historical-import.ts` line 465
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`; Gate: `writeGuard`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/historical-imports` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/historical-import.ts`.
2. **Handler.** `services/ai/src/routes/historical-import.ts:465` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`; Gate: `writeGuard`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `knowledgeCandidate.updateMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/historical-imports/:id/bulk-reject
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → writeGuard]
  → handler  services/ai/src/routes/historical-import.ts:465
      → db write  prisma.knowledgeCandidate.updateMany
```

### `GET /api/historical-imports/:id/candidates`

- **Purpose:** GET /api/historical-imports/:id/candidates
- **Handler:** `services/ai/src/routes/historical-import.ts` line 129
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/historical-imports` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/historical-import.ts`.
2. **Handler.** `services/ai/src/routes/historical-import.ts:129` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeCandidate.findMany`
   - `knowledgeCandidate.groupBy`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/historical-imports/:id/candidates
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/historical-import.ts:129
      → db read   prisma.knowledgeCandidate.findMany
      → db read   prisma.knowledgeCandidate.groupBy
```

### `GET /api/historical-imports/:id/customers`

- **Purpose:** GET /api/historical-imports/:id/customers
- **Handler:** `services/ai/src/routes/historical-import.ts` line 565
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/historical-imports` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/historical-import.ts`.
2. **Handler.** `services/ai/src/routes/historical-import.ts:565` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `historicalCustomer.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/historical-imports/:id/customers
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/historical-import.ts:565
      → db read   prisma.historicalCustomer.findMany
```

### `GET /api/historical-imports/:id/events`

- **Purpose:** GET /api/historical-imports/:id/events
- **Handler:** `services/ai/src/routes/historical-import.ts` line 97
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/historical-imports` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/historical-import.ts`.
2. **Handler.** `services/ai/src/routes/historical-import.ts:97` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `historicalImport.findFirst`
   - `historicalImportEvent.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/historical-imports/:id/events
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/historical-import.ts:97
      → db read   prisma.historicalImport.findFirst
      → db read   prisma.historicalImportEvent.findMany
```

### `POST /api/historical-imports/:id/rerun-intelligence`

- **Purpose:** POST /api/historical-imports/:id/rerun-intelligence
- **Handler:** `services/ai/src/routes/historical-import.ts` line 492
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`; Gate: `writeGuard`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/historical-imports` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/historical-import.ts`.
2. **Handler.** `services/ai/src/routes/historical-import.ts:492` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`; Gate: `writeGuard`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/historical-imports/:id/rerun-intelligence
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → writeGuard]
  → handler  services/ai/src/routes/historical-import.ts:492
      → (responds directly; no downstream calls detected)
```

### `POST /api/historical-imports/candidates/:candidateId/approve`

- **Purpose:** POST /api/historical-imports/candidates/:candidateId/approve
- **Handler:** `services/ai/src/routes/historical-import.ts` line 263
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`; Gate: `writeGuard`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/historical-imports` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/historical-import.ts`.
2. **Handler.** `services/ai/src/routes/historical-import.ts:263` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`; Gate: `writeGuard`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeCandidate.findFirst`
   _Writes:_
   - `knowledgeCandidate.updateMany`
   - `knowledgeDocument.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/historical-imports/candidates/:candidateId/approve
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → writeGuard]
  → handler  services/ai/src/routes/historical-import.ts:263
      → db read   prisma.knowledgeCandidate.findFirst
      → db write  prisma.knowledgeCandidate.updateMany
      → db write  prisma.knowledgeDocument.create
```

### `GET /api/historical-imports/candidates/:candidateId/evidence`

- **Purpose:** GET /api/historical-imports/candidates/:candidateId/evidence
- **Handler:** `services/ai/src/routes/historical-import.ts` line 224
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/historical-imports` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/historical-import.ts`.
2. **Handler.** `services/ai/src/routes/historical-import.ts:224` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeCandidate.findFirst`
   - `knowledgeCandidateEvidence.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/historical-imports/candidates/:candidateId/evidence
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/historical-import.ts:224
      → db read   prisma.knowledgeCandidate.findFirst
      → db read   prisma.knowledgeCandidateEvidence.findMany
```

### `POST /api/historical-imports/candidates/:candidateId/reject`

- **Purpose:** POST /api/historical-imports/candidates/:candidateId/reject
- **Handler:** `services/ai/src/routes/historical-import.ts` line 399
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`; Gate: `writeGuard`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/historical-imports` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/historical-import.ts`.
2. **Handler.** `services/ai/src/routes/historical-import.ts:399` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`; Gate: `writeGuard`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `knowledgeCandidate.updateMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/historical-imports/candidates/:candidateId/reject
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → writeGuard]
  → handler  services/ai/src/routes/historical-import.ts:399
      → db write  prisma.knowledgeCandidate.updateMany
```

### `GET /api/historical-imports/conversations/:conversationId/messages`

- **Purpose:** GET /api/historical-imports/conversations/:conversationId/messages
- **Handler:** `services/ai/src/routes/historical-import.ts` line 631
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/historical-imports` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/historical-import.ts`.
2. **Handler.** `services/ai/src/routes/historical-import.ts:631` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findFirst`
   - `message.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/historical-imports/conversations/:conversationId/messages
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/historical-import.ts:631
      → db read   prisma.conversation.findFirst
      → db read   prisma.message.findMany
```

### `GET /api/historical-imports/customer-context`

- **Purpose:** GET /api/historical-imports/customer-context?externalId=<phone>
- **Handler:** `services/ai/src/routes/historical-import.ts` line 518
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/historical-imports` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/historical-import.ts`.
2. **Handler.** `services/ai/src/routes/historical-import.ts:518` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `customerHistoricalMemory.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/historical-imports/customer-context
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/historical-import.ts:518
      → db read   prisma.customerHistoricalMemory.findFirst
```

### `GET /api/historical-imports/customers/:customerId/memory`

- **Purpose:** GET /api/historical-imports/customers/:customerId/memory
- **Handler:** `services/ai/src/routes/historical-import.ts` line 602
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/historical-imports` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/historical-import.ts`.
2. **Handler.** `services/ai/src/routes/historical-import.ts:602` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `historicalCustomer.findFirst`
   - `customerHistoricalMemory.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/historical-imports/customers/:customerId/memory
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/historical-import.ts:602
      → db read   prisma.historicalCustomer.findFirst
      → db read   prisma.customerHistoricalMemory.findFirst
```

## Integrations

71 endpoints.

### `GET /api/commerce-context/:conversationId`

- **Purpose:** Reads a single commerce context. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/commerce-context.ts` line 72
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Permission: `customer:commerce:read`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/commerce-context` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/commerce-context.ts`.
2. **Handler.** `services/ai/src/routes/commerce-context.ts:72` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Permission: `customer:commerce:read`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/commerce-context/:conversationId
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requirePermission(customer:commerce:read)]
  → handler  services/ai/src/routes/commerce-context.ts:72
      → (responds directly; no downstream calls detected)
```

### `POST /api/commerce-context/:conversationId/actions`

- **Purpose:** Performs the `actions` action on the referenced commerce context. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/commerce-context.ts` line 103
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Permission: `customer:commerce:cancel`, `customer:commerce:refund`, `customer:commerce:return`, `customer:commerce:tag`, `customer:commerce:note`, `customer:commerce:notify`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/commerce-context` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/commerce-context.ts`.
2. **Handler.** `services/ai/src/routes/commerce-context.ts:103` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Permission: `customer:commerce:cancel`, `customer:commerce:refund`, `customer:commerce:return`, `customer:commerce:tag`, `customer:commerce:note`, `customer:commerce:notify`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/commerce-context/:conversationId/actions
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requirePermission(customer:commerce:cancel) → requirePermission(customer:commerce:refund) → requirePermission(customer:commerce:return) → requirePermission(customer:commerce:tag) → requirePermission(customer:commerce:note) → requirePermission(customer:commerce:notify)]
  → handler  services/ai/src/routes/commerce-context.ts:103
      → (responds directly; no downstream calls detected)
```

### `POST /api/connectors/:slug/config`

- **Purpose:** Performs the `config` action on the referenced connector. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 161
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `canManageSystems`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:161` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `canManageSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/connectors/:slug/config
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → canManageSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:161
      → (responds directly; no downstream calls detected)
```

### `POST /api/connectors/:slug/connect`

- **Purpose:** Performs the `connect` action on the referenced connector. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 226
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:226` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/connectors/:slug/connect
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:226
      → (responds directly; no downstream calls detected)
```

### `POST /api/connectors/:slug/disconnect`

- **Purpose:** Performs the `disconnect` action on the referenced connector. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 147
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `canManageSystems`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:147` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `canManageSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/connectors/:slug/disconnect
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → canManageSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:147
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/:slug/status`

- **Purpose:** Reads a single statu for the referenced connector. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 133
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `canReadSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:133` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `canReadSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/:slug/status
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → canReadSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:133
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/airtable/mapping`

- **Purpose:** Read the mapping currently on the connection config - powers the post-onboarding "refresh fields / edit mapping" card. The onboarding wizard never needed this (it writes a fresh mapping), which is why editing the mapping after onboarding used to be impossible without reconnecting.
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 826
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:826` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/airtable/mapping
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:826
      → (responds directly; no downstream calls detected)
```

### `POST /api/connectors/airtable/mapping`

- **Purpose:** Save the mapping onto the connection config. Optionally auto-create the notes / idempotency columns we OWN (never identifier columns) when create_missing=true and the token carries schema.bases:write.
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 853
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:853` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/connectors/airtable/mapping
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:853
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/airtable/meta/bases`

- **Purpose:** Lists bases. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 656
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:656` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/airtable/meta/bases
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:656
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/airtable/meta/tables/:baseId`

- **Purpose:** Reads a single table. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 671
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:671` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/airtable/meta/tables/:baseId
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:671
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/airtable/oauth/bases`

- **Purpose:** Lists bases. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 784
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:784` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/airtable/oauth/bases
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:784
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/airtable/oauth/callback`

- **Purpose:** Lists callback. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 719
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:719` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/connectors/airtable/oauth/callback
  → handler  services/ai/src/routes/connectors-admin.ts:719
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/airtable/oauth/fields`

- **Purpose:** Lists fields. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 808
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:808` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/airtable/oauth/fields
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:808
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/airtable/oauth/init`

- **Purpose:** Airtable mandates PKCE (S256). Our other OAuth flows are plain auth-code; here we generate a code_verifier, send its S256 challenge on init, and carry the verifier inside the signed, short-lived state JWT so the callback can complete the token exchange. Confidential client → also HTTP Basic with the client secret. Reachable during onboarding (requireOnboardingOrActiveTenant).
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 694
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:694` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/airtable/oauth/init
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:694
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/airtable/oauth/tables`

- **Purpose:** Lists tables. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 795
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:795` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/airtable/oauth/tables
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:795
      → (responds directly; no downstream calls detected)
```

### `POST /api/connectors/aws_rds/meta/tables`

- **Purpose:** Creates a table. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 1381
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:1381` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/connectors/aws_rds/meta/tables
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:1381
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/hubspot/oauth/callback`

- **Purpose:** Lists callback. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 391
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:391` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/connectors/hubspot/oauth/callback
  → handler  services/ai/src/routes/connectors-admin.ts:391
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/hubspot/oauth/init`

- **Purpose:** Lists init. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 332
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:332` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/hubspot/oauth/init
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:332
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/monday/meta/boards`

- **Purpose:** Lists boards. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 1231
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:1231` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/monday/meta/boards
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:1231
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/monday/oauth/callback`

- **Purpose:** Lists callback. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 1167
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:1167` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/connectors/monday/oauth/callback
  → handler  services/ai/src/routes/connectors-admin.ts:1167
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/monday/oauth/init`

- **Purpose:** Lists init. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 1139
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:1139` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/monday/oauth/init
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:1139
      → (responds directly; no downstream calls detected)
```

### `POST /api/connectors/mongodb/meta/collections`

- **Purpose:** Creates a collection. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 1320
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:1320` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/connectors/mongodb/meta/collections
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:1320
      → (responds directly; no downstream calls detected)
```

### `POST /api/connectors/mongodb/meta/databases`

- **Purpose:** Creates a database. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 1350
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:1350` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/connectors/mongodb/meta/databases
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:1350
      → (responds directly; no downstream calls detected)
```

### `POST /api/connectors/postgres/meta/tables`

- **Purpose:** Creates a table. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 1278
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:1278` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/connectors/postgres/meta/tables
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:1278
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/salesforce/oauth/callback`

- **Purpose:** Lists callback. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 1087
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:1087` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/connectors/salesforce/oauth/callback
  → handler  services/ai/src/routes/connectors-admin.ts:1087
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/salesforce/oauth/init`

- **Purpose:** Lists init. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 1062
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:1062` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/salesforce/oauth/init
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:1062
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/shopify/install`

- **Purpose:** Shopify's signed app-entry request. PUBLIC by requirement.
- **Handler:** `services/ai/src/routes/shopify-install.ts` line 201
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-install.ts`.
2. **Handler.** `services/ai/src/routes/shopify-install.ts:201` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
GET /api/connectors/shopify/install
  → handler  services/ai/src/routes/shopify-install.ts:201
      → (responds directly; no downstream calls detected)
```

### `POST /api/connectors/shopify/install/cancel`

- **Purpose:** Abandon an intent without installing.
- **Handler:** `services/ai/src/routes/shopify-install.ts` line 381
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-install.ts`.
2. **Handler.** `services/ai/src/routes/shopify-install.ts:381` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/connectors/shopify/install/cancel
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/shopify-install.ts:381
      → (responds directly; no downstream calls detected)
```

### `POST /api/connectors/shopify/install/claim`

- **Purpose:** Bind a verified-but-unclaimed installation to the caller's workspace.
- **Handler:** `services/ai/src/routes/shopify-install.ts` line 301
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-install.ts`.
2. **Handler.** `services/ai/src/routes/shopify-install.ts:301` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/connectors/shopify/install/claim
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/shopify-install.ts:301
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/shopify/install/pending`

- **Purpose:** What is waiting for this browser to claim, if anything.
- **Handler:** `services/ai/src/routes/shopify-install.ts` line 275
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-install.ts`.
2. **Handler.** `services/ai/src/routes/shopify-install.ts:275` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/shopify/install/pending
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/shopify-install.ts:275
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/shopify/install/start`

- **Purpose:** "Connect Shopify", pressed by a signed-in user.
- **Handler:** `services/ai/src/routes/shopify-install.ts` line 135
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-install.ts`.
2. **Handler.** `services/ai/src/routes/shopify-install.ts:135` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `503`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/shopify/install/start
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/shopify-install.ts:135
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/shopify/oauth/callback`

- **Purpose:** The OAuth callback. Public by necessity - Shopify calls it, not a browser we authenticated.
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 523
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:523` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/connectors/shopify/oauth/callback
  → handler  services/ai/src/routes/connectors-admin.ts:523
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/shopify/oauth/init`

- **Purpose:** Installation NO LONGER STARTS HERE. The merchant-facing entry point is `/connectors/shopify/install/start` (routes/shopify-install.ts), which sends them to a Shopify-owned page; Shopify then calls our public install handler with a signed request and OAuth begins there.
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 459
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:459` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/shopify/oauth/init
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:459
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/square/oauth/callback`

- **Purpose:** Lists callback. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 1011
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:1011` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/connectors/square/oauth/callback
  → handler  services/ai/src/routes/connectors-admin.ts:1011
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/square/oauth/init`

- **Purpose:** Lists init. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 987
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:987` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/square/oauth/init
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:987
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/stripe/oauth/callback`

- **Purpose:** Lists callback. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 281
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:281` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/connectors/stripe/oauth/callback
  → handler  services/ai/src/routes/connectors-admin.ts:281
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/stripe/oauth/init`

- **Purpose:** Lists init. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 260
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:260` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/stripe/oauth/init
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:260
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/wix/oauth/callback`

- **Purpose:** Lists callback. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 935
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:935` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/connectors/wix/oauth/callback
  → handler  services/ai/src/routes/connectors-admin.ts:935
      → (responds directly; no downstream calls detected)
```

### `GET /api/connectors/wix/oauth/init`

- **Purpose:** CORRECT flow for "any Wix store owner connects their store to us":   1. We send the user to https://www.wix.com/installer/install?appId=…      &redirectUrl=…&state=… - Wix shows them a "Add to site" picker.   2. After they pick a site + approve permissions, Wix redirects back to      our callback with `?code=…&instanceId=<site-instance>&state=…`.   3. We POST that code to https://www.wixapis.com/oauth/access      (grant_type=authorization_code) for an access_token + refresh_token      scoped to that instanceId.
- **Handler:** `services/ai/src/routes/connectors-admin.ts` line 915
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canConnectSystems`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/connectors` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/connectors-admin.ts`.
2. **Handler.** `services/ai/src/routes/connectors-admin.ts:915` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/connectors/wix/oauth/init
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canConnectSystems]
  → handler  services/ai/src/routes/connectors-admin.ts:915
      → (responds directly; no downstream calls detected)
```

### `GET /api/customer-snapshot`

- **Purpose:** Lists customer snapshot. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/customer-snapshot.ts` line 17
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/customer-snapshot` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/customer-snapshot.ts`.
2. **Handler.** `services/ai/src/routes/customer-snapshot.ts:17` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/customer-snapshot
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/customer-snapshot.ts:17
      → (responds directly; no downstream calls detected)
```

### `POST /api/customer-summary`

- **Purpose:** Creates a customer summary. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/customer-summary.ts` line 33
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/customer-summary` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/customer-summary.ts`.
2. **Handler.** `services/ai/src/routes/customer-summary.ts:33` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/customer-summary
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/customer-summary.ts:33
      → db read   prisma.conversation.findUnique
```

### `GET /api/integration-workspace`

- **Purpose:** Lists integration workspace. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/integration-workspace.ts` line 83
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `ai:tools:read` **or** role `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integration-workspace` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/integration-workspace.ts`.
2. **Handler.** `services/ai/src/routes/integration-workspace.ts:83` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `ai:tools:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenantIntegration.findMany({
        where: { tenantId },
        select: { status: true, config: true, integration: { select: { slug: true } } },
      })`
   - `knowledgeIntegration.findMany({
        where: { tenantId },
        select: { provider: true, isActive: true },
      })`
   - `knowledgeIntegration.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/integration-workspace
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/integration-workspace.ts:83
      → db read   prisma.tenantIntegration.findMany({
        where: { tenantId },
        select: { status: true, config: true, integration: { select: { slug: true } } },
      })
      → db read   prisma.knowledgeIntegration.findMany({
        where: { tenantId },
        select: { provider: true, isActive: true },
      })
      → db read   prisma.knowledgeIntegration.findMany
```

### `GET /api/integration-workspace/:id`

- **Purpose:** Reads a single integration workspace. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/integration-workspace.ts` line 184
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `ai:tools:read` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integration-workspace` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/integration-workspace.ts`.
2. **Handler.** `services/ai/src/routes/integration-workspace.ts:184` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `ai:tools:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount
          .findMany({ where: { tenantId }, select: { channel: true, connectionStatus: true } })
          `
   - `channelAccount
          .findMany`
   - `tenantIntegration.findFirst`
   - `integrationCatalog.findUnique`
   - `catalogTool.count`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/integration-workspace/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/integration-workspace.ts:184
      → db read   prisma.channelAccount
          .findMany({ where: { tenantId }, select: { channel: true, connectionStatus: true } })
          
      → db read   prisma.channelAccount
          .findMany
      → db read   prisma.tenantIntegration.findFirst
      → db read   prisma.integrationCatalog.findUnique
      → db read   prisma.catalogTool.count
```

### `GET /api/integrations`

- **Purpose:** GET / - List all published catalog integrations with tenant connection status
- **Handler:** `services/ai/src/routes/integrations.ts` line 29
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canReadSystems`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/integrations.ts`.
2. **Handler.** `services/ai/src/routes/integrations.ts:29` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canReadSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `integrationCatalog.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/integrations
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canReadSystems]
  → handler  services/ai/src/routes/integrations.ts:29
      → db read   prisma.integrationCatalog.findMany
```

### `GET /api/integrations/:slug`

- **Purpose:** GET /:slug - Get single catalog integration with connection status + tools
- **Handler:** `services/ai/src/routes/integrations.ts` line 288
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canReadSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/integrations.ts`.
2. **Handler.** `services/ai/src/routes/integrations.ts:288` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canReadSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `integrationCatalog.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/integrations/:slug
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canReadSystems]
  → handler  services/ai/src/routes/integrations.ts:288
      → db read   prisma.integrationCatalog.findUnique
```

### `PUT /api/integrations/:slug/audience-config`

- **Purpose:** PUT /:slug/audience-config - persist leadsBoardId / contactsBoardId.
- **Handler:** `services/ai/src/routes/integrations.ts` line 885
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canReadSystems`, `canManageSystems`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/integrations.ts`.
2. **Handler.** `services/ai/src/routes/integrations.ts:885` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canReadSystems`, `canManageSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `integrationCatalog.findUnique`
   - `tenantIntegration.findFirst`
   _Writes:_
   - `tenantIntegration.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/integrations/:slug/audience-config
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canReadSystems → canManageSystems]
  → handler  services/ai/src/routes/integrations.ts:885
      → db read   prisma.integrationCatalog.findUnique
      → db read   prisma.tenantIntegration.findFirst
      → db write  prisma.tenantIntegration.update
```

### `POST /api/integrations/:slug/connect`

- **Purpose:** POST /:slug/connect - Connect tenant to integration (API_KEY/BASIC_AUTH path)
- **Handler:** `services/ai/src/routes/integrations.ts` line 333
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canReadSystems`, `canConnectSystems`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/integrations.ts`.
2. **Handler.** `services/ai/src/routes/integrations.ts:333` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canReadSystems`, `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `integrationCatalog.findUnique`
   - `tenantIntegration.findFirst`
   _Writes:_
   - `tenantIntegration.update`
   - `tenantIntegration.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/integrations/:slug/connect
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canReadSystems → canConnectSystems]
  → handler  services/ai/src/routes/integrations.ts:333
      → db read   prisma.integrationCatalog.findUnique
      → db read   prisma.tenantIntegration.findFirst
      → db write  prisma.tenantIntegration.update
      → db write  prisma.tenantIntegration.create
```

### `PUT /api/integrations/:slug/credentials`

- **Purpose:** PUT /:slug/credentials - Update credentials (encrypted, in place)
- **Handler:** `services/ai/src/routes/integrations.ts` line 607
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canReadSystems`, `canConnectSystems`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/integrations.ts`.
2. **Handler.** `services/ai/src/routes/integrations.ts:607` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canReadSystems`, `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `integrationCatalog.findUnique`
   - `tenantIntegration.findFirst`
   _Writes:_
   - `tenantIntegration.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/integrations/:slug/credentials
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canReadSystems → canConnectSystems]
  → handler  services/ai/src/routes/integrations.ts:607
      → db read   prisma.integrationCatalog.findUnique
      → db read   prisma.tenantIntegration.findFirst
      → db write  prisma.tenantIntegration.update
```

### `PUT /api/integrations/:slug/crm-source`

- **Purpose:** PUT /:slug/crm-source - Opt this integration in/out as the tenant's CRM source of truth. Today only Shopify supports this (it's an ECOMMERCE integration a tenant may elect as their customer system of record). Sets `config.useAsCrm` and invalidates the CRM resolver cache so the next bot turn reads customer context from Shopify instead of any CRM-category integration. Toggling OFF restores the default resolution order - it never disturbs tenants who don't opt in.
- **Handler:** `services/ai/src/routes/integrations.ts` line 655
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canReadSystems`, `canSelectSot`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/integrations.ts`.
2. **Handler.** `services/ai/src/routes/integrations.ts:655` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canReadSystems`, `canSelectSot`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `integrationCatalog.findUnique`
   - `tenantIntegration.findFirst`
   _Writes:_
   - `tenantIntegration.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/integrations/:slug/crm-source
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canReadSystems → canSelectSot]
  → handler  services/ai/src/routes/integrations.ts:655
      → db read   prisma.integrationCatalog.findUnique
      → db read   prisma.tenantIntegration.findFirst
      → db write  prisma.tenantIntegration.update
```

### `POST /api/integrations/:slug/disconnect`

- **Purpose:** POST /:slug/disconnect - Disconnect and delete tenant tools (cascade handles child rows)
- **Handler:** `services/ai/src/routes/integrations.ts` line 559
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canReadSystems`, `canManageSystems`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/integrations.ts`.
2. **Handler.** `services/ai/src/routes/integrations.ts:559` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canReadSystems`, `canManageSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `integrationCatalog.findUnique`
   - `tenantIntegration.findFirst`
   _Writes:_
   - `tenantTool.deleteMany`
   - `tenantIntegration.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/integrations/:slug/disconnect
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canReadSystems → canManageSystems]
  → handler  services/ai/src/routes/integrations.ts:559
      → db read   prisma.integrationCatalog.findUnique
      → db read   prisma.tenantIntegration.findFirst
      → db write  prisma.tenantTool.deleteMany
      → db write  prisma.tenantIntegration.update
```

### `GET /api/integrations/:slug/monday-boards`

- **Purpose:** Monday's "schema" lives on a board (columns), not on a global Lead/Contact object. The audience builder needs the operator to pick which board acts as Leads and which acts as Contacts; without these, the schema fetcher has nothing to describe and CRM-side filter rules can't run.
- **Handler:** `services/ai/src/routes/integrations.ts` line 856
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canReadSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/integrations.ts`.
2. **Handler.** `services/ai/src/routes/integrations.ts:856` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canReadSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/integrations/:slug/monday-boards
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canReadSystems]
  → handler  services/ai/src/routes/integrations.ts:856
      → (responds directly; no downstream calls detected)
```

### `POST /api/integrations/:slug/test`

- **Purpose:** POST /:slug/test - Live connection test.
- **Handler:** `services/ai/src/routes/integrations.ts` line 411
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canReadSystems`, `canConnectSystems`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/integrations.ts`.
2. **Handler.** `services/ai/src/routes/integrations.ts:411` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canReadSystems`, `canConnectSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `integrationCatalog.findUnique`
   - `tenantIntegration.findFirst`
   _Writes:_
   - `tenantIntegration.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/integrations/:slug/test
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canReadSystems → canConnectSystems]
  → handler  services/ai/src/routes/integrations.ts:411
      → db read   prisma.integrationCatalog.findUnique
      → db read   prisma.tenantIntegration.findFirst
      → db write  prisma.tenantIntegration.update
```

### `GET /api/integrations/:slug/tools`

- **Purpose:** GET /:slug/tools?aiAgentId=… - List catalog tools with tenant activation status, plus (when ?aiAgentId is passed) the per-agent permission state. Used by the IntegrationDrawer in two contexts:   - marketplace page (no aiAgentId) → toggles reflect TenantTool.isEnabled   - agent edit page (aiAgentId set) → toggles reflect     AgentToolPermission.isAllowed for that agent.
- **Handler:** `services/ai/src/routes/integrations.ts` line 712
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canReadSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/integrations.ts`.
2. **Handler.** `services/ai/src/routes/integrations.ts:712` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canReadSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `integrationCatalog.findUnique`
   - `tenantIntegration.findFirst`
   - `catalogTool.findMany`
   - `aIAgent.findFirst`
   - `agentToolPermission.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/integrations/:slug/tools
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canReadSystems]
  → handler  services/ai/src/routes/integrations.ts:712
      → db read   prisma.integrationCatalog.findUnique
      → db read   prisma.tenantIntegration.findFirst
      → db read   prisma.catalogTool.findMany
      → db read   prisma.aIAgent.findFirst
      → db read   prisma.agentToolPermission.findMany
```

### `PUT /api/integrations/:slug/tools/:toolSlug`

- **Purpose:** PUT /:slug/tools/:toolSlug - Toggle tool enabled/disabled for tenant
- **Handler:** `services/ai/src/routes/integrations.ts` line 780
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canReadSystems`, `canManageSystems`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/integrations.ts`.
2. **Handler.** `services/ai/src/routes/integrations.ts:780` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canReadSystems`, `canManageSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `integrationCatalog.findUnique`
   - `tenantIntegration.findFirst`
   - `catalogTool.findFirst`
   - `tenantTool.findFirst`
   _Writes:_
   - `tenantTool.update`
   - `tenantTool.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/integrations/:slug/tools/:toolSlug
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canReadSystems → canManageSystems]
  → handler  services/ai/src/routes/integrations.ts:780
      → db read   prisma.integrationCatalog.findUnique
      → db read   prisma.tenantIntegration.findFirst
      → db read   prisma.catalogTool.findFirst
      → db read   prisma.tenantTool.findFirst
      → db write  prisma.tenantTool.update
      → db write  prisma.tenantTool.create
```

### `POST /api/integrations/calendar/disconnect`

- **Purpose:** Creates a disconnect. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/calendar-oauth.ts` line 314
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/calendar-oauth.ts`.
2. **Handler.** `services/ai/src/routes/calendar-oauth.ts:314` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/integrations/calendar/disconnect
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/calendar-oauth.ts:314
      → (responds directly; no downstream calls detected)
```

### `GET /api/integrations/oauth/calendly/callback`

- **Purpose:** Lists callback. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/calendar-oauth.ts` line 222
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/calendar-oauth.ts`.
2. **Handler.** `services/ai/src/routes/calendar-oauth.ts:222` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/integrations/oauth/calendly/callback
  → handler  services/ai/src/routes/calendar-oauth.ts:222
      → (responds directly; no downstream calls detected)
```

### `GET /api/integrations/oauth/calendly/init`

- **Purpose:** Lists init. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/calendar-oauth.ts` line 190
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/calendar-oauth.ts`.
2. **Handler.** `services/ai/src/routes/calendar-oauth.ts:190` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/integrations/oauth/calendly/init
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/calendar-oauth.ts:190
      → (responds directly; no downstream calls detected)
```

### `GET /api/integrations/oauth/google_calendar/callback`

- **Purpose:** Lists callback. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/calendar-oauth.ts` line 93
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/calendar-oauth.ts`.
2. **Handler.** `services/ai/src/routes/calendar-oauth.ts:93` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/integrations/oauth/google_calendar/callback
  → handler  services/ai/src/routes/calendar-oauth.ts:93
      → (responds directly; no downstream calls detected)
```

### `GET /api/integrations/oauth/google_calendar/init`

- **Purpose:** The marketplace UI calls /api/integrations/oauth/{catalog_slug}/init - the catalog slug for this provider is `google_calendar` (underscore). We accept BOTH the marketplace path and the dashed alias so direct callers (Scheduler settings page) and the marketplace flow both work.
- **Handler:** `services/ai/src/routes/calendar-oauth.ts` line 53
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/calendar-oauth.ts`.
2. **Handler.** `services/ai/src/routes/calendar-oauth.ts:53` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/integrations/oauth/google_calendar/init
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/calendar-oauth.ts:53
      → (responds directly; no downstream calls detected)
```

### `GET /api/integrations/oauth/google-calendar/callback`

- **Purpose:** Lists callback. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/calendar-oauth.ts` line 93
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/calendar-oauth.ts`.
2. **Handler.** `services/ai/src/routes/calendar-oauth.ts:93` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/integrations/oauth/google-calendar/callback
  → handler  services/ai/src/routes/calendar-oauth.ts:93
      → (responds directly; no downstream calls detected)
```

### `GET /api/integrations/oauth/google-calendar/init`

- **Purpose:** The marketplace UI calls /api/integrations/oauth/{catalog_slug}/init - the catalog slug for this provider is `google_calendar` (underscore). We accept BOTH the marketplace path and the dashed alias so direct callers (Scheduler settings page) and the marketplace flow both work.
- **Handler:** `services/ai/src/routes/calendar-oauth.ts` line 53
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/calendar-oauth.ts`.
2. **Handler.** `services/ai/src/routes/calendar-oauth.ts:53` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/integrations/oauth/google-calendar/init
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/calendar-oauth.ts:53
      → (responds directly; no downstream calls detected)
```

### `GET /api/integrations/oauth/zoho_crm/callback`

- **Purpose:** Lists callback. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/crm-oauth.ts` line 69
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/crm-oauth.ts`.
2. **Handler.** `services/ai/src/routes/crm-oauth.ts:69` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `integrationCatalog.findUnique`
   - `tenantIntegration.findFirst`
   - `tenantTool.findMany`
   _Writes:_
   - `tenantIntegration.update`
   - `tenantIntegration.create`
   - `tenantTool.createMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/integrations/oauth/zoho_crm/callback
  → handler  services/ai/src/routes/crm-oauth.ts:69
      → db read   prisma.integrationCatalog.findUnique
      → db read   prisma.tenantIntegration.findFirst
      → db read   prisma.tenantTool.findMany
      → db write  prisma.tenantIntegration.update
      → db write  prisma.tenantIntegration.create
      → db write  prisma.tenantTool.createMany
```

### `GET /api/integrations/oauth/zoho_crm/init`

- **Purpose:** Lists init. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/crm-oauth.ts` line 32
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/crm-oauth.ts`.
2. **Handler.** `services/ai/src/routes/crm-oauth.ts:32` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/integrations/oauth/zoho_crm/init
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/crm-oauth.ts:32
      → (responds directly; no downstream calls detected)
```

### `GET /api/integrations/source-of-truth`

- **Purpose:** GET /source-of-truth - the tenant's ELECTED customer system of record as the AI-side resolver actually sees it, with its truthful capability set. This is what the UI must render (never frontend guesses): which system answers "who is this customer", whether back-office writes are available, and which operations the provider genuinely supports. Registered BEFORE /:slug so the literal path wins.
- **Handler:** `services/ai/src/routes/integrations.ts` line 87
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Gate: `canReadSystems`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/integrations.ts`.
2. **Handler.** `services/ai/src/routes/integrations.ts:87` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Gate: `canReadSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/integrations/source-of-truth
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canReadSystems]
  → handler  services/ai/src/routes/integrations.ts:87
      → (responds directly; no downstream calls detected)
```

### `GET /api/integrations/source-of-truth/customers`

- **Purpose:** GET /source-of-truth/customers?q= - unified, tenant-scoped customer search for outbound calling. Searches THE resolved source of truth (Shopify-as-CRM included), so the dialer offers the same identities that answer "who is this customer" everywhere else - never another tenant's data, never a free-text number promoted to an identity.
- **Handler:** `services/ai/src/routes/integrations.ts` line 130
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `crm:contacts:read` **or** role `ADMIN`; Gate: `canReadSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/integrations.ts`.
2. **Handler.** `services/ai/src/routes/integrations.ts:130` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `crm:contacts:read` **or** role `ADMIN`; Gate: `canReadSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/integrations/source-of-truth/customers
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canReadSystems]
  → handler  services/ai/src/routes/integrations.ts:130
      → (responds directly; no downstream calls detected)
```

### `GET /api/integrations/source-of-truth/customers/detail`

- **Purpose:** GET /source-of-truth/customers/detail?id=&kind= - full contact data for ONE explicitly selected candidate (the list above is masked). Same permission gate; the adapter resolves within the active tenant's own connection, so a foreign id can never cross tenants - it simply doesn't exist there.
- **Handler:** `services/ai/src/routes/integrations.ts` line 250
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `crm:contacts:read` **or** role `ADMIN`; Gate: `canReadSystems`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/integrations` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/integrations.ts`.
2. **Handler.** `services/ai/src/routes/integrations.ts:250` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `crm:contacts:read` **or** role `ADMIN`; Gate: `canReadSystems`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/integrations/source-of-truth/customers/detail
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → canReadSystems]
  → handler  services/ai/src/routes/integrations.ts:250
      → (responds directly; no downstream calls detected)
```

### `GET /api/scheduler/calendar-accounts`

- **Purpose:** Lists calendar accounts. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/scheduler-admin.ts` line 107
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/scheduler` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/scheduler-admin.ts`.
2. **Handler.** `services/ai/src/routes/scheduler-admin.ts:107` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/scheduler/calendar-accounts
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/scheduler-admin.ts:107
      → (responds directly; no downstream calls detected)
```

### `GET /api/scheduler/meeting-types`

- **Purpose:** Lists meeting types. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/scheduler-admin.ts` line 25
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/scheduler` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/scheduler-admin.ts`.
2. **Handler.** `services/ai/src/routes/scheduler-admin.ts:25` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/scheduler/meeting-types
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/scheduler-admin.ts:25
      → (responds directly; no downstream calls detected)
```

### `POST /api/scheduler/meeting-types`

- **Purpose:** Creates a meeting type. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/scheduler-admin.ts` line 33
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/scheduler` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/scheduler-admin.ts`.
2. **Handler.** `services/ai/src/routes/scheduler-admin.ts:33` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/scheduler/meeting-types
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/scheduler-admin.ts:33
      → (responds directly; no downstream calls detected)
```

### `DELETE /api/scheduler/meeting-types/:id`

- **Purpose:** Deletes the referenced meeting type. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/scheduler-admin.ts` line 98
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/scheduler` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/scheduler-admin.ts`.
2. **Handler.** `services/ai/src/routes/scheduler-admin.ts:98` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/scheduler/meeting-types/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/scheduler-admin.ts:98
      → (responds directly; no downstream calls detected)
```

### `PATCH /api/scheduler/meeting-types/:id`

- **Purpose:** Updates the referenced meeting type. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/scheduler-admin.ts` line 76
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/scheduler` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/scheduler-admin.ts`.
2. **Handler.** `services/ai/src/routes/scheduler-admin.ts:76` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/scheduler/meeting-types/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/scheduler-admin.ts:76
      → (responds directly; no downstream calls detected)
```

## Internal

31 endpoints.

### `POST /api/ai-bot/execution-message`

- **Purpose:** POST /api/ai-bot/oneshot   body: { tenantId, aiAgentId, userInput, maxTokens?, feature? }   returns: { reply, modelUsed, totalTokens } POST /api/ai-bot/execution-message   The ONE generator for post-execution customer messages (approval   continuations, proactive completion updates). Same humanizer/style layer   as live replies + deterministic grounding validation with a safe   template fallback - callers always get a sendable, fact-true message.   body: { tenantId, aiAgentId, facts, inboundSample?, customerName? }
- **Handler:** `services/ai/src/routes/ai-bot.ts` line 62
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-bot` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-bot.ts`.
2. **Handler.** `services/ai/src/routes/ai-bot.ts:62` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/ai-bot/execution-message
  → [internalAuth]
  → handler  services/ai/src/routes/ai-bot.ts:62
      → (responds directly; no downstream calls detected)
```

### `POST /api/ai-bot/oneshot`

- **Purpose:** Creates an oneshot. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/ai-bot.ts` line 83
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-bot` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-bot.ts`.
2. **Handler.** `services/ai/src/routes/ai-bot.ts:83` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/ai-bot/oneshot
  → [internalAuth]
  → handler  services/ai/src/routes/ai-bot.ts:83
      → (responds directly; no downstream calls detected)
```

### `POST /api/ai-bot/reply`

- **Purpose:** POST /api/ai-bot/reply   body: { tenantId, conversationId, aiAgentId, incomingMessage, closedHours? }   `closedHours` (worker-computed from the tenant's persisted business hours)   marks the business as currently CLOSED with the "active" outside-hours   policy - the reply must not imply immediate human availability.   returns: AIBotReplyResult
- **Handler:** `services/ai/src/routes/ai-bot.ts` line 26
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/ai-bot` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/ai-bot.ts`.
2. **Handler.** `services/ai/src/routes/ai-bot.ts:26` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `499`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/ai-bot/reply
  → [internalAuth]
  → handler  services/ai/src/routes/ai-bot.ts:26
      → (responds directly; no downstream calls detected)
```

### `POST /api/auto-buy/orders`

- **Purpose:** Creates an order. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/auto-buy.ts` line 33
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Feature flag: `AUTO_BUY`
- **Input validation:** `validate(placeOrderSchema)` (Zod). Accepts: `productSku`, `quantity`, `maxPriceCents`. Required: `productSku`, `quantity`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/auto-buy` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/auto-buy.ts`.
2. **Handler.** `services/conversation/src/routes/auto-buy.ts:33` - inline `async (req, res)` handler.
3. **Validation.** `validate(placeOrderSchema)` (Zod). Accepts: `productSku`, `quantity`, `maxPriceCents`. Required: `productSku`, `quantity`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Feature flag: `AUTO_BUY`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `202`, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/auto-buy/orders
  → [authenticate → resolveTenant → validate(placeOrderSchema)]
  → handler  services/conversation/src/routes/auto-buy.ts:33
      → (responds directly; no downstream calls detected)
```

### `GET /api/auto-buy/status`

- **Purpose:** Tenant-level status check (no user-level gate - any authenticated user can see whether the feature is available to the org, even if they can't personally place orders). Useful for the frontend to hide/show UI.
- **Handler:** `services/conversation/src/routes/auto-buy.ts` line 60
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/auto-buy` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/auto-buy.ts`.
2. **Handler.** `services/conversation/src/routes/auto-buy.ts:60` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/auto-buy/status
  → [authenticate → resolveTenant]
  → handler  services/conversation/src/routes/auto-buy.ts:60
      → (responds directly; no downstream calls detected)
```

### `POST /api/copilot/cue-outcome`

- **Purpose:** Creates a cue outcome. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/copilot-outcomes.ts` line 39
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/copilot` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/copilot-outcomes.ts`.
2. **Handler.** `services/ai/src/routes/copilot-outcomes.ts:39` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200`, answering the `{ data: ... }` envelope. Error paths: `400`, `401`, `403`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/copilot/cue-outcome
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/copilot-outcomes.ts:39
      → db read   prisma.conversation.findFirst
```

### `POST /api/internal/auth/payment-succeeded`

- **Purpose:** A tenant's payment cleared: welcome them and hand over the way in.
- **Handler:** `services/auth/src/routes/internal.ts` line 31
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/internal` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/internal.ts`.
2. **Handler.** `services/auth/src/routes/internal.ts:31` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200`, answering the `{ data: ... }` envelope. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/auth/payment-succeeded
  → [internalAuth]
  → handler  services/auth/src/routes/internal.ts:31
      → (responds directly; no downstream calls detected)
```

### `POST /api/internal/billing/activate-manual-contract`

- **Purpose:** Activate an externally settled contract.
- **Handler:** `services/billing/src/routes/internal.ts` line 373
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:373` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/billing/activate-manual-contract
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:373
      → (responds directly; no downstream calls detected)
```

### `POST /api/internal/billing/auto-purchase`

- **Purpose:** Called by the AI runtime gate when balance crosses the auto-purchase threshold.
- **Handler:** `services/billing/src/routes/internal.ts` line 57
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:57` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/billing/auto-purchase
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:57
      → (responds directly; no downstream calls detected)
```

### `POST /api/internal/billing/chargeback`

- **Purpose:** Record a chargeback the bank has already made.
- **Handler:** `services/billing/src/routes/internal.ts` line 129
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:129` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/billing/chargeback
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:129
      → (responds directly; no downstream calls detected)
```

### `POST /api/internal/billing/ensure-entity`

- **Purpose:** Creates an ensure entity. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/internal.ts` line 37
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:37` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/billing/ensure-entity
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:37
      → (responds directly; no downstream calls detected)
```

### `POST /api/internal/billing/grandfather`

- **Purpose:** Creates a grandfather. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/billing/src/routes/internal.ts` line 97
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:97` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
POST /api/internal/billing/grandfather
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:97
      → (responds directly; no downstream calls detected)
```

### `POST /api/internal/billing/grant-credits`

- **Purpose:** SYSTEM_ADMIN console: top up credits (PURCHASED bucket - never expires).
- **Handler:** `services/billing/src/routes/internal.ts` line 206
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:206` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/billing/grant-credits
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:206
      → (responds directly; no downstream calls detected)
```

### `GET /api/internal/billing/manual-contracts/:tenantId`

- **Purpose:** Manual contracts on record for a tenant, for billing history.
- **Handler:** `services/billing/src/routes/internal.ts` line 395
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:395` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
GET /api/internal/billing/manual-contracts/:tenantId
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:395
      → (responds directly; no downstream calls detected)
```

### `POST /api/internal/billing/payg-accrue`

- **Purpose:** Usage the wallet could not cover, reported by the AI runtime.
- **Handler:** `services/billing/src/routes/internal.ts` line 88
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:88` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/billing/payg-accrue
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:88
      → (responds directly; no downstream calls detected)
```

### `GET /api/internal/billing/poc-feature-domains`

- **Purpose:** The domains a POC's feature areas may be chosen from.
- **Handler:** `services/billing/src/routes/internal.ts` line 191
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** No input.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:191` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
GET /api/internal/billing/poc-feature-domains
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:191
      → (responds directly; no downstream calls detected)
```

### `POST /api/internal/billing/provision-paid-tenant`

- **Purpose:** Create the billing scaffolding for an already-created paid tenant.
- **Handler:** `services/billing/src/routes/internal.ts` line 264
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:264` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/billing/provision-paid-tenant
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:264
      → (responds directly; no downstream calls detected)
```

### `POST /api/internal/billing/refund`

- **Purpose:** Ops-initiated refund of a successful charge (claws back purchased Units).
- **Handler:** `services/billing/src/routes/internal.ts` line 108
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:108` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/billing/refund
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:108
      → (responds directly; no downstream calls detected)
```

### `POST /api/internal/billing/refund-confirmation`

- **Purpose:** Record a refund the provider made outside GOTCHA.
- **Handler:** `services/billing/src/routes/internal.ts` line 144
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:144` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/billing/refund-confirmation
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:144
      → (responds directly; no downstream calls detected)
```

### `POST /api/internal/billing/resend-payment-link`

- **Purpose:** Issue a replacement continuation link.
- **Handler:** `services/billing/src/routes/internal.ts` line 296
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:296` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `pendingCheckout.findFirst`
   - `paymentContinuationLink.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `409`, `429`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/billing/resend-payment-link
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:296
      → db read   prisma.pendingCheckout.findFirst
      → db read   prisma.paymentContinuationLink.findFirst
```

### `POST /api/internal/billing/revoke-payment-links`

- **Purpose:** Revoke every active link for a tenant's checkout, without reissuing.
- **Handler:** `services/billing/src/routes/internal.ts` line 353
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:353` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `pendingCheckout.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/billing/revoke-payment-links
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:353
      → db read   prisma.pendingCheckout.findFirst
```

### `POST /api/internal/billing/run-cycle`

- **Purpose:** Ops/scheduler hook - process trials, renewals, pending changes, dunning.
- **Handler:** `services/billing/src/routes/internal.ts` line 152
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:152` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
POST /api/internal/billing/run-cycle
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:152
      → (responds directly; no downstream calls detected)
```

### `POST /api/internal/billing/setup-poc`

- **Purpose:** Provision a card-less POC in full: an ENFORCED subscription, the operator's credit budget, the expiry, and an explicit entitlement row for every feature domain - enabled for the chosen ones, denied for the rest.
- **Handler:** `services/billing/src/routes/internal.ts` line 169
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:169` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/billing/setup-poc
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:169
      → (responds directly; no downstream calls detected)
```

### `POST /api/internal/billing/shopify/connected`

- **Purpose:** Called by services/ai, which owns the Shopify OAuth callback and the verified webhook handlers. The split is deliberate: services/ai decides WHO the store and workspace are (it holds the HMAC verification and the session), and billing decides what that means COMMERCIALLY. Neither reaches into the other's tables to answer the other's question.
- **Handler:** `services/billing/src/routes/internal.ts` line 428
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:428` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `409`, `500`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/billing/shopify/connected
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:428
      → (responds directly; no downstream calls detected)
```

### `GET /api/internal/billing/shopify/state/:tenantId`

- **Purpose:** Current Shopify billing state for a workspace. Read-only.
- **Handler:** `services/billing/src/routes/internal.ts` line 491
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:491` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
GET /api/internal/billing/shopify/state/:tenantId
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:491
      → (responds directly; no downstream calls detected)
```

### `POST /api/internal/billing/shopify/uninstalled`

- **Purpose:** A verified `app/uninstalled` for a Shopify store.
- **Handler:** `services/billing/src/routes/internal.ts` line 459
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:459` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `commerceConnection.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/billing/shopify/uninstalled
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:459
      → db read   prisma.commerceConnection.findFirst
```

### `POST /api/internal/billing/start-trial`

- **Purpose:** Signup completes here: card already tokenized → start the 14-day trial.
- **Handler:** `services/billing/src/routes/internal.ts` line 45
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:45` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/billing/start-trial
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:45
      → (responds directly; no downstream calls detected)
```

### `GET /api/internal/billing/summary`

- **Purpose:** SYSTEM_ADMIN console: subscription + wallet snapshot for one tenant.
- **Handler:** `services/billing/src/routes/internal.ts` line 216
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:216` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `subscription.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/internal/billing/summary
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:216
      → db read   prisma.subscription.findUnique
```

### `GET /api/internal/billing/tenant-plan-audit`

- **Purpose:** Every tenant, grouped by how it holds access. Read-only by design: it assigns nothing and grants nothing, because a tenant with no plan needs an operator's decision, not an automatic one.
- **Handler:** `services/billing/src/routes/internal.ts` line 200
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** No input.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** read

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:200` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
GET /api/internal/billing/tenant-plan-audit
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:200
      → (responds directly; no downstream calls detected)
```

### `POST /api/internal/billing/usage-threshold`

- **Purpose:** Called by the AI runtime when Unit consumption crosses 80/90/95/100%. Emits owner alerts and attempts auto-purchase (which self-gates on policy + monthly ceiling). The AI gate already blocked the turn if balance hit zero.
- **Handler:** `services/billing/src/routes/internal.ts` line 69
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:69` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/billing/usage-threshold
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:69
      → (responds directly; no downstream calls detected)
```

### `POST /api/internal/billing/validate-paid-plan`

- **Purpose:** Validate a plan + volume selection WITHOUT creating anything.
- **Handler:** `services/billing/src/routes/internal.ts` line 241
- **Authentication / tenant resolution:** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `billing` service mounts this router at `/api/internal` in `services/billing/src/index.ts`; the route is declared in `services/billing/src/routes/internal.ts`.
2. **Handler.** `services/billing/src/routes/internal.ts:241` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Internal only.** Authenticated by the shared service secret, not a user token. Not routed by the gateway.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/internal/billing/validate-paid-plan
  → [internalAuth]
  → handler  services/billing/src/routes/internal.ts:241
      → (responds directly; no downstream calls detected)
```

## Knowledge

26 endpoints.

### `GET /api/knowledge-bases`

- **Purpose:** List knowledge bases
- **Handler:** `services/ai/src/routes/knowledge.ts` line 44
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge-bases` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge.ts`.
2. **Handler.** `services/ai/src/routes/knowledge.ts:44` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeBase.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/knowledge-bases
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/knowledge.ts:44
      → db read   prisma.knowledgeBase.findMany
```

### `POST /api/knowledge-bases`

- **Purpose:** Create knowledge base
- **Handler:** `services/ai/src/routes/knowledge.ts` line 72
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`; `ai:knowledge:write` **or** role `ADMIN`; Entitlement: `ai.knowledge_base`; Plan limit: `limit:knowledge_sources`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge-bases` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge.ts`.
2. **Handler.** `services/ai/src/routes/knowledge.ts:72` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`; `ai:knowledge:write` **or** role `ADMIN`; Entitlement: `ai.knowledge_base`; Plan limit: `limit:knowledge_sources`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `knowledgeBase.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/knowledge-bases
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/knowledge.ts:72
      → db write  prisma.knowledgeBase.create
```

### `DELETE /api/knowledge-bases/:id`

- **Purpose:** Delete knowledge base
- **Handler:** `services/ai/src/routes/knowledge.ts` line 124
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`; `ai:knowledge:write` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge-bases` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge.ts`.
2. **Handler.** `services/ai/src/routes/knowledge.ts:124` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`; `ai:knowledge:write` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeBase.findFirst`
   _Writes:_
   - `knowledgeBase.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/knowledge-bases/:id
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/knowledge.ts:124
      → db read   prisma.knowledgeBase.findFirst
      → db write  prisma.knowledgeBase.delete
```

### `PATCH /api/knowledge-bases/:id`

- **Purpose:** Update knowledge base
- **Handler:** `services/ai/src/routes/knowledge.ts` line 100
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`; `ai:knowledge:write` **or** role `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge-bases` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge.ts`.
2. **Handler.** `services/ai/src/routes/knowledge.ts:100` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`; `ai:knowledge:write` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeBase.findFirst`
   _Writes:_
   - `knowledgeBase.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/knowledge-bases/:id
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/knowledge.ts:100
      → db read   prisma.knowledgeBase.findFirst
      → db write  prisma.knowledgeBase.update
```

### `POST /api/knowledge-bases/:id/documents`

- **Purpose:** Upload document to knowledge base
- **Handler:** `services/ai/src/routes/knowledge.ts` line 141
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`; `ai:knowledge:write` **or** role `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge-bases` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge.ts`.
2. **Handler.** `services/ai/src/routes/knowledge.ts:141` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`; `ai:knowledge:write` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeBase.findFirst`
   _Writes:_
   - `knowledgeDocument.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/knowledge-bases/:id/documents
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/knowledge.ts:141
      → db read   prisma.knowledgeBase.findFirst
      → db write  prisma.knowledgeDocument.create
```

### `DELETE /api/knowledge-bases/:id/documents/:docId`

- **Purpose:** Delete document
- **Handler:** `services/ai/src/routes/knowledge.ts` line 281
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`; `ai:knowledge:write` **or** role `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge-bases` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge.ts`.
2. **Handler.** `services/ai/src/routes/knowledge.ts:281` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`; `ai:knowledge:write` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeDocument.findFirst`
   _Writes:_
   - `knowledgeDocument.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/knowledge-bases/:id/documents/:docId
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/knowledge.ts:281
      → db read   prisma.knowledgeDocument.findFirst
      → db write  prisma.knowledgeDocument.delete
```

### `PUT /api/knowledge-bases/:id/documents/:docId`

- **Purpose:** Update a document in place (content and/or provenance metadata).
- **Handler:** `services/ai/src/routes/knowledge.ts` line 223
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`; `ai:knowledge:write` **or** role `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge-bases` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge.ts`.
2. **Handler.** `services/ai/src/routes/knowledge.ts:223` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`; `ai:knowledge:write` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeDocument.findFirst`
   _Writes:_
   - `knowledgeDocument.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/knowledge-bases/:id/documents/:docId
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/knowledge.ts:223
      → db read   prisma.knowledgeDocument.findFirst
      → db write  prisma.knowledgeDocument.update
```

### `POST /api/knowledge-bases/:id/documents/:docId/process`

- **Purpose:** Trigger document processing (embedding generation)
- **Handler:** `services/ai/src/routes/knowledge.ts` line 351
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`; `ai:knowledge:write` **or** role `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge-bases` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge.ts`.
2. **Handler.** `services/ai/src/routes/knowledge.ts:351` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`; `ai:knowledge:write` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeDocument.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/knowledge-bases/:id/documents/:docId/process
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/knowledge.ts:351
      → db read   prisma.knowledgeDocument.findFirst
```

### `POST /api/knowledge-bases/:id/documents/upload`

- **Purpose:** Upload file document to knowledge base
- **Handler:** `services/ai/src/routes/knowledge.ts` line 298
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** `ai:knowledge:read` **or** role `ADMIN`; `ai:knowledge:write` **or** role `ADMIN`
- **Input validation:** `multer` handles a `multipart/form-data` upload. No Zod schema.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge-bases` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge.ts`.
2. **Handler.** `services/ai/src/routes/knowledge.ts:298` - inline `async (req, res)` handler.
3. **Validation.** `multer` handles a `multipart/form-data` upload. No Zod schema.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** `ai:knowledge:read` **or** role `ADMIN`; `ai:knowledge:write` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeBase.findFirst`
   _Writes:_
   - `knowledgeDocument.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/knowledge-bases/:id/documents/upload
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE)]
  → handler  services/ai/src/routes/knowledge.ts:298
      → db read   prisma.knowledgeBase.findFirst
      → db write  prisma.knowledgeDocument.create
```

### `DELETE /api/knowledge/integrations/:intId`

- **Purpose:** Delete (disconnect) integration.
- **Handler:** `services/ai/src/routes/knowledge-oauth.ts` line 349
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge-oauth.ts`.
2. **Handler.** `services/ai/src/routes/knowledge-oauth.ts:349` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `googleDriveService.removeSourceData()`
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeIntegration.findFirst`
   _Writes:_
   - `knowledgeIntegration.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/knowledge/integrations/:intId
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/knowledge-oauth.ts:349
      → googleDriveService.removeSourceData()
      → db read   prisma.knowledgeIntegration.findFirst
      → db write  prisma.knowledgeIntegration.delete
```

### `PATCH /api/knowledge/integrations/:intId/auto-sync`

- **Purpose:** Enable/disable the hourly background re-sync for one integration. Stored on config.autoSync; the scheduler only refreshes integrations where it's not explicitly false and that have a persisted selection (spaceKeys / fileIds).
- **Handler:** `services/ai/src/routes/knowledge-oauth.ts` line 636
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge-oauth.ts`.
2. **Handler.** `services/ai/src/routes/knowledge-oauth.ts:636` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeIntegration.findFirst`
   _Writes:_
   - `knowledgeIntegration.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/knowledge/integrations/:intId/auto-sync
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/knowledge-oauth.ts:636
      → db read   prisma.knowledgeIntegration.findFirst
      → db write  prisma.knowledgeIntegration.update
```

### `GET /api/knowledge/integrations/:intId/confluence/spaces`

- **Purpose:** Reads a single space for the referenced confluence. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/knowledge-oauth.ts` line 371
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge-oauth.ts`.
2. **Handler.** `services/ai/src/routes/knowledge-oauth.ts:371` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `confluenceService.listSpaces()`
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeIntegration.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/knowledge/integrations/:intId/confluence/spaces
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/knowledge-oauth.ts:371
      → confluenceService.listSpaces()
      → db read   prisma.knowledgeIntegration.findFirst
```

### `GET /api/knowledge/integrations/:intId/confluence/spaces/:key/pages`

- **Purpose:** Reads a single page for the referenced space. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/knowledge-oauth.ts` line 388
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge-oauth.ts`.
2. **Handler.** `services/ai/src/routes/knowledge-oauth.ts:388` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `confluenceService.listPages()`
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeIntegration.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/knowledge/integrations/:intId/confluence/spaces/:key/pages
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/knowledge-oauth.ts:388
      → confluenceService.listPages()
      → db read   prisma.knowledgeIntegration.findFirst
```

### `POST /api/knowledge/integrations/:intId/confluence/sync`

- **Purpose:** Creates a sync for the referenced confluence. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/knowledge-oauth.ts` line 406
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge-oauth.ts`.
2. **Handler.** `services/ai/src/routes/knowledge-oauth.ts:406` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `confluenceService.syncSpace()`
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeIntegration.findFirst`
   _Writes:_
   - `knowledgeIntegration.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/knowledge/integrations/:intId/confluence/sync
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/knowledge-oauth.ts:406
      → confluenceService.syncSpace()
      → db read   prisma.knowledgeIntegration.findFirst
      → db write  prisma.knowledgeIntegration.update
```

### `GET /api/knowledge/integrations/:intId/drive/files`

- **Purpose:** Reads a single file for the referenced drive. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/knowledge-oauth.ts` line 456
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge-oauth.ts`.
2. **Handler.** `services/ai/src/routes/knowledge-oauth.ts:456` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `googleDriveService.listFiles()`
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeIntegration.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/knowledge/integrations/:intId/drive/files
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/knowledge-oauth.ts:456
      → googleDriveService.listFiles()
      → db read   prisma.knowledgeIntegration.findFirst
```

### `GET /api/knowledge/integrations/:intId/drive/shared-drives`

- **Purpose:** Reads a single shared drive for the referenced drive. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/knowledge-oauth.ts` line 439
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge-oauth.ts`.
2. **Handler.** `services/ai/src/routes/knowledge-oauth.ts:439` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `googleDriveService.listSharedDrives()`
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeIntegration.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/knowledge/integrations/:intId/drive/shared-drives
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/knowledge-oauth.ts:439
      → googleDriveService.listSharedDrives()
      → db read   prisma.knowledgeIntegration.findFirst
```

### `GET /api/knowledge/integrations/:intId/drive/sources`

- **Purpose:** Reads a single source for the referenced drive. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/knowledge-oauth.ts` line 547
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge-oauth.ts`.
2. **Handler.** `services/ai/src/routes/knowledge-oauth.ts:547` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `googleDriveService.normalizeSources()`
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeIntegration.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/knowledge/integrations/:intId/drive/sources
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/knowledge-oauth.ts:547
      → googleDriveService.normalizeSources()
      → db read   prisma.knowledgeIntegration.findFirst
```

### `DELETE /api/knowledge/integrations/:intId/drive/sources/:sourceKey`

- **Purpose:** `removeData=1` also deletes the knowledge that source produced. Without it the documents stay: unsubscribing from a folder is not the same request as throwing away everything the AI learned from it.
- **Handler:** `services/ai/src/routes/knowledge-oauth.ts` line 598
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge-oauth.ts`.
2. **Handler.** `services/ai/src/routes/knowledge-oauth.ts:598` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `googleDriveService.normalizeSources()`
   - `googleDriveService.removeSourceData()`
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeIntegration.findFirst`
   _Writes:_
   - `knowledgeIntegration.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/knowledge/integrations/:intId/drive/sources/:sourceKey
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/knowledge-oauth.ts:598
      → googleDriveService.normalizeSources()
      → googleDriveService.removeSourceData()
      → db read   prisma.knowledgeIntegration.findFirst
      → db write  prisma.knowledgeIntegration.update
```

### `PATCH /api/knowledge/integrations/:intId/drive/sources/:sourceKey`

- **Purpose:** Updates the referenced source. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/knowledge-oauth.ts` line 564
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge-oauth.ts`.
2. **Handler.** `services/ai/src/routes/knowledge-oauth.ts:564` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `googleDriveService.normalizeSources()`
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeIntegration.findFirst`
   _Writes:_
   - `knowledgeIntegration.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/knowledge/integrations/:intId/drive/sources/:sourceKey
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/knowledge-oauth.ts:564
      → googleDriveService.normalizeSources()
      → db read   prisma.knowledgeIntegration.findFirst
      → db write  prisma.knowledgeIntegration.update
```

### `POST /api/knowledge/integrations/:intId/drive/sync`

- **Purpose:** Accepts the structured `sources` list (files and/or folders) and still accepts the original bare `fileIds`, so an older client keeps working.
- **Handler:** `services/ai/src/routes/knowledge-oauth.ts` line 478
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge-oauth.ts`.
2. **Handler.** `services/ai/src/routes/knowledge-oauth.ts:478` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `googleDriveService.mergeSources()`
   - `googleDriveService.normalizeSources()`
   - `googleDriveService.syncIntegration()`
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeIntegration.findFirst`
   _Writes:_
   - `knowledgeIntegration.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/knowledge/integrations/:intId/drive/sync
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/knowledge-oauth.ts:478
      → googleDriveService.mergeSources()
      → googleDriveService.normalizeSources()
      → googleDriveService.syncIntegration()
      → db read   prisma.knowledgeIntegration.findFirst
      → db write  prisma.knowledgeIntegration.update
```

### `POST /api/knowledge/integrations/:intId/drive/sync-now`

- **Purpose:** Creates a sync now for the referenced drive. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/knowledge-oauth.ts` line 524
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge-oauth.ts`.
2. **Handler.** `services/ai/src/routes/knowledge-oauth.ts:524` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls**, in source order:
   - `googleDriveService.syncIntegration()`
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeIntegration.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/knowledge/integrations/:intId/drive/sync-now
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/knowledge-oauth.ts:524
      → googleDriveService.syncIntegration()
      → db read   prisma.knowledgeIntegration.findFirst
```

### `GET /api/knowledge/kb/:kbId/integrations`

- **Purpose:** List integrations for a knowledge base
- **Handler:** `services/ai/src/routes/knowledge-oauth.ts` line 315
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge-oauth.ts`.
2. **Handler.** `services/ai/src/routes/knowledge-oauth.ts:315` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeBase.findFirst`
   - `knowledgeIntegration.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/knowledge/kb/:kbId/integrations
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/knowledge-oauth.ts:315
      → db read   prisma.knowledgeBase.findFirst
      → db read   prisma.knowledgeIntegration.findMany
```

### `GET /api/knowledge/oauth/confluence/callback`

- **Purpose:** Lists callback. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/knowledge-oauth.ts` line 96
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge-oauth.ts`.
2. **Handler.** `services/ai/src/routes/knowledge-oauth.ts:96` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/knowledge/oauth/confluence/callback
  → handler  services/ai/src/routes/knowledge-oauth.ts:96
      → (responds directly; no downstream calls detected)
```

### `GET /api/knowledge/oauth/confluence/init`

- **Purpose:** Lists init. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/knowledge-oauth.ts` line 75
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge-oauth.ts`.
2. **Handler.** `services/ai/src/routes/knowledge-oauth.ts:75` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/knowledge/oauth/confluence/init
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/knowledge-oauth.ts:75
      → (responds directly; no downstream calls detected)
```

### `GET /api/knowledge/oauth/google-drive/callback`

- **Purpose:** Lists callback. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/knowledge-oauth.ts` line 216
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge-oauth.ts`.
2. **Handler.** `services/ai/src/routes/knowledge-oauth.ts:216` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeIntegration.findFirst`
   _Writes:_
   - `knowledgeIntegration.update`
   - `knowledgeIntegration.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/knowledge/oauth/google-drive/callback
  → handler  services/ai/src/routes/knowledge-oauth.ts:216
      → db read   prisma.knowledgeIntegration.findFirst
      → db write  prisma.knowledgeIntegration.update
      → db write  prisma.knowledgeIntegration.create
```

### `GET /api/knowledge/oauth/google-drive/init`

- **Purpose:** PENDING_ONBOARDING allowed: onboarding Movement 6 connects Drive before the tenant flips ACTIVE. `flow=onboarding` rides the state so the callback can land back on /setup instead of the app's knowledge page.
- **Handler:** `services/ai/src/routes/knowledge-oauth.ts` line 176
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/knowledge` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/knowledge-oauth.ts`.
2. **Handler.** `services/ai/src/routes/knowledge-oauth.ts:176` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **PENDING_ONBOARDING or ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeIntegration.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/knowledge/oauth/google-drive/init
  → [authenticate → resolveTenant → requireTenantState(PENDING_ONBOARDING or ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/knowledge-oauth.ts:176
      → db read   prisma.knowledgeIntegration.findFirst
```

## Notifications

8 endpoints.

### `GET /api/notification-preferences`

- **Purpose:** Lists notification preferences. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/notifications/src/routes/notification-preferences-admin.ts` line 88
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `notifications` service mounts this router at `/api/notification-preferences` in `services/notifications/src/index.ts`; the route is declared in `services/notifications/src/routes/notification-preferences-admin.ts`.
2. **Handler.** `services/notifications/src/routes/notification-preferences-admin.ts:88` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/notification-preferences
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/notifications/src/routes/notification-preferences-admin.ts:88
      → (responds directly; no downstream calls detected)
```

### `POST /api/notification-preferences`

- **Purpose:** Creates a notification preference. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/notifications/src/routes/notification-preferences-admin.ts` line 103
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `notifications` service mounts this router at `/api/notification-preferences` in `services/notifications/src/index.ts`; the route is declared in `services/notifications/src/routes/notification-preferences-admin.ts`.
2. **Handler.** `services/notifications/src/routes/notification-preferences-admin.ts:103` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/notification-preferences
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/notifications/src/routes/notification-preferences-admin.ts:103
      → (responds directly; no downstream calls detected)
```

### `DELETE /api/notification-preferences/:id`

- **Purpose:** Deletes the referenced notification preference. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/notifications/src/routes/notification-preferences-admin.ts` line 186
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `notifications` service mounts this router at `/api/notification-preferences` in `services/notifications/src/index.ts`; the route is declared in `services/notifications/src/routes/notification-preferences-admin.ts`.
2. **Handler.** `services/notifications/src/routes/notification-preferences-admin.ts:186` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/notification-preferences/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/notifications/src/routes/notification-preferences-admin.ts:186
      → (responds directly; no downstream calls detected)
```

### `PATCH /api/notification-preferences/:id`

- **Purpose:** Updates the referenced notification preference. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/notifications/src/routes/notification-preferences-admin.ts` line 148
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `notifications` service mounts this router at `/api/notification-preferences` in `services/notifications/src/index.ts`; the route is declared in `services/notifications/src/routes/notification-preferences-admin.ts`.
2. **Handler.** `services/notifications/src/routes/notification-preferences-admin.ts:148` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/notification-preferences/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/notifications/src/routes/notification-preferences-admin.ts:148
      → (responds directly; no downstream calls detected)
```

### `GET /api/notifications`

- **Purpose:** Lists notifications. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/notifications/src/routes/notifications.ts` line 23
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `notifications` service mounts this router at `/api/notifications` in `services/notifications/src/index.ts`; the route is declared in `services/notifications/src/routes/notifications.ts`.
2. **Handler.** `services/notifications/src/routes/notifications.ts:23` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `401`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/notifications
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/notifications/src/routes/notifications.ts:23
      → (responds directly; no downstream calls detected)
```

### `POST /api/notifications/:id/mark-read`

- **Purpose:** Performs the `mark read` action on the referenced notification. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/notifications/src/routes/notifications.ts` line 68
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `notifications` service mounts this router at `/api/notifications` in `services/notifications/src/index.ts`; the route is declared in `services/notifications/src/routes/notifications.ts`.
2. **Handler.** `services/notifications/src/routes/notifications.ts:68` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `401`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/notifications/:id/mark-read
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/notifications/src/routes/notifications.ts:68
      → (responds directly; no downstream calls detected)
```

### `POST /api/notifications/mark-all-read`

- **Purpose:** Creates a mark all read. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/notifications/src/routes/notifications.ts` line 90
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `notifications` service mounts this router at `/api/notifications` in `services/notifications/src/index.ts`; the route is declared in `services/notifications/src/routes/notifications.ts`.
2. **Handler.** `services/notifications/src/routes/notifications.ts:90` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `401`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/notifications/mark-all-read
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/notifications/src/routes/notifications.ts:90
      → (responds directly; no downstream calls detected)
```

### `GET /api/notifications/unread-count`

- **Purpose:** Lists unread count. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/notifications/src/routes/notifications.ts` line 53
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `notifications` service mounts this router at `/api/notifications` in `services/notifications/src/index.ts`; the route is declared in `services/notifications/src/routes/notifications.ts`.
2. **Handler.** `services/notifications/src/routes/notifications.ts:53` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `401`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/notifications/unread-count
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/notifications/src/routes/notifications.ts:53
      → (responds directly; no downstream calls detected)
```

## Onboarding

69 endpoints.

### `GET /api/onboarding/agent-config/:departmentId`

- **Purpose:** Reads a single agent config. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1090
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1090` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findFirst`
   - `routerRule.findFirst`
   - `aIAgent.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/onboarding/agent-config/:departmentId
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:1090
      → db read   prisma.department.findFirst
      → db read   prisma.routerRule.findFirst
      → db read   prisma.aIAgent.findUnique
```

### `POST /api/onboarding/ai-chat`

- **Purpose:** Creates an ai chat. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 736
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:736` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessProfile.findUnique`
   - `department.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/onboarding/ai-chat
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:736
      → db read   prisma.businessProfile.findUnique
      → db read   prisma.department.findMany
```

### `POST /api/onboarding/analyze-domain`

- **Purpose:** Creates an analyze domain. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1616
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(analyzeDomainSchema)` (Zod). Accepts: `domain`, `locale`. Required: `domain`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1616` - inline `async (req, res)` handler.
3. **Validation.** `validate(analyzeDomainSchema)` (Zod). Accepts: `domain`, `locale`. Required: `domain`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `auditLog.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/onboarding/analyze-domain
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(analyzeDomainSchema)]
  → handler  services/auth/src/routes/onboarding.ts:1616
      → db write  prisma.auditLog.create
```

### `GET /api/onboarding/business-profile`

- **Purpose:** Lists business profile. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1061
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1061` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessProfile.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/onboarding/business-profile
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:1061
      → db read   prisma.businessProfile.findUnique
```

### `POST /api/onboarding/business-profile`

- **Purpose:** Creates a business profile. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 542
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(businessProfileSchema)` (Zod). Accepts: `organizationName`, `businessDescription`, `locale`, `businessGoals`, `websiteDomain`, `industry`, `country`, `primaryLanguage`. Required: `organizationName`, `businessDescription`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:542` - inline `async (req, res)` handler.
3. **Validation.** `validate(businessProfileSchema)` (Zod). Accepts: `organizationName`, `businessDescription`, `locale`, `businessGoals`, `websiteDomain`, `industry`, `country`, `primaryLanguage`. Required: `organizationName`, `businessDescription`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `businessProfile.findUnique`
   _Writes:_
   - `businessProfile.upsert`
   - `tenant.update`
   - `tenantOnboarding.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/onboarding/business-profile
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(businessProfileSchema)]
  → handler  services/auth/src/routes/onboarding.ts:542
      → db read   prisma.tenant.findUnique
      → db read   prisma.businessProfile.findUnique
      → db write  prisma.businessProfile.upsert
      → db write  prisma.tenant.update
      → db write  prisma.tenantOnboarding.upsert
```

### `POST /api/onboarding/complete`

- **Purpose:** The new 2-screen flow makes this the synchronous critical path:   1. Verify profile exists   2. Auto-create a default "General" department if none exists   3. Flip tenant.status = ACTIVE (so AppLayout stops redirecting to /setup)   4. Fire-and-forget the AI config generation - the user enters the app      immediately; agents materialize within a few seconds in the background.
- **Handler:** `services/auth/src/routes/onboarding.ts` line 859
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:859` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `businessProfile.findUnique`
   - `businessDiscovery
      .findUnique({ where: { tenantId: req`
   - `businessDiscovery
      .findUnique`
   - `department.findMany`
   - `knowledgeDocument.findMany`
   _Writes:_
   - `department.create`
   - `tenant.update`
   - `tenantOnboarding.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/onboarding/complete
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:859
      → db read   prisma.tenant.findUnique
      → db read   prisma.businessProfile.findUnique
      → db read   prisma.businessDiscovery
      .findUnique({ where: { tenantId: req
      → db read   prisma.businessDiscovery
      .findUnique
      → db read   prisma.department.findMany
      → db read   prisma.knowledgeDocument.findMany
      → db write  prisma.department.create
      → db write  prisma.tenant.update
      → db write  prisma.tenantOnboarding.upsert
```

### `POST /api/onboarding/core-system`

- **Purpose:** Creates a core system. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1434
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(coreSystemSchema)` (Zod). Accepts: `slug`. Required: `slug`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1434` - inline `async (req, res)` handler.
3. **Validation.** `validate(coreSystemSchema)` (Zod). Accepts: `slug`. Required: `slug`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenantIntegration.findFirst`
   - `recommendation.updateMany({
        where: { tenantId: req`
   _Writes:_
   - `businessProfile.updateMany`
   - `recommendation.updateMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/onboarding/core-system
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(coreSystemSchema)]
  → handler  services/auth/src/routes/onboarding.ts:1434
      → db read   prisma.tenantIntegration.findFirst
      → db read   prisma.recommendation.updateMany({
        where: { tenantId: req
      → db write  prisma.businessProfile.updateMany
      → db write  prisma.recommendation.updateMany
```

### `GET /api/onboarding/departments`

- **Purpose:** Lists departments. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1075
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1075` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/onboarding/departments
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:1075
      → db read   prisma.department.findMany
```

### `POST /api/onboarding/departments`

- **Purpose:** Creates a department. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 661
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(departmentConfigSchema)` (Zod). Accepts: `departments`. Required: `departments`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:661` - inline `async (req, res)` handler.
3. **Validation.** `validate(departmentConfigSchema)` (Zod). Accepts: `departments`. Required: `departments`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `businessProfile.findUnique`
   _Writes:_
   - `department.deleteMany`
   - `department.create`
   - `tenantOnboarding.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/onboarding/departments
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(departmentConfigSchema)]
  → handler  services/auth/src/routes/onboarding.ts:661
      → db read   prisma.tenant.findUnique
      → db read   prisma.businessProfile.findUnique
      → db write  prisma.department.deleteMany
      → db write  prisma.department.create
      → db write  prisma.tenantOnboarding.upsert
```

### `POST /api/onboarding/discover`

- **Purpose:** Creates a discover. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2130
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(discoverSchema)` (Zod). Accepts: `domain`, `locale`, `legalAccepted`. Required: `domain`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2130` - inline `async (req, res)` handler.
3. **Validation.** `validate(discoverSchema)` (Zod). Accepts: `domain`, `locale`, `legalAccepted`. Required: `domain`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessDiscovery.findUnique`
   - `businessDiscovery.update({
        where: { tenantId: req`
   - `businessDiscovery.update({ where: { tenantId: req`
   - `businessDiscovery.update({
      where: { tenantId: req`
   - `channelAccount.findFirst({
      where: { tenantId: req`
   - `channelAccount.findFirst`
   - `businessProfile.findUnique({
        where: { tenantId: req`
   - `businessProfile.findUnique`
   _Writes:_
   - `auditLog.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/onboarding/discover
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(discoverSchema)]
  → handler  services/auth/src/routes/onboarding.ts:2130
      → db read   prisma.businessDiscovery.findUnique
      → db read   prisma.businessDiscovery.update({
        where: { tenantId: req
      → db read   prisma.businessDiscovery.update({ where: { tenantId: req
      → db read   prisma.businessDiscovery.update({
      where: { tenantId: req
      → db read   prisma.channelAccount.findFirst({
      where: { tenantId: req
      → db read   prisma.channelAccount.findFirst
      → db read   prisma.businessProfile.findUnique({
        where: { tenantId: req
      → db read   prisma.businessProfile.findUnique
      → db write  prisma.auditLog.create
```

### `POST /api/onboarding/discover/plan`

- **Purpose:** Creates a plan. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2516
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(planSchema)` (Zod). Accepts: `domain`, `locale`. Required: `domain`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2516` - inline `async (req, res)` handler.
3. **Validation.** `validate(planSchema)` (Zod). Accepts: `domain`, `locale`. Required: `domain`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/onboarding/discover/plan
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(planSchema)]
  → handler  services/auth/src/routes/onboarding.ts:2516
      → (responds directly; no downstream calls detected)
```

### `GET /api/onboarding/discovery`

- **Purpose:** GET /discovery - the persisted Business Intelligence Report (re-hydrates the review, health, and recommendation surfaces on refresh / OAuth return).
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2536
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2536` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessDiscovery.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/onboarding/discovery
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:2536
      → db read   prisma.businessDiscovery.findUnique
```

### `PATCH /api/onboarding/discovery`

- **Purpose:** Updates the referenced discovery. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2592
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(discoveryPatchSchema)` (Zod). Accepts: `employeeName`, `business`, `brand`, `communication`, `progress`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2592` - inline `async (req, res)` handler.
3. **Validation.** `validate(discoveryPatchSchema)` (Zod). Accepts: `employeeName`, `business`, `brand`, `communication`, `progress`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessDiscovery.findUnique`
   _Writes:_
   - `businessDiscovery.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/onboarding/discovery
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(discoveryPatchSchema)]
  → handler  services/auth/src/routes/onboarding.ts:2592
      → db read   prisma.businessDiscovery.findUnique
      → db write  prisma.businessDiscovery.update
```

### `POST /api/onboarding/discovery/correct`

- **Purpose:** Creates a correct. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2644
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(correctSchema)` (Zod). Accepts: `target`, `action`, `key`. Required: `target`, `action`, `key`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2644` - inline `async (req, res)` handler.
3. **Validation.** `validate(correctSchema)` (Zod). Accepts: `target`, `action`, `key`. Required: `target`, `action`, `key`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `recommendation.updateMany({
        where: { tenantId: req`
   _Writes:_
   - `recommendation.updateMany`
   - `businessDiscovery.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/onboarding/discovery/correct
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(correctSchema)]
  → handler  services/auth/src/routes/onboarding.ts:2644
      → db read   prisma.recommendation.updateMany({
        where: { tenantId: req
      → db write  prisma.recommendation.updateMany
      → db write  prisma.businessDiscovery.update
```

### `POST /api/onboarding/employee-chat`

- **Purpose:** Creates an employee chat. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 3032
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(employeeChatSchema)` (Zod). Accepts: `persona`, `messages`, `locale`, `personaOnly`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:3032` - inline `async (req, res)` handler.
3. **Validation.** `validate(employeeChatSchema)` (Zod). Accepts: `persona`, `messages`, `locale`, `personaOnly`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessDiscovery.update({
          where: { tenantId: req`
   - `businessDiscovery.update({
        where: { tenantId: req`
   _Writes:_
   - `businessDiscovery.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/onboarding/employee-chat
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(employeeChatSchema)]
  → handler  services/auth/src/routes/onboarding.ts:3032
      → db read   prisma.businessDiscovery.update({
          where: { tenantId: req
      → db read   prisma.businessDiscovery.update({
        where: { tenantId: req
      → db write  prisma.businessDiscovery.update
```

### `POST /api/onboarding/generate-configs`

- **Purpose:** Creates a generate config. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 796
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:796` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessProfile.findUnique`
   - `department.findMany`
   - `aIAgent.findMany`
   _Writes:_
   - `tenantOnboarding.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/onboarding/generate-configs
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:796
      → db read   prisma.businessProfile.findUnique
      → db read   prisma.department.findMany
      → db read   prisma.aIAgent.findMany
      → db write  prisma.tenantOnboarding.upsert
```

### `POST /api/onboarding/goal`

- **Purpose:** Creates a goal. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2927
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(goalSchema)` (Zod). Accepts: `goals`, `goal`, `detail`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2927` - inline `async (req, res)` handler.
3. **Validation.** `validate(goalSchema)` (Zod). Accepts: `goals`, `goal`, `detail`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessProfile.updateMany({
      where: { tenantId: req`
   _Writes:_
   - `businessProfile.updateMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/onboarding/goal
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(goalSchema)]
  → handler  services/auth/src/routes/onboarding.ts:2927
      → db read   prisma.businessProfile.updateMany({
      where: { tenantId: req
      → db write  prisma.businessProfile.updateMany
```

### `GET /api/onboarding/guides`

- **Purpose:** GET/PATCH /guides - per-user first-time guidance state for the persistent onboarding layer. Available to ALL roles (agents get guidance too), so no requireRole gate. State machine per feature: unseen -> snoozed | done.
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1523
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1523` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `401`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/onboarding/guides
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/onboarding.ts:1523
      → db read   prisma.user.findUnique
```

### `PATCH /api/onboarding/guides`

- **Purpose:** Updates the referenced guide. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1539
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** `validate(guidePatchSchema)` (Zod). Accepts: `feature`, `state`. Required: `feature`, `state`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1539` - inline `async (req, res)` handler.
3. **Validation.** `validate(guidePatchSchema)` (Zod). Accepts: `feature`, `state`. Required: `feature`, `state`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findUnique`
   _Writes:_
   - `user.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `401`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/onboarding/guides
  → [authenticate → resolveTenant → validate(guidePatchSchema)]
  → handler  services/auth/src/routes/onboarding.ts:1539
      → db read   prisma.user.findUnique
      → db write  prisma.user.update
```

### `GET /api/onboarding/health`

- **Purpose:** GET /health - Business Health (Movement 3), recomputed against LIVE connection state so a channel/system connected after the scan flips its ⚠ to ✓.
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2687
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2687` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst({
      where: { tenantId: req`
   - `channelAccount.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/onboarding/health
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:2687
      → db read   prisma.channelAccount.findFirst({
      where: { tenantId: req
      → db read   prisma.channelAccount.findFirst
```

### `POST /api/onboarding/invite-link`

- **Purpose:** Creates an invite link. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 3182
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:3182` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   _Writes:_
   - `tenantInvite.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/onboarding/invite-link
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:3182
      → db read   prisma.tenant.findUnique
      → db write  prisma.tenantInvite.create
```

### `POST /api/onboarding/invite-team`

- **Purpose:** Creates an invite team. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 3101
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(inviteTeamSchema)` (Zod). Accepts: `emails`, `role`. Required: `emails`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:3101` - inline `async (req, res)` handler.
3. **Validation.** `validate(inviteTeamSchema)` (Zod). Accepts: `emails`, `role`. Required: `emails`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findUnique`
   - `tenant.findUnique`
   _Writes:_
   - `tenantInvite.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/onboarding/invite-team
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(inviteTeamSchema)]
  → handler  services/auth/src/routes/onboarding.ts:3101
      → db read   prisma.user.findUnique
      → db read   prisma.tenant.findUnique
      → db write  prisma.tenantInvite.create
```

### `GET /api/onboarding/journey`

- **Purpose:** GET /journey - THE canonical setup-readiness state. The Getting Started page, the sidebar "Complete setup" panel, and the nav badge all derive from this one endpoint, so they can never contradict each other.
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1243
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1243` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst({
        where: { tenantId, status: "DRAFT" },
        select: { id: true },
      })`
   - `tenantIntegration.findMany({
        where: { tenantId, integration: { slug: { in: CORE_SYSTEM_SLUGS as unknown as string[] } } },
        select: { status: true },
      })`
   - `channelAccount.findMany({
        where: { tenantId },
        select: { connectionStatus: true, isActive: true },
      })`
   - `businessDiscovery.findUnique({
        where: { tenantId },
        select: { communication: true, business: true },
      })`
   - `knowledgeBase.count({
        where: {
          tenantId,
          isActive: true,
          OR: [{ documents: { some: {} } }, { integrations: { some: { isActive: true } } }],
        },
      })`
   - `flowCanvas.findUnique({
        where: { tenantId },
        select: { nodes: true, edges: true },
      })`
   - `chatbotFlow.count({ where: { tenantId } })`
   - `businessProfile.findUnique({
        where: { tenantId },
        select: { organizationName: true, industry: true },
      })`
   - `businessProfile.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/onboarding/journey
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:1243
      → db read   prisma.aIAgent.findFirst({
        where: { tenantId, status: "DRAFT" },
        select: { id: true },
      })
      → db read   prisma.tenantIntegration.findMany({
        where: { tenantId, integration: { slug: { in: CORE_SYSTEM_SLUGS as unknown as string[] } } },
        select: { status: true },
      })
      → db read   prisma.channelAccount.findMany({
        where: { tenantId },
        select: { connectionStatus: true, isActive: true },
      })
      → db read   prisma.businessDiscovery.findUnique({
        where: { tenantId },
        select: { communication: true, business: true },
      })
      → db read   prisma.knowledgeBase.count({
        where: {
          tenantId,
          isActive: true,
          OR: [{ documents: { some: {} } }, { integrations: { some: { isActive: true } } }],
        },
      })
      → db read   prisma.flowCanvas.findUnique({
        where: { tenantId },
        select: { nodes: true, edges: true },
      })
      → db read   prisma.chatbotFlow.count({ where: { tenantId } })
      → db read   prisma.businessProfile.findUnique({
        where: { tenantId },
        select: { organizationName: true, industry: true },
      })
      → db read   prisma.businessProfile.findUnique
```

### `GET /api/onboarding/missions`

- **Purpose:** Lists missions. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1143
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1143` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessDiscovery.findUnique({
        where: { tenantId },
        select: { communication: true },
      })`
   - `recommendation.findMany({
        where: { tenantId, status: "OPEN" },
        select: { kind: true, dedupeKey: true },
      })`
   - `recommendation.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/onboarding/missions
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:1143
      → db read   prisma.businessDiscovery.findUnique({
        where: { tenantId },
        select: { communication: true },
      })
      → db read   prisma.recommendation.findMany({
        where: { tenantId, status: "OPEN" },
        select: { kind: true, dedupeKey: true },
      })
      → db read   prisma.recommendation.findMany
```

### `POST /api/onboarding/notify-integration`

- **Purpose:** Creates a notify integration. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2964
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(notifyIntegrationSchema)` (Zod). Accepts: `integration`, `note`, `source`. Required: `integration`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2964` - inline `async (req, res)` handler.
3. **Validation.** `validate(notifyIntegrationSchema)` (Zod). Accepts: `integration`, `note`, `source`. Required: `integration`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findUnique({ where: { id: req`
   - `businessDiscovery.findUnique({ where: { tenantId: req`
   - `businessDiscovery.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/onboarding/notify-integration
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(notifyIntegrationSchema)]
  → handler  services/auth/src/routes/onboarding.ts:2964
      → db read   prisma.user.findUnique({ where: { id: req
      → db read   prisma.businessDiscovery.findUnique({ where: { tenantId: req
      → db read   prisma.businessDiscovery.findUnique
```

### `GET /api/onboarding/recommendation`

- **Purpose:** GET /recommendation - the first AI Recommendation (Movement 4), read straight off the persisted report.
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2712
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2712` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessDiscovery.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/onboarding/recommendation
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:2712
      → db read   prisma.businessDiscovery.findUnique
```

### `GET /api/onboarding/recommendations`

- **Purpose:** GET /recommendations - the persistent living backlog (defaults to OPEN).
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2864
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2864` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/onboarding/recommendations
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:2864
      → (responds directly; no downstream calls detected)
```

### `POST /api/onboarding/recommendations/:id/:decision`

- **Purpose:** POST /recommendations/:id/complete | /dismiss | /reopen. A resolved rec is never resurrected by a future re-scan; `reopen` is the explicit human undo (with confirmation in the UI) and restores OPEN. Every transition writes an audit event with the acting user.
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2882
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2882` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/onboarding/recommendations/:id/:decision
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:2882
      → (responds directly; no downstream calls detected)
```

### `GET /api/onboarding/setup-map`

- **Purpose:** GET /setup-map - the non-linear Setup Hub tiles + done flags, derived from real product signals (no manual check-offs). Tiles tagged stage:"later" (Channels, extra Integrations) are shown but excluded from the headline progress count.
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1475
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1475` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst({ where: { tenantId }, select: { id: true, status: true } })`
   - `flowCanvas.findUnique({ where: { tenantId }, select: { nodes: true, edges: true } })`
   - `chatbotFlow.count({ where: { tenantId } })`
   - `chatbotFlow.count`
   - `channelAccount.findFirst({ where: { tenantId, connectionStatus: "CONNECTED" }, select: { id: true } })`
   - `tenantIntegration.count({ where: { tenantId, status: "CONNECTED" } })`
   - `businessProfile.findUnique({ where: { tenantId }, select: { primarySystem: true } })`
   - `businessProfile.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/onboarding/setup-map
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:1475
      → db read   prisma.aIAgent.findFirst({ where: { tenantId }, select: { id: true, status: true } })
      → db read   prisma.flowCanvas.findUnique({ where: { tenantId }, select: { nodes: true, edges: true } })
      → db read   prisma.chatbotFlow.count({ where: { tenantId } })
      → db read   prisma.chatbotFlow.count
      → db read   prisma.channelAccount.findFirst({ where: { tenantId, connectionStatus: "CONNECTED" }, select: { id: true } })
      → db read   prisma.tenantIntegration.count({ where: { tenantId, status: "CONNECTED" } })
      → db read   prisma.businessProfile.findUnique({ where: { tenantId }, select: { primarySystem: true } })
      → db read   prisma.businessProfile.findUnique
```

### `GET /api/onboarding/status`

- **Purpose:** Lists status. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 461
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:461` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `tenantOnboarding.findUnique`
   - `businessProfile.findUnique`
   - `department.findMany`
   - `aIAgent.count`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/onboarding/status
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:461
      → db read   prisma.tenant.findUnique
      → db read   prisma.tenantOnboarding.findUnique
      → db read   prisma.businessProfile.findUnique
      → db read   prisma.department.findMany
      → db read   prisma.aIAgent.count
```

### `POST /api/onboarding/teach`

- **Purpose:** Creates a teach. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2755
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(teachSchema)` (Zod). Accepts: `label`, `method`, `value`. Required: `label`, `method`, `value`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/onboarding` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2755` - inline `async (req, res)` handler.
3. **Validation.** `validate(teachSchema)` (Zod). Accepts: `label`, `method`, `value`. Required: `label`, `method`, `value`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeDocument.findFirst`
   - `knowledgeBase.findFirst`
   - `knowledgeDocument
          .findMany({
            where: { tenantId: req`
   - `knowledgeDocument
          .findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/onboarding/teach
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(teachSchema)]
  → handler  services/auth/src/routes/onboarding.ts:2755
      → db read   prisma.knowledgeDocument.findFirst
      → db read   prisma.knowledgeBase.findFirst
      → db read   prisma.knowledgeDocument
          .findMany({
            where: { tenantId: req
      → db read   prisma.knowledgeDocument
          .findMany
```

### `GET /api/public/onboarding/agent-config/:departmentId`

- **Purpose:** Reads a single agent config. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1090
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1090` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findFirst`
   - `routerRule.findFirst`
   - `aIAgent.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/public/onboarding/agent-config/:departmentId
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:1090
      → db read   prisma.department.findFirst
      → db read   prisma.routerRule.findFirst
      → db read   prisma.aIAgent.findUnique
```

### `POST /api/public/onboarding/ai-chat`

- **Purpose:** Creates an ai chat. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 736
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:736` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessProfile.findUnique`
   - `department.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/public/onboarding/ai-chat
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:736
      → db read   prisma.businessProfile.findUnique
      → db read   prisma.department.findMany
```

### `POST /api/public/onboarding/analyze-domain`

- **Purpose:** Creates an analyze domain. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1616
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(analyzeDomainSchema)` (Zod). Accepts: `domain`, `locale`. Required: `domain`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1616` - inline `async (req, res)` handler.
3. **Validation.** `validate(analyzeDomainSchema)` (Zod). Accepts: `domain`, `locale`. Required: `domain`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `auditLog.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/public/onboarding/analyze-domain
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(analyzeDomainSchema)]
  → handler  services/auth/src/routes/onboarding.ts:1616
      → db write  prisma.auditLog.create
```

### `GET /api/public/onboarding/business-profile`

- **Purpose:** Lists business profile. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1061
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1061` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessProfile.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/public/onboarding/business-profile
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:1061
      → db read   prisma.businessProfile.findUnique
```

### `POST /api/public/onboarding/business-profile`

- **Purpose:** Creates a business profile. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 542
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(businessProfileSchema)` (Zod). Accepts: `organizationName`, `businessDescription`, `locale`, `businessGoals`, `websiteDomain`, `industry`, `country`, `primaryLanguage`. Required: `organizationName`, `businessDescription`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:542` - inline `async (req, res)` handler.
3. **Validation.** `validate(businessProfileSchema)` (Zod). Accepts: `organizationName`, `businessDescription`, `locale`, `businessGoals`, `websiteDomain`, `industry`, `country`, `primaryLanguage`. Required: `organizationName`, `businessDescription`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `businessProfile.findUnique`
   _Writes:_
   - `businessProfile.upsert`
   - `tenant.update`
   - `tenantOnboarding.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/public/onboarding/business-profile
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(businessProfileSchema)]
  → handler  services/auth/src/routes/onboarding.ts:542
      → db read   prisma.tenant.findUnique
      → db read   prisma.businessProfile.findUnique
      → db write  prisma.businessProfile.upsert
      → db write  prisma.tenant.update
      → db write  prisma.tenantOnboarding.upsert
```

### `POST /api/public/onboarding/complete`

- **Purpose:** The new 2-screen flow makes this the synchronous critical path:   1. Verify profile exists   2. Auto-create a default "General" department if none exists   3. Flip tenant.status = ACTIVE (so AppLayout stops redirecting to /setup)   4. Fire-and-forget the AI config generation - the user enters the app      immediately; agents materialize within a few seconds in the background.
- **Handler:** `services/auth/src/routes/onboarding.ts` line 859
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:859` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `businessProfile.findUnique`
   - `businessDiscovery
      .findUnique({ where: { tenantId: req`
   - `businessDiscovery
      .findUnique`
   - `department.findMany`
   - `knowledgeDocument.findMany`
   _Writes:_
   - `department.create`
   - `tenant.update`
   - `tenantOnboarding.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/public/onboarding/complete
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:859
      → db read   prisma.tenant.findUnique
      → db read   prisma.businessProfile.findUnique
      → db read   prisma.businessDiscovery
      .findUnique({ where: { tenantId: req
      → db read   prisma.businessDiscovery
      .findUnique
      → db read   prisma.department.findMany
      → db read   prisma.knowledgeDocument.findMany
      → db write  prisma.department.create
      → db write  prisma.tenant.update
      → db write  prisma.tenantOnboarding.upsert
```

### `POST /api/public/onboarding/core-system`

- **Purpose:** Creates a core system. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1434
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(coreSystemSchema)` (Zod). Accepts: `slug`. Required: `slug`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1434` - inline `async (req, res)` handler.
3. **Validation.** `validate(coreSystemSchema)` (Zod). Accepts: `slug`. Required: `slug`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenantIntegration.findFirst`
   - `recommendation.updateMany({
        where: { tenantId: req`
   _Writes:_
   - `businessProfile.updateMany`
   - `recommendation.updateMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/public/onboarding/core-system
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(coreSystemSchema)]
  → handler  services/auth/src/routes/onboarding.ts:1434
      → db read   prisma.tenantIntegration.findFirst
      → db read   prisma.recommendation.updateMany({
        where: { tenantId: req
      → db write  prisma.businessProfile.updateMany
      → db write  prisma.recommendation.updateMany
```

### `GET /api/public/onboarding/departments`

- **Purpose:** Lists departments. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1075
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1075` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/public/onboarding/departments
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:1075
      → db read   prisma.department.findMany
```

### `POST /api/public/onboarding/departments`

- **Purpose:** Creates a department. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 661
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(departmentConfigSchema)` (Zod). Accepts: `departments`. Required: `departments`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:661` - inline `async (req, res)` handler.
3. **Validation.** `validate(departmentConfigSchema)` (Zod). Accepts: `departments`. Required: `departments`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `businessProfile.findUnique`
   _Writes:_
   - `department.deleteMany`
   - `department.create`
   - `tenantOnboarding.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/public/onboarding/departments
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(departmentConfigSchema)]
  → handler  services/auth/src/routes/onboarding.ts:661
      → db read   prisma.tenant.findUnique
      → db read   prisma.businessProfile.findUnique
      → db write  prisma.department.deleteMany
      → db write  prisma.department.create
      → db write  prisma.tenantOnboarding.upsert
```

### `POST /api/public/onboarding/discover`

- **Purpose:** Creates a discover. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2130
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(discoverSchema)` (Zod). Accepts: `domain`, `locale`, `legalAccepted`. Required: `domain`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2130` - inline `async (req, res)` handler.
3. **Validation.** `validate(discoverSchema)` (Zod). Accepts: `domain`, `locale`, `legalAccepted`. Required: `domain`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessDiscovery.findUnique`
   - `businessDiscovery.update({
        where: { tenantId: req`
   - `businessDiscovery.update({ where: { tenantId: req`
   - `businessDiscovery.update({
      where: { tenantId: req`
   - `channelAccount.findFirst({
      where: { tenantId: req`
   - `channelAccount.findFirst`
   - `businessProfile.findUnique({
        where: { tenantId: req`
   - `businessProfile.findUnique`
   _Writes:_
   - `auditLog.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/public/onboarding/discover
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(discoverSchema)]
  → handler  services/auth/src/routes/onboarding.ts:2130
      → db read   prisma.businessDiscovery.findUnique
      → db read   prisma.businessDiscovery.update({
        where: { tenantId: req
      → db read   prisma.businessDiscovery.update({ where: { tenantId: req
      → db read   prisma.businessDiscovery.update({
      where: { tenantId: req
      → db read   prisma.channelAccount.findFirst({
      where: { tenantId: req
      → db read   prisma.channelAccount.findFirst
      → db read   prisma.businessProfile.findUnique({
        where: { tenantId: req
      → db read   prisma.businessProfile.findUnique
      → db write  prisma.auditLog.create
```

### `POST /api/public/onboarding/discover/plan`

- **Purpose:** Creates a plan. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2516
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(planSchema)` (Zod). Accepts: `domain`, `locale`. Required: `domain`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2516` - inline `async (req, res)` handler.
3. **Validation.** `validate(planSchema)` (Zod). Accepts: `domain`, `locale`. Required: `domain`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/public/onboarding/discover/plan
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(planSchema)]
  → handler  services/auth/src/routes/onboarding.ts:2516
      → (responds directly; no downstream calls detected)
```

### `GET /api/public/onboarding/discovery`

- **Purpose:** GET /discovery - the persisted Business Intelligence Report (re-hydrates the review, health, and recommendation surfaces on refresh / OAuth return).
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2536
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2536` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessDiscovery.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/public/onboarding/discovery
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:2536
      → db read   prisma.businessDiscovery.findUnique
```

### `PATCH /api/public/onboarding/discovery`

- **Purpose:** Updates the referenced discovery. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2592
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(discoveryPatchSchema)` (Zod). Accepts: `employeeName`, `business`, `brand`, `communication`, `progress`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2592` - inline `async (req, res)` handler.
3. **Validation.** `validate(discoveryPatchSchema)` (Zod). Accepts: `employeeName`, `business`, `brand`, `communication`, `progress`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessDiscovery.findUnique`
   _Writes:_
   - `businessDiscovery.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/public/onboarding/discovery
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(discoveryPatchSchema)]
  → handler  services/auth/src/routes/onboarding.ts:2592
      → db read   prisma.businessDiscovery.findUnique
      → db write  prisma.businessDiscovery.update
```

### `POST /api/public/onboarding/discovery/correct`

- **Purpose:** Creates a correct. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2644
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(correctSchema)` (Zod). Accepts: `target`, `action`, `key`. Required: `target`, `action`, `key`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2644` - inline `async (req, res)` handler.
3. **Validation.** `validate(correctSchema)` (Zod). Accepts: `target`, `action`, `key`. Required: `target`, `action`, `key`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `recommendation.updateMany({
        where: { tenantId: req`
   _Writes:_
   - `recommendation.updateMany`
   - `businessDiscovery.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/public/onboarding/discovery/correct
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(correctSchema)]
  → handler  services/auth/src/routes/onboarding.ts:2644
      → db read   prisma.recommendation.updateMany({
        where: { tenantId: req
      → db write  prisma.recommendation.updateMany
      → db write  prisma.businessDiscovery.update
```

### `POST /api/public/onboarding/employee-chat`

- **Purpose:** Creates an employee chat. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 3032
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(employeeChatSchema)` (Zod). Accepts: `persona`, `messages`, `locale`, `personaOnly`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:3032` - inline `async (req, res)` handler.
3. **Validation.** `validate(employeeChatSchema)` (Zod). Accepts: `persona`, `messages`, `locale`, `personaOnly`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessDiscovery.update({
          where: { tenantId: req`
   - `businessDiscovery.update({
        where: { tenantId: req`
   _Writes:_
   - `businessDiscovery.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/public/onboarding/employee-chat
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(employeeChatSchema)]
  → handler  services/auth/src/routes/onboarding.ts:3032
      → db read   prisma.businessDiscovery.update({
          where: { tenantId: req
      → db read   prisma.businessDiscovery.update({
        where: { tenantId: req
      → db write  prisma.businessDiscovery.update
```

### `POST /api/public/onboarding/generate-configs`

- **Purpose:** Creates a generate config. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 796
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:796` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessProfile.findUnique`
   - `department.findMany`
   - `aIAgent.findMany`
   _Writes:_
   - `tenantOnboarding.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/public/onboarding/generate-configs
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:796
      → db read   prisma.businessProfile.findUnique
      → db read   prisma.department.findMany
      → db read   prisma.aIAgent.findMany
      → db write  prisma.tenantOnboarding.upsert
```

### `POST /api/public/onboarding/goal`

- **Purpose:** Creates a goal. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2927
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(goalSchema)` (Zod). Accepts: `goals`, `goal`, `detail`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2927` - inline `async (req, res)` handler.
3. **Validation.** `validate(goalSchema)` (Zod). Accepts: `goals`, `goal`, `detail`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessProfile.updateMany({
      where: { tenantId: req`
   _Writes:_
   - `businessProfile.updateMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/public/onboarding/goal
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(goalSchema)]
  → handler  services/auth/src/routes/onboarding.ts:2927
      → db read   prisma.businessProfile.updateMany({
      where: { tenantId: req
      → db write  prisma.businessProfile.updateMany
```

### `GET /api/public/onboarding/guides`

- **Purpose:** GET/PATCH /guides - per-user first-time guidance state for the persistent onboarding layer. Available to ALL roles (agents get guidance too), so no requireRole gate. State machine per feature: unseen -> snoozed | done.
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1523
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1523` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `401`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/public/onboarding/guides
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/onboarding.ts:1523
      → db read   prisma.user.findUnique
```

### `PATCH /api/public/onboarding/guides`

- **Purpose:** Updates the referenced guide. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1539
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** `validate(guidePatchSchema)` (Zod). Accepts: `feature`, `state`. Required: `feature`, `state`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1539` - inline `async (req, res)` handler.
3. **Validation.** `validate(guidePatchSchema)` (Zod). Accepts: `feature`, `state`. Required: `feature`, `state`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findUnique`
   _Writes:_
   - `user.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `401`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/public/onboarding/guides
  → [authenticate → resolveTenant → validate(guidePatchSchema)]
  → handler  services/auth/src/routes/onboarding.ts:1539
      → db read   prisma.user.findUnique
      → db write  prisma.user.update
```

### `GET /api/public/onboarding/health`

- **Purpose:** GET /health - Business Health (Movement 3), recomputed against LIVE connection state so a channel/system connected after the scan flips its ⚠ to ✓.
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2687
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2687` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst({
      where: { tenantId: req`
   - `channelAccount.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/public/onboarding/health
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:2687
      → db read   prisma.channelAccount.findFirst({
      where: { tenantId: req
      → db read   prisma.channelAccount.findFirst
```

### `POST /api/public/onboarding/invite-link`

- **Purpose:** Creates an invite link. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 3182
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:3182` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   _Writes:_
   - `tenantInvite.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/public/onboarding/invite-link
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:3182
      → db read   prisma.tenant.findUnique
      → db write  prisma.tenantInvite.create
```

### `POST /api/public/onboarding/invite-team`

- **Purpose:** Creates an invite team. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 3101
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(inviteTeamSchema)` (Zod). Accepts: `emails`, `role`. Required: `emails`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:3101` - inline `async (req, res)` handler.
3. **Validation.** `validate(inviteTeamSchema)` (Zod). Accepts: `emails`, `role`. Required: `emails`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findUnique`
   - `tenant.findUnique`
   _Writes:_
   - `tenantInvite.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/public/onboarding/invite-team
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(inviteTeamSchema)]
  → handler  services/auth/src/routes/onboarding.ts:3101
      → db read   prisma.user.findUnique
      → db read   prisma.tenant.findUnique
      → db write  prisma.tenantInvite.create
```

### `GET /api/public/onboarding/journey`

- **Purpose:** GET /journey - THE canonical setup-readiness state. The Getting Started page, the sidebar "Complete setup" panel, and the nav badge all derive from this one endpoint, so they can never contradict each other.
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1243
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1243` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst({
        where: { tenantId, status: "DRAFT" },
        select: { id: true },
      })`
   - `tenantIntegration.findMany({
        where: { tenantId, integration: { slug: { in: CORE_SYSTEM_SLUGS as unknown as string[] } } },
        select: { status: true },
      })`
   - `channelAccount.findMany({
        where: { tenantId },
        select: { connectionStatus: true, isActive: true },
      })`
   - `businessDiscovery.findUnique({
        where: { tenantId },
        select: { communication: true, business: true },
      })`
   - `knowledgeBase.count({
        where: {
          tenantId,
          isActive: true,
          OR: [{ documents: { some: {} } }, { integrations: { some: { isActive: true } } }],
        },
      })`
   - `flowCanvas.findUnique({
        where: { tenantId },
        select: { nodes: true, edges: true },
      })`
   - `chatbotFlow.count({ where: { tenantId } })`
   - `businessProfile.findUnique({
        where: { tenantId },
        select: { organizationName: true, industry: true },
      })`
   - `businessProfile.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/public/onboarding/journey
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:1243
      → db read   prisma.aIAgent.findFirst({
        where: { tenantId, status: "DRAFT" },
        select: { id: true },
      })
      → db read   prisma.tenantIntegration.findMany({
        where: { tenantId, integration: { slug: { in: CORE_SYSTEM_SLUGS as unknown as string[] } } },
        select: { status: true },
      })
      → db read   prisma.channelAccount.findMany({
        where: { tenantId },
        select: { connectionStatus: true, isActive: true },
      })
      → db read   prisma.businessDiscovery.findUnique({
        where: { tenantId },
        select: { communication: true, business: true },
      })
      → db read   prisma.knowledgeBase.count({
        where: {
          tenantId,
          isActive: true,
          OR: [{ documents: { some: {} } }, { integrations: { some: { isActive: true } } }],
        },
      })
      → db read   prisma.flowCanvas.findUnique({
        where: { tenantId },
        select: { nodes: true, edges: true },
      })
      → db read   prisma.chatbotFlow.count({ where: { tenantId } })
      → db read   prisma.businessProfile.findUnique({
        where: { tenantId },
        select: { organizationName: true, industry: true },
      })
      → db read   prisma.businessProfile.findUnique
```

### `GET /api/public/onboarding/missions`

- **Purpose:** Lists missions. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1143
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1143` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessDiscovery.findUnique({
        where: { tenantId },
        select: { communication: true },
      })`
   - `recommendation.findMany({
        where: { tenantId, status: "OPEN" },
        select: { kind: true, dedupeKey: true },
      })`
   - `recommendation.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/public/onboarding/missions
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:1143
      → db read   prisma.businessDiscovery.findUnique({
        where: { tenantId },
        select: { communication: true },
      })
      → db read   prisma.recommendation.findMany({
        where: { tenantId, status: "OPEN" },
        select: { kind: true, dedupeKey: true },
      })
      → db read   prisma.recommendation.findMany
```

### `POST /api/public/onboarding/notify-integration`

- **Purpose:** Creates a notify integration. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2964
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(notifyIntegrationSchema)` (Zod). Accepts: `integration`, `note`, `source`. Required: `integration`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2964` - inline `async (req, res)` handler.
3. **Validation.** `validate(notifyIntegrationSchema)` (Zod). Accepts: `integration`, `note`, `source`. Required: `integration`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findUnique({ where: { id: req`
   - `businessDiscovery.findUnique({ where: { tenantId: req`
   - `businessDiscovery.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/public/onboarding/notify-integration
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(notifyIntegrationSchema)]
  → handler  services/auth/src/routes/onboarding.ts:2964
      → db read   prisma.user.findUnique({ where: { id: req
      → db read   prisma.businessDiscovery.findUnique({ where: { tenantId: req
      → db read   prisma.businessDiscovery.findUnique
```

### `GET /api/public/onboarding/recommendation`

- **Purpose:** GET /recommendation - the first AI Recommendation (Movement 4), read straight off the persisted report.
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2712
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2712` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `businessDiscovery.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/public/onboarding/recommendation
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:2712
      → db read   prisma.businessDiscovery.findUnique
```

### `GET /api/public/onboarding/recommendations`

- **Purpose:** GET /recommendations - the persistent living backlog (defaults to OPEN).
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2864
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2864` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/public/onboarding/recommendations
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:2864
      → (responds directly; no downstream calls detected)
```

### `POST /api/public/onboarding/recommendations/:id/:decision`

- **Purpose:** POST /recommendations/:id/complete | /dismiss | /reopen. A resolved rec is never resurrected by a future re-scan; `reopen` is the explicit human undo (with confirmation in the UI) and restores OPEN. Every transition writes an audit event with the acting user.
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2882
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2882` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/public/onboarding/recommendations/:id/:decision
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:2882
      → (responds directly; no downstream calls detected)
```

### `GET /api/public/onboarding/setup-map`

- **Purpose:** GET /setup-map - the non-linear Setup Hub tiles + done flags, derived from real product signals (no manual check-offs). Tiles tagged stage:"later" (Channels, extra Integrations) are shown but excluded from the headline progress count.
- **Handler:** `services/auth/src/routes/onboarding.ts` line 1475
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:1475` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `aIAgent.findFirst({ where: { tenantId }, select: { id: true, status: true } })`
   - `flowCanvas.findUnique({ where: { tenantId }, select: { nodes: true, edges: true } })`
   - `chatbotFlow.count({ where: { tenantId } })`
   - `chatbotFlow.count`
   - `channelAccount.findFirst({ where: { tenantId, connectionStatus: "CONNECTED" }, select: { id: true } })`
   - `tenantIntegration.count({ where: { tenantId, status: "CONNECTED" } })`
   - `businessProfile.findUnique({ where: { tenantId }, select: { primarySystem: true } })`
   - `businessProfile.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/public/onboarding/setup-map
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:1475
      → db read   prisma.aIAgent.findFirst({ where: { tenantId }, select: { id: true, status: true } })
      → db read   prisma.flowCanvas.findUnique({ where: { tenantId }, select: { nodes: true, edges: true } })
      → db read   prisma.chatbotFlow.count({ where: { tenantId } })
      → db read   prisma.chatbotFlow.count
      → db read   prisma.channelAccount.findFirst({ where: { tenantId, connectionStatus: "CONNECTED" }, select: { id: true } })
      → db read   prisma.tenantIntegration.count({ where: { tenantId, status: "CONNECTED" } })
      → db read   prisma.businessProfile.findUnique({ where: { tenantId }, select: { primarySystem: true } })
      → db read   prisma.businessProfile.findUnique
```

### `GET /api/public/onboarding/status`

- **Purpose:** Lists status. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 461
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:461` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `tenantOnboarding.findUnique`
   - `businessProfile.findUnique`
   - `department.findMany`
   - `aIAgent.count`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/public/onboarding/status
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/onboarding.ts:461
      → db read   prisma.tenant.findUnique
      → db read   prisma.tenantOnboarding.findUnique
      → db read   prisma.businessProfile.findUnique
      → db read   prisma.department.findMany
      → db read   prisma.aIAgent.count
```

### `POST /api/public/onboarding/teach`

- **Purpose:** Creates a teach. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/onboarding.ts` line 2755
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(teachSchema)` (Zod). Accepts: `label`, `method`, `value`. Required: `label`, `method`, `value`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/public` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/onboarding.ts`.
2. **Handler.** `services/auth/src/routes/onboarding.ts:2755` - inline `async (req, res)` handler.
3. **Validation.** `validate(teachSchema)` (Zod). Accepts: `label`, `method`, `value`. Required: `label`, `method`, `value`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `knowledgeDocument.findFirst`
   - `knowledgeBase.findFirst`
   - `knowledgeDocument
          .findMany({
            where: { tenantId: req`
   - `knowledgeDocument
          .findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/public/onboarding/teach
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(teachSchema)]
  → handler  services/auth/src/routes/onboarding.ts:2755
      → db read   prisma.knowledgeDocument.findFirst
      → db read   prisma.knowledgeBase.findFirst
      → db read   prisma.knowledgeDocument
          .findMany({
            where: { tenantId: req
      → db read   prisma.knowledgeDocument
          .findMany
```

### `POST /api/waitlist`

- **Purpose:** Creates a waitlist. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/waitlist.ts` line 36
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** `validate(waitlistSchema)` (Zod). Accepts: `firstName`, `email`, `phone`, `company`, `companyDomain`, `role`, `companySize`, `frustration`, `source`. Required: `firstName`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/waitlist` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/waitlist.ts`.
2. **Handler.** `services/auth/src/routes/waitlist.ts:36` - inline `async (req, res)` handler.
3. **Validation.** `validate(waitlistSchema)` (Zod). Accepts: `firstName`, `email`, `phone`, `company`, `companyDomain`, `role`, `companySize`, `frustration`, `source`. Required: `firstName`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `waitlistEntry.findUnique`
   - `waitlistEntry.findFirst`
   - `waitlistEntry.count`
   _Writes:_
   - `waitlistEntry.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `409`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/waitlist
  → [validate(waitlistSchema)]
  → handler  services/auth/src/routes/waitlist.ts:36
      → db read   prisma.waitlistEntry.findUnique
      → db read   prisma.waitlistEntry.findFirst
      → db read   prisma.waitlistEntry.count
      → db write  prisma.waitlistEntry.create
```

### `GET /api/waitlist/leads`

- **Purpose:** ═══════════════════════════════════════════════════════════════ Admin endpoints (SYSTEM_ADMIN only) ═══════════════════════════════════════════════════════════════
- **Handler:** `services/auth/src/routes/waitlist.ts` line 118
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/waitlist` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/waitlist.ts`.
2. **Handler.** `services/auth/src/routes/waitlist.ts:118` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `waitlistEntry.findMany`
   - `waitlistEntry.count`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/waitlist/leads
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/waitlist.ts:118
      → db read   prisma.waitlistEntry.findMany
      → db read   prisma.waitlistEntry.count
```

### `DELETE /api/waitlist/leads/:id`

- **Purpose:** Deletes the referenced lead. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/waitlist.ts` line 315
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/waitlist` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/waitlist.ts`.
2. **Handler.** `services/auth/src/routes/waitlist.ts:315` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `waitlistEntry.findUnique`
   _Writes:_
   - `waitlistEntry.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/waitlist/leads/:id
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/waitlist.ts:315
      → db read   prisma.waitlistEntry.findUnique
      → db write  prisma.waitlistEntry.delete
```

### `GET /api/waitlist/leads/:id`

- **Purpose:** Reads a single lead. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/waitlist.ts` line 299
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/waitlist` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/waitlist.ts`.
2. **Handler.** `services/auth/src/routes/waitlist.ts:299` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `waitlistEntry.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/waitlist/leads/:id
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/waitlist.ts:299
      → db read   prisma.waitlistEntry.findUnique
```

### `PATCH /api/waitlist/leads/:id`

- **Purpose:** Updates the referenced lead. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/waitlist.ts` line 267
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** `validate(updateLeadSchema)` (Zod). Accepts: `status`, `notes`, `leadScore`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/waitlist` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/waitlist.ts`.
2. **Handler.** `services/auth/src/routes/waitlist.ts:267` - inline `async (req, res)` handler.
3. **Validation.** `validate(updateLeadSchema)` (Zod). Accepts: `status`, `notes`, `leadScore`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `waitlistEntry.findUnique`
   _Writes:_
   - `waitlistEntry.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/waitlist/leads/:id
  → [authenticate → requireSystemAdmin → validate(updateLeadSchema)]
  → handler  services/auth/src/routes/waitlist.ts:267
      → db read   prisma.waitlistEntry.findUnique
      → db write  prisma.waitlistEntry.update
```

### `GET /api/waitlist/leads/export`

- **Purpose:** Lists export. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/waitlist.ts` line 210
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/waitlist` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/waitlist.ts`.
2. **Handler.** `services/auth/src/routes/waitlist.ts:210` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `waitlistEntry.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/waitlist/leads/export
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/waitlist.ts:210
      → db read   prisma.waitlistEntry.findMany
```

### `GET /api/waitlist/leads/stats`

- **Purpose:** Lists stats. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/waitlist.ts` line 176
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/waitlist` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/waitlist.ts`.
2. **Handler.** `services/auth/src/routes/waitlist.ts:176` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `waitlistEntry.count`
   - `waitlistEntry.groupBy`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/waitlist/leads/stats
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/waitlist.ts:176
      → db read   prisma.waitlistEntry.count
      → db read   prisma.waitlistEntry.groupBy
```

## Platform Admin

37 endpoints.

### `GET /api/system/features`

- **Purpose:** Lists features. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system-features.ts` line 39
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system-features.ts`.
2. **Handler.** `services/auth/src/routes/system-features.ts:39` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system/features
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system-features.ts:39
      → (responds directly; no downstream calls detected)
```

### `POST /api/system/knowledge-backfill`

- **Purpose:** POST /api/system/knowledge-backfill body: { dryRun?: boolean (default true), tenantIds?: string[], limit?: number }
- **Handler:** `services/auth/src/routes/knowledge-backfill.ts` line 100
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/knowledge-backfill.ts`.
2. **Handler.** `services/auth/src/routes/knowledge-backfill.ts:100` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique({
        where: { id: disc`
   - `businessProfile.findUnique({
        where: { tenantId: tenant`
   - `knowledgeDocument.findMany({
          where: { tenantId: tenant`
   - `knowledgeDocument.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/system/knowledge-backfill
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/knowledge-backfill.ts:100
      → db read   prisma.tenant.findUnique({
        where: { id: disc
      → db read   prisma.businessProfile.findUnique({
        where: { tenantId: tenant
      → db read   prisma.knowledgeDocument.findMany({
          where: { tenantId: tenant
      → db read   prisma.knowledgeDocument.findMany
```

### `GET /api/system/onboarding-console`

- **Purpose:** One row per tenant with every onboarding milestone, derived progress, health, and the Next Recommended Action. The operational board.
- **Handler:** `services/auth/src/routes/system.ts` line 1322
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1322` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system/onboarding-console
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:1322
      → (responds directly; no downstream calls detected)
```

### `GET /api/system/poc-feature-domains`

- **Purpose:** The feature areas a POC may be scoped to.
- **Handler:** `services/auth/src/routes/system.ts` line 1027
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1027` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system/poc-feature-domains
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:1027
      → (responds directly; no downstream calls detected)
```

### `GET /api/system/pricing/trends`

- **Purpose:** Daily cost-per-category buckets for the period. Used by the trends chart on /system/pricing. Returns dense buckets (zero-filled) so the chart x-axis never has gaps.
- **Handler:** `services/auth/src/routes/system.ts` line 2186
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:2186` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system/pricing/trends
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:2186
      → (responds directly; no downstream calls detected)
```

### `GET /api/system/pricing/unit-costs`

- **Purpose:** Powers /system/pricing. For each customer-facing AI surface (Autonomous Agent, Inbox Co-pilot, Call-pilot, Embedded Chat, etc) we return raw totals plus unit averages: $/call and $/conversation. The frontend turns those into a pricing calculator (markup % → suggested price per unit) and a trends chart.
- **Handler:** `services/auth/src/routes/system.ts` line 1945
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1945` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system/pricing/unit-costs
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:1945
      → (responds directly; no downstream calls detected)
```

### `POST /api/system/seed`

- **Purpose:** Creates a seed. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system.ts` line 1629
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** None.
- **Input validation:** `validate(seedSchema)` (Zod). Accepts: `email`, `name`, `setupSecret`. Required: `email`, `name`, `setupSecret`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1629` - inline `async (req, res)` handler.
3. **Validation.** `validate(seedSchema)` (Zod). Accepts: `email`, `name`, `setupSecret`. Required: `email`, `name`, `setupSecret`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findFirst`
   - `tenant.findUnique`
   _Writes:_
   - `tenant.create`
   - `identity.upsert`
   - `user.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `403`, `409`, `500`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/system/seed
  → [validate(seedSchema)]
  → handler  services/auth/src/routes/system.ts:1629
      → db read   prisma.user.findFirst
      → db read   prisma.tenant.findUnique
      → db write  prisma.tenant.create
      → db write  prisma.identity.upsert
      → db write  prisma.user.create
```

### `GET /api/system/stats`

- **Purpose:** REMOVED. System admins authenticate through Authentik like everyone else; there is no second password login. SYSTEM_ADMIN is a local RBAC role, so requireSystemAdmin() still gates every route below - the role is read from the database after Authentik proves the identity.
- **Handler:** `services/auth/src/routes/system.ts` line 64
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:64` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.count`
   - `user.count`
   - `conversation.count`
   - `message.count`
   - `channelAccount.count`
   - `tenant.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system/stats
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:64
      → db read   prisma.tenant.count
      → db read   prisma.user.count
      → db read   prisma.conversation.count
      → db read   prisma.message.count
      → db read   prisma.channelAccount.count
      → db read   prisma.tenant.findMany
```

### `GET /api/system/tenants`

- **Purpose:** Lists tenants. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system.ts` line 100
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:100` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findMany`
   - `tenant.count`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system/tenants
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:100
      → db read   prisma.tenant.findMany
      → db read   prisma.tenant.count
```

### `POST /api/system/tenants`

- **Purpose:** Creates a tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system.ts` line 347
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** `validate(createTenantSchema)` (Zod). Accepts: `name`, `slug`, `adminEmail`, `adminName`, `billing`. Required: `name`, `slug`, `adminEmail`, `adminName`, `billing`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:347` - inline `async (req, res)` handler.
3. **Validation.** `validate(createTenantSchema)` (Zod). Accepts: `name`, `slug`, `adminEmail`, `adminName`, `billing`. Required: `name`, `slug`, `adminEmail`, `adminName`, `billing`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   _Writes:_
   - `identity.upsert`
   - `tenant.create`
   - `user.create`
   - `tenantOnboarding.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `409`, `500`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/system/tenants
  → [authenticate → requireSystemAdmin → validate(createTenantSchema)]
  → handler  services/auth/src/routes/system.ts:347
      → db read   prisma.tenant.findUnique
      → db write  prisma.identity.upsert
      → db write  prisma.tenant.create
      → db write  prisma.user.create
      → db write  prisma.tenantOnboarding.create
```

### `GET /api/system/tenants-plan-audit`

- **Purpose:** Every tenant, grouped by how it holds access.
- **Handler:** `services/auth/src/routes/system.ts` line 1040
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1040` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system/tenants-plan-audit
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:1040
      → (responds directly; no downstream calls detected)
```

### `DELETE /api/system/tenants/:id`

- **Purpose:** Deletes the referenced tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system.ts` line 1226
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1226` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/system/tenants/:id
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:1226
      → db read   prisma.tenant.findUnique
```

### `GET /api/system/tenants/:id`

- **Purpose:** Reads a single tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system.ts` line 171
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:171` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `user.findMany`
   - `channelAccount.findMany`
   - `department.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system/tenants/:id
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:171
      → db read   prisma.tenant.findUnique
      → db read   prisma.user.findMany
      → db read   prisma.channelAccount.findMany
      → db read   prisma.department.findMany
```

### `PATCH /api/system/tenants/:id`

- **Purpose:** Updates the referenced tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system.ts` line 1371
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** `validate(updateTenantSchema)` (Zod). Accepts: `name`, `isActive`, `voiceCopilotEnabled`, `voiceInboxUiEnabled`, `voiceIncomingEnabled`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1371` - inline `async (req, res)` handler.
3. **Validation.** `validate(updateTenantSchema)` (Zod). Accepts: `name`, `isActive`, `voiceCopilotEnabled`, `voiceInboxUiEnabled`, `voiceIncomingEnabled`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   _Writes:_
   - `tenant.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/system/tenants/:id
  → [authenticate → requireSystemAdmin → validate(updateTenantSchema)]
  → handler  services/auth/src/routes/system.ts:1371
      → db read   prisma.tenant.findUnique
      → db write  prisma.tenant.update
```

### `POST /api/system/tenants/:id/activate-manual-contract`

- **Purpose:** Performs the `activate manual contract` action on the referenced tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system.ts` line 681
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** `validate(manualContractSchema)` (Zod). `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:681` - inline `async (req, res)` handler.
3. **Validation.** `validate(manualContractSchema)` (Zod). `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/system/tenants/:id/activate-manual-contract
  → [authenticate → requirePlatformPermission → validate(manualContractSchema)]
  → handler  services/auth/src/routes/system.ts:681
      → (responds directly; no downstream calls detected)
```

### `POST /api/system/tenants/:id/assign-paid-plan`

- **Purpose:** Performs the `assign paid plan` action on the referenced tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system.ts` line 909
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** `validate(assignPaidPlanSchema)` (Zod). `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:909` - inline `async (req, res)` handler.
3. **Validation.** `validate(assignPaidPlanSchema)` (Zod). `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `user.findFirst`
   _Writes:_
   - `tenant.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/system/tenants/:id/assign-paid-plan
  → [authenticate → requireSystemAdmin → validate(assignPaidPlanSchema)]
  → handler  services/auth/src/routes/system.ts:909
      → db read   prisma.tenant.findUnique
      → db read   prisma.user.findFirst
      → db write  prisma.tenant.update
```

### `GET /api/system/tenants/:id/billing-provisioning`

- **Purpose:** Safe provisioning state for the Sysadmin tenant UI.
- **Handler:** `services/auth/src/routes/system.ts` line 879
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:879` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system/tenants/:id/billing-provisioning
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:879
      → (responds directly; no downstream calls detected)
```

### `PATCH /api/system/tenants/:id/bot-config`

- **Purpose:** Updates the referenced bot config for the referenced tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system.ts` line 1557
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** `validate(botConfigSchema)` (Zod). Accepts: `botEnabled`, `botType`. Required: `botEnabled`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1557` - inline `async (req, res)` handler.
3. **Validation.** `validate(botConfigSchema)` (Zod). Accepts: `botEnabled`, `botType`. Required: `botEnabled`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   _Writes:_
   - `tenant.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/system/tenants/:id/bot-config
  → [authenticate → requireSystemAdmin → validate(botConfigSchema)]
  → handler  services/auth/src/routes/system.ts:1557
      → db read   prisma.tenant.findUnique
      → db write  prisma.tenant.update
```

### `PATCH /api/system/tenants/:id/first-take-care`

- **Purpose:** Updates the referenced first take care for the referenced tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system.ts` line 1594
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1594` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   _Writes:_
   - `tenant.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/system/tenants/:id/first-take-care
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:1594
      → db read   prisma.tenant.findUnique
      → db write  prisma.tenant.update
```

### `GET /api/system/tenants/:id/manual-contracts`

- **Purpose:** Manual contracts on record, for the Sysadmin billing history view.
- **Handler:** `services/auth/src/routes/system.ts` line 733
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Gate: `requirePlatformPermission`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:733` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Gate: `requirePlatformPermission`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system/tenants/:id/manual-contracts
  → [authenticate → requirePlatformPermission]
  → handler  services/auth/src/routes/system.ts:733
      → (responds directly; no downstream calls detected)
```

### `POST /api/system/tenants/:id/nudge`

- **Purpose:** Admin-triggered manual nudge - arms + delivers immediately, returns outcome.
- **Handler:** `services/auth/src/routes/system.ts` line 1345
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1345` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/system/tenants/:id/nudge
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:1345
      → db read   prisma.tenant.findUnique
```

### `GET /api/system/tenants/:id/onboarding-snapshot`

- **Purpose:** Single-tenant snapshot (used by the tenant detail drawer).
- **Handler:** `services/auth/src/routes/system.ts` line 1333
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1333` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system/tenants/:id/onboarding-snapshot
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:1333
      → (responds directly; no downstream calls detected)
```

### `POST /api/system/tenants/:id/repair-billing-provisioning`

- **Purpose:** Finish billing setup for a tenant whose provisioning did not complete.
- **Handler:** `services/auth/src/routes/system.ts` line 756
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:756` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `user.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/system/tenants/:id/repair-billing-provisioning
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:756
      → db read   prisma.tenant.findUnique
      → db read   prisma.user.findFirst
```

### `POST /api/system/tenants/:id/resend-onboarding`

- **Purpose:** Performs the `resend onboarding` action on the referenced tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system.ts` line 1160
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1160` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `user.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/system/tenants/:id/resend-onboarding
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:1160
      → db read   prisma.tenant.findUnique
      → db read   prisma.user.findFirst
```

### `POST /api/system/tenants/:id/resend-payment-link`

- **Purpose:** Issue a replacement continuation link for a tenant awaiting payment.
- **Handler:** `services/auth/src/routes/system.ts` line 1059
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1059` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `user.findFirst`
   - `plan.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/system/tenants/:id/resend-payment-link
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:1059
      → db read   prisma.tenant.findUnique
      → db read   prisma.user.findFirst
      → db read   prisma.plan.findFirst
```

### `POST /api/system/tenants/:id/reset-onboarding`

- **Purpose:** Returns a tenant to a brand-new onboarding state WITHOUT deleting the tenant or its people. Deterministic + idempotent.
- **Handler:** `services/auth/src/routes/system.ts` line 1278
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1278` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `aIAgent.findMany`
   _Writes:_
   - `aIAgentKnowledge.deleteMany`
   - `routerRule.deleteMany`
   - `aIAgent.deleteMany`
   - `departmentMember.deleteMany`
   - `department.deleteMany`
   - `businessProfile.deleteMany`
   - `tenantOnboarding.deleteMany`
   - `tenant.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/system/tenants/:id/reset-onboarding
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:1278
      → db read   prisma.tenant.findUnique
      → db read   prisma.aIAgent.findMany
      → db write  prisma.aIAgentKnowledge.deleteMany
      → db write  prisma.routerRule.deleteMany
      → db write  prisma.aIAgent.deleteMany
      → db write  prisma.departmentMember.deleteMany
      → db write  prisma.department.deleteMany
      → db write  prisma.businessProfile.deleteMany
      → db write  prisma.tenantOnboarding.deleteMany
      → db write  prisma.tenant.update
```

### `POST /api/system/tenants/:id/users`

- **Purpose:** Performs the `users` action on the referenced tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system.ts` line 1417
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** `validate(createUserSchema)` (Zod). Accepts: `email`, `name`, `role`. Required: `email`, `name`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1417` - inline `async (req, res)` handler.
3. **Validation.** `validate(createUserSchema)` (Zod). Accepts: `email`, `name`, `role`. Required: `email`, `name`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/system/tenants/:id/users
  → [authenticate → requireSystemAdmin → validate(createUserSchema)]
  → handler  services/auth/src/routes/system.ts:1417
      → db read   prisma.tenant.findUnique
```

### `DELETE /api/system/tenants/:id/users/:userId`

- **Purpose:** Deletes the referenced user. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system.ts` line 1514
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1514` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findFirst`
   _Writes:_
   - `user.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/system/tenants/:id/users/:userId
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:1514
      → db read   prisma.user.findFirst
      → db write  prisma.user.delete
```

### `PATCH /api/system/tenants/:id/users/:userId`

- **Purpose:** Updates the referenced user. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system.ts` line 1449
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** `validate(updateUserSchema)` (Zod). Accepts: `name`, `email`, `role`, `isActive`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1449` - inline `async (req, res)` handler.
3. **Validation.** `validate(updateUserSchema)` (Zod). Accepts: `name`, `email`, `role`, `isActive`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `user.findFirst`
   _Writes:_
   - `user.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/system/tenants/:id/users/:userId
  → [authenticate → requireSystemAdmin → validate(updateUserSchema)]
  → handler  services/auth/src/routes/system.ts:1449
      → db read   prisma.user.findFirst
      → db write  prisma.user.update
```

### `POST /api/system/tenants/:tenantId/credits`

- **Purpose:** Performs the `credits` action on the referenced tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system-features.ts` line 312
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** `validate(creditsSchema)` (Zod). Accepts: `units`. Required: `units`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system-features.ts`.
2. **Handler.** `services/auth/src/routes/system-features.ts:312` - inline `async (req, res)` handler.
3. **Validation.** `validate(creditsSchema)` (Zod). Accepts: `units`. Required: `units`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/system/tenants/:tenantId/credits
  → [authenticate → requireSystemAdmin → validate(creditsSchema)]
  → handler  services/auth/src/routes/system-features.ts:312
      → db read   prisma.tenant.findUnique
```

### `GET /api/system/tenants/:tenantId/entitlements`

- **Purpose:** Reads a single entitlement for the referenced tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system-features.ts` line 183
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system-features.ts`.
2. **Handler.** `services/auth/src/routes/system-features.ts:183` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `tenantFeature.findMany`
   - `tenantEntitlement.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system/tenants/:tenantId/entitlements
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system-features.ts:183
      → db read   prisma.tenant.findUnique
      → db read   prisma.tenantFeature.findMany
      → db read   prisma.tenantEntitlement.findMany
```

### `PUT /api/system/tenants/:tenantId/entitlements/:key`

- **Purpose:** Updates the referenced entitlement. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system-features.ts` line 218
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** `validate(entitlementSchema)` (Zod). Accepts: `enabled`. Required: `enabled`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system-features.ts`.
2. **Handler.** `services/auth/src/routes/system-features.ts:218` - inline `async (req, res)` handler.
3. **Validation.** `validate(entitlementSchema)` (Zod). Accepts: `enabled`. Required: `enabled`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/system/tenants/:tenantId/entitlements/:key
  → [authenticate → requireSystemAdmin → validate(entitlementSchema)]
  → handler  services/auth/src/routes/system-features.ts:218
      → db read   prisma.tenant.findUnique
```

### `GET /api/system/tenants/:tenantId/features`

- **Purpose:** Reads a single feature for the referenced tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system-features.ts` line 51
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system-features.ts`.
2. **Handler.** `services/auth/src/routes/system-features.ts:51` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `tenantFeature.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system/tenants/:tenantId/features
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system-features.ts:51
      → db read   prisma.tenant.findUnique
      → db read   prisma.tenantFeature.findMany
```

### `PUT /api/system/tenants/:tenantId/features/:feature`

- **Purpose:** Updates the referenced feature. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system-features.ts` line 97
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** `validate(updateFeatureSchema)` (Zod). Accepts: `enabled`, `config`. Required: `enabled`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system-features.ts`.
2. **Handler.** `services/auth/src/routes/system-features.ts:97` - inline `async (req, res)` handler.
3. **Validation.** `validate(updateFeatureSchema)` (Zod). Accepts: `enabled`, `config`. Required: `enabled`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   _Writes:_
   - `tenantFeature.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/system/tenants/:tenantId/features/:feature
  → [authenticate → requireSystemAdmin → validate(updateFeatureSchema)]
  → handler  services/auth/src/routes/system-features.ts:97
      → db read   prisma.tenant.findUnique
      → db write  prisma.tenantFeature.upsert
```

### `POST /api/system/tenants/:tenantId/poc`

- **Purpose:** Performs the `poc` action on the referenced tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system-features.ts` line 251
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** `validate(pocSchema)` (Zod). Accepts: `credits`, `expiresAt`, `features`, `note`. Required: `credits`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system-features.ts`.
2. **Handler.** `services/auth/src/routes/system-features.ts:251` - inline `async (req, res)` handler.
3. **Validation.** `validate(pocSchema)` (Zod). Accepts: `credits`, `expiresAt`, `features`, `note`. Required: `credits`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/system/tenants/:tenantId/poc
  → [authenticate → requireSystemAdmin → validate(pocSchema)]
  → handler  services/auth/src/routes/system-features.ts:251
      → db read   prisma.tenant.findUnique
```

### `GET /api/system/usage/by-tenant`

- **Purpose:** Lists by tenant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system.ts` line 1810
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1810` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `usageLog.groupBy`
   - `tenant.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system/usage/by-tenant
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:1810
      → db read   prisma.usageLog.groupBy
      → db read   prisma.tenant.findMany
```

### `GET /api/system/usage/stats`

- **Purpose:** Lists stats. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/system.ts` line 1692
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** Platform staff gate: `requireSystemAdmin`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/system` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/system.ts`.
2. **Handler.** `services/auth/src/routes/system.ts:1692` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. No `resolveTenant`: this route is not tenant-scoped by middleware. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** Platform staff gate: `requireSystemAdmin`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `usageLog.groupBy`
   - `usageLog.aggregate`
   - `usageLog.count`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/system/usage/stats
  → [authenticate → requireSystemAdmin]
  → handler  services/auth/src/routes/system.ts:1692
      → db read   prisma.usageLog.groupBy
      → db read   prisma.usageLog.aggregate
      → db read   prisma.usageLog.count
```

## Shopify

20 endpoints.

### `POST /api/shopify-chat/bootstrap`

- **Purpose:** Creates a bootstrap. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/shopify-chat-public.ts` line 244
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-chat-public.ts`.
2. **Handler.** `services/ai/src/routes/shopify-chat-public.ts:244` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `429` from `bootstrapLimiter`.

#### Call graph

```text
POST /api/shopify-chat/bootstrap
  → handler  services/ai/src/routes/shopify-chat-public.ts:244
      → (responds directly; no downstream calls detected)
```

### `POST /api/shopify-chat/cart/result`

- **Purpose:** Creates a result. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/shopify-chat-public.ts` line 723
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-chat-public.ts`.
2. **Handler.** `services/ai/src/routes/shopify-chat-public.ts:723` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `429` from `cartLimiter`.

#### Call graph

```text
POST /api/shopify-chat/cart/result
  → handler  services/ai/src/routes/shopify-chat-public.ts:723
      → (responds directly; no downstream calls detected)
```

### `POST /api/shopify-chat/cart/validate`

- **Purpose:** Creates a validate. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/shopify-chat-public.ts` line 669
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-chat-public.ts`.
2. **Handler.** `services/ai/src/routes/shopify-chat-public.ts:669` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `403`, `409`, `500`, answering `{ error: ... }`. `429` from `cartLimiter`.

#### Call graph

```text
POST /api/shopify-chat/cart/validate
  → handler  services/ai/src/routes/shopify-chat-public.ts:669
      → (responds directly; no downstream calls detected)
```

### `POST /api/shopify-chat/conversation`

- **Purpose:** Creates a conversation. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/shopify-chat-public.ts` line 426
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-chat-public.ts`.
2. **Handler.** `services/ai/src/routes/shopify-chat-public.ts:426` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `429` from `conversationLimiter`.

#### Call graph

```text
POST /api/shopify-chat/conversation
  → handler  services/ai/src/routes/shopify-chat-public.ts:426
      → (responds directly; no downstream calls detected)
```

### `POST /api/shopify-chat/events`

- **Purpose:** Creates an event. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/shopify-chat-public.ts` line 860
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-chat-public.ts`.
2. **Handler.** `services/ai/src/routes/shopify-chat-public.ts:860` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `429` from `eventLimiter`.

#### Call graph

```text
POST /api/shopify-chat/events
  → handler  services/ai/src/routes/shopify-chat-public.ts:860
      → (responds directly; no downstream calls detected)
```

### `POST /api/shopify-chat/handoff`

- **Purpose:** Creates a handoff. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/shopify-chat-public.ts` line 735
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-chat-public.ts`.
2. **Handler.** `services/ai/src/routes/shopify-chat-public.ts:735` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `conversation.update`
   - `message.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `403`, `500`, answering `{ error: ... }`. `429` from `conversationLimiter`.

#### Call graph

```text
POST /api/shopify-chat/handoff
  → handler  services/ai/src/routes/shopify-chat-public.ts:735
      → db write  prisma.conversation.update
      → db write  prisma.message.create
```

### `POST /api/shopify-chat/lead`

- **Purpose:** Creates a lead. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/shopify-chat-public.ts` line 779
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-chat-public.ts`.
2. **Handler.** `services/ai/src/routes/shopify-chat-public.ts:779` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `conversation.update`
   - `message.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, `500`, answering `{ error: ... }`. `429` from `conversationLimiter`.

#### Call graph

```text
POST /api/shopify-chat/lead
  → handler  services/ai/src/routes/shopify-chat-public.ts:779
      → db write  prisma.conversation.update
      → db write  prisma.message.create
```

### `POST /api/shopify-chat/message`

- **Purpose:** Creates a message. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/shopify-chat-public.ts` line 515
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-chat-public.ts`.
2. **Handler.** `services/ai/src/routes/shopify-chat-public.ts:515` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.**
   - Enqueues `incomingMessageQueue.add` (BullMQ). The response returns before this work runs; retries and backoff are the queue's.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `413`, `500`, answering `{ error: ... }`. `429` from `messageLimiter`.

#### Call graph

```text
POST /api/shopify-chat/message
  → handler  services/ai/src/routes/shopify-chat-public.ts:515
      → queue     incomingMessageQueue.add  (async, outlives the response)
```

### `GET /api/shopify-chat/messages`

- **Purpose:** Lists messages. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/shopify-chat-public.ts` line 586
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-chat-public.ts`.
2. **Handler.** `services/ai/src/routes/shopify-chat-public.ts:586` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `conversation.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `429` from `pollLimiter`.

#### Call graph

```text
GET /api/shopify-chat/messages
  → handler  services/ai/src/routes/shopify-chat-public.ts:586
      → db read   prisma.conversation.findFirst
```

### `GET /api/shopify-chat/proxy/identity`

- **Purpose:** Reached ONLY through Shopify's App Proxy: the browser calls the merchant's own origin (`https://shop.myshopify.com/apps/gotcha-chat/ identity`) and Shopify forwards it here, appending `logged_in_customer_id` and a `signature` made with our app secret.
- **Handler:** `services/ai/src/routes/shopify-chat-public.ts` line 384
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-chat-public.ts`.
2. **Handler.** `services/ai/src/routes/shopify-chat-public.ts:384` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `401`, `500`, answering `{ error: ... }`. `429` from `bootstrapLimiter`.

#### Call graph

```text
GET /api/shopify-chat/proxy/identity
  → handler  services/ai/src/routes/shopify-chat-public.ts:384
      → (responds directly; no downstream calls detected)
```

### `GET /api/shopify-live-chat/channels`

- **Purpose:** Lists channels. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/shopify-live-chat.ts` line 102
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Feature flag: `SHOPIFY_LIVE_CHAT`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-live-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-live-chat.ts`.
2. **Handler.** `services/ai/src/routes/shopify-live-chat.ts:102` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Feature flag: `SHOPIFY_LIVE_CHAT`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/shopify-live-chat/channels
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/shopify-live-chat.ts:102
      → (responds directly; no downstream calls detected)
```

### `POST /api/shopify-live-chat/channels`

- **Purpose:** Create the channel.
- **Handler:** `services/ai/src/routes/shopify-live-chat.ts` line 124
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Feature flag: `SHOPIFY_LIVE_CHAT`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-live-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-live-chat.ts`.
2. **Handler.** `services/ai/src/routes/shopify-live-chat.ts:124` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Feature flag: `SHOPIFY_LIVE_CHAT`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `channelAccount.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/shopify-live-chat/channels
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/shopify-live-chat.ts:124
      → db write  prisma.channelAccount.create
```

### `DELETE /api/shopify-live-chat/channels/:id`

- **Purpose:** Deletes the referenced channel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/shopify-live-chat.ts` line 231
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Feature flag: `SHOPIFY_LIVE_CHAT`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-live-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-live-chat.ts`.
2. **Handler.** `services/ai/src/routes/shopify-live-chat.ts:231` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Feature flag: `SHOPIFY_LIVE_CHAT`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `channelAccount.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/shopify-live-chat/channels/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/shopify-live-chat.ts:231
      → db write  prisma.channelAccount.delete
```

### `GET /api/shopify-live-chat/channels/:id`

- **Purpose:** Reads a single channel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/shopify-live-chat.ts` line 178
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Feature flag: `SHOPIFY_LIVE_CHAT`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-live-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-live-chat.ts`.
2. **Handler.** `services/ai/src/routes/shopify-live-chat.ts:178` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Feature flag: `SHOPIFY_LIVE_CHAT`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/shopify-live-chat/channels/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/shopify-live-chat.ts:178
      → (responds directly; no downstream calls detected)
```

### `PUT /api/shopify-live-chat/channels/:id`

- **Purpose:** Updates the referenced channel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/shopify-live-chat.ts` line 191
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Feature flag: `SHOPIFY_LIVE_CHAT`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-live-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-live-chat.ts`.
2. **Handler.** `services/ai/src/routes/shopify-live-chat.ts:191` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Feature flag: `SHOPIFY_LIVE_CHAT`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `channelAccount.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/shopify-live-chat/channels/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/shopify-live-chat.ts:191
      → db write  prisma.channelAccount.update
```

### `GET /api/shopify-live-chat/channels/:id/diagnostics`

- **Purpose:** Reads a single diagnostic for the referenced channel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/shopify-live-chat.ts` line 257
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Feature flag: `SHOPIFY_LIVE_CHAT`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-live-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-live-chat.ts`.
2. **Handler.** `services/ai/src/routes/shopify-live-chat.ts:257` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Feature flag: `SHOPIFY_LIVE_CHAT`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/shopify-live-chat/channels/:id/diagnostics
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/shopify-live-chat.ts:257
      → (responds directly; no downstream calls detected)
```

### `GET /api/shopify-live-chat/channels/:id/install`

- **Purpose:** Reads a single install for the referenced channel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/shopify-live-chat.ts` line 396
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Feature flag: `SHOPIFY_LIVE_CHAT`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-live-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-live-chat.ts`.
2. **Handler.** `services/ai/src/routes/shopify-live-chat.ts:396` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Feature flag: `SHOPIFY_LIVE_CHAT`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/shopify-live-chat/channels/:id/install
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/shopify-live-chat.ts:396
      → (responds directly; no downstream calls detected)
```

### `POST /api/shopify-live-chat/conversations/:conversationId/products`

- **Purpose:** Send one product or a carousel into a conversation as the human agent.
- **Handler:** `services/ai/src/routes/shopify-live-chat.ts` line 474
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Feature flag: `SHOPIFY_PRODUCT_MESSAGING`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-live-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-live-chat.ts`.
2. **Handler.** `services/ai/src/routes/shopify-live-chat.ts:474` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Feature flag: `SHOPIFY_PRODUCT_MESSAGING`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `auditLog.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `403`, `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/shopify-live-chat/conversations/:conversationId/products
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/shopify-live-chat.ts:474
      → db write  prisma.auditLog.create
```

### `GET /api/shopify-live-chat/products`

- **Purpose:** Lists products. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/shopify-live-chat.ts` line 437
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Feature flag: `SHOPIFY_PRODUCT_MESSAGING`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-live-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-live-chat.ts`.
2. **Handler.** `services/ai/src/routes/shopify-live-chat.ts:437` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Feature flag: `SHOPIFY_PRODUCT_MESSAGING`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/shopify-live-chat/products
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/shopify-live-chat.ts:437
      → (responds directly; no downstream calls detected)
```

### `GET /api/shopify-live-chat/store`

- **Purpose:** Theme Editor deep links are built from the CHAT app's identity, never the Core integration's: the App Embed belongs to the Chat app, and pointing `activateAppId` at the Core client id opens the editor with nothing selected.
- **Handler:** `services/ai/src/routes/shopify-live-chat.ts` line 74
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Feature flag: `SHOPIFY_LIVE_CHAT`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/shopify-live-chat` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/shopify-live-chat.ts`.
2. **Handler.** `services/ai/src/routes/shopify-live-chat.ts:74` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Feature flag: `SHOPIFY_LIVE_CHAT`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/shopify-live-chat/store
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/shopify-live-chat.ts:74
      → (responds directly; no downstream calls detected)
```

## Tenant Settings

28 endpoints.

### `GET /api/business-policies`

- **Purpose:** Lists business policies. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/business-policies.ts` line 70
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `canRead`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/business-policies` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/business-policies.ts`.
2. **Handler.** `services/conversation/src/routes/business-policies.ts:70` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `canRead`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/business-policies
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → canRead]
  → handler  services/conversation/src/routes/business-policies.ts:70
      → (responds directly; no downstream calls detected)
```

### `PUT /api/business-policies/:actionKind`

- **Purpose:** Updates the referenced business policy. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/business-policies.ts` line 86
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `canManage`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/business-policies` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/business-policies.ts`.
2. **Handler.** `services/conversation/src/routes/business-policies.ts:86` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `canManage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/business-policies/:actionKind
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → canManage]
  → handler  services/conversation/src/routes/business-policies.ts:86
      → (responds directly; no downstream calls detected)
```

### `POST /api/business-policies/:actionKind/preview`

- **Purpose:** Dry-run a sample scenario against the ACTIVE policy - powers the settings UI "test this rule" box. Records a PolicyDecision like any evaluation (the evaluationPoint makes clear it was a preview via correlationId prefix).
- **Handler:** `services/conversation/src/routes/business-policies.ts` line 119
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `canPreview`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/business-policies` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/business-policies.ts`.
2. **Handler.** `services/conversation/src/routes/business-policies.ts:119` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `canPreview`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/business-policies/:actionKind/preview
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → canPreview]
  → handler  services/conversation/src/routes/business-policies.ts:119
      → (responds directly; no downstream calls detected)
```

### `GET /api/departments`

- **Purpose:** GET / - List departments (any authenticated user)
- **Handler:** `services/auth/src/routes/departments.ts` line 9
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/departments` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/departments.ts`.
2. **Handler.** `services/auth/src/routes/departments.ts:9` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/departments
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/departments.ts:9
      → db read   prisma.department.findMany
```

### `POST /api/departments`

- **Purpose:** Plan limit enforced before the create runs, not after.
- **Handler:** `services/auth/src/routes/departments.ts` line 100
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`; Plan limit: `limit:departments`
- **Input validation:** `validate(createDepartmentSchema)` (Zod). Accepts: `name`, `description`, `queueMode`, `parentId`. Required: `name`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/departments` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/departments.ts`.
2. **Handler.** `services/auth/src/routes/departments.ts:100` - inline `async (req, res)` handler.
3. **Validation.** `validate(createDepartmentSchema)` (Zod). Accepts: `name`, `description`, `queueMode`, `parentId`. Required: `name`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`; Plan limit: `limit:departments`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findUnique`
   - `department.findFirst`
   _Writes:_
   - `department.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/departments
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(createDepartmentSchema)]
  → handler  services/auth/src/routes/departments.ts:100
      → db read   prisma.department.findUnique
      → db read   prisma.department.findFirst
      → db write  prisma.department.create
```

### `DELETE /api/departments/:id`

- **Purpose:** DELETE /:id - Delete department (ADMIN only)
- **Handler:** `services/auth/src/routes/departments.ts` line 163
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/departments` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/departments.ts`.
2. **Handler.** `services/auth/src/routes/departments.ts:163` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findFirst`
   _Writes:_
   - `department.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/departments/:id
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/departments.ts:163
      → db read   prisma.department.findFirst
      → db write  prisma.department.delete
```

### `PATCH /api/departments/:id`

- **Purpose:** Updates the referenced department. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/departments.ts` line 138
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(updateDepartmentSchema)` (Zod). Accepts: `name`, `description`, `queueMode`, `isActive`, `parentId`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/departments` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/departments.ts`.
2. **Handler.** `services/auth/src/routes/departments.ts:138` - inline `async (req, res)` handler.
3. **Validation.** `validate(updateDepartmentSchema)` (Zod). Accepts: `name`, `description`, `queueMode`, `isActive`, `parentId`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findFirst`
   _Writes:_
   - `department.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/departments/:id
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(updateDepartmentSchema)]
  → handler  services/auth/src/routes/departments.ts:138
      → db read   prisma.department.findFirst
      → db write  prisma.department.update
```

### `GET /api/departments/:id/ai-employee`

- **Purpose:** GET /:id/ai-employee - Get assigned AI Employee for department
- **Handler:** `services/auth/src/routes/departments.ts` line 389
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Gate: `requireDepartmentRole(MANAGER)`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/departments` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/departments.ts`.
2. **Handler.** `services/auth/src/routes/departments.ts:389` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Gate: `requireDepartmentRole(MANAGER)`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findFirst`
   - `routerRule.findFirst`
   - `aIAgent.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/departments/:id/ai-employee
  → [authenticate → resolveTenant → requireDepartmentRole(MANAGER)]
  → handler  services/auth/src/routes/departments.ts:389
      → db read   prisma.department.findFirst
      → db read   prisma.routerRule.findFirst
      → db read   prisma.aIAgent.findUnique
```

### `PUT /api/departments/:id/ai-employee`

- **Purpose:** Updates the referenced ai employee for the referenced department. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/departments.ts` line 427
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(assignAIEmployeeSchema)` (Zod). Accepts: `aiAgentId`. Required: `aiAgentId`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/departments` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/departments.ts`.
2. **Handler.** `services/auth/src/routes/departments.ts:427` - inline `async (req, res)` handler.
3. **Validation.** `validate(assignAIEmployeeSchema)` (Zod). Accepts: `aiAgentId`. Required: `aiAgentId`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findFirst`
   - `routerRule.findFirst`
   - `aIAgent.findFirst`
   - `routerRule.aggregate`
   _Writes:_
   - `routerRule.delete`
   - `routerRule.update`
   - `routerRule.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/departments/:id/ai-employee
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(assignAIEmployeeSchema)]
  → handler  services/auth/src/routes/departments.ts:427
      → db read   prisma.department.findFirst
      → db read   prisma.routerRule.findFirst
      → db read   prisma.aIAgent.findFirst
      → db read   prisma.routerRule.aggregate
      → db write  prisma.routerRule.delete
      → db write  prisma.routerRule.update
      → db write  prisma.routerRule.create
```

### `GET /api/departments/:id/ai-employees`

- **Purpose:** GET /:id/ai-employees - list ALL AI employees attached to the department
- **Handler:** `services/auth/src/routes/departments.ts` line 517
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Gate: `requireDepartmentRole(MANAGER)`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/departments` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/departments.ts`.
2. **Handler.** `services/auth/src/routes/departments.ts:517` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Gate: `requireDepartmentRole(MANAGER)`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/departments/:id/ai-employees
  → [authenticate → resolveTenant → requireDepartmentRole(MANAGER)]
  → handler  services/auth/src/routes/departments.ts:517
      → db read   prisma.department.findFirst
```

### `POST /api/departments/:id/ai-employees`

- **Purpose:** Performs the `ai employees` action on the referenced department. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/departments.ts` line 530
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Permission: `settings:members:manage`
- **Input validation:** `validate(attachAIEmployeeSchema)` (Zod). Accepts: `aiAgentId`. Required: `aiAgentId`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/departments` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/departments.ts`.
2. **Handler.** `services/auth/src/routes/departments.ts:530` - inline `async (req, res)` handler.
3. **Validation.** `validate(attachAIEmployeeSchema)` (Zod). Accepts: `aiAgentId`. Required: `aiAgentId`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Permission: `settings:members:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findFirst`
   - `aIAgent.findFirst`
   - `routerRule.findFirst`
   - `routerRule.aggregate`
   _Writes:_
   - `routerRule.update`
   - `routerRule.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/departments/:id/ai-employees
  → [authenticate → resolveTenant → requirePermission(settings:members:manage) → validate(attachAIEmployeeSchema)]
  → handler  services/auth/src/routes/departments.ts:530
      → db read   prisma.department.findFirst
      → db read   prisma.aIAgent.findFirst
      → db read   prisma.routerRule.findFirst
      → db read   prisma.routerRule.aggregate
      → db write  prisma.routerRule.update
      → db write  prisma.routerRule.create
```

### `DELETE /api/departments/:id/ai-employees/:aiAgentId`

- **Purpose:** DELETE /:id/ai-employees/:aiAgentId - detach ONE AI employee, leaving the rest
- **Handler:** `services/auth/src/routes/departments.ts` line 575
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Permission: `settings:members:manage`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/departments` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/departments.ts`.
2. **Handler.** `services/auth/src/routes/departments.ts:575` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Permission: `settings:members:manage`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findFirst`
   _Writes:_
   - `routerRule.deleteMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/departments/:id/ai-employees/:aiAgentId
  → [authenticate → resolveTenant → requirePermission(settings:members:manage)]
  → handler  services/auth/src/routes/departments.ts:575
      → db read   prisma.department.findFirst
      → db write  prisma.routerRule.deleteMany
```

### `GET /api/departments/:id/copilot`

- **Purpose:** Reads a single copilot for the referenced department. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/departments.ts` line 319
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/departments` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/departments.ts`.
2. **Handler.** `services/auth/src/routes/departments.ts:319` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/departments/:id/copilot
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/departments.ts:319
      → db read   prisma.department.findFirst
```

### `PUT /api/departments/:id/copilot`

- **Purpose:** Updates the referenced copilot for the referenced department. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/departments.ts` line 339
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(updateCopilotSchema)` (Zod). `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/departments` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/departments.ts`.
2. **Handler.** `services/auth/src/routes/departments.ts:339` - inline `async (req, res)` handler.
3. **Validation.** `validate(updateCopilotSchema)` (Zod). `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findFirst`
   _Writes:_
   - `aIAgent.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/departments/:id/copilot
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(updateCopilotSchema)]
  → handler  services/auth/src/routes/departments.ts:339
      → db read   prisma.department.findFirst
      → db write  prisma.aIAgent.update
```

### `GET /api/departments/:id/members`

- **Purpose:** GET /:id/members - List department members (ADMIN or MANAGER)
- **Handler:** `services/auth/src/routes/departments.ts` line 177
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Gate: `requireDepartmentRole(MANAGER)`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/departments` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/departments.ts`.
2. **Handler.** `services/auth/src/routes/departments.ts:177` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Gate: `requireDepartmentRole(MANAGER)`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findFirst`
   - `departmentMember.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/departments/:id/members
  → [authenticate → resolveTenant → requireDepartmentRole(MANAGER)]
  → handler  services/auth/src/routes/departments.ts:177
      → db read   prisma.department.findFirst
      → db read   prisma.departmentMember.findMany
```

### `POST /api/departments/:id/members`

- **Purpose:** Performs the `members` action on the referenced department. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/departments.ts` line 205
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(addMemberSchema)` (Zod). Accepts: `userId`, `departmentRole`. Required: `userId`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/departments` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/departments.ts`.
2. **Handler.** `services/auth/src/routes/departments.ts:205` - inline `async (req, res)` handler.
3. **Validation.** `validate(addMemberSchema)` (Zod). Accepts: `userId`, `departmentRole`. Required: `userId`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findFirst`
   - `user.findFirst`
   - `departmentMember.findFirst`
   _Writes:_
   - `departmentMember.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/departments/:id/members
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(addMemberSchema)]
  → handler  services/auth/src/routes/departments.ts:205
      → db read   prisma.department.findFirst
      → db read   prisma.user.findFirst
      → db read   prisma.departmentMember.findFirst
      → db write  prisma.departmentMember.create
```

### `DELETE /api/departments/:id/members/:userId`

- **Purpose:** DELETE /:id/members/:userId - Remove member (ADMIN only)
- **Handler:** `services/auth/src/routes/departments.ts` line 261
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/departments` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/departments.ts`.
2. **Handler.** `services/auth/src/routes/departments.ts:261` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `departmentMember.findFirst`
   _Writes:_
   - `departmentMember.delete`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/departments/:id/members/:userId
  → [authenticate → resolveTenant → requireRole(ADMIN)]
  → handler  services/auth/src/routes/departments.ts:261
      → db read   prisma.departmentMember.findFirst
      → db write  prisma.departmentMember.delete
```

### `PATCH /api/departments/:id/members/:userId`

- **Purpose:** Updates the referenced member. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/departments.ts` line 241
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** Role: `ADMIN`
- **Input validation:** `validate(updateMemberSchema)` (Zod). Accepts: `departmentRole`. Required: `departmentRole`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/departments` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/departments.ts`.
2. **Handler.** `services/auth/src/routes/departments.ts:241` - inline `async (req, res)` handler.
3. **Validation.** `validate(updateMemberSchema)` (Zod). Accepts: `departmentRole`. Required: `departmentRole`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `departmentMember.findFirst`
   _Writes:_
   - `departmentMember.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/departments/:id/members/:userId
  → [authenticate → resolveTenant → requireRole(ADMIN) → validate(updateMemberSchema)]
  → handler  services/auth/src/routes/departments.ts:241
      → db read   prisma.departmentMember.findFirst
      → db write  prisma.departmentMember.update
```

### `GET /api/departments/tree`

- **Purpose:** GET /tree - Get departments as hierarchical tree
- **Handler:** `services/auth/src/routes/departments.ts` line 34
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/departments` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/departments.ts`.
2. **Handler.** `services/auth/src/routes/departments.ts:34` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. `enforceMfaEnrollment` - the caller must have MFA set up.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `department.findMany`
   - `aIAgent.findMany`
   - `routerRule.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/departments/tree
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/departments.ts:34
      → db read   prisma.department.findMany
      → db read   prisma.aIAgent.findMany
      → db read   prisma.routerRule.findMany
```

### `GET /api/field-definitions`

- **Purpose:** Lists field definitions. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/field-definitions.ts` line 36
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/field-definitions` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/field-definitions.ts`.
2. **Handler.** `services/ai/src/routes/field-definitions.ts:36` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/field-definitions
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/field-definitions.ts:36
      → (responds directly; no downstream calls detected)
```

### `POST /api/field-definitions`

- **Purpose:** Creates a field definition. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/field-definitions.ts` line 48
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/field-definitions` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/field-definitions.ts`.
2. **Handler.** `services/ai/src/routes/field-definitions.ts:48` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `409`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/field-definitions
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/field-definitions.ts:48
      → (responds directly; no downstream calls detected)
```

### `DELETE /api/field-definitions/:id`

- **Purpose:** Deletes the referenced field definition. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/field-definitions.ts` line 92
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/field-definitions` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/field-definitions.ts`.
2. **Handler.** `services/ai/src/routes/field-definitions.ts:92` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/field-definitions/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/field-definitions.ts:92
      → (responds directly; no downstream calls detected)
```

### `PUT /api/field-definitions/:id`

- **Purpose:** Updates the referenced field definition. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/field-definitions.ts` line 68
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/field-definitions` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/field-definitions.ts`.
2. **Handler.** `services/ai/src/routes/field-definitions.ts:68` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/field-definitions/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/ai/src/routes/field-definitions.ts:68
      → (responds directly; no downstream calls detected)
```

### `GET /api/tenant-settings`

- **Purpose:** Lists tenant settings. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/tenant-settings.ts` line 23
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/tenant-settings` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/tenant-settings.ts`.
2. **Handler.** `services/conversation/src/routes/tenant-settings.ts:23` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   - `communicationChannel.count`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/tenant-settings
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/conversation/src/routes/tenant-settings.ts:23
      → db read   prisma.tenant.findUnique
      → db read   prisma.communicationChannel.count
```

### `PATCH /api/tenant-settings`

- **Purpose:** Updates the referenced tenant setting. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/tenant-settings.ts` line 59
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/tenant-settings` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/tenant-settings.ts`.
2. **Handler.** `services/conversation/src/routes/tenant-settings.ts:59` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `tenant.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/tenant-settings
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/conversation/src/routes/tenant-settings.ts:59
      → db write  prisma.tenant.update
```

### `GET /api/tenant/security`

- **Purpose:** GET current policy + compliance. Any workspace admin may view.
- **Handler:** `services/auth/src/routes/tenant-security.ts` line 114
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** `settings:workspace:update` **or** role `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/tenant` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/tenant-security.ts`.
2. **Handler.** `services/auth/src/routes/tenant-security.ts:114` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** `settings:workspace:update` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/tenant/security
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/tenant-security.ts:114
      → db read   prisma.tenant.findUnique
```

### `PATCH /api/tenant/security`

- **Purpose:** PATCH policy flags. Enabling never locks out already-enrolled members; the backfill stamps them compliant, and only the un-enrolled get gated.
- **Handler:** `services/auth/src/routes/tenant-security.ts` line 135
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** `settings:workspace:update` **or** role `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/tenant` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/tenant-security.ts`.
2. **Handler.** `services/auth/src/routes/tenant-security.ts:135` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** `settings:workspace:update` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
   _Writes:_
   - `tenant.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/tenant/security
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/tenant-security.ts:135
      → db read   prisma.tenant.findUnique
      → db write  prisma.tenant.update
```

### `GET /api/tenant/security/review`

- **Purpose:** GET the per-member roster ("Review users" - who still needs MFA).
- **Handler:** `services/auth/src/routes/tenant-security.ts` line 169
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** `settings:workspace:update` **or** role `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/tenant` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/tenant-security.ts`.
2. **Handler.** `services/auth/src/routes/tenant-security.ts:169` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** `settings:workspace:update` **or** role `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `tenant.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/tenant/security/review
  → [authenticate → resolveTenant]
  → handler  services/auth/src/routes/tenant-security.ts:169
      → db read   prisma.tenant.findUnique
```

## Voice

63 endpoints.

### `POST /api/post-call/analyze`

- **Purpose:** Creates an analyze. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/ai/src/routes/post-call.ts` line 46
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `ai` service mounts this router at `/api/post-call` in `services/ai/src/index.ts`; the route is declared in `services/ai/src/routes/post-call.ts`.
2. **Handler.** `services/ai/src/routes/post-call.ts:46` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `202`, answering the `{ data: ... }` envelope. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/post-call/analyze
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/ai/src/routes/post-call.ts:46
      → (responds directly; no downstream calls detected)
```

### `GET /api/voice-channels`

- **Purpose:** Lists voice channels. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 325
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:325` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/voice-channels
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:325
      → db read   prisma.communicationChannel.findMany
```

### `POST /api/voice-channels`

- **Purpose:** Create a BYO channel. The server uses the just-provided credentials to list `incomingPhoneNumbers` from Twilio and persists each as a child `VoiceChannelPhoneNumber` row (isActive=false). Admin activates a number in step 3 of the wizard via /numbers/:numberId/activate. apiKeySid / apiKeySecret / twimlAppSid are now auto-provisioned - legacy clients may still send them, but they are ignored.
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 441
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`; Entitlement: `voice.call_pilot`; Plan limit: `limit:voice_channels`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:441` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`; Entitlement: `voice.call_pilot`; Plan limit: `limit:voice_channels`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
   _Writes:_
   - `communicationChannel.create`
   - `voiceChannel.create`
   - `communicationChannel.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `500`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-channels
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:441
      → db read   prisma.communicationChannel.findUnique
      → db write  prisma.communicationChannel.create
      → db write  prisma.voiceChannel.create
      → db write  prisma.communicationChannel.update
```

### `DELETE /api/voice-channels/:id`

- **Purpose:** Also deactivates every active number for the channel and best-effort reverts `voiceUrl` on Twilio side to empty.
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 1010
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:1010` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
   _Writes:_
   - `communicationChannel.update`
   - `voiceChannelPhoneNumber.updateMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/voice-channels/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:1010
      → db read   prisma.communicationChannel.findUnique
      → db write  prisma.communicationChannel.update
      → db write  prisma.voiceChannelPhoneNumber.updateMany
```

### `GET /api/voice-channels/:id`

- **Purpose:** Fetch a single channel with its numbers - used by the detail page.
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 540
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:540` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/voice-channels/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:540
      → db read   prisma.communicationChannel.findUnique
```

### `PATCH /api/voice-channels/:id`

- **Purpose:** Updates the referenced voice channel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 558
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:558` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
   _Writes:_
   - `communicationChannel.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/voice-channels/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:558
      → db read   prisma.communicationChannel.findUnique
      → db write  prisma.communicationChannel.update
```

### `GET /api/voice-channels/:id/ai-agent`

- **Purpose:** Returns { aiAgentId } - the AI Employee bound to this voice channel. Phase 6 moved this off `copilot_config.aiAgentId` (JSONB) onto a real FK column. Empty body when nothing is configured; the live runner then falls back to the legacy per-channel copilot config (until Phase 7 retires that path entirely).
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 672
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:672` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/voice-channels/:id/ai-agent
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:672
      → db read   prisma.communicationChannel.findUnique
```

### `PUT /api/voice-channels/:id/ai-agent`

- **Purpose:** Body: { aiAgentId: string | null }. Validates the agent belongs to the same tenant before writing - never trust the id from the client. Pass null/"" to detach the channel from any AI Employee (falls back to legacy channel config for the duration of Phase 6).
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 695
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:695` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
   - `aIAgent.findUnique`
   _Writes:_
   - `voiceChannel.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/voice-channels/:id/ai-agent
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:695
      → db read   prisma.communicationChannel.findUnique
      → db read   prisma.aIAgent.findUnique
      → db write  prisma.voiceChannel.update
```

### `GET /api/voice-channels/:id/copilot-config`

- **Purpose:** Returns the per-channel Live Call Copilot config (language, persona, goals, required questions, data collection fields). Empty `{}` means "use platform defaults".
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 599
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:599` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/voice-channels/:id/copilot-config
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:599
      → db read   prisma.communicationChannel.findUnique
```

### `PUT /api/voice-channels/:id/copilot-config`

- **Purpose:** Replaces the per-channel copilot config. Validated against CopilotConfigSchema before persistence. Returns the persisted config.
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 620
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:620` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
   _Writes:_
   - `voiceChannel.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/voice-channels/:id/copilot-config
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:620
      → db read   prisma.communicationChannel.findUnique
      → db write  prisma.voiceChannel.update
```

### `GET /api/voice-channels/:id/funnel`

- **Purpose:** Returns { funnelId } - the per-channel pipeline funnel override. Stored inside the copilot_config JSONB blob today (`copilot_config.funnelId`); Phase 7 will promote this to a real FK column. Null/empty means "fall back to the department-scoped funnel resolution".
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 744
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:744` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/voice-channels/:id/funnel
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:744
      → db read   prisma.communicationChannel.findUnique
```

### `PUT /api/voice-channels/:id/funnel`

- **Purpose:** Body: { funnelId: string | null }. Validates the funnel belongs to the same tenant before writing. Pass null/"" to clear the override so the channel falls back to the department-scoped funnel.
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 770
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:770` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
   - `tenantFunnel.findUnique`
   _Writes:_
   - `voiceChannel.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/voice-channels/:id/funnel
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:770
      → db read   prisma.communicationChannel.findUnique
      → db read   prisma.tenantFunnel.findUnique
      → db write  prisma.voiceChannel.update
```

### `GET /api/voice-channels/:id/numbers`

- **Purpose:** Reads a single number for the referenced voice channel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 1069
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:1069` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/voice-channels/:id/numbers
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:1069
      → db read   prisma.communicationChannel.findUnique
```

### `POST /api/voice-channels/:id/numbers/:numberId/activate`

- **Purpose:** Performs the `activate` action on the referenced number. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 1172
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:1172` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `voiceChannelPhoneNumber.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `409`, `500`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-channels/:id/numbers/:numberId/activate
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:1172
      → db write  prisma.voiceChannelPhoneNumber.update
```

### `POST /api/voice-channels/:id/numbers/:numberId/deactivate`

- **Purpose:** Performs the `deactivate` action on the referenced number. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 1208
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:1208` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `voiceChannelPhoneNumber.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-channels/:id/numbers/:numberId/deactivate
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:1208
      → db write  prisma.voiceChannelPhoneNumber.update
```

### `POST /api/voice-channels/:id/numbers/refresh`

- **Purpose:** Creates a refresh for the referenced number. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 1114
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:1114` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
   - `voiceChannelPhoneNumber.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-channels/:id/numbers/refresh
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:1114
      → db read   prisma.communicationChannel.findUnique
      → db read   prisma.voiceChannelPhoneNumber.findMany
```

### `POST /api/voice-channels/:id/rotate-secrets`

- **Purpose:** Performs the `rotate secrets` action on the referenced voice channel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 1325
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:1325` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
   _Writes:_
   - `communicationChannel.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-channels/:id/rotate-secrets
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:1325
      → db read   prisma.communicationChannel.findUnique
      → db write  prisma.communicationChannel.update
```

### `GET /api/voice-channels/:id/routing`

- **Purpose:** Per-channel inbound routing: defaultAgentId, fallbackDepartmentId, ringTimeoutSeconds. Returns null IDs when nothing is configured.
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 827
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:827` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/voice-channels/:id/routing
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:827
      → db read   prisma.communicationChannel.findUnique
```

### `PUT /api/voice-channels/:id/routing`

- **Purpose:** Validates IDs belong to the tenant before persisting so a channel can never point at someone else's user / department.
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 860
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:860` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
   - `user.findUnique`
   - `department.findUnique`
   _Writes:_
   - `voiceChannel.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PUT /api/voice-channels/:id/routing
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:860
      → db read   prisma.communicationChannel.findUnique
      → db read   prisma.user.findUnique
      → db read   prisma.department.findUnique
      → db write  prisma.voiceChannel.update
```

### `POST /api/voice-channels/:id/validate`

- **Purpose:** Performs the `validate` action on the referenced voice channel. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/voice-channels.ts` line 1240
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-channels` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-channels.ts`.
2. **Handler.** `services/conversation/src/routes/voice-channels.ts:1240` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
   _Writes:_
   - `communicationChannel.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `429`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-channels/:id/validate
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-channels.ts:1240
      → db read   prisma.communicationChannel.findUnique
      → db write  prisma.communicationChannel.update
```

### `GET /api/voice-copilot/callbacks/click/:token`

- **Purpose:** Public endpoint reached by the customer tapping the URL button in the missed-call WhatsApp template. Verifies the signed token and initiates the AGENT_FIRST callback. Returns a tiny HTML page so the customer's browser shows a confirmation while we ring the agent server-side.
- **Handler:** `services/voice-copilot/src/routes/voice-callback.ts` line 437
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/voice-callback.ts`.
2. **Handler.** `services/voice-copilot/src/routes/voice-callback.ts:437` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200`, answering the `{ data: ... }` envelope. Error paths: `400`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/voice-copilot/callbacks/click/:token
  → handler  services/voice-copilot/src/routes/voice-callback.ts:437
      → db read   prisma.communicationChannel.findUnique
```

### `POST /api/voice-copilot/callbacks/ensure-template`

- **Purpose:** Make sure the tenant's WABA has the missed-call template approved. Idempotent: checks the existing list first via Graph API and only POSTs a create when the template name isn't already registered. Returns { exists, created } so the caller can show the admin whether anything new was submitted for review.
- **Handler:** `services/voice-copilot/src/routes/voice-callback.ts` line 675
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/voice-callback.ts`.
2. **Handler.** `services/voice-copilot/src/routes/voice-callback.ts:675` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, `409`, `500`, `502`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice-copilot/callbacks/ensure-template
  → handler  services/voice-copilot/src/routes/voice-callback.ts:675
      → db read   prisma.channelAccount.findFirst
```

### `POST /api/voice-copilot/callbacks/initiate`

- **Purpose:** Creates an initiate. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/voice-copilot/src/routes/voice-callback.ts` line 112
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/voice-callback.ts`.
2. **Handler.** `services/voice-copilot/src/routes/voice-callback.ts:112` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
   - `user.findFirst`
   - `voiceCallSession.update({
        where: { id: session`
   _Writes:_
   - `conversation.create`
   - `voiceCallSession.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, `404`, `409`, `500`, `502`, `503`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice-copilot/callbacks/initiate
  → handler  services/voice-copilot/src/routes/voice-callback.ts:112
      → db read   prisma.communicationChannel.findUnique
      → db read   prisma.user.findFirst
      → db read   prisma.voiceCallSession.update({
        where: { id: session
      → db write  prisma.conversation.create
      → db write  prisma.voiceCallSession.update
```

### `POST /api/voice-copilot/callbacks/missed-template`

- **Purpose:** Fires the WhatsApp "X tried to reach you" template to the customer of a MISSED voice session, with a URL button containing the pre-signed callback link. Idempotent per sessionId (records the outgoing message id under session.meta to prevent re-sends).
- **Handler:** `services/voice-copilot/src/routes/voice-callback.ts` line 494
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/voice-callback.ts`.
2. **Handler.** `services/voice-copilot/src/routes/voice-callback.ts:494` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `voiceCallSession.findUnique`
   - `communicationChannel.findUnique`
   - `contact.findFirst({
      where: { tenantId: session`
   - `contact.findFirst`
   - `voiceCallSession.update({
      where: { id: session`
   _Writes:_
   - `voiceCallSession.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.**
   - Enqueues `outgoingMessageQueue.add` (BullMQ). The response returns before this work runs; retries and backoff are the queue's.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, `404`, `409`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice-copilot/callbacks/missed-template
  → handler  services/voice-copilot/src/routes/voice-callback.ts:494
      → db read   prisma.voiceCallSession.findUnique
      → db read   prisma.communicationChannel.findUnique
      → db read   prisma.contact.findFirst({
      where: { tenantId: session
      → db read   prisma.contact.findFirst
      → db read   prisma.voiceCallSession.update({
      where: { id: session
      → db write  prisma.voiceCallSession.update
      → queue     outgoingMessageQueue.add  (async, outlives the response)
```

### `GET /api/voice-copilot/live`

- **Purpose:** Lists live. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/voice-copilot/src/routes/live.ts` line 19
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/live.ts`.
2. **Handler.** `services/voice-copilot/src/routes/live.ts:19` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/voice-copilot/live
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/voice-copilot/src/routes/live.ts:19
      → (responds directly; no downstream calls detected)
```

### `POST /api/voice-copilot/sessions/:sessionId/add-participant`

- **Purpose:** Performs the `add participant` action on the referenced session. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/voice-copilot/src/routes/voice-add-participant.ts` line 32
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/voice-add-participant.ts`.
2. **Handler.** `services/voice-copilot/src/routes/voice-add-participant.ts:32` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `voiceCallSession.findUnique`
   - `contact.findFirst`
   - `voiceSessionParticipant.update({
        where: { id: participantId },
        data: { status: "FAILED", leftAt: new Date(), endReason: "dial_failed" },
      })`
   _Writes:_
   - `voiceSessionParticipant.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, `404`, `409`, `500`, `502`, `503`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice-copilot/sessions/:sessionId/add-participant
  → handler  services/voice-copilot/src/routes/voice-add-participant.ts:32
      → db read   prisma.voiceCallSession.findUnique
      → db read   prisma.contact.findFirst
      → db read   prisma.voiceSessionParticipant.update({
        where: { id: participantId },
        data: { status: "FAILED", leftAt: new Date(), endReason: "dial_failed" },
      })
      → db write  prisma.voiceSessionParticipant.update
```

### `POST /api/voice-copilot/sessions/:sessionId/agent-leave`

- **Purpose:** Performs the `agent leave` action on the referenced session. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/voice-copilot/src/routes/voice-conference-control.ts` line 129
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/voice-conference-control.ts`.
2. **Handler.** `services/voice-copilot/src/routes/voice-conference-control.ts:129` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `409`, `500`, `502`, `503`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice-copilot/sessions/:sessionId/agent-leave
  → handler  services/voice-copilot/src/routes/voice-conference-control.ts:129
      → (responds directly; no downstream calls detected)
```

### `POST /api/voice-copilot/sessions/:sessionId/customer-hold`

- **Purpose:** Performs the `customer hold` action on the referenced session. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/voice-copilot/src/routes/voice-conference-control.ts` line 71
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/voice-conference-control.ts`.
2. **Handler.** `services/voice-copilot/src/routes/voice-conference-control.ts:71` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `409`, `500`, `502`, `503`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice-copilot/sessions/:sessionId/customer-hold
  → handler  services/voice-copilot/src/routes/voice-conference-control.ts:71
      → (responds directly; no downstream calls detected)
```

### `GET /api/voice-copilot/sessions/:sessionId/participants`

- **Purpose:** Reads a single participant for the referenced session. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/voice-copilot/src/routes/voice-participants.ts` line 76
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/voice-participants.ts`.
2. **Handler.** `services/voice-copilot/src/routes/voice-participants.ts:76` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `voiceSessionParticipant.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/voice-copilot/sessions/:sessionId/participants
  → handler  services/voice-copilot/src/routes/voice-participants.ts:76
      → db read   prisma.voiceSessionParticipant.findMany
```

### `POST /api/voice-copilot/sessions/:sessionId/participants/:participantId/hold`

- **Purpose:** Performs the `hold` action on the referenced participant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/voice-copilot/src/routes/voice-participants.ts` line 113
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/voice-participants.ts`.
2. **Handler.** `services/voice-copilot/src/routes/voice-participants.ts:113` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `voiceSessionParticipant.update({
        where: { id: participantId },
        data: { onHold: hold },
      })`
   _Writes:_
   - `voiceSessionParticipant.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `500`, `502`, `503`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice-copilot/sessions/:sessionId/participants/:participantId/hold
  → handler  services/voice-copilot/src/routes/voice-participants.ts:113
      → db read   prisma.voiceSessionParticipant.update({
        where: { id: participantId },
        data: { onHold: hold },
      })
      → db write  prisma.voiceSessionParticipant.update
```

### `POST /api/voice-copilot/sessions/:sessionId/participants/:participantId/kick`

- **Purpose:** Performs the `kick` action on the referenced participant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/voice-copilot/src/routes/voice-participants.ts` line 186
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/voice-participants.ts`.
2. **Handler.** `services/voice-copilot/src/routes/voice-participants.ts:186` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `voiceSessionParticipant.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `409`, `500`, `502`, `503`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice-copilot/sessions/:sessionId/participants/:participantId/kick
  → handler  services/voice-copilot/src/routes/voice-participants.ts:186
      → db read   prisma.voiceSessionParticipant.findUnique
```

### `POST /api/voice-copilot/sessions/:sessionId/terminate`

- **Purpose:** Performs the `terminate` action on the referenced session. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/voice-copilot/src/routes/voice-terminate.ts` line 28
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/voice-terminate.ts`.
2. **Handler.** `services/voice-copilot/src/routes/voice-terminate.ts:28` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, `404`, `500`, `502`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice-copilot/sessions/:sessionId/terminate
  → handler  services/voice-copilot/src/routes/voice-terminate.ts:28
      → (responds directly; no downstream calls detected)
```

### `POST /api/voice-copilot/token`

- **Purpose:** Creates a token. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/voice-copilot/src/routes/twilio-token.ts` line 23
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/twilio-token.ts`.
2. **Handler.** `services/voice-copilot/src/routes/twilio-token.ts:23` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `401`, `500`, `503`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice-copilot/token
  → handler  services/voice-copilot/src/routes/twilio-token.ts:23
      → (responds directly; no downstream calls detected)
```

### `POST /api/voice-copilot/twiml/callback-agent-status`

- **Purpose:** Status callback for the agent's mobile leg. Lets us mark the session MISSED/FAILED if the agent's phone never answered.
- **Handler:** `services/voice-copilot/src/routes/voice-callback.ts` line 387
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/voice-callback.ts`.
2. **Handler.** `services/voice-copilot/src/routes/voice-callback.ts:387` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `voiceCallSession.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200`, answering the `{ data: ... }` envelope. Error paths: `400`, `403`, `404`, `500`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice-copilot/twiml/callback-agent-status
  → handler  services/voice-copilot/src/routes/voice-callback.ts:387
      → db write  prisma.voiceCallSession.update
```

### `POST /api/voice-copilot/twiml/callback-bridge`

- **Purpose:** Creates a callback bridge. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/voice-copilot/src/routes/voice-callback.ts` line 342
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/voice-callback.ts`.
2. **Handler.** `services/voice-copilot/src/routes/voice-callback.ts:342` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, `404`, `500`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice-copilot/twiml/callback-bridge
  → handler  services/voice-copilot/src/routes/voice-callback.ts:342
      → (responds directly; no downstream calls detected)
```

### `POST /api/voice-copilot/twiml/conference-status`

- **Purpose:** Tenant for this callback lives in the redis stash keyed by FriendlyName, so we resolve meta FIRST and only then build the provider + verify signature. (The signature middleware can't run upfront because Twilio doesn't echo our tenantId on conference callbacks.)
- **Handler:** `services/voice-copilot/src/routes/twilio-twiml.ts` line 262
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/twilio-twiml.ts`.
2. **Handler.** `services/voice-copilot/src/routes/twilio-twiml.ts:262` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `voiceSessionParticipant.count`
   - `voiceCallSession.findUnique`
   _Writes:_
   - `voiceSessionParticipant.updateMany`
   - `voiceSessionParticipant.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `204`, answering the `{ data: ... }` envelope. Error paths: `403`, `500`, `503`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice-copilot/twiml/conference-status
  → handler  services/voice-copilot/src/routes/twilio-twiml.ts:262
      → db read   prisma.voiceSessionParticipant.count
      → db read   prisma.voiceCallSession.findUnique
      → db write  prisma.voiceSessionParticipant.updateMany
      → db write  prisma.voiceSessionParticipant.upsert
```

### `POST /api/voice-copilot/twiml/customer-status`

- **Purpose:** Twilio POSTs here when the customer-side dial completes. The leg may never have joined the conference (no-answer/busy/canceled/failed) - in that case we transition the VoiceCallSession to MISSED/FAILED and hang up the agent leg so the agent isn't left alone in an empty conference.
- **Handler:** `services/voice-copilot/src/routes/twilio-twiml.ts` line 597
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/twilio-twiml.ts`.
2. **Handler.** `services/voice-copilot/src/routes/twilio-twiml.ts:597` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `204`, answering the `{ data: ... }` envelope. Error paths: `403`, `500`, `503`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice-copilot/twiml/customer-status
  → handler  services/voice-copilot/src/routes/twilio-twiml.ts:597
      → (responds directly; no downstream calls detected)
```

### `POST /api/voice-copilot/twiml/outbound`

- **Purpose:** Creates an outbound. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/voice-copilot/src/routes/twilio-twiml.ts` line 110
- **Authentication / tenant resolution:** **Provider signature.** The caller is a webhook; verification is mandatory and fails closed.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/twilio-twiml.ts`.
2. **Handler.** `services/voice-copilot/src/routes/twilio-twiml.ts:110` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Provider signature.** The caller is a webhook; verification is mandatory and fails closed.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `voiceCallSession.findUnique`
   _Writes:_
   - `voiceCallSession.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
POST /api/voice-copilot/twiml/outbound
  → [verifySignature]
  → handler  services/voice-copilot/src/routes/twilio-twiml.ts:110
      → db read   prisma.voiceCallSession.findUnique
      → db write  prisma.voiceCallSession.create
```

### `POST /api/voice-copilot/twiml/outbound-status`

- **Purpose:** TwiML-App-level StatusCallback registered during channel provisioning (see services/conversation/src/routes/voice-channels.ts: Applications.json StatusCallback URL). Twilio's payload here is the standard webhook shape (CallSid, CallStatus, ApplicationSid, AccountSid, From, To...) - there is no tenantId field, so the standard signature verifier can't run. The endpoint is logging-only; Twilio doesn't accept commands from the response, so an unverified 204 is safe and stops the 404 noise in logs.
- **Handler:** `services/voice-copilot/src/routes/twilio-twiml.ts` line 737
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/twilio-twiml.ts`.
2. **Handler.** `services/voice-copilot/src/routes/twilio-twiml.ts:737` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `204`, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
POST /api/voice-copilot/twiml/outbound-status
  → handler  services/voice-copilot/src/routes/twilio-twiml.ts:737
      → (responds directly; no downstream calls detected)
```

### `POST /api/voice-copilot/twiml/status`

- **Purpose:** Creates a statu. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/voice-copilot/src/routes/twilio-twiml.ts` line 714
- **Authentication / tenant resolution:** **Provider signature.** The caller is a webhook; verification is mandatory and fails closed.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice-copilot` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/twilio-twiml.ts`.
2. **Handler.** `services/voice-copilot/src/routes/twilio-twiml.ts:714` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **Provider signature.** The caller is a webhook; verification is mandatory and fails closed.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `204`, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
POST /api/voice-copilot/twiml/status
  → [verifySignature]
  → handler  services/voice-copilot/src/routes/twilio-twiml.ts:714
      → (responds directly; no downstream calls detected)
```

### `GET /api/voice-sessions/:id`

- **Purpose:** Reads a single voice session. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 364
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:364` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/voice-sessions/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:364
      → (responds directly; no downstream calls detected)
```

### `POST /api/voice-sessions/:id/add-participant`

- **Purpose:** Brings a third-party number into the live conference (3-way). Body: { to: "+972..." }. Only the assigned agent (or an ADMIN) can add; the session must be ACTIVE/HOLD. Proxies to voice-copilot which holds the Twilio credentials.
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 610
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:610` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, `404`, `500`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-sessions/:id/add-participant
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:610
      → (responds directly; no downstream calls detected)
```

### `POST /api/voice-sessions/:id/agent-leave`

- **Purpose:** Cold transfer: drop the agent leg so the customer + the added 3rd party continue the call without the agent.
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 699
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:699` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `403`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-sessions/:id/agent-leave
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:699
      → (responds directly; no downstream calls detected)
```

### `POST /api/voice-sessions/:id/answer`

- **Purpose:** Performs the `answer` action on the referenced voice session. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 501
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:501` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-sessions/:id/answer
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:501
      → (responds directly; no downstream calls detected)
```

### `GET /api/voice-sessions/:id/context`

- **Purpose:** CRM enrichment for the right-panel cards. Returns prior conversations, any persisted summaries, and the contact record (if matched on phone). Heavy CRM lookups (lead_search/contact_search) are NOT triggered here - the AI service handles those at call-start via its existing prefetch.
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 420
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:420` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `contact.findUnique`
   - `callAnalysis.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/voice-sessions/:id/context
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:420
      → db read   prisma.contact.findUnique
      → db read   prisma.callAnalysis.findUnique
```

### `POST /api/voice-sessions/:id/customer-hold`

- **Purpose:** Whisper/consult mode: toggle hold music on the customer's leg so the agent can talk privately with an added 3rd party. Body: { hold: bool }.
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 664
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:664` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `403`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-sessions/:id/customer-hold
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:664
      → (responds directly; no downstream calls detected)
```

### `POST /api/voice-sessions/:id/decline`

- **Purpose:** Performs the `decline` action on the referenced voice session. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 526
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:526` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `410`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-sessions/:id/decline
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:526
      → (responds directly; no downstream calls detected)
```

### `POST /api/voice-sessions/:id/hangup`

- **Purpose:** Performs the `hangup` action on the referenced voice session. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 559
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:559` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `403`, `404`, `409`, `410`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-sessions/:id/hangup
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:559
      → (responds directly; no downstream calls detected)
```

### `GET /api/voice-sessions/:id/participants`

- **Purpose:** Live + already-left legs for the conference, with optional CRM enrichment (when the leg matched a Contact by phone at join time). Used by the voice workspace to render per-row controls (hold/kick).
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 867
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:867` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `voiceSessionParticipant.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/voice-sessions/:id/participants
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:867
      → db read   prisma.voiceSessionParticipant.findMany
```

### `POST /api/voice-sessions/:id/participants/:participantId/hold`

- **Purpose:** Performs the `hold` action on the referenced participant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 900
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:900` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `voiceSessionParticipant.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-sessions/:id/participants/:participantId/hold
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:900
      → db read   prisma.voiceSessionParticipant.findUnique
```

### `POST /api/voice-sessions/:id/participants/:participantId/kick`

- **Purpose:** Performs the `kick` action on the referenced participant. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 945
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:945` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `voiceSessionParticipant.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-sessions/:id/participants/:participantId/kick
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:945
      → db read   prisma.voiceSessionParticipant.findUnique
```

### `GET /api/voice-sessions/:id/transcript`

- **Purpose:** Reads a single transcript for the referenced voice session. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 379
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:379` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `message.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/voice-sessions/:id/transcript
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:379
      → db read   prisma.message.findMany
```

### `GET /api/voice-sessions/active`

- **Purpose:** Lists active. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 67
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:67` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `voiceCallSession.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/voice-sessions/active
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:67
      → db read   prisma.voiceCallSession.findMany
```

### `GET /api/voice-sessions/missed`

- **Purpose:** Recent MISSED inbound calls for the agent's tenant. Enriched with the matching Contact row (if any) so the UI can render a name + tags without an extra round trip. Limited to inbound - outbound MISSED rows are a Twilio quirk (caller-decline) and don't belong in this inbox view.
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 103
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:103` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `voiceCallSession.findMany`
   - `contact.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/voice-sessions/missed
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:103
      → db read   prisma.voiceCallSession.findMany
      → db read   prisma.contact.findMany
```

### `POST /api/voice-sessions/missed/:id/callback`

- **Purpose:** Return-call entry point for the missed-calls inbox. Looks up the missed session, then proxies to voice-copilot's /callbacks/initiate endpoint with the channel's outboundMode policy applied. IN_PLATFORM mode is unsupported here (the agent would need to dial from the browser-side dialer - return a 409 so the UI can guide them).
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 153
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:153` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-sessions/missed/:id/callback
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:153
      → db read   prisma.communicationChannel.findUnique
```

### `GET /api/voice-sessions/missed/:id/detail`

- **Purpose:** Rich enrichment for the missed-call detail drawer: contact + CRM custom fields, the latest CustomerBrief (persistent behavioral summary), and the 3 most-recent prior conversations with their aiSummary. Read-only; missed-call list is cheap, but this endpoint pulls heavier data only when the agent actually opens the drawer.
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 253
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:253` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `contact.findFirst`
   - `customerBrief.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/voice-sessions/missed/:id/detail
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:253
      → db read   prisma.contact.findFirst
      → db read   prisma.customerBrief.findFirst
```

### `POST /api/voice-sessions/missed/:id/handle`

- **Purpose:** Explicit "Mark as handled" from the inbox UI. Stamps `handledAt` on every MISSED inbound session for the same (tenant, customerNumber) - same cascade semantics as the auto-handle on ACTIVE. Idempotent.
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 219
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:219` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `voiceCallSession.updateMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-sessions/missed/:id/handle
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:219
      → db write  prisma.voiceCallSession.updateMany
```

### `POST /api/voice-sessions/presence/heartbeat`

- **Purpose:** Agent client pings every 30s to bump lastSeenAt. Tenants where voiceCopilotEnabled = false already 404 via the gate above.
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 988
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:988` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-sessions/presence/heartbeat
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:988
      → (responds directly; no downstream calls detected)
```

### `POST /api/voice-sessions/start-outbound`

- **Purpose:** Outbound-dial gate. The frontend always calls this BEFORE opening the browser dialer; the response decides which path to take based on the channel's `outboundMode`:
- **Handler:** `services/conversation/src/routes/voice-sessions.ts` line 749
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** `outbound:calls:create` **or** role `ADMIN`; Gate: `voiceCopilotGate`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `conversation` service mounts this router at `/api/voice-sessions` in `services/conversation/src/index.ts`; the route is declared in `services/conversation/src/routes/voice-sessions.ts`.
2. **Handler.** `services/conversation/src/routes/voice-sessions.ts:749` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** `outbound:calls:create` **or** role `ADMIN`; Gate: `voiceCopilotGate`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `communicationChannel.findFirst`
   - `conversation.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `409`, `500`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/voice-sessions/start-outbound
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → voiceCopilotGate]
  → handler  services/conversation/src/routes/voice-sessions.ts:749
      → db read   prisma.communicationChannel.findFirst
      → db read   prisma.conversation.findFirst
```

### `POST /api/voice/incoming/forward-complete`

- **Purpose:** Action callback on the <Dial> emitted by FORWARD_TO_AGENT mode. Twilio posts DialCallStatus once the forwarded leg finishes (answered, busy, no-answer, failed, canceled). We use this to transition the session FSM - without it the session would stay RINGING because the conference-status events never fire.
- **Handler:** `services/voice-copilot/src/routes/voice-incoming.ts` line 798
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/voice-incoming.ts`.
2. **Handler.** `services/voice-copilot/src/routes/voice-incoming.ts:798` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `voiceCallSession.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, `404`, `500`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice/incoming/forward-complete
  → handler  services/voice-copilot/src/routes/voice-incoming.ts:798
      → db read   prisma.voiceCallSession.findUnique
```

### `POST /api/voice/incoming/recording`

- **Purpose:** Creates a recording. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/voice-copilot/src/routes/voice-incoming.ts` line 870
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/voice-incoming.ts`.
2. **Handler.** `services/voice-copilot/src/routes/voice-incoming.ts:870` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `voiceCallSession.updateMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200`, answering the `{ data: ... }` envelope. Error paths: `403`, `404`, `500`, `503`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice/incoming/recording
  → handler  services/voice-copilot/src/routes/voice-incoming.ts:870
      → db write  prisma.voiceCallSession.updateMany
```

### `POST /api/voice/incoming/status`

- **Purpose:** Creates a statu. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/voice-copilot/src/routes/voice-incoming.ts` line 671
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/voice-incoming.ts`.
2. **Handler.** `services/voice-copilot/src/routes/voice-incoming.ts:671` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `voiceCallSession.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200`, answering the `{ data: ... }` envelope. Error paths: `400`, `403`, `404`, `422`, `500`, `503`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice/incoming/status
  → handler  services/voice-copilot/src/routes/voice-incoming.ts:671
      → db read   prisma.voiceCallSession.findUnique
```

### `POST /api/voice/incoming/voice`

- **Purpose:** Creates a voice. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/voice-copilot/src/routes/voice-incoming.ts` line 187
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `voice-copilot` service mounts this router at `/api/voice` in `services/voice-copilot/src/index.ts`; the route is declared in `services/voice-copilot/src/routes/voice-incoming.ts`.
2. **Handler.** `services/voice-copilot/src/routes/voice-incoming.ts:187` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `voiceCallSession.findFirst({
            where: {
              tenantId,
              direction: "inbound",
              state: "MISSED",
              startedAt: { gte: cutoff },
              OR: [
                { assignedAgentId: matchingAgent`
   - `voiceCallSession.findFirst`
   - `channelAccount.findFirst`
   - `conversation.upsert({
        where: {
          id: `voice-inbound-placeholder-${callSid}`,
        },
        update: {},
        create: {
          id: `voice-inbound-${callSid}`,
          tenantId,
          channel: "VOICE",
          channelAccountId: channelAccount?`
   - `voiceCallSession.findUnique`
   _Writes:_
   - `conversation.create`
   - `voiceCallSession.upsert`
   - `voiceCallSession.create`
   - `conversation.upsert`
   - `voiceCallSession.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `403`, `404`, `500`, `503`, answering `{ error: ... }`.

#### Call graph

```text
POST /api/voice/incoming/voice
  → handler  services/voice-copilot/src/routes/voice-incoming.ts:187
      → db read   prisma.voiceCallSession.findFirst({
            where: {
              tenantId,
              direction: "inbound",
              state: "MISSED",
              startedAt: { gte: cutoff },
              OR: [
                { assignedAgentId: matchingAgent
      → db read   prisma.voiceCallSession.findFirst
      → db read   prisma.channelAccount.findFirst
      → db read   prisma.conversation.upsert({
        where: {
          id: `voice-inbound-placeholder-${callSid}`,
        },
        update: {},
        create: {
          id: `voice-inbound-${callSid}`,
          tenantId,
          channel: "VOICE",
          channelAccountId: channelAccount?
      → db read   prisma.voiceCallSession.findUnique
      → db write  prisma.conversation.create
      → db write  prisma.voiceCallSession.upsert
      → db write  prisma.voiceCallSession.create
      → db write  prisma.conversation.upsert
      → db write  prisma.voiceCallSession.update
```

## Webhooks

11 endpoints.

### `GET /api/webhook`

- **Purpose:** Webhook verification (GET) - shared by WhatsApp and Messenger
- **Handler:** `services/webhook/src/routes/webhook.ts` line 94
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** None.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `webhook` service mounts this router at `/api/webhook` in `services/webhook/src/index.ts`; the route is declared in `services/webhook/src/routes/webhook.ts`.
2. **Handler.** `services/webhook/src/routes/webhook.ts:94` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200`, answering the `{ data: ... }` envelope. Error paths: `403`, answering `{ error: ... }`.

#### Call graph

```text
GET /api/webhook
  → handler  services/webhook/src/routes/webhook.ts:94
      → (responds directly; no downstream calls detected)
```

### `POST /api/webhook`

- **Purpose:** Unified webhook handler (POST) - detects platform via adapter pattern
- **Handler:** `services/webhook/src/routes/webhook.ts` line 119
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `webhook` service mounts this router at `/api/webhook` in `services/webhook/src/index.ts`; the route is declared in `services/webhook/src/routes/webhook.ts`.
2. **Handler.** `services/webhook/src/routes/webhook.ts:119` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findMany`
   - `channelAccount.findFirst`
   _Writes:_
   - `messageTemplate.updateMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.**
   - Enqueues `incomingMessageQueue.add` (BullMQ). The response returns before this work runs; retries and backoff are the queue's.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
POST /api/webhook
  → handler  services/webhook/src/routes/webhook.ts:119
      → db read   prisma.channelAccount.findMany
      → db read   prisma.channelAccount.findFirst
      → db write  prisma.messageTemplate.updateMany
      → queue     incomingMessageQueue.add  (async, outlives the response)
```

### `GET /api/webhook-triggers`

- **Purpose:** GET /api/webhook-triggers?workflowId=... Returns the trigger for a workflow (tenant-scoped), or { data: null } if the workflow has no trigger yet.
- **Handler:** `services/webhook/src/routes/trigger-admin.ts` line 111
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** None beyond authentication. Any authenticated member of the tenant may call this.
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** read

#### Execution flow

1. **Route registration.** `webhook` service mounts this router at `/api/webhook-triggers` in `services/webhook/src/index.ts`; the route is declared in `services/webhook/src/routes/trigger-admin.ts`.
2. **Handler.** `services/webhook/src/routes/trigger-admin.ts:111` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** None beyond authentication. Any authenticated member of the tenant may call this.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `webhookTrigger.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/webhook-triggers
  → [authenticate → resolveTenant → requireTenantState(ACTIVE)]
  → handler  services/webhook/src/routes/trigger-admin.ts:111
      → db read   prisma.webhookTrigger.findFirst
```

### `POST /api/webhook-triggers`

- **Purpose:** POST /api/webhook-triggers  { workflowId } Idempotently provisions a trigger (token + secret) for a workflow the tenant owns. Returns the existing trigger if one already exists.
- **Handler:** `services/webhook/src/routes/trigger-admin.ts` line 132
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `webhook` service mounts this router at `/api/webhook-triggers` in `services/webhook/src/index.ts`; the route is declared in `services/webhook/src/routes/trigger-admin.ts`.
2. **Handler.** `services/webhook/src/routes/trigger-admin.ts:132` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `chatbotFlow.findFirst`
   - `webhookTrigger.findFirst`
   _Writes:_
   - `webhookTrigger.update`
   - `webhookTrigger.create`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/webhook-triggers
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/webhook/src/routes/trigger-admin.ts:132
      → db read   prisma.chatbotFlow.findFirst
      → db read   prisma.webhookTrigger.findFirst
      → db write  prisma.webhookTrigger.update
      → db write  prisma.webhookTrigger.create
```

### `PATCH /api/webhook-triggers/:id`

- **Purpose:** PATCH /api/webhook-triggers/:id  { enabled?, targetMode?, bodySchema? } Update the trigger's enabled flag, its target mode ("flow" | "connected"), and/or its declared body schema ([{ key, type }]). At least one field is required. A disabled trigger answers inbound POSTs with 403 (see routes/triggers.ts). bodySchema is declaration only - it is not enforced against inbound payloads.
- **Handler:** `services/webhook/src/routes/trigger-admin.ts` line 243
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `webhook` service mounts this router at `/api/webhook-triggers` in `services/webhook/src/index.ts`; the route is declared in `services/webhook/src/routes/trigger-admin.ts`.
2. **Handler.** `services/webhook/src/routes/trigger-admin.ts:243` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `webhookTrigger.findFirst`
   _Writes:_
   - `webhookTrigger.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
PATCH /api/webhook-triggers/:id
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/webhook/src/routes/trigger-admin.ts:243
      → db read   prisma.webhookTrigger.findFirst
      → db write  prisma.webhookTrigger.update
```

### `POST /api/webhook-triggers/:id/regenerate-secret`

- **Purpose:** POST /api/webhook-triggers/:id/regenerate-secret Rotates the shared secret. The token (and therefore the URL) is unchanged.
- **Handler:** `services/webhook/src/routes/trigger-admin.ts` line 211
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
- **Authorization:** Role: `ADMIN`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** **Not** routed by the gateway; internal traffic only.
- **Operation type:** write

#### Execution flow

1. **Route registration.** `webhook` service mounts this router at `/api/webhook-triggers` in `services/webhook/src/index.ts`; the route is declared in `services/webhook/src/routes/trigger-admin.ts`.
2. **Handler.** `services/webhook/src/routes/trigger-admin.ts:211` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body. Tenant status gate: **ACTIVE**.
5. **Authorization.** Role: `ADMIN`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `webhookTrigger.findFirst`
   _Writes:_
   - `webhookTrigger.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/webhook-triggers/:id/regenerate-secret
  → [authenticate → resolveTenant → requireTenantState(ACTIVE) → requireRole(ADMIN)]
  → handler  services/webhook/src/routes/trigger-admin.ts:211
      → db read   prisma.webhookTrigger.findFirst
      → db write  prisma.webhookTrigger.update
```

### `POST /api/webhook/email`

- **Purpose:** Creates an email. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/webhook/src/routes/webhook.ts` line 651
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `webhook` service mounts this router at `/api/webhook` in `services/webhook/src/index.ts`; the route is declared in `services/webhook/src/routes/webhook.ts`.
2. **Handler.** `services/webhook/src/routes/webhook.ts:651` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.**
   - Enqueues `incomingMessageQueue.add` (BullMQ). The response returns before this work runs; retries and backoff are the queue's.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
POST /api/webhook/email
  → handler  services/webhook/src/routes/webhook.ts:651
      → db read   prisma.channelAccount.findFirst
      → queue     incomingMessageQueue.add  (async, outlives the response)
```

### `POST /api/webhook/gmail`

- **Purpose:** Creates a gmail. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/webhook/src/routes/webhook.ts` line 729
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `webhook` service mounts this router at `/api/webhook` in `services/webhook/src/index.ts`; the route is declared in `services/webhook/src/routes/webhook.ts`.
2. **Handler.** `services/webhook/src/routes/webhook.ts:729` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
   _Writes:_
   - `channelAccount.update`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.**
   - Enqueues `incomingMessageQueue.add` (BullMQ). The response returns before this work runs; retries and backoff are the queue's.
10. **Response and error paths.** Success: `201` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
POST /api/webhook/gmail
  → handler  services/webhook/src/routes/webhook.ts:729
      → db read   prisma.channelAccount.findFirst
      → db write  prisma.channelAccount.update
      → queue     incomingMessageQueue.add  (async, outlives the response)
```

### `POST /api/webhook/outlook`

- **Purpose:** Creates an outlook. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/webhook/src/routes/webhook.ts` line 860
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `webhook` service mounts this router at `/api/webhook` in `services/webhook/src/index.ts`; the route is declared in `services/webhook/src/routes/webhook.ts`.
2. **Handler.** `services/webhook/src/routes/webhook.ts:860` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.**
   - Enqueues `incomingMessageQueue.add` (BullMQ). The response returns before this work runs; retries and backoff are the queue's.
10. **Response and error paths.** Success: `200`, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
POST /api/webhook/outlook
  → handler  services/webhook/src/routes/webhook.ts:860
      → db read   prisma.channelAccount.findMany
      → queue     incomingMessageQueue.add  (async, outlives the response)
```

### `POST /api/webhook/slack`

- **Purpose:** Creates a slack. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/webhook/src/routes/webhook.ts` line 949
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `webhook` service mounts this router at `/api/webhook` in `services/webhook/src/index.ts`; the route is declared in `services/webhook/src/routes/webhook.ts`.
2. **Handler.** `services/webhook/src/routes/webhook.ts:949` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.**
   - Enqueues `incomingMessageQueue.add` (BullMQ). The response returns before this work runs; retries and backoff are the queue's.
10. **Response and error paths.** Success: `200`, answering the `{ data: ... }` envelope. No explicit error statuses in the handler.

#### Call graph

```text
POST /api/webhook/slack
  → handler  services/webhook/src/routes/webhook.ts:949
      → db read   prisma.channelAccount.findFirst
      → queue     incomingMessageQueue.add  (async, outlives the response)
```

### `POST /webhooks/:token`

- **Purpose:** POST /webhooks/:token
- **Handler:** `services/webhook/src/routes/triggers.ts` line 45
- **Authentication / tenant resolution:** **No authentication middleware.** This route is reachable without a token. `crossTenantMiddleware` - deliberately spans tenants.
- **Authorization:** None.
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `webhook` service mounts this router at `/webhooks/:token` in `services/webhook/src/index.ts`; the route is declared in `services/webhook/src/routes/triggers.ts`.
2. **Handler.** `services/webhook/src/routes/triggers.ts:45` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** **No authentication middleware.** This route is reachable without a token. `crossTenantMiddleware` - deliberately spans tenants.
5. **Authorization.** None.
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `webhookTrigger.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.**
   - Enqueues `incomingMessageQueue.add` (BullMQ). The response returns before this work runs; retries and backoff are the queue's.
10. **Response and error paths.** Success: `200`, answering the `{ data: ... }` envelope. Error paths: `401`, `403`, `404`, `500`, answering `{ error: ... }`.

#### Call graph

```text
POST /webhooks/:token
  → handler  services/webhook/src/routes/triggers.ts:45
      → db read   prisma.webhookTrigger.findUnique
      → queue     incomingMessageQueue.add  (async, outlives the response)
```

## WhatsApp

11 endpoints.

### `POST /api/channels/whatsapp/connect`

- **Purpose:** Connect exactly one number.
- **Handler:** `services/auth/src/routes/whatsapp-numbers.ts` line 314
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:update`; Plan limit: `limit:channels`
- **Input validation:** `validate(connectSchema)` (Zod). Accepts: `sessionId`, `phoneNumberId`, `twoStepPin`, `dataLocalizationRegion`. Required: `sessionId`, `phoneNumberId`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/whatsapp-numbers.ts`.
2. **Handler.** `services/auth/src/routes/whatsapp-numbers.ts:314` - inline `async (req, res)` handler.
3. **Validation.** `validate(connectSchema)` (Zod). Accepts: `sessionId`, `phoneNumberId`, `twoStepPin`, `dataLocalizationRegion`. Required: `sessionId`, `phoneNumberId`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:update`; Plan limit: `limit:channels`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `whatsAppNumber.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `409`, `410`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/channels/whatsapp/connect
  → [authenticate → resolveTenant → requirePermission(channels:manage:update) → validate(connectSchema)]
  → handler  services/auth/src/routes/whatsapp-numbers.ts:314
      → db read   prisma.whatsAppNumber.findUnique
```

### `GET /api/channels/whatsapp/exclusions`

- **Purpose:** Coexistence delivers EVERY conversation on the number to us, including the ones that were never meant for a shared inbox. These rules are the only way to say "that one is mine" short of not connecting Coexistence at all.
- **Handler:** `services/auth/src/routes/whatsapp-numbers.ts` line 530
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:read`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/whatsapp-numbers.ts`.
2. **Handler.** `services/auth/src/routes/whatsapp-numbers.ts:530` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:read`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `inboundExclusion.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/channels/whatsapp/exclusions
  → [authenticate → resolveTenant → requirePermission(channels:manage:read)]
  → handler  services/auth/src/routes/whatsapp-numbers.ts:530
      → db read   prisma.inboundExclusion.findMany
```

### `POST /api/channels/whatsapp/exclusions`

- **Purpose:** Creates an exclusion. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/whatsapp-numbers.ts` line 553
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:update`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/whatsapp-numbers.ts`.
2. **Handler.** `services/auth/src/routes/whatsapp-numbers.ts:553` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:update`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `channelAccount.findFirst`
   _Writes:_
   - `inboundExclusion.upsert`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `201`, answering the `{ data: ... }` envelope. Error paths: `400`, `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/channels/whatsapp/exclusions
  → [authenticate → resolveTenant → requirePermission(channels:manage:update)]
  → handler  services/auth/src/routes/whatsapp-numbers.ts:553
      → db read   prisma.channelAccount.findFirst
      → db write  prisma.inboundExclusion.upsert
```

### `DELETE /api/channels/whatsapp/exclusions/:id`

- **Purpose:** Deletes the referenced exclusion. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/whatsapp-numbers.ts` line 608
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:update`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/whatsapp-numbers.ts`.
2. **Handler.** `services/auth/src/routes/whatsapp-numbers.ts:608` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:update`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Writes:_
   - `inboundExclusion.deleteMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/channels/whatsapp/exclusions/:id
  → [authenticate → resolveTenant → requirePermission(channels:manage:update)]
  → handler  services/auth/src/routes/whatsapp-numbers.ts:608
      → db write  prisma.inboundExclusion.deleteMany
```

### `POST /api/channels/whatsapp/inspect`

- **Purpose:** Look at what the customer has, and say which flow each number would take.
- **Handler:** `services/auth/src/routes/whatsapp-numbers.ts` line 172
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:update`
- **Input validation:** `validate(inspectSchema)` (Zod). Accepts: `code`, `path`, `businessPortfolioId`, `wabaIds`. Required: `code`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/whatsapp-numbers.ts`.
2. **Handler.** `services/auth/src/routes/whatsapp-numbers.ts:172` - inline `async (req, res)` handler.
3. **Validation.** `validate(inspectSchema)` (Zod). Accepts: `code`, `path`, `businessPortfolioId`, `wabaIds`. Required: `code`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:update`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `whatsAppNumber.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `400`, `502`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/channels/whatsapp/inspect
  → [authenticate → resolveTenant → requirePermission(channels:manage:update) → validate(inspectSchema)]
  → handler  services/auth/src/routes/whatsapp-numbers.ts:172
      → db read   prisma.whatsAppNumber.findMany
```

### `GET /api/channels/whatsapp/numbers`

- **Purpose:** Every WhatsApp number on this workspace, each with its own health.
- **Handler:** `services/auth/src/routes/whatsapp-numbers.ts` line 89
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:read`
- **Input validation:** No input.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/whatsapp-numbers.ts`.
2. **Handler.** `services/auth/src/routes/whatsapp-numbers.ts:89` - inline `async (req, res)` handler.
3. **Validation.** No input.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:read`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `whatsAppNumber.findMany`
   - `channelAccount.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success: `200` _(inferred - the handler sets no explicit success status; Express defaults to 200)_, answering the `{ data: ... }` envelope. No explicit error statuses in the handler. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/channels/whatsapp/numbers
  → [authenticate → resolveTenant → requirePermission(channels:manage:read)]
  → handler  services/auth/src/routes/whatsapp-numbers.ts:89
      → db read   prisma.whatsAppNumber.findMany
      → db read   prisma.channelAccount.findMany
```

### `DELETE /api/channels/whatsapp/numbers/:id`

- **Purpose:** Remove ONE number.
- **Handler:** `services/auth/src/routes/whatsapp-numbers.ts` line 507
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:update`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/whatsapp-numbers.ts`.
2. **Handler.** `services/auth/src/routes/whatsapp-numbers.ts:507` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:update`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** No direct Prisma access in the handler body. Any persistence happens inside the services listed above.
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
DELETE /api/channels/whatsapp/numbers/:id
  → [authenticate → resolveTenant → requirePermission(channels:manage:update)]
  → handler  services/auth/src/routes/whatsapp-numbers.ts:507
      → (responds directly; no downstream calls detected)
```

### `GET /api/channels/whatsapp/numbers/:id/events`

- **Purpose:** Every step taken against one number, with Meta's own responses.
- **Handler:** `services/auth/src/routes/whatsapp-numbers.ts` line 633
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:read`
- **Input validation:** No body schema. Path and query parameters are read directly by the handler.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** read

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/whatsapp-numbers.ts`.
2. **Handler.** `services/auth/src/routes/whatsapp-numbers.ts:633` - inline `async (req, res)` handler.
3. **Validation.** No body schema. Path and query parameters are read directly by the handler.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:read`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `whatsAppNumber.findFirst`
   - `whatsAppNumberEvent.findMany`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
GET /api/channels/whatsapp/numbers/:id/events
  → [authenticate → resolveTenant → requirePermission(channels:manage:read)]
  → handler  services/auth/src/routes/whatsapp-numbers.ts:633
      → db read   prisma.whatsAppNumber.findFirst
      → db read   prisma.whatsAppNumberEvent.findMany
```

### `POST /api/channels/whatsapp/numbers/:id/refresh`

- **Purpose:** Performs the `refresh` action on the referenced number. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/whatsapp-numbers.ts` line 458
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:read`
- **Input validation:** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/whatsapp-numbers.ts`.
2. **Handler.** `services/auth/src/routes/whatsapp-numbers.ts:458` - inline `async (req, res)` handler.
3. **Validation.** **No schema.** The handler reads `req.body` directly, so the accepted shape is not verifiable from source and is not enforced.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:read`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `whatsAppNumber.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/channels/whatsapp/numbers/:id/refresh
  → [authenticate → resolveTenant → requirePermission(channels:manage:read)]
  → handler  services/auth/src/routes/whatsapp-numbers.ts:458
      → db read   prisma.whatsAppNumber.findFirst
```

### `POST /api/channels/whatsapp/numbers/:id/repair`

- **Purpose:** Performs the `repair` action on the referenced number. _(Derived from the route shape; no descriptive comment in source.)_
- **Handler:** `services/auth/src/routes/whatsapp-numbers.ts` line 479
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:update`
- **Input validation:** `validate(repairSchema)` (Zod). Accepts: `action`. Required: `action`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/whatsapp-numbers.ts`.
2. **Handler.** `services/auth/src/routes/whatsapp-numbers.ts:479` - inline `async (req, res)` handler.
3. **Validation.** `validate(repairSchema)` (Zod). Accepts: `action`. Required: `action`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:update`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `whatsAppNumber.findFirst`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/channels/whatsapp/numbers/:id/repair
  → [authenticate → resolveTenant → requirePermission(channels:manage:update) → validate(repairSchema)]
  → handler  services/auth/src/routes/whatsapp-numbers.ts:479
      → db read   prisma.whatsAppNumber.findFirst
```

### `POST /api/channels/whatsapp/numbers/:id/resume`

- **Purpose:** Supply what the customer was asked for and continue that number's pipeline.
- **Handler:** `services/auth/src/routes/whatsapp-numbers.ts` line 397
- **Authentication / tenant resolution:** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
- **Authorization:** Permission: `channels:manage:update`
- **Input validation:** `validate(resumeSchema)` (Zod). Accepts: `twoStepPin`, `verificationCode`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
- **Exposure:** Routed by the gateway (reachable from the internet).
- **Operation type:** write

#### Execution flow

1. **Route registration.** `auth` service mounts this router at `/api/channels` in `services/auth/src/index.ts`; the route is declared in `services/auth/src/routes/whatsapp-numbers.ts`.
2. **Handler.** `services/auth/src/routes/whatsapp-numbers.ts:397` - inline `async (req, res)` handler.
3. **Validation.** `validate(resumeSchema)` (Zod). Accepts: `twoStepPin`, `verificationCode`. `validate` reassigns `req.body` to the parsed value, so any property not named by the schema is stripped before the handler runs.
4. **Authentication / tenant resolution.** `authenticate()` (`packages/shared/src/middleware/auth.ts`) verifies the Authentik JWT against JWKS and resolves `sub` → `User.authentikSubject`. `resolveTenant` derives `req.tenantId` from the principal; it is never read from the request body.
5. **Authorization.** Permission: `channels:manage:update`
6. **Service calls.** None; the handler works directly against Prisma and/or the response.
7. **Database.** Prisma models touched (source order, not guaranteed runtime order - branches may skip some):
   _Reads:_
   - `whatsAppNumber.findFirst`
   - `channelAccount.findUnique`
   - `whatsAppNumber.findUnique`
8. **External integrations.** None detected in the handler body.
9. **Background work, events and audit.** None triggered by this endpoint.
10. **Response and error paths.** Success status not determinable from source. Error paths: `404`, `409`, `500`, answering `{ error: ... }`. `401` is returned by `authenticate()` before the handler runs.

#### Call graph

```text
POST /api/channels/whatsapp/numbers/:id/resume
  → [authenticate → resolveTenant → requirePermission(channels:manage:update) → validate(resumeSchema)]
  → handler  services/auth/src/routes/whatsapp-numbers.ts:397
      → db read   prisma.whatsAppNumber.findFirst
      → db read   prisma.channelAccount.findUnique
      → db read   prisma.whatsAppNumber.findUnique
```
